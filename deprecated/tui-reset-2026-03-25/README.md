# tui-reset-2026-03-25

这一份目录用于归档 2026-03-25 放弃的 TUI 重写主线。

## 定位

- 这里只保存“当前这次重写”的代码、文档、阶段性设计和计划
- 它和 `deprecated/tui-legacy/` 是两次完全不同的废弃资产，不能混用目录语义
- `deprecated/tui-legacy/` 保留的是更早一代、接近产品完成态的旧 TUI 参考实现
- `deprecated/tui-reset-2026-03-25/` 保留的是这次中途放弃的新架构重写尝试

## 使用原则

- 两边都只作为参考区
- 可以抽取被验证过的渲染、布局、命中测试、交互处理思路
- 不应把这里的目录直接恢复成新的主线实现
