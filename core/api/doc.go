// Package api 定义 owning daemon 对 client application 暴露的 wire/UI-independent projection contract。
// core implementation 是字段语义 owner；protocol 只编码，client runtime 只增加 endpoint/session context。
package api
