# Archived: Remote Rebuild Workflow

This file is retained only as historical migration context.

The current source of truth for active work is the repository-root `workflow.md`.

Do not use this file to plan or prioritize new work.

Historical note:
- This file captured the last migration slice before remote ownership moved out of `termx-core`.
- The migration is now complete.
- Current work should focus on Hub, local Agent, managed connection flow, and browser-side integration against the unified remote architecture.
