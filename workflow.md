# 工作流：screen app 无限历史清场与重建主线

本文件是当前分支唯一有效的活动驱动文件。后续分析、实现、测试、提交都先看这里；如果本文件与旧说明、聊天记录、旧代码行为或局部假设冲突，以本文件为准。

本文件已经从旧 remote + app 迁移队列重置为 screen app 无限历史主线。旧队列、旧完成记录和旧补丁路径只通过 git 历史追溯，不再作为当前实现依据。

## 0. 怎么读

开始任何工作前只看这几段：

- `1. 当前目标`
- `2. 技术设计基准`
- `3. 工作范围`
- `4. 不可违反的语义`
- `5. 清场规则`
- `6. 任务队列`
- `7. 测试准入`
- `8. 提交规则`

如果用户请求和任务队列冲突，先更新本文件，再改代码；不要靠口头约定跳过范围和顺序。

## 1. 当前目标

### 1.1 一句话目标

实现接近 tmux/普通终端体验的无限历史：只要内容真实经过 PTY 并被终端语义解释到 primary/history 轨道，就应能在历史模式中回看、搜索和复制；实现方式必须以 core-v2 authoritative screen model 的 physical row/cell 为 ingest 真值，再在 query/copy/history 阶段投影 logical line。

### 1.2 这轮只关心什么

当前阶段从 logical-line-first reducer 转向 screen-backed history：先锁定现有链路和迁移基线，再引入 physical row/cell screen model，最后逐步把 pseudo-TUI path、projection、HistoryWindow 和旧 journal reducer 切过去。

1. 引入 `ScreenHistoryBuffer`，把 PTY semantic ops 先作用到 main/alt screen、physical rows、cells、cursor 和 scroll region。
2. physical row 必须有稳定 RowID 和 Version；同一 RowID 不允许 seal 两次。
3. 只有真实 scroll-out、明确 boundary、frame close、process close、alt-screen exit 等语义才能把 physical row seal 进 committed physical history。
4. logical line 只作为 HistoryWindow/Copy/Search 阶段的 projection，必须保留 RowIDs 并按 RowID 去重。
5. journal 可暂时保留为 backlog/boundary/meta queue，但正文 history truth 必须下沉到 screen model。
6. tui-v3 和 App 只消费 core-v2 authoritative history window，不从本地 VTerm scrollback、snapshot、DOM/canvas rows 或 live cache 推断历史真值。

### 1.3 不在当前阶段做什么

- 不修旧历史代码上的单点 bug。
- 不为旧 storage、旧 protocol、旧 snapshot/workbench schema 或旧运行时行为做兼容。
- 不恢复旧 `termx-core/` 或 `tuiv2/`。
- 不把 Codex、Claude Code、htop、vim 这类程序名写成特殊分支；只能通过终端语义和屏幕行为分类。
- 不保证读取程序没有输出到 PTY 的内部状态。目标是 tmux/终端级别的可观察历史，不是读取 Codex 内部 session 数据库。
- 不迁 `web-control/`、`termx-hub/`、`termx-remote-v2/`。
- 不继续推进旧 remote + app 队列里的 Web desktop 可见性问题；那条线已退出当前主线。

## 2. 技术设计基准

- 当前 screen-backed history 重构基准：`termx-core-v2/docs/screen-backed-history-rebuild-design.md`
  - R419 后，history ingest 真值改为 `ScreenHistoryBuffer` 的 physical row/cell 状态机；logical line 是 query/copy/history 阶段从 physical rows 投影出的视图，不再是 ingest 阶段的正文 truth。
  - 新模型必须保留 RowID、Version、OwnerKind、seal-once、current/committed RowID 去重、alt-screen 隔离和 scroll region 语义。
  - 旧 compact journal 可在迁移期作为 backlog/boundary/meta/event queue 存在，但不得继续作为 pseudo-TUI 正文 truth 的最终 owner。
- 旧 history logical renderer 设计基准：`termx-core-v2/docs/history-logical-renderer-design.md`
  - R318 后，history 被定义为消费 vterm terminal semantic transaction 的 logical-line renderer。
  - 新模型使用 `mutable` / `sealed` / `timeline` / `frame journal` 语义，不再把 `commit` / `committed` 作为领域概念。
  - R419 后该文档作为 pre-screen-buffer 背景；若它与 `screen-backed-history-rebuild-design.md` 冲突，以 screen-backed 设计为准。
- 旧 screen app 无限历史定案：`termx-core-v2/docs/screen-app-infinite-history-final-plan.md`
  - 该文档保留 R300-R317 的问题背景、vterm transaction 边界和已完成切片记录；其中 `commit/committed` 术语和 projector/store 分层若与新文档冲突，以 `screen-backed-history-rebuild-design.md` 为准。
- core-v2 架构：`termx-core-v2/docs/architecture.md`
- tui-v3 架构：`termx-tui-v3/docs/architecture.md`
- live native screen 重构基准：`termx-core-v2/docs/live-native-screen-rebuild-design.md`
  - R348 后，普通实时显示被定义为 core 维护最新 native screen，TUI 主导 render loop；core 只发布 live invalidation，不推送待渲染帧队列，也不关心客户端渲染进度。
  - 旧 `Snapshot/CompactSnapshot`、`ScreenUpdate` stream、`grid.viewport`、snapshot/history 拼接和 TUI live union event 只能作为清场对象，不得继续作为 v3 live display 主合同。
- compact semantic history journal 设计基准：`termx-core-v2/docs/compact-semantic-history-journal-design.md`
  - R396 后，高压 PTY 输出恢复 live latest 与 history semantic 的双消费边界：live `SurfaceTrack` 只维护当前 native screen 与唯一 response owner，history `SemanticTap` / journal consumer 只消费同一 PTY semantic pass。
  - R419 后 journal 必须继续来自 history semantic consumer 的 vterm 语义 pass，不能来自 raw PTY parser、live snapshot diff、TUI/render rows 或 live `SurfaceTrack`。journal 是未应用语义命令队列，不是第二份 history truth；authoritative truth 将迁移为 core-v2 screen-backed physical row store。
  - live latest screen 更新必须恢复到 `SurfaceTrack.Write -> WriteForLatestFrame` 快路径成本；history semantic write hot path 不应反压 live wake，不应为普通输出构造 full frame、full SourceDamage、per-write native snapshot clone 或逐 op history reducer backlog。
- vterm terminal 语义来源：`termx-vterm/`
- 旧 history 说明、session history 说明和同步输出说明只能作为问题背景；如果与本文件或定案冲突，以本文件和定案为准。

如果实现发现设计文档过期，必须和当前切片一起更新；不要代码先跑偏，文档以后再补。

## 3. 工作范围

### 3.1 当前主线允许主动修改

- `workflow.md`
- `AGENTS.md`
- `termx-core-v2/`
- `termx-core-v2/docs/screen-app-infinite-history-final-plan.md`
- `termx-vterm/`，仅限提供 terminal semantic transaction 所需的最小接口、事件和 harness

### 3.2 受限联动范围

只有当前切片确实需要时，才允许最小化触及：

- `termx-tui-v3/`，只接 authoritative history window、copy mode、scroll/selection/render harness
- `internal/protocol/` 与 `termx-proto/`，只在 `history.window`、history copy 或 semantic history contract 需要跨进程时修改
- `termx-cli/`，只在默认入口守卫、smoke 或必要 CLI glue 需要时修改
- `termx-shared/`
- `termx-testkit/`
- `scripts/`
- `Makefile`
- `go.work`
- `go.work.sum`
- 必要顶层说明文档

### 3.3 已删除旧目录

默认不得恢复：

- `termx-core/`
- `tuiv2/`

### 3.4 冻结范围

不得主动触碰，除非本文件先明确解冻：

- `termx-remote/`
- `termx-remote-v2/`
- `termx-app/`
- `remote-ui/`
- `web-control/`
- `termx-hub/`
- `bin/`
- `.claude/`
- 顶层可执行产物和测试产物
- 未在本文件列出的目录

## 4. 不可违反的语义

### 4.1 历史真值

- R419 后，history ingest truth 的基本单位是 authoritative physical row/cell，不是 append-only logical line、visual row、wrapped row、snapshot scrollback、grid viewport 或 xterm buffer row。
- core-v2 `ScreenHistoryBuffer` 是 main/alt screen、physical rows、cells、cursor、scroll region、RowID、Version 和 seal-once 的 domain owner。
- logical line 是 query/copy/history 阶段从 physical rows 投影出的视图；projection 必须保留 RowIDs，不能用纯文本 dedupe 当 correctness。
- 同一个 RowID 不允许 seal 两次；latest/current screen 与 committed physical history 拼接时必须按 RowID 去重。
- `mutable` 表示 physical row 仍属于 current screen/session，可被 terminal 语义改写；`sealed` 表示 physical row 已按 scroll-out/boundary/frame close/process close/alt exit 等语义离开当前可变区域。
- `commit` / `committed` 不是新模型的 ingest 概念；旧代码名若尚未迁移，只能按 sealed physical row 或 projected sealed window 语义理解。
- physical row store、sealed row index、logical projection、segment cursor、storage backend、cache、adapter、TUI/App projection 都不能演变成第二份历史 truth。
- `persisted` 或落盘不表示 sealed，也不表示不可修改；是否可修改由 terminal/session/row lifecycle 语义决定。

### 4.2 terminal 语义来源

- 不能把 raw PTY bytes parser 作为 terminal 语义 owner；raw parser 不能 fallback 出第二套历史。
- core-v2 应消费 termx-vterm 解释过程中的语义 transaction，而不是消费最终屏幕快照。
- vterm 的当前屏幕不是无限历史来源；它最多证明“终端语义解释过程可以产生可记录事件”。
- 与 tmux 一样，只有程序真实输出到 PTY 的内容才能进入历史；没有输出过的程序内部状态不在目标内。

### 4.3 普通输出

- 普通 shell/程序 stdout 先作用到 screen buffer；只有 hard line boundary、真实 scroll-out 或生命周期边界能让对应 physical row seal，随后在查询阶段投影成 logical line。
- 普通输出不应长时间持有 screen app session。
- 进程退出必须 seal 普通输出 open line。

### 4.4 screen app session

- pseudo-TUI 类程序在 primary screen 上反复改写时，必须有一个可变 session。
- session 内允许程序修改从 session 开始到当前的可变内容。
- session 暴露给历史模式前，必须先通过 segment cursor/archive 形成一致视图，不能展示半更新状态。
- session 最终退出或模式切换时，按分类决定 seal 或丢弃：普通 primary screen app 可以 seal final screen-frame；纯 alt-screen transient 不写 primary history。

### 4.5 alt-screen

- alt-screen 不写入 primary history。
- htop/vim 这类纯 alt-screen transient：运行期间可在当前屏幕选择；退出时不把屏幕内容 commit 到 primary history。
- Codex/Claude Code 这类 primary screen app 临时进入 alt-screen 选择器时：进入 alt 前必须 archive/hide 当前 primary frame；alt 内容作为 live transient 展示和选择；退出 alt 后如果出现新的 primary 输出，必须作为新的 primary frame publish，可以接回同一 session journal，但不得复活 pre-alt current frame，也不得凭空 commit alt 屏幕。
- 如果程序运行期间用户进入历史模式，当前 session 的文本内容必须作为历史视图的一部分，但仍需标记为 mutable/frozen projection，而不是当成 sealed truth。

### 4.6 final screen-frame

- primary screen app 退出时允许把最后一屏作为固定宽度 screen-frame seal。
- final screen-frame 是为了保存“屏幕程序最终展示的内容”，不是普通 logical line reflow。
- final screen-frame 后续不随 resize 重排。

### 4.7 resize

- resize 不得重写 sealed history。
- 普通 logical line 是 projection，可以在展示层按新宽度重新 wrap；sealed physical row 本身不得因 resize 被重写。
- mutable screen app session 在 resize 时必须保留可变语义，不能凭空产生 sealed history。
- final screen-frame 必须固定生成时的列宽。
- Codex/Claude Code 运行中 resize 是硬 harness，不能靠程序名特殊适配。

### 4.8 色彩和主题

- 历史应保存终端语义属性，不应把“当时主题下的默认前景/背景 RGB”提前烘焙成不可变颜色。
- SGR 明确指定的颜色可以作为具体颜色或调色板索引保存。
- default fg/bg、bold、dim、inverse 等应尽量保存为语义属性，由查看历史时的主题解析。
- 如果终端应用写入明确 RGB 背景，则该 RGB 属于内容属性，不能被后续主题替换。

### 4.9 TUI/App 边界

- tui-v3 不拥有 sealed history truth，只消费 core-v2 authoritative `HistoryWindow`。
- tui-v3 copy mode 不得从本地 VTerm scrollback、snapshot totals、row ownership、LoadedRows、wrapped 拼接结果推断历史。
- App/Web/native live display 可以有本地短缓存，但 copy/search/history truth 必须走 core-v2 screen-backed authoritative HistoryWindow。
- renderer 只消费 view-model，不读 core client、history source、runtime service 或 protocol client。

## 5. 清场规则

这条主线允许先删后写，但删除必须有边界。R301 的“清场”是审计和最小隔离优先，
只删除会继续误导实现的补丁入口；大规模删除旧 raw parser / fallback 必须等 R302/R303
接口和 harness 站稳后再做。

- 可以删除旧补丁式 screen app/history 代码、raw parser fallback、程序名分支、snapshot/history 拼接 fallback、重复同步和隐式状态修正。
- 删除后必须保留可编译的最小骨架，或者在当前切片内补上替代 interface/harness。
- 不要为了保留旧测试而保留错误模型；旧测试若表达的是旧补丁语义，应删除或改写成新模型 harness。
- 任何“先兼容旧路径，后面再切”的方案默认不合格，除非本文件明确列为当前切片。
- 不得用 storage scrub、定时刷新、重复 attach、局部 fallback 分支掩盖状态错乱。
- 关键代码需要写简短中文注释，说明 domain owner、truth source、消息链路或失败条件。

## 6. 任务队列

状态只能使用：`待开始`、`进行中`、`完成`、`阻塞`。同一时间只能有一个切片处于 `进行中`。

自动执行时只看下面这张表，按顺序取最早未完成切片：

| 切片 | 状态 | 范围 | 白话说明 |
| --- | --- | --- | --- |
| R300. SK 无限历史工作流重置与定案纳入 | 完成 | `AGENTS.md`、`workflow.md`、`termx-core-v2/docs/screen-app-infinite-history-final-plan.md` | 旧 remote + app 队列退出当前主线；screen app 无限历史定案成为当前实现基准 |
| R301. SK 旧历史实现清场审计与最小隔离 | 完成 | `termx-core-v2/`、只读 `termx-vterm/`、相关 docs | 找出旧 screen app/history 补丁路径、raw parser fallback、snapshot 拼接和程序名特殊逻辑；只删除或隔离会继续误导后续实现的入口，保留可编译骨架和清场 harness，大规模删除等 R302/R303 接口站稳后再做 |
| R302. SK terminal semantic transaction 接口 | 完成 | `termx-core-v2/`、`termx-vterm/` | 先立 vterm 到 core-v2 的语义事件接口：普通写入、cursor move、erase、scroll、alt enter/leave、resize、SGR 属性、flush/transaction boundary |
| R303. SK history projector domain harness | 完成 | `termx-core-v2/` | 用 fake semantic transaction 驱动 projector，锁定普通输出 commit、mutable session、segment cursor、final screen-frame、alt transient 的基本语义 |
| R304. SK 普通输出最小实现 | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | shell/stdout 输出完整 logical line 后直接进入 committed history；process exit force commit primary mutable frontier |
| R305. SK primary screen app session 最小实现 | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | 支持 Codex/Claude Code 这类 primary screen app 在退出前修改 session 内文本；历史模式看到一致的当前 session projection |
| R306. SK alt-screen transient 与 primary archive | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | htop/vim 纯 alt 不进 primary history；primary app 临时进 alt 前 archive/hide 当前 frame，退出 alt 后的新 primary 输出作为新 frame publish，不能复活 pre-alt current frame |
| R307. SK resize 与 final screen-frame harness | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | 覆盖运行中 resize、final screen-frame 固定宽度、committed logical line 不被 resize 重写 |
| R308. SK 色彩属性与主题解析边界 | 完成 | `termx-core-v2/`、按需 `termx-tui-v3/` | 保存 default fg/bg 语义属性而不是提前烘焙主题 RGB；明确 RGB/256 色/默认色在历史里的存储和渲染规则 |
| R309. SK storage backend 无限历史接口 | 完成 | `termx-core-v2/` | 以 append/update transaction、segment cursor、index/window 为边界接文件存储；不得让文件格式成为第二份历史模型 |
| R310. SK protocol/TUI history window 接入 | 完成 | `termx-core-v2/`、`termx-tui-v3/`、按需 `internal/protocol/`、`termx-proto/` | tui-v3 copy/history 只消费 authoritative history window；live display 和 history surface 分层 |
| R311. SK synchronized output scroll-out proof 修复 | 完成 | `termx-vterm/`、`termx-core-v2/` | 修复 primary screen app 一次性输出超过屏幕时只剩最后一屏的问题；vterm 必须把 scroll-out runs/cells 作为 semantic proof 交给 core，core 在 primary session 中保留这些真实经过 PTY 的历史 |
| R312. SK current frame 历史分页与冻结边界 | 完成 | `termx-core-v2/`、按需 `termx-tui-v3/` | 修复进入 copy/history 后 current primary frame 之外上滑为空的问题；latest token 必须冻结 current frame，older 必须能从 current/archive segment 接回 committed history |
| R313. SK older prepend window tail boundary 修复 | 完成 | `termx-core-v2/`、按需 `termx-tui-v3/` | 复现 tmux 可见完整历史但 TUI 上滑黑屏；修复 older response 的 tail boundary，使 TUI 不再把 current-frame older 页判成 stale |
| R314. SK authoritative history dump 诊断命令 | 完成 | `termx-cli/`、`termx-core-v2/`、按需 `internal/protocol/` | 新增命令行诊断入口，把指定 terminal 的 core-v2 authoritative history window 分页 dump 到文件，用来区分历史存储/分页错误和 TUI 展示错误 |
| R315. SK screen-frame trailing blank rows 裁剪 | 完成 | `termx-core-v2/` | 对比 tmux 与 authoritative dump，修复 archived/current screen-frame 将整屏尾部默认空白行当作历史 rows 返回，导致 TUI 上滑大片黑/空的问题 |
| R316. SK copy/history PageUp 翻页与 frame 空白展示修复 | 完成 | `termx-tui-v3/`、按需 `termx-cli/` | 用 tmux 自测和 authoritative dump 对比确认 core 已有 committed 历史；修复 TUI copy/history 连续 PageUp 仍停在最新 frame/空白区域的问题，并避免默认空白 frame row 被渲染成黑块 |
| R317. SK archived frame 与 committed 投影顺序修复 | 完成 | `termx-core-v2/` | authoritative dump 显示早期 archived primary frame 被直接追加到全部 committed 日志之后，导致历史顺序错乱；修复 latest/older 的 segment 投影，让 archive 通过 current-frame older cursor 返回并按事务顺序夹在 committed 历史中 |
| R318. SK history logical renderer 设计重写 | 完成 | `workflow.md`、`termx-core-v2/docs/history-logical-renderer-design.md`、相关旧设计文档标注 | 去掉 commit/committed 领域概念，把 history 明确定义为消费同一份 vterm terminal semantic transaction 的 logical-line renderer；列出对象、接口、timeline、frame journal、mutable/sealed、freeze/window/storage 边界 |
| R319. SK semantic 覆盖矩阵与旧 projector/store 清场 | 完成 | `workflow.md`、`termx-core-v2/docs/history-logical-renderer-design.md`、`termx-core-v2/history/`、`termx-core-v2/terminal.go`、相关 core-v2 harness | 补全 vterm semantic transaction 到 logical-line renderer 的覆盖矩阵；删除旧内存 projector/store 补丁实现和旧 harness；落地新 renderer/state/interface/type 边界，真实实现前 history.window 明确返回未重建 |
| R320. SK ordered semantic event contract 与 coverage harness | 完成 | `termx-core-v2/history/`、必要时只读或最小触及 `termx-vterm/` | 根据 semantic 覆盖矩阵定义 renderer 内部 ordered event 输入边界；明确 ops、scroll-out proof、primary/alt frame、alt enter/exit、resize/full-replace 如何进入同一消费链；若 vterm side proof 缺少顺序信息，用 harness 暴露 contract 缺口并标注后续联动 |
| R321. SK StreamLineReducer 最小实现 | 完成 | `termx-core-v2/history/` | 实现 ordinary stream 的 open line、row ownership 和 cursor model；按 semantic op 的 row/col/cursor/erase/insert/delete 处理 WriteSpan、CR、LF/IND/NEL、soft-wrap、BS/CUB/CUF/CHA/HPA、CUP/VPA/CUU/CUD、EL、ED/ClearRect、ECH/DCH/ICH，并让 PrimaryScrollOut proof exactly-once seal |
| R322. SK FrameReducer 最小实现 | 完成 | `termx-core-v2/history/` | 实现 primary current frame、archived primary frame 和 alt transient frame journal；PrimaryFrame 全量替换 current，alt enter archive/clear primary 并 publish alt，alt exit clear transient，resize 不 seal history，final frame 固定生成宽度 |
| R323. SK HistoryStore 内存实现与 window/copy/freeze | 完成 | `termx-core-v2/history/` | 实现新的内存 HistoryStore，只接收 HistoryMutationBatch，不解释 terminal ops；维护 LogicalLineStore、SealedTimeline、OpenLine、FrameJournal、FrozenHistorySnapshot，并实现 latest/older/freeze/copy/storage recovery |
| R324. SK terminal history renderer 接入 | 完成 | `termx-core-v2/terminal.go`、`termx-core-v2/history/`、必要时 live semantic result 最小联动 | Terminal 持有新的 HistoryLogicalRenderer 与 HistoryStore；write/resize/close 只交 semantic transaction 或 CloseReason 给 renderer；移除 ErrHistoryNotRebuilt 临时返回并恢复 TerminalHistoryWindow/Copy/Freeze/Release |
| R325. SK protocol/CLI authoritative history 恢复 | 完成 | `termx-core-v2/protocol_service.go`、`termx-core-v2/history/`、`termx-cli/` | protocol history.window/history.copy/history.release 重新返回 authoritative HistoryStore 数据，保留 row kind、segment、logical line id、generation、token、cursor boundary，并修复 CLI history-dump 临时失败；若 CLI 暴露 history payload 缺陷，最小回到 history owner 修复 |
| R326. SK tmux/vterm harness 对齐 | 完成 | `termx-core-v2/`、必要时最小触及 `scripts/` 或 `termx-vterm/` | 用真实 PTY/tmux 对比 harness 覆盖 primary screen app 上滑历史、长同步输出、current frame 之外 older paging、alt enter archive、final screen-frame、resize；若 vterm transaction 缺少 ordered proof，先补 vterm contract/harness 再回 core |
| R327. SK Codex resume 重绘历史与 CJK 回归 | 完成 | `workflow.md`、`termx-core-v2/`、必要时最小触及 `termx-vterm/` | 总结并复现 `/resume` 选择新 session 后 clear+redraw 应重建 authoritative history、二次 `/resume` 应形成新的 session 历史边界、中文宽字符不应被 continuation cell 投影成额外空格，并验证 vterm semantic event 覆盖；先写 harness，再修模型，不写程序名特殊分支 |
| R328. SK clear/repaint session boundary 与 tmux 对齐 | 完成 | `workflow.md`、`termx-core-v2/`、必要时最小触及 `termx-vterm/` | 复现 Codex 类 primary screen app 在切换 session 时 clear 旧画面并 redraw 新历史后，authoritative history 出现旧新混杂或截断的问题；用 tmux 受控序列确认 ED2/ED3/clear+redraw 对 scrollback 的可观察语义，明确 clear 是否只清 mutable current、是否应清除/裁剪 sealed timeline、以及 redraw 是否应作为新 session projection 重写；先补 harness，再修 owner 边界，不写程序名特殊分支 |
| R329. SK vterm 产出全覆盖与 scrollback proof 消费 | 完成 | `workflow.md`、`termx-core-v2/`、必要时最小触及 `termx-vterm/` | 对齐 vterm 解析 PTY 后的虚拟屏幕与 scrollback 产出：凡进入 primary scrollback 的内容都必须作为 ordered proof 或明确边界进入 authoritative history；`il/dl/su/sd/ri/ris/tab/scroll-region` 等 vterm control 不得被 history 静默忽略；先补覆盖 harness，再修消费链 |
| R330. SK Codex resume 编号输出诊断脚本 | 完成 | `workflow.md`、`scripts/` | 新增受控 Python 输出脚本，用 `Sxx 001..100` 这种编号行模拟 Codex `/resume` clear+redraw、多次切换和 CJK 文本，方便人工或 tmux/TermX history dump 对照缺行、混行和中文空格问题 |
| R331. SK screen frame 后普通 prompt 顺序修复 | 完成 | `workflow.md`、`termx-core-v2/` | 复现编号脚本结束后 shell prompt 被插入 `S03` current frame 中间的问题；修复 primary screen app current frame 到普通 stream 的 session 边界和 sealed timeline sequence，使 prompt 排在已关闭 frame 之后 |
| R332. SK frozen history 全量分页与普通 CJK 列坐标修复 | 完成 | `workflow.md`、`termx-core-v2/` | 用编号脚本和 authoritative history dump 复验 Ghostty/tmux 观察；修复 frozen token 只保存 latest 页导致 older 重复最新页的问题，并修复 ordinary stream 按 slice index 写入宽字符导致中文之间出现真实空格的问题 |
| R333. SK Codex 类历史分页 cursor 契约修复 | 完成 | `workflow.md`、`termx-core-v2/`、`internal/protocol/`、`termx-tui-v3/`、按需 `termx-cli/` | 修复 Codex 类 primary screen app clear+redraw 后 copy/history 上滑仍截断或混排的问题；把 older 分页 cursor 的 projection absolute row index 与 logical line 内 row 区分开，TUI 不再用本地 row-in-line 重建 core cursor，并放宽 older prepend boundary 校验与 SourceLines identity |
| R334. SK primary frame 启动时不重复 sealed shell tail | 完成 | `workflow.md`、`termx-core-v2/` | 复现普通 shell 已有 5 条 sealed 输出后启动 Codex 类 primary screen app，latest/history 中 shell tail 被 current primary frame 再投影一次的问题；修复首次/增量 synchronized primary frame 只拥有本 transaction 触达的 rows，不把已 sealed 普通屏幕行纳入 current frame |
| R335. SK full-replace primary frame 启动行归属修复 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-vterm/` | 修复 Codex 类程序启动时 vterm 因大面积 direct damage 产出 full-replace primary frame，core 把整屏 shell tail 再接管为 current frame，导致普通 shell 历史从 5 行变 10 行的问题；vterm 需要透出 direct damage 触达行，core 只按 transaction proof 接管 current frame rows |
| R336. SK ED2 clear-time primary frame scrollback 保留 | 完成 | `workflow.md`、`termx-core-v2/`、按需 `termx-vterm/` | 修复 Codex 内 `/clear` 后当前屏只显示新 Codex 内容，但 history/copy 上滑看不到清屏前 primary frame 的问题；ED2 只能清当前 viewport，不等同 ED3 clear scrollback，core 需要在不重复 sealed shell tail 的前提下保留 primary current frame 离开 viewport 的 clear-time proof |
| R337. SK ordinary prompt 收口使用当前屏幕 proof | 完成 | `workflow.md`、`termx-core-v2/` | 修复 Codex/Claude 类 primary screen app 退出时一闪而过的 transient 行被旧 current frame seal 进 history、同时 shell prompt 恢复后看不到 iTerm2 式旧历史的问题；普通输出恢复前关闭 primary frame 时应使用本 transaction 的 vterm 当前屏幕 proof，并排除本次普通输出触达行 |
| R338. SK pseudo-TUI ED2 stale scroll-out seal 清理 | 完成 | `workflow.md`、`termx-core-v2/` | 修复 primary frame 曾经发生 payload scroll-out 后，后续没有 clear-time proof 的 ED2 repaint 仍靠旧状态把 current tail seal 进 history 的问题；ED2 只清 current ownership，旧屏进入 history 必须来自同一 transaction 的 vterm proof |
| R339. SK iTerm2 式 Codex `/clear` 历史保留 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-vterm/` | 明确 TermX 与 tmux/Ghostty 不同：Codex primary screen app 内部 `/clear` 只清 live viewport，不清 authoritative history；进入 history/copy 后仍能向上看到 clear 前 shell 历史和 Codex 旧 frame |
| R340. SK clear-scrollback 软页边界 | 完成 | `workflow.md`、`termx-core-v2/`、按需 `termx-vterm/` | 按“新建一页，不撕掉前页”的 iTerm2 式无限历史目标处理 ED3/clear-scrollback：它只能失效当前窗口边界，不能物理删除 core-v2 logical-line history；ED2/ED3 组合后 history/copy 仍能向上看到旧页 |
| R341. SK cursor-backed sealed timeline store | 完成 | `workflow.md`、`termx-core-v2/history/`、按需 `termx-core-v2/terminal.go` | 为 10 万行普通输出先落地 cursor-backed store：sealed logical-line payload 可驻留 backend，latest/older/copy 按 cursor/window 读取，不为窗口分页先 materialize 全量 rows；current frame spill 留后续切片 |
| R342. SK 二进制历史 payload 后端 | 完成 | `workflow.md`、`termx-core-v2/history/` | 删除 R341 临时 JSONL 文件后端，改成长度前缀二进制 append-only payload 文件；文件后端不得依赖 `encoding/json`，为后续 mmap/zero-copy/index 文件预留稳定 record layout |
| R343. SK history store Apply 热路径去全量扫描 | 完成 | `workflow.md`、`termx-core-v2/history/` | 修复 10 万行普通输出被 authoritative history store 拖慢的问题；HistoryStore Apply 不得在每个 mutation 后全量 reindex 已有 lines/timeline/frame records，计数器只能随当前 mutation 增量维护，恢复路径才允许全量扫描 |
| R344. SK TUI live refresh latest-only backlog 合并 | 完成 | `workflow.md`、`termx-tui-v3/services/`、按需 `termx-tui-v3/app/` | 修复压力输出期间 TUI 按积压 changed 事件逐帧追屏的问题；普通 `terminal.changed` 只表示 live surface 失效，必须在 service/app 边界合并为 latest-only refresh，不能按固定事件数量分批拉 snapshot 和渲染中间屏 |
| R345. SK TUI live refresh 全链路 perf 抓取与饥饿修复 | 完成 | `workflow.md`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、按需 `termx-core-v2/` | 建立压力输出全链路 perf harness，抓 core changed/protocol refresh/snapshot/app render/FrameSink 写帧计数；修复普通 changed backlog 被无限吞并导致持续输出时 latest screen 刷新不足的问题 |
| R346. SK 生产历史 backend 接入与 live 批次交互上限 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-cli/` | 修复真实 daemon 仍使用内存 history store 导致 10 万行后 RSS 暴涨的问题，并把 core live ingest 单批上限降到 PTY 读粒度级别，让 latest screen 在压力输出期间持续刷新而不是 1MB 一跳 |
| R347. SK live snapshot revision 与普通 refresh 边界 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-tui-v3/`、按需 `internal/protocol/` | 修复 core 已跑完后 TUI 仍可能按旧 snapshot 追帧的问题；live screen 必须有 core 单调 revision，普通 refresh 拉取的 surface 不能伪装成 lifecycle 边界，TUI 能拒绝 stale projection 并只渲染 latest |
| R348. SK live native screen 重构设计与清场清单 | 完成 | `workflow.md`、`termx-core-v2/docs/live-native-screen-rebuild-design.md` | 明确 core 只维护最新 native screen 和 live revision，TUI 自己主导渲染循环；core invalidation 只是唤醒信号，不是 frame delivery；梳理现有 `Snapshot/ScreenUpdate/grid.viewport/LiveSurface` 补丁链路的删除清单和必须保留边界 |
| R349. SK core live native screen contract 清场重写 | 完成 | `termx-core-v2/`、`internal/protocol/`、按需 `termx-vterm/` | 新增明确 `NativeScreenSnapshot` / `LiveScreenInvalidated` / `LiveRevision` 合同；替换 `HistoryGeneration` 承载 live revision 和 nil `terminal.changed` 表示普通 live changed 的临时语义；保留 SurfaceTrack、semantic transaction、PTY response、attachment/resize owner |
| R350. SK 旧 live projection 链路删除 | 完成 | `termx-core-v2/`、`internal/protocol/`、`termx-tui-v3/` | 先删除旧 live projection 运行时路径：v3 live 不再消费 `Snapshot/CompactSnapshot`，core attach channel 不再推 `TypeScreenUpdate` screen stream，`grid.viewport` 与 snapshot overlap trimming 清场；只保留 `live.screen.get` + `terminal.live.invalidated` 的可编译骨架 |
| R351. SK TUI live render loop 接口锁定与重写 | 完成 | `termx-tui-v3/services/`、`termx-tui-v3/state/`、`termx-tui-v3/app/` | 在旧链路删除后，先定接口锁范围：native screen source、live invalidation source、lifecycle source、render request state；再按接口重写 TUI render loop，维护 `wanted_revision + in_flight + dirty`，渲染完再拉 latest；screen、lifecycle、resize-control 消息分离，不再用 LiveSurface union event 和 runtime 旧队列补丁追帧 |
| R352. SK native screen result latest-only 队列语义 | 完成 | `workflow.md`、`termx-tui-v3/app/`、按需 `termx-tui-v3/state/` | 修复压力输出时 core 命令已结束但 TUI 仍按已返回的 native screen fetch result 一帧帧补渲染的问题；普通 `LiveSurfaceMsg` 是 live projection，不是语义帧，runtime 队列必须按 terminal latest-only 合并，生命周期/resize/attach 仍是不可跨越边界 |
| R353. SK live render request revision 去旧唤醒 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/` | 修复旧 invalidation 和已被 latest snapshot 覆盖的 dirty 状态仍触发下一轮 `live.screen.get` 的问题；TUI 只能在 wanted revision 超过 current revision 时继续拉 latest，已渲染 revision 之后到达的旧唤醒必须丢弃 |
| R354. SK live invalidation one-shot arm 接口 | 完成 | `workflow.md`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、按需 `internal/protocol/` | 把 TUI live invalidation 从持续 stream 改成 one-shot arm：TUI 只有在准备好拉下一张 latest screen 时才 arm 一次 callback；callback fired 后自动 disable，期间 core 的 N 次更新只合并成一次 pending，不允许 TUI 拉 N 份 screen 数据 |
| R355. SK FrameSink 完成边界与输入异步化 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/terminalhost/` | 修复真实 LatestFrameSink 只把 frame 入队就触发 afterRender，导致 one-shot callback 过早 re-arm 并继续拉中间 latest screen；FrameSink 必须在完整帧真实写出后才唤醒 live loop，同时 terminal input 发送不得同步等待 ack 卡住 TUI 主循环 |
| R356. SK TUI live loop 迁回 c4ee latest-only 背压 | 完成 | `workflow.md`、`termx-core-v2/docs/live-native-screen-rebuild-design.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/terminalhost/` | 删除 R351-R355 形成的 revision 追赶和 FrameSink 完成回调路径，迁回 `c4ee7923` 的 live refresh 模型：普通 invalidation 只置 `InFlight/Dirty`，fetch 返回期间有 dirty 就再拉一次 core latest screen；runtime 队列和压力处理只渲染最新普通 surface，不补中间 revision |
| R357. SK history disabled live-only 压力验证模式 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-cli/` | 增加显式关闭无限历史的运行模式，用来验证 native live screen 链路；关闭后 core 只维护 live screen/revision，PTY hot path 不构造 history semantic evidence 或 store mutation，history.window/copy 返回 disabled |
| R358. SK core live native 接口迁回 c4ee | 完成 | `workflow.md`、`termx-core-v2/` | 如果当前 live 接口仍把最新屏链路拖进 history semantic transaction，就退回 `c4ee7923` 的 native live surface / ingest queue 接口形状：SurfaceTrack 只持有 VTerm 当前屏，live queue 恢复 1MB latest 批次且无 flush fence；history 语义源从 live surface 中剥离 |
| R359. SK terminal input 异步顺序修复 | 完成 | `workflow.md`、`termx-tui-v3/app/` | 修复真实 tmux stress harness 暴露的长命令字符乱序问题；terminal input 发送仍异步不阻塞主循环，但同一 terminal/view/channel 的 byte stream 必须串行保序，不影响 live latest-only fetch 并发 |
| R360. SK stress memory harness 与 oldest replace 合同修复 | 完成 | `workflow.md`、`scripts/termx_tui_stress_memory.sh`、`termx-core-v2/history/`、`termx-core-v2/` | 修复验证脚本仍统计旧 `core-v2-history/*.compact` 的问题，并修正 core-v2 `history.window mode=oldest` 仍复用 older/prepend 的合同错误；oldest 必须返回 frozen projection head 的 replace window，让 TUI `g` 能直接跳到最老页 |
| R361. SK live invalidation 渲染进度合同清场 | 完成 | `workflow.md`、`internal/protocol/`、`termx-core-v2/`、`termx-tui-v3/services/`、`termx-tui-v3/app/` | 删除 `live.invalidation.next` 中的客户端 rendered revision 参数；core 只能提供 terminal-scoped one-shot wake，不接收或判断 TUI 已渲染到哪一帧，TUI wake 后仍只拉 latest screen |
| R362. SK file-backed history daemon RSS 驻留修复 | 完成 | `workflow.md`、`termx-core-v2/history/`、`termx-core-v2/` | 默认 history enabled 的 1M stress 中 TUI/live RSS 已稳定，但 core daemon RSS 仍随输出显著增长；定位 file-backed HistoryStore 中 payload/index/frozen projection 驻留来源，按 history owner 修复，不用 live 路径兜底 |
| R363. SK live invalidation params codec 回归修复 | 完成 | `workflow.md`、`internal/protocol/`、`termx-core-v2/`、`termx-proto/`、`termx-cli/` | 修复 `live.invalidation.next` 在真实 TUI arm one-shot 时返回 `no protobuf params codec` 的协议回归；锁住 protobuf params codec 与 core protocol dispatch，并用 wire version/socket 分代避开旧 daemon |
| R364. SK stress 输出期间 live 刷新与 copy 输入响应 | 完成 | `workflow.md`、`termx-tui-v3/`、按需 `termx-core-v2/`、`scripts/` | 修复 `generate_terminal_stress.py --lines 1000000` 输出期间 live screen 长时间不刷新、`Ctrl+V` 不能进入 copy mode 的交互饥饿；先定位主循环、live effect、history/copy 请求的 owner 边界，不用定时刷新或本地 history fallback |
| R365. SK live invalidation observed revision 补边沿 | 完成 | `workflow.md`、`internal/protocol/`、`termx-core-v2/`、`termx-tui-v3/`、按需 `termx-cli`、`scripts/` | 用户复现 `generate_terminal_stress.py --lines 100000` 两秒后直接跳最终屏，输出期间没有连续 live 过渡；修复 one-shot arm 间隙丢失 live invalidation 的边沿问题：TUI 只回传上次已观察到的 live snapshot revision，core 若当前 latest 已超过该 revision 立即返回 wake，否则等待下一次 wake；core 仍不接收 rendered revision、不追中间帧，并用 tmux stress harness 验证真实交互 |
| R366. SK high-pressure live refresh 全链路耗时诊断 | 完成 | `workflow.md`、`termx-shared/`、`termx-core-v2/`、`termx-tui-v3/`、按需 `termx-cli`、`scripts/` | 用户复现高压输出下 live 刷新仍只有零点几帧到一帧一帧；补默认关闭的 perftrace 环境入口和全链路计时点，真实 tmux stress 拆出 core live ingest、live.screen.get、protocol、TUI reducer/render、FrameSink 写出耗时；定位到 TUI render pressure gate 因 dirty successor 长时间跳过普通 surface 渲染，按 16ms frame budget 放行 latest-only 压力帧 |
| R367. SK live refresh ready/enable 背压重构 | 完成 | `workflow.md`、`termx-tui-v3/app/`、按需 `termx-tui-v3/render/`、`termx-tui-v3/terminalhost/` | 删除固定 16ms 刷新率假设；TUI 只有在 latest surface 对应的本地 frame 确认写出后才重新 arm 一次 core one-shot live invalidation。core 仍只维护 latest native screen/revision，不接收 rendered revision，不追中间帧，不把 live projection 变成 history truth |
| R368. SK 同 terminal live callback pending 去重 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/` | 用户复现同一 TUI 两个 panel 连接同一个 terminal 后 100K stress 展示变慢；确认已有 frame-ready 目标按 terminalID 去重，再把 per-terminal one-shot callback 的 pending/armed 状态纳入 reducer-owned live refresh 状态，保证同一个 TUI+terminalID 同时最多一个 enabled callback，不靠 effect token 反复取消重挂 |
| R369. SK copy/history 滚轮光标滚动语义 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/input/` | 用户反馈 copy/history 模式鼠标滚轮或键盘滚动体验和 tmux 不一致；将 wheel/PageUp/PageDown/u/d 这类浏览输入收敛为 cursor-driven scroll：先移动 copy cursor，越过可见边界时 viewport 跟随，越过已加载 history window 边界时再请求 older/newer |
| R370. SK copy/latest native 列宽锚定与等待态响应 | 完成 | `workflow.md`、`termx-tui-v3/app/`、按需 `termx-tui-v3/state/`、`termx-core-v2/` | 用户复现 100K stress 后进入 copy/history 卡顿且历史模式软换行与 live 最后一屏不一致；先锁定 copy latest 请求必须按 terminal native screen 宽度冻结 authoritative window，而不是按 pane 可见裁剪宽度重排；protocol latest 在 freeze 追平后读取同一 frozen token window 不再二次 flush，等待 latest 期间只保留可取消/可滚动的 pending 交互，不用 live fallback 伪造历史 |
| R371. SK high-pressure history/copy 重构合同重置 | 完成 | `workflow.md`、`termx-core-v2/docs/live-native-screen-rebuild-design.md`、`termx-core-v2/docs/history-logical-renderer-design.md`、按需 `termx-tui-v3/docs/architecture.md` | 推翻 R371 旧的 storage/flush/bulk reducer 补丁方向，重定 single `SemanticTap`、tap 后 live/history fan-out、ordinary logical-line fast lane、copy entry projection 合同；明确 live 只丢 render wakeup/snapshot，不丢 vterm 状态；history 不 replay raw PTY；copy enter 不复用 `Freeze` 做半追平 token |
| R372. SK single SemanticTap 合同与 harness | 完成 | `termx-core-v2/`、`termx-vterm/`、按需 `termx-core-v2/docs/*` | 定义唯一 vterm owner：顺序消费 PTY bytes 与 resize，维护 latest native screen，产出 immutable semantic transaction，并拥有 OSC/DA/DSR response 回写；用 harness 锁定当前双 vterm 链路问题、resize 顺序边界、response 只回写一次和 live 不丢 emulator 状态 |
| R373. SK SemanticTap fan-out 接入 | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | 真实 terminal PTY 输出改为进入 single tap；live publisher 只发布 latest invalidation/snapshot，可合并 wakeup；history queue 接完整 semantic transaction，不再把 raw PTY text 交给第二个 `vterm.SemanticSource` replay；保留 history disabled live-only 模式边界 |
| R374. SK history semantic transaction backlog 与追平边界 | 完成 | `termx-core-v2/`、`termx-core-v2/history/`、按需 `scripts/` | 把 history 不可丢 backlog 从 raw PTY spool 改成 semantic transaction 或等价 durable queue；暴露 applied/target seq 诊断；history/window/freeze 的完整 authoritative 路径仍可 flush，live hot path 和 copy enter 快速路径不得被 history backlog 反压 |
| R375. SK ordinary LogicalLineSink fast lane | 完成 | `termx-core-v2/history/`、按需 `termx-vterm/` | 在 history renderer 内为普通 stdout 增加 logical-line-first fast lane：WriteSpan append、LF/NEL/IND seal、CR/EL/BS/CUP 修改当前 mutable line、scroll-out proof exactly-once；screen app、alt、ED2/ED3、full replace、primary frame touched rows 保留现有 classifier/frame reducer 边界或复杂 fallback |
| R376. SK copy entry projection 合同 | 完成 | `termx-core-v2/`、`internal/protocol/`、按需 `termx-proto/`、`termx-tui-v3/services/` | 新增 `CopyEntryProjection`/`MaterializedHistoryProjection` 类合同，读取 core 已应用 frontier 但不 flush 全量 backlog；返回 native cols、generation、cursor/boundary、applied/target seq、catchup_pending 和能力位；不得复用 `Freeze` 表达半追平 token，不得把 live native screen 提升为可复制历史 truth |
| R377. SK TUI copy preview/materialized/frozen 三态 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、按需 `termx-tui-v3/services/` | copy mode 进入拆成 entering live preview、materialized projection、frozen authoritative history 三态；preview 只用于即时显示和吞输入，不能搜索/复制/分页；materialized 区间可按能力位选择复制，跨 catchup pending 区域显示 pending 或触发等待；frozen token 继续服务 older/newer/search/full copy |
| R378. SK 高压普通输出 tap/backlog 热路径修复 | 完成 | `workflow.md`、`termx-core-v2/`、按需 `termx-vterm/`、`scripts/` | 用户用 tmux 复现 100K stress：Python 程序约 9 秒已结束，但 TUI 45 秒后仍停在 076419 行，daemon CPU/RSS 持续飙升；修复 single SemanticTap 后普通输出热路径的语义 payload/拷贝/backlog 边界，让 live 最新屏和 DONE marker 不被 history transaction 重 payload 拖住，同时不得恢复 raw PTY replay 或从 live snapshot 反推 history truth |
| R379. SK compact semantic history journal 设计定案 | 完成 | `workflow.md`、`termx-core-v2/docs/compact-semantic-history-journal-design.md` | 用户确认要以 scrollback-like 行生命周期降低 high-pressure semantic history 热路径成本；新增 compact semantic history journal 设计，明确 single SemanticTap 不变、journal 必须来自同一次 vterm semantic pass、live hot path 接近 `WriteForLatestFrame`、普通输出按 line batch、pseudo-TUI 走 mutable frame/boundary state machine，并把后续实现拆成 R380-R386 |
| R380. SK HistoryJournal 合同与覆盖矩阵 harness | 完成 | `termx-core-v2/history/`、按需 `termx-vterm/`、`termx-core-v2/docs/*` | 先定义 history-specific compact semantic journal 类型和 coverage matrix harness：OrdinaryLineBatch、Boundary、ScrollOutProof、FrameEvent 必须来自 single tap semantic pass；覆盖 ordinary、SGR attrs、ED2/ED3/RIS、alt、sync、resize、scroll-out 和 frame proof；禁止 raw PTY replay、live snapshot diff、第二 vterm consumer |
| R381. SK tap latest 快路径去 per-write snapshot clone | 完成 | `termx-core-v2/`、按需 `termx-vterm/` | 修复 single SemanticTap live 热路径仍被完整 semantic result 定速的问题：tap write 后只更新 latest screen、bump revision、产出 compact journal，不在每个 PTY batch 上 deep clone native screen snapshot；`live.screen.get` 请求时再 pull latest snapshot；保留 response owner 和 resize/order harness |
| R382. SK ordinary line batch journal fast path | 完成 | `termx-core-v2/history/`、`termx-core-v2/`、按需 `termx-vterm/` | 普通 stdout 不再逐 `WriteSpan/LF/CR` op 进入 `StreamLineReducer` 热路径；journal 以 sealed logical line batch 和 open-line command 表达普通输出，100K/1M 普通行 harness 验证 line batch 成本、CJK/SGR 属性、CR/BS/EL/CUP 行内编辑和遇 ED/RIS/alt/sync 时退出 fast path |
| R383. SK boundary journal 与状态机 | 完成 | `termx-core-v2/history/`、`termx-core-v2/`、按需 `termx-vterm/` | 落地 Boundary journal 和 history state machine：OrdinaryPrimary、PrimaryMutableFrame、AltTransient、ResizeBoundary；ED2/ED3/RIS/resize/alt enter-exit/sync begin-end 必须按 journal order 处理，不能跨 boundary 合并 ordinary batch，也不能从 live snapshot 回填 history |
| R384. SK frame journal 与 pseudo-TUI mutable frame | 完成 | `termx-core-v2/history/`、`termx-core-v2/`、按需 `termx-vterm/` | pseudo-TUI repaint 只更新 current mutable frame，不逐帧 append history；FrameEvent 只能来自 semantic boundary proof，覆盖 primary replace/archive/clear/final、alt transient、ED2 clear-time proof、sync begin/end 同包 prompt 顺序和 resize fixed-width final frame |
| R385. SK journal backlog 队列与 flush/copy entry 接入 | 完成 | `termx-core-v2/`、`termx-core-v2/history/`、按需 `internal/protocol/`、`termx-tui-v3/services/` | 将 history backlog 从 full transaction queue 迁移为 compact journal queue，保留 terminal-scoped applied/target seq、flush fence、catchup_pending 和 copy-entry materialized projection；journal backlog 是未应用语义命令队列，不得成为第二份 history store |
| R386. SK compact journal 高压压力准入与旧热路径清场 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-vterm/`、`scripts/`、按需 `termx-tui-v3/` | 用真实 tmux/TermX 100K/1M stress 验证 Python 完成时间与 live DONE 接近、copy latest/oldest 正确、daemon RSS/CPU 稳定；清理普通输出 full transaction hot-path 依赖，保留复杂 semantic coverage，不恢复 raw PTY replay 或 double vterm |
| R387. SK stress live 可见延迟指标 | 完成 | `workflow.md`、`scripts/` | 用户复现 10K stress pane 内 Python `time` 约 0.3 秒但肉眼可见 DONE 约 5 秒；给真实 tmux/TermX stress harness 增加 `stress-latency.tsv`，直接记录 `tmux send-keys -> DONE capture 可见`、pane 内 `python real` 和两者差值，作为后续定位 PTY drain、SemanticTap、history journal、TUI render 或 FrameSink 的基线；不得因此恢复 double vterm |
| R388. SK TUI live frame-ready 覆盖丢帧修复 | 完成 | `workflow.md`、`termx-tui-v3/app/` | 修复真实 latest-only FrameSink 下 `LiveSurfaceMsg` 对应帧被后续普通 UI 帧覆盖后不再 arm 下一次 core live invalidation 的问题；frame-ready 以成功写出的 live surface frame 为边界回到 reducer，继续由 terminal revision guard 和 one-shot state 去重，不恢复 double vterm、不把 TUI row 当 history truth |
| R389. SK core live wake 与 journal lazy 边界 | 完成 | `workflow.md`、`termx-core-v2/` | 将 process tap output 的 live invalidation publish 移到 history journal fan-out 之前，并让 `SemanticTapResult.HistoryJournal` 在 history fan-out 阶段 lazy 构造；harness 证明 blocked history Apply 不再阻塞 live wake，但真实 10K stress 仍约 3.08 秒可见，剩余瓶颈继续指向 single SemanticTap/vterm semantic write 与 full ordered ops 构造，不得恢复 double vterm |
| R390. SK TUI live arm 合并与 perftrace 诊断 | 完成 | `workflow.md`、`scripts/`、`termx-tui-v3/app/`、`termx-vterm/` | stress harness 增加 daemon/TUI perftrace；TUI live frame-ready 后对下一次 one-shot arm 做 25ms 合并，减少高压输出下中间 latest screen 拉取/渲染；SemanticSource 将本次 WriteDamage ordered ops 所有权直接交给 transaction，避免 tap 内部额外 deep clone；真实 10K 可见延迟从约 3.08s 降到约 2.06s，copy-oldest 仍正确 |
| R391. SK live one-shot snapshot 与 terminal input 合并 | 完成 | `workflow.md`、`internal/protocol/`、`termx-core-v2/`、`termx-tui-v3/app/`、`termx-tui-v3/services/` | 移除 R390 固定 25ms one-shot arm 等待；core `live.invalidation.next` 在 one-shot 响应中携带同一 SemanticTap 的 latest native snapshot，TUI 直接应用 ready surface，避免 wake 后串行 `live.screen.get` 追中间帧；runtime 将连续普通 terminal key bytes 在 drain 边界合并成一个 ordered input RPC，特殊 raw keys、overlay/copy/prefix 边界仍保留独立消息 |
| R392. SK history journal 批量应用与吞吐 harness | 完成 | `workflow.md`、`termx-core-v2/` | 用户确认 R391 的体感变快主要来自减少 live/TUI 帧而不是 history ingest；本切片只优化 core-v2 history consumer：对同一 worker batch 内可顺序应用的 compact journal 合并成一次 store Apply/storage flush，并补 100K 普通输出 history-only benchmark，证明 history 侧吞吐，不恢复 double VTerm、不修改 TUI live consumer |
| R393. SK 回退 one-shot snapshot live 低帧率回归 | 完成 | `workflow.md`、`internal/protocol/`、`termx-core-v2/`、`termx-tui-v3/app/`、`termx-tui-v3/services/` | 用户反馈 R391 后 live 输出非常卡、帧率很低；回滚 `live.invalidation.next` 直接携带 native snapshot 并转 ready surface 的路径，恢复 one-shot 只发 wake、TUI 再按背压拉 latest screen，并恢复 R390 的小合并窗口；保留 terminal input bytes 合并，不恢复 double VTerm |
| R394. SK 移除 live arm 固定 25ms 延迟 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/state/` | 用户反馈实际执行约 2 秒但展示七八秒，固定 25ms arm 合并窗口在多轮追 live 时会累积成人为尾延迟；移除固定 sleep，合并只保留 runtime 队列 latest-only、`InFlight/Dirty` 和 `Armed` 去重状态机；同时 dirty in-flight 返回的普通 live surface 不再先渲染过期中间屏 |
| R395. SK live wake-only tap queue coalescing | 完成 | `workflow.md`、`termx-core-v2/` | R394 证明去掉固定 sleep 后 TUI 会追大量中间 revision；恢复 `live.invalidation.next` 在返回 wake 前只 flush 调用时已进入 core tap queue 的 PTY payload，把 wake 合并到当前 latest revision，但不携带 snapshot、不等待 future output、不等待 history/render |
| R396. SK live SurfaceTrack 与 history semantic consumer 重拆分 | 完成 | `workflow.md`、`termx-core-v2/`、相关 docs、`scripts/termx_tui_stress_memory.sh` | 用户真实观察 R395 后仍出现序号从 600 多跳到 40 万、画面中间停住、pane 内显示约 2 秒但手表实测总展示约 12 秒；这说明 core 计时/perftrace 和 TUI 观察窗口不等价。恢复 f3c6070 附近的 live 快路径：live consumer 用 `SurfaceTrack.WriteForLatestFrame` 独立维护 latest screen 与唯一 PTY response owner，history consumer 独立消费同一 PTY bytes 生成 compact journal；history truth 仍只来自 core-v2 logical-line store，不能从 live rows、TUI rows 或 raw parser fallback 推断；同时修正 stress harness 的 DONE marker 回显误判，避免用假可见延迟验收。 |
| R397. SK live consumer queue 硬隔离与 live-only 手测入口 | 完成 | `workflow.md`、`termx-core-v2/`、`scripts/termx_tui_stress_memory.sh` | 本轮只收敛 live consumer，不改 history truth。`terminalLiveIngestQueue` 改为固定页 pending 队列，live queue 与 history tap queue 都不再因 backlog drain 或 append 扩容在锁内移动大 slice，降低 history consumer 慢时反向拖住 PTY reader 的风险；stress harness 新增 `--history-disabled`，可用同一 tmux 真实路径只测 live SurfaceTrack DONE 可见时间，默认历史准入保持不变。 |
| R398. SK synchronized repaint 不封存中间帧 | 完成 | `workflow.md`、`termx-core-v2/` | 用户截图显示 history 模式中 Codex 类原地更新被重复写成多段历史，说明 synchronized output begin 被误当作 screen app session boundary。修正 terminal classifier：连续 synchronized repaint 只能替换 mutable current primary frame，不能 close/archive 中间帧；只有普通输出恢复、alt boundary 或 process close 才能把 primary frame 写入 sealed timeline。 |
| R399. SK screen-frame 默认空格布局保留 | 完成 | `workflow.md`、`termx-core-v2/history/` | 用户反馈进入 history 后空格被吞；screen-frame 是 fixed-grid payload，行内和行尾经过 PTY 写入的 default blank 属于屏幕布局，不能按 ordinary logical line 的 trim 规则裁掉。修正 frame reducer：screen-frame row 保留 default blank cells，只裁整屏尾部纯空白行。 |
| R400. SK history 空格吞噬链路 trace | 完成 | `workflow.md`、`termx-core-v2/`、`termx-tui-v3/state/` | 用户手测 R399 后空格依然消失；本切片只加诊断不改语义。在 `TERMX_HISTORY_TRACE=1` 下记录 vterm semantic frame、core history window、TUI window/apply/render-visible 的 `cells/width/trailing spaces/text` 摘要，用于定位空格在哪个 truth 边界丢失。 |
| R401. SK cursor-addressed repaint 归属修复 | 完成 | `workflow.md`、`termx-core-v2/` | 自测复现 Codex 类无 DEC 2026 的 `CUP home + repaint` 被当 ordinary committed 重复写入 history，导致 history 模式出现多帧中间状态和空格/排版错位表象；classifier 对带 `PrimaryFrame` proof 的 cursor-addressed primary repaint 归入 mutable screen-frame，只接管 touched rows 并替换 current frame，不 archive 中间帧。 |
| R402. SK copy/history 进入态顶部空白显示锚点 | 完成 | `workflow.md`、`termx-tui-v3/` | 用户截图显示 live 画面顶部有空白但进入 history 后重复 prompt 被挤到顶部；TUI 进入 history 时不能把 live viewport 顶部空白写成 history truth，也不能直接裁掉。copy mode 保存 display-only `ViewportTopPadding`，renderer 用它恢复首屏顶部空白，用户滚动/搜索/resize/分页后清零。 |
| R403. SK logical-line 构建补丁路径清场与 guard | 完成 | `workflow.md`、`termx-core-v2/`、按需 `termx-tui-v3/` | 删除或隔离现有 logical-line/history 构建里的补丁式路径、诊断残留和旧 fallback，补清场 guard；删除后必须保持可编译，不留下双 truth、raw parser replay、live snapshot/history 拼接或程序名特殊分支。 |
| R404. SK semantic journal 到 logical-line reducer 重建 | 完成 | `termx-core-v2/history/`、按需 `termx-vterm/` | 先用 domain harness 重建 compact semantic journal -> logical-line reducer：ordinary output、CUP repaint、sync repaint、ED2/ED3、alt、resize、final frame、fixed-grid spaces、repeated prompt 和 10 万行 stress 都必须由同一模型覆盖。 |
| R405. SK terminal/vterm 真实接入与边界锁定 | 完成 | `termx-core-v2/`、`termx-vterm/`、按需 `internal/protocol/` | 把重建后的 reducer 接入真实 PTY/vterm semantic transaction，锁住 ordinary、primary frame、alt transient、clear、resize、final frame 的 owner、truth source、消息链路和失败条件。 |
| R406. SK tmux correctness/perf harness | 完成 | `scripts/`、`termx-core-v2/`、`termx-tui-v3/`、按需 `termx-cli/` | 建立真实 tmux harness，对比 PTY 可观察输出、TermX live capture、history-dump、copy-oldest/copy-latest；记录程序实际耗时、DONE 可见耗时、history 可查询耗时、daemon/TUI CPU/RSS。 |
| R407. SK history consumer 极致性能优化 | 完成 | `termx-core-v2/history/`、`termx-core-v2/`、按需 `termx-vterm/` | 针对 10K/100K/1M 高压输出优化 history consumer，目标接近 live SurfaceTrack 快路径；禁止靠减少 TUI 刷新帧、丢 history 事件或 weak harness 假装变快。 |
| R408. SK 子 Agent review 与完整准入 | 完成 | `workflow.md`、相关改动目录 | 使用子 Agent 做只读 review，重点检查补丁式 fallback、双 truth、live/history 反向耦合、正确性缺口和性能热路径；主 Agent 处理 findings 后跑完整准入并提交。 |
| R409. SK history current-frame 屏幕行锚点合同 | 完成 | `workflow.md`、`termx-core-v2/history/`、`termx-core-v2/`、`internal/protocol/`、`termx-tui-v3/` | 修复 live 当前屏进入 history 后顶部空白丢失、原地屏幕布局被压扁并混入旧 prompt 的问题；screen-frame 行号必须来自 core frame journal 的 authoritative screen row 元数据，经 protocol 传给 TUI display projection，不能恢复 live snapshot/top-padding 补丁或从 TUI rows 反推 history。 |
| R410. SK Codex current-frame 历史入口视口锚定 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/` | 普通 shell history 已正常，但打开 Codex 后 latest window 同时含旧 shell rows 和 current primary frame，进入 history 时仍按 newest tail 选 viewport，导致旧 shell rows 被塞到 current frame 上方。入口视口必须用 core authoritative `ScreenRow` 反推可保留的 frame 前最近 rows，并由 renderer 只补缺失的 display-only 空白；不得恢复 live snapshot、程序名分支或第二份 history truth。滚出当前屏时不得让 copy-history 增量 patch 把旧帧残留和 older rows 拼错。 |
| R411. SK copy/history 连续视口坐标模型恢复 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、按需 `scripts/` | 用户确认正确性优先，当前 R409/R410 的 `ScreenRow` display padding、cursor-first 滚动和 patch 双坐标仍导致 Codex 历史重排/重复。参考 `6cc64c0c55c44e0152b0748caa24145dbe82f87a` 文件里的正确模型，TUI copy/history 恢复单一连续 row viewport：latest entry 可用 live snapshot 只做视口锚点匹配，但 renderer 不插 display-only padding；滚轮/PageUp/older prepend 恢复 viewport-first；patch 只能在连续坐标下启用。先补 reducer/render/patch harness，再改实现，性能优化以不破坏 authoritative logical-line truth 为前提。 |
| R412. SK 1M stress live/copy-latest 追平失败定位 | 完成 | `workflow.md`、`scripts/` | R411 的 1M live/copy-latest 失败先定位为 harness 观察条件不严：脚本只等 copy mode 状态栏就截图，可能截到刚进入历史模式的空白/提示屏。本切片把 copy-latest 准入改为等待最新 stress 行和 DONE marker 真正出现在历史视图，再复跑 10K/100K/1M；live 与 copy-latest 均能追上，剩余慢点收敛为 full `history-dump` 诊断导出路径。 |
| R413. SK current screen 历史入口改由 PTY 语义投影自洽 | 完成 | `workflow.md`、`termx-core-v2/history/`、按需 `termx-tui-v3/`、`scripts/` | 用户截图显示 live 当前屏正常，但进入 history 后旧 shell rows 被填进 current screen app 上方。正确性不能依赖 TUI 与 live surface 匹配；history 入口应只消费 core-v2 从 PTY semantic 得出的 authoritative current frame/window 结论。对比 `6cc64c0c55c44e0152b0748caa24145dbe82f87a` 的文件行为，先确认当时 core history 如何让 current primary screen 的布局行与旧 shell scrollback 在同一 projection 中自洽，再补 harness 修复当前入口，不允许用 live surface 校验、renderer padding 或程序名分支掩盖。 |
| R414. SK current-frame 内部空白与入口锚点修复 | 完成 | `workflow.md`、`termx-core-v2/history/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 用户复现输入 `codex` 后进入 history，current screen app 布局被旧 prompt 顶下并压缩空白行。core frame reducer 对 touched-row primary frame 只补已接管范围内部的 default blank gap，保留 fixed-grid 布局且不接管非空 shell tail；TUI latest/materialized 入口用 core authoritative current-primary-frame `ScreenRow` 计算初始 viewport top，丢弃过量旧 rows，但 renderer 仍只渲染连续 authoritative rows，不从 live surface 匹配、不插 display padding。 |
| R415. SK primary scroll-out 不重复接管 sealed shell tail | 完成 | `workflow.md`、`termx-core-v2/` | 用户手测确认不退出 Codex 也会在 history 中把之前 shell/ls 区域重复到 Codex frame 前面；authoritative dump 显示重复已经存在于 core history，而不是 TUI 展示层。修正 synchronized primary frame 带 scroll-out proof 时的 ownership：没有 ED2 clear 等明确清场边界时仍只接管本 transaction touched/scroll-shifted rows；新 primary frame 从已 sealed shell tail 上启动时，transaction 开头顶出的旧 shell proof 不再 seal 第二遍，但同一次 screen app 输出自己的 scroll-out 和最终 frame rows 仍保留。 |
| R416. SK latest/freeze current-frame screen-row 入口锚点修复 | 完成 | `workflow.md`、`termx-core-v2/history/`、`termx-core-v2/` | 用户复现普通 shell rows 已 sealed，进入 Codex 类 primary current frame 后 copy/history latest 仍把过多旧 prompt 塞到 current frame 前。core latest/freeze 入口必须使用 current primary frame authoritative `ScreenRow` 计算最多可接入多少 sealed tail；`ScreenRow=0` 表示 current frame 已顶到屏幕第一行，不能再接旧 rows。更早 sealed rows 仍通过 older cursor 可达；R376 `CopyEntryProjection` 半追平预览继续读取已应用 frontier，不伪装成正式 frozen latest。 |
| R417. SK primary pseudo-TUI 语义分类扩展 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-vterm/` | 用户判断重复根因在 parser/classifier 边界：primary screen pseudo-TUI 被当 ordinary stream 写入，又以 current frame 展示。vterm frame attach 只能按 ordered semantic ops 判定，普通单调 stdout 不因 live diff touched rows 升级成 frame；CR+EL、相对光标、字符/行编辑、clear/scroll/copy rect 等主屏改写语义必须携带 PrimaryFrame+touched-row proof。core classifier 将这些“改已有屏幕内容”的 transaction 归到 mutable primary frame session，并继续只发布 touched rows，不按程序名、不做 history-mode 文本去重。 |
| R418. SK deep research touched-row proof 与 history render guard | 完成 | `workflow.md`、`termx-vterm/`、`termx-tui-v3/render/` | 处理 `/Users/lozzow/Downloads/deep-research-reportx.md` 对 R417 的反馈：R417 仍让 `PrimaryFrameTouchedRows` 直接来自 direct damage touched rows。非 full-replace primary pseudo-TUI 的 ownership proof 必须优先由 ordered semantic ops 推导，full-replace 才保留 direct damage proof；TUI render VM 需要 guard 证明 `HistoryRenderable()` 后同一 pane 的 live surface 不会混入 copy-history 内容。 |
| R419. SK screen-backed history 勘察与工作流重置 | 完成 | `workflow.md`、`AGENTS.md`、`termx-core-v2/docs/screen-backed-history-rebuild-design.md` | 按用户新目标确认当前 PTY/vterm/HistoryJournal/HistoryStore/TUI window 链路，记录 pseudo-TUI frame/touched rows 仍直接变成 logical history 的位置；把活动基准改为 ScreenHistoryBuffer physical row/cell ingest truth，logical line 只作为 HistoryWindow/Copy/Search projection，并拆出 R420-R427 迁移切片。 |
| R420. SK ScreenHistoryBuffer domain model 与基础 harness | 完成 | `termx-core-v2/history/` | 只引入 screen-backed domain model 和单元测试，不接 terminal/TUI：`PhysicalRow`、`Cell`、RowID、Version、OwnerKind、main screen、cursor、CR/LF/BS/TAB、printable text、basic scroll-out、seq idempotency 和 seal-once invariant。 |
| R421. SK ScreenHistoryBuffer VT 控制语义覆盖 | 完成 | `termx-core-v2/history/`、按需 `termx-vterm/` | 在 screen buffer 层覆盖 CUP/HVP/CUU/CUD/CUF/CUB/CHA/HPA/VPA、EL/ED/ECH/DCH/ICH、IL/DL/SU/SD、DECSTBM、alt-screen 1047/1048/1049、sync 2026 boundary、wide char continuation；缺失控制序列必须有 TODO 和防重复 seal 测试。 |
| R422. SK physical row 到 logical line projection | 完成 | `termx-core-v2/history/` | 新增 projection layer：从 committed physical rows + current main/alt screen rows 生成 logical lines/HistoryWindow rows，保留 RowIDs、Wrapped/Continued、Version，按 RowID 去重，latest window 不重复拼 current screen 与 sealed history。 |
| R423. SK terminal history ANSI harness 与 fixtures | 完成 | `scripts/` 或 `termx-core-v2/cmd/history-harness/`、`fixtures/history/`、按需 `termx-core-v2/history/` | 新增可 debug harness，接收 ANSI 文件或 stdin，经过 vterm semantic source 和 ScreenHistoryBuffer 后 dump current screen、committed physical rows、projected logical lines；每行带 RowID、Version、OwnerKind、Sealed、Wrapped、text，并补 plain shell、pseudo-TUI、progress、alt、scroll region、wide char fixtures。 |
| R424. SK primary pseudo-TUI path 接入 screen buffer | 完成 | `termx-core-v2/`、`termx-core-v2/history/`、按需 `termx-vterm/` | 把 CUU/EL/DCH/ICH/IL/DL/SU/SD、CUP repaint、sync repaint 等 primary pseudo-TUI path 从 journal logical/frame reducer 切到 ScreenHistoryBuffer；普通 shell append path 可暂留旧逻辑，但 pseudo-TUI 不能再直接 append committed logical rows。 |
| R425. SK HistoryWindow/Copy screen-backed projection 接入 | 完成 | `termx-core-v2/history/`、按需 `internal/protocol/`、`termx-tui-v3/` | HistoryStore 新增或迁移为 screen-backed implementation，latest/older/newer/freeze/copy 从 screen snapshot + committed physical rows 投影；保留外部 HistoryWindow/CopyMode 接口和 TUI authoritative boundary。 |
| R426. SK 旧 journal 正文 reducer 收缩与清场 | 完成 | `termx-core-v2/history/`、`termx-core-v2/`、按需 `termx-vterm/` | 将 journal 长期职责收缩为 boundary/meta/event backlog，删除或隔离 pseudo-TUI 正文 truth、frame snapshot append、logical-line ingest truth 旧路径；保留必要 boundary、process、clear、alt、sync 事件。 |
| R427. SK screen-backed history review 与完整验收 | 完成 | `workflow.md`、相关改动目录 | 进行 Architecture、VT Semantics、History Correctness、Performance、Test/Harness 五类审核；跑 `go test ./...`、r328/r331/r334、新 screen model 单测、harness fixture、Codex-style synthetic、alt/progress/latest RowID 去重和无进程名特判准入。 |
| R428. SK screen-backed production 默认入口切换 | 完成 | `termx-core-v2/`、`termx-core-v2/history/`、按需 `termx-vterm/` | Terminal 默认 history path 必须创建同一个 `ScreenHistoryBuffer`，journal/transaction consumer 写入 screen buffer，HistoryWindow/Freeze/Copy 默认读取 `ScreenBackedHistoryStore`；普通输出也不能继续只写旧 logical-line store。 |
| R429. SK screen-backed physical row backend 与恢复 | 完成 | `termx-core-v2/history/`、按需 `termx-core-v2/`、`termx-cli/` | 为 sealed physical rows 建立 production storage/backend 与 recovery/window 分页边界，避免无限历史退化为内存列表；backend 只能承载 physical row payload/index，不能成为第二份 history truth。 |
| R430. SK 旧 mutation-backed history store 清场与文档收敛 | 完成 | `workflow.md`、`termx-core-v2/history/`、相关 docs | 在默认 screen-backed path 和 storage 站稳后，删除或冻结旧 logical-line ingest/store 默认路径，更新 architecture/design 文档中过期 logical renderer/store 表述，保留必要 legacy test 只能作为迁移背景。 |
| R431. LL vterm ungated EvictedRows seal 信号 | 完成 | `termx-vterm/`、`termx-core-v2/semantic_tap.go` | 用户确认不再修补 screen-backed 路径，无限历史按"单一真值 + seal-on-eviction"重做（设计见 `/Users/lozzow/.claude/plans/quiet-mapping-whistle.md`）。vterm 暴露不带 PrimaryScrollOut gate 的 `TerminalSemanticTransaction.EvictedRows`，无条件携带每次 write 真正离开 primary 可见区的物理行（cells/runs + Wrapped），并补齐 eviction 完整性：空行保留、alt 归属按有序 damage 流、shape-changed 批次不丢滚出、ring 关闭仍可用。 |
| R432. LL logical-line 拼装与文件存储 domain | 完成 | `termx-core-v2/history/linehist/`、`termx-vterm/internal/vt/` | 从滚出物理行 + Wrapped 标志拼 logical line（宽度无关，不存 fixed grid），append-only 二进制文件 + offset 索引 + 绝对序号分页读；纯合成事务 harness，不接 terminal/TUI。顺带修复 charmvt 宽字符 autowrap 丢字。 |
| R433. LL 接 Terminal 与 HistoryWindow 投影 | 完成 | `termx-core-v2/`、`termx-core-v2/history/linehist/` | linehist `Store` 实现 `history.HistoryStore`：冷段 = logical-line 文件按请求 cols 惰性 prefix-sum 索引投影，热段 = ingest gate 内采集的 emulator 当前屏 + OpenTail 拼接；Terminal 在 tapOpMu 临界区内消费 tap 事务 EvictedRows，查询共享同一 gate 保证不重不漏；`LineHistoryStoreFactory` 为显式接入入口，协议与 TUI 零改动。 |
| R434. LL 边界：resize / ED3-RIS / alt-screen | 完成 | `termx-core-v2/history/linehist/`、`termx-vterm/` | resize 滚出走 EvictedRows（ring 关闭无反拉回，无需待定尾）；ED3/ClearScrollback 落 append-only boundary 分段（frozen token 跨 clear 有效，RIS 不清历史）；alt 期间 primary 屏保存行经 `PrimarySavedScreenRows` 继续投影为 mutable 时间线尾部。 |
| R435. LL 验收：fixture + 回归等价 + e2e + stress | 完成 | `fixtures/history/`、`termx-core-v2/`、`termx-vterm/` | 真实 vterm 事务喂 ANSI fixtures（每行恰一次、CR/pseudo-TUI 重绘不污染、分包不变式、alt 瞬态排除、宽字符保真）；r328/r331/r334 行为期望在 linehist 重建等价断言；2 万行 stress 投影精确 + 冷历史跨重启不丢不重；顺带修复 ED2 滚出行缺失 EvictedRows（ControlDamage.ScrollOut 并入 eviction 流）。真实 daemon+TUI e2e 留到 R436 切默认后手动跑。 |
| R436. LL 切默认入口与删旧 screen-backed 路径 | 完成 | `termx-core-v2/` | Terminal 默认 history 引擎切到 linehist；移除 terminal 侧旧 journal/classifier/screen-backed runtime 队列与内部架构测试，保留 `history/` 旧包到后续清场 slice 处理（本切片不混入既有 `screen_buffer*` 未提交改动）。 |

## 7. 测试准入

每个切片提交前必须运行本切片相关命令。测试无法运行时，最终说明必须写清原因。

- 文档-only：`git diff --check`
- core-v2 改动：`cd termx-core-v2 && go test ./... -count=1`
- vterm 改动：`cd termx-vterm && go test ./... -count=1`
- tui-v3 改动：`cd termx-tui-v3 && go test ./... -count=1`
- protocol 改动：`go test ./internal/protocol/... -count=1`，如果修改 `.proto` 还必须同步生成物并运行相关生成/descriptor 测试
- CLI 守卫改动：运行对应 focused Go test；如果不确定，用 `cd termx-cli && go test ./cmd/termx -count=1`
- 任意提交前都要运行：`git diff --check`

## 8. 提交规则

- `/goal` 自动模式下，每个完成切片必须使用中文提交信息提交。
- 用户明确要求不要提交时，按用户最新指令执行，并在最终说明未提交。
- 不得 amend commit，除非用户明确要求。
- 不得使用 destructive git 命令。
- 不得覆盖用户或其他代理的未提交改动；发现冲突时停下说明。

## 9. 当前状态

- `R436` 已完成：默认 history 入口切到 linehist：`server.newHistoryStore` 不再创建 `ScreenHistoryBuffer`/screen-backed store，未配置 factory 时使用 `LineHistoryStoreFactory` 和 server 级临时/配置目录；`Terminal` 删除旧 history renderer / journal renderer / screenHistory / historyQ 字段，PTY/resize 在 `tapOpMu` 内直接把 vterm `EvictedRows` 交给 linehist，`FlushHistory` 只需等待 history tap 队列，process exit/remove 仅 `SealOpenTail`/`Close`。移除 terminal 侧旧 journal ingest worker 与耦合内部实现的 r301/r327/r328/r334/r392/queue 测试，保留旧 `history/` 包本体到后续清场 slice（本切片不混入既有 `history/screen_buffer*` 未提交改动）。旧 screen-backed ownership 断言改为 linehist 内容/顺序断言：protocol window 不再强制 committed segment，tmux/ED2/prompt-order harness 只断言可观察历史内容不丢、不重、不插队、alt 瞬态不污染；新增/更新 R436 默认 linehist store 与 storage-dir recovery 测试。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R435` 已完成：linehist 验收切片（fixture + 回归等价 + stress），并顺带修复一处 vterm eviction 完整性缺口。**fixture 级**（`history/linehist/fixture_test.go`，真实 vterm 事务喂 `fixtures/history/*.ansi`）：plain-shell 每行恰一次且有序；progress-cr-repaint / pseudo-tui-repaint 的 CR/光标重绘中间态不进历史、只留最终态；codex_pseudotui 已滚出的 shell 尾部（sealed truth）不被 pseudo-TUI 重绘复制、被 2K 擦掉的屏上行不进历史、且按字节分包 ingest 与单次写入投影完全一致（分包不变式）；scroll-region 不重复；alt-screen 瞬态不留在历史；宽字符跨滚出/落盘/窄列重投影不撕裂（codex_pseudotui.ansi 本切片入库）。**stress 级**（`history/linehist/stress_test.go`）：2 万行写入后 latest/oldest/collect-all 全序精确（0.1s 量级，冷索引首查按 cols 一次性 O(N) 扫 header 建 prefix，可接受）；5000 行跨 Close/reopen 冷历史逐行不丢不重（仍在屏上的行随 vterm 生命周期消失，属 seal-on-eviction 口径）。**server 级回归等价**（`r435_linehist_acceptance_test.go`，对应旧 r328/r331/r334 行为期望）：多会话 ED2 clear 之间的内容全部留在可翻页历史且有序恰一次；屏内重绘后的 prompt 不插到已滚出内容之前；codex fixture 走完整 Server 逐字节 ingest 不重复 sealed 尾部；CJK 跨冷热段窗口/Freeze/Copy 逐字保真。**顺带修复（vterm）**：ED2（`ClearWithScrollback`）的滚出行只随 `ControlDamage("ed").ScrollOut` 携带、不走 `recordScrollbackLine`，此前完全进不了 `EvictedAppend`→`EvictedRows`，`clear` 前的屏幕内容会从 linehist 历史凭空消失；`evictedAppendOpsFromCharmVTDamages` 现按流内顺序并入 ControlDamage.ScrollOut（沿用 ModeDamage alt 归属跟踪，alt 内 ED2 归 alternate 不混入主历史），回归测试 `semantic_source_evicted_test.go`（TestEvictedRowsCoverED2ClearScreen / TestEvictedRowsED2InsideAltStaysOutOfPrimary）。真实 daemon+TUI 的 codex 会话 history-dump 对比是手动 e2e，留到 R436 切默认入口后跑更有意义；`cmd/history-harness` 仍用旧 `ScreenHistoryBuffer`，R436 一并处理。已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。- `R434` 已完成：linehist 三类边界语义落地，全部沿用"单一真值 + 投影"路线，不引入对账。**resize**：调查确认生产 tap 关闭 scrollback ring（size=0）后 `internal/vt/reflow.go` 的变高反拉回分支（`if sb != nil` gate）不生效，resize 事务的滚出行已经由 `resizeWithDamageLocked` 的 `ScrollbackAppend`→`EvictedRows` 兜底路径完整携带——计划里的"待定尾"规则不需要，resize 只补验收（变窄/变高/连续 resize 后 collect-all 不重不漏不乱序）。**ED3/ClearScrollback**：`LineFile` 增加 kind=2 boundary 记录（`AppendBoundary`/`Base`，恢复扫描与顺序读都识别并跳过），文件保持 append-only、记录读取保持绝对序号；`Engine.ClearHistory` = `Assembler.DiscardOpen`（未闭合尾部的头概念上在被清的 scrollback 里，屏上续行由当前屏继续投影）+ 落 boundary，`VisibleLineRange` 暴露分段后可见区间；`Store.ApplyTransaction` 在 EvictedRows 落盘**之后**处理 `tx.ClearScrollback`（`clear` = ED2 挤滚出 + ED3 清空在同一次写入，先滚出后清空才与真实终端一致）；投影/窗口数学转到"视图域"（coldBase+coldCount），LineID 在 clear 后从 1 重新计数，frozen token 记录 coldBase 且文件读走绝对序号，因此跨 clear 仍可窗口/复制；RIS 刻意不清历史（与 xterm 默认一致，本 emulator 的 ED3 同时清屏属 vterm 既有行为，历史照实投影）。**alt-screen**：不在 alt-enter 时 seal（退出 alt 后程序仍可改写主屏行），vterm 新增 `PrimarySavedScreenRows`（`internal/vt` `PrimaryLine/PrimaryLineWrapped/PrimaryHeight` + `vterm.go` 转换包装，live 读投影而非第二份 truth），`ScreenSnapshot` 在 alt 期间带 `PrimaryRows`，`hotRowsFromScreen` 把 openTail+主屏保存行拼成 mutable current-primary-frame 行后再接 alt fixed-grid 行——alt 期间主屏时间线尾部保持可见且退出后不重复。准入：`termx-vterm/vterm/primary_saved_screen_test.go`、`history/linehist/store_boundary_test.go`（ED3 隐藏冷段+LineID 重计数、ED2+ED3 全清、boundary 跨 close/reopen 持久、frozen token 跨 clear、RIS 不清、resize 变窄/变高 collect-all 精确、alt primary 尾部可见且 mutable）、`r434_linehist_boundary_test.go`（真实 Server：ResizeTerminal 前后不重不漏、clear 后 live 清空 frozen 保留、alt 期间 primary 尾部可见退出后恰一次）。已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R433` 已完成：linehist 接入真实 Terminal ingest 链路并交付完整 `history.HistoryStore` 投影。`history/linehist/store.go` 的 `Store` 是单一真值查询层：冷段 = R432 append-only 文件里的宽度无关 logical line，按请求 cols 用惰性 prefix-sum 行索引（`coldRowIndex`，1024 条/批延伸，`countWrappedRows` 不 materialize cells）投影成绝对稠密 `ProjectionRowIndex` 的 HistoryRow；热段 = 查询时刻 emulator 当前屏（`ScreenSnapshotFromVTerm`：VisitTrimmedScreenRows + ScreenWrapped，尾部空白行裁剪）与 Engine `OpenTail` 按 Wrapped 标志拼回 logical line 后按 cols 重新换行（primary mutable current-primary-frame；alt 期间按 fixed grid 投影 current-alt-frame，openTail 单独成行）。一致性由 ingest gate 保证：Terminal 在 tapOpMu 临界区内 `ApplyPTYWrite` 之后调用 `Store.ApplyTransaction(tx.EvictedRows)` 落盘；`Bind` 把 `SemanticTap.LineHistoryScreenSnapshot` 与 `&terminal.tapOpMu` 注入 store，查询在同一 gate 内采集 coldCount+OpenTail+屏幕快照后释放（冷段是不可变前缀，文件分页读不阻塞 ingest），滚出行在冷/热段间不重不漏；锁序固定为 historyMu → tapOpMu → store.mu，Resize 走 `lineHistory.ApplyTransaction` 直接消费避免旧 resize 队列的锁序倒置，`forceCloseHistory`/`closeWithReason` 映射到 `SealOpenTail`/`Close`。Latest/Older/Oldest/Newer 窗口数学与旧 backend 路径逐字段对齐（BeforeRowIndex 绝对行号分页、boundary cursor 语义），Freeze 只记 coldCount + materialize 当时热段（token `linehist-N`，冷段前缀不可变故文件读有界），Copy 按 LineID 首行区间取行文本，`Apply(mutation batch)` 为 no-op（写 truth 唯一入口是 ApplyTransaction），journal/classifier fanout 在 lineHistory 存在时被旁路。`server.LineHistoryStoreFactory(dir)` 是显式接入入口（默认入口切换留给 R436）；`charmbracelet/x/ansi` 因 grapheme 展开转为 go.mod 直接依赖。准入：`history/linehist/store_test.go`（真实 SemanticSource + gate harness：冷热拼接不重不漏、older 分页闭环、宽/窄 cols 重投影、openTail 与屏上续行拼回、alt fixed-grid、freeze 稳定 + copy 区间、空 store/无效 cursor）与 `r433_linehist_terminal_test.go`（真实 Server+Terminal ingest：latest 恰一次、30 行分页闭环、软换行按查询 cols 拼回、freeze/copy 跨冷热段、alt 内容不进 committed 且退出后不残留）。已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R432` 已完成：新包 `termx-core-v2/history/linehist` 交付 logical-line 拼装与文件存储 domain（不接 terminal/TUI）。`Assembler` 是纯拼装器：消费 vterm `EvictedRows` 的滚出物理行，按 `Wrapped` 软换行标志把续行并入未闭合行、硬换行闭合为一条宽度无关 logical line（styled runs + HardEnd，不存 fixed grid/ScreenCols，查询时按当前 cols 重新换行）；空行保留为空 Runs 硬行；硬结束行尾无样式空格 padding 修剪、带样式/link 的尾部空白保留；病态超长行按 `DefaultMaxOpenLineBytes` 落 HardEnd=false chunk。`LineFile` 是每 terminal 一个的 append-only 二进制文件（record magic "TXLL"，kind=1 正文、kind=2 预留 R434 boundary），内存只留 offset 索引，恢复只扫 header 并截掉崩溃残留的半截尾记录，`Lines(start,end)` 按绝对序号分页读。`Engine` 组合两者：`ApplyEvictedRows` 批量落盘、`OpenTail` 暴露未闭合尾部给投影层、`SealOpenTail`/`Close` 在边界把尾部按硬结束落盘。合成事务 harness 用真实 `NewSemanticSource(cols,rows,0,nil)`（与生产 tap 同样 ring 关闭）验收：普通滚出每行恰一次、软换行拼回、空行保留、alt 滚动不进主历史、宽字符跨行拼回、close/reopen 冷历史不丢不重。顺带修复 vterm `internal/vt/utf8.go` 宽字符 autowrap 两处丢字：行尾剩余列不足一个宽字符时整字软换行（原先直接被 SetCell 丢弃）、宽字符恰好填满行尾时 pending-wrap 判定按 cell 结束列（x+Width）算（原先 x>=width-1 判不中导致下一字符擦掉行尾宽字符），回归测试在 `internal/vt/wide_wrap_test.go`。准入已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R431` 已完成：无限历史按用户决定重做为"单一真值 + seal-on-eviction"路线（不修补 screen-backed 旧路径，对外 API 不变，新引擎并行开发验收后切换）。本切片交付 vterm 的 ungated seal 信号：`TerminalSemanticTransaction.EvictedRows` 无条件携带每次 write 真正离开 primary 可见区的物理行（cells/runs + Wrapped 软换行标志），不经过 `PrimaryScrollOut` 的 sync/alt/clear attach gate；`WriteDamage.EvictedAppend` 是其 damage 层来源，`ScrollbackAppend` 旧契约不变。完整性修补：(1) `internal/vt` DeleteLine 慢路径空行 `append(nil, line[:0]...)` 得到 nil 被 `recordScrollbackLine` 丢弃，改 make+copy 后滚出空白行保留段落间距；(2) eviction 归属按有序 damage 流中 ModeDamage(47/1047/1049) 跟踪 alt 状态，同一 write 内"主屏滚出→进 alt"不丢主屏行、瞬时 alt 会话不污染主历史；(3) `screen_shape_changed` 批次此前不带 eviction payload，现同样填 `EvictedAppend`；(4) 生产 SemanticTap 关闭 scrollback ring（size=0）时 seal 信号仍完整（damage 捕获不依赖 ring diff）。DECSLRM/DECSTBM region scroll 的 eviction 判定沿用 charmvt 现有 full-width gate，与 emulator ring 语义一致，不另造第二套判定。resize 变高反拉回的待定尾规则留给 R434。准入已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R430` 已完成：旧 mutation-backed `HistoryLogicalRenderer`、`NewInMemoryHistoryStore`、logical-line file backend 和 `LogicalLineStore` 注释已冻结为显式 `WithHistoryStoreFactory` / legacy harness 路径；默认 daemon 不能把 `WithHistoryStorageDir` 或 nil store 回退到旧 logical-line store。新增 `TestR430DefaultHistoryPathDoesNotUseMutationBackedStore` 源码守卫，锁定 `server.newHistoryStore` 默认返回 nil、`WithHistoryStorageDir` 不重装旧 file-backed factory、`newTerminal` nil-store 分支只创建 `ScreenHistoryBuffer` / `ScreenBackedHistoryStore`。`architecture.md`、`history-logical-renderer-design.md`、`compact-semantic-history-journal-design.md`、`history-semantic-ingest.md`、`screen-app-session-history.md`、`synchronized-output-history.md` 和旧 final plan 已标注 R430 后 screen-backed physical row/cell 为当前 production truth，logical line 只作为 HistoryWindow/Copy/Search projection 或 legacy harness payload。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R429` 已完成：新增 screen-backed sealed physical row backend 合同与二进制文件实现，文件只 append sealed `PhysicalRow` payload 和 row range index，恢复阶段只恢复 row count、NextRowID 和 AppliedSeq 元数据，不把全部 sealed rows 塞回 `ScreenHistoryBuffer.Committed`。带 backend 的 buffer 在 `sealRow` 成功写入 backend 后只保留当前 transaction recent rows 用于 proof 去重；`ScreenBackedHistoryStore` 的 latest/older/oldest/newer 已按绝对 row range 从 physical backend 分页读取 sealed rows，再拼接当前 main/alt screen projection。`WithHistoryStorageDir` 默认接入 `.screen-rows.bin`，同 terminal id 重建 server/terminal 后可恢复 sealed history；坏 storage dir 会让 terminal 创建失败，不再 silent fallback。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R428` 已完成：Terminal 默认 history path 已改为创建 terminal-scoped `ScreenHistoryBuffer`，transaction renderer、screen-backed journal renderer 和 `ScreenBackedHistoryStore` 共享同一个 physical row truth；`WithHistoryStorageDir` 不再把默认 daemon 带回旧 mutation-backed logical file store，显式 `WithHistoryStoreFactory` 仍保留给旧 backlog/迁移 harness。普通输出 hard line boundary、ED2/ED3/RIS、alt archive、process close/final frame 和 resize fixed-grid 宽度已在 screen-backed path 中按 physical row seal/projection 处理；RowSet scroll-out proof 改为消费 proof payload 并按 clear-time/pre-existing shell tail 过滤，避免丢 `sync01` 或重复 sealed shell rows。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- 用户复核后更正：R419-R427 只能算 screen-backed history 的阶段性迁移与验收完成，不能算“当前目标”整体完成。该问题已通过 R428-R430 收敛：默认入口、普通输出、storage/recovery 和旧 mutation-backed 路径已经切到 screen-backed model；旧 logical-line renderer/store 只保留为显式 legacy harness。
- `R427` 已完成：完成 screen-backed history 五类审核与验收记录。Architecture：R420-R426 已形成 `ScreenHistoryBuffer -> ProjectHistory -> ScreenBackedHistoryStore` 查询链路，并新增 screen-backed journal renderer 隔离旧正文 mutation；本轮不继续扩大到 protocol/TUI，默认 mutation-backed 生产入口仍作为过渡兼容路径保留。VT Semantics：vterm ordered ops、ED2/ED3、alt、sync、scroll region、wide char 和 frame/touched-row proof 均由现有 harness 覆盖，未发现新增进程名特判。History Correctness：RowID/Version/seal-once、sealed/current RowID 去重、freeze/copy、alt transient、primary close seal 和 latest/older/newer cursor 均有 R420-R426 harness。Performance：R426 screen-backed renderer 返回空 mutation batch，不为 screen truth 构造旧 logical/frame mutation；旧 hot path 未在本切片扩大。Test/Harness：已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -run 'TestR328|TestR331|TestR334' -count=1`、`cd termx-core-v2 && go test ./history ./cmd/history-harness -run 'TestR42[0-6]' -count=1`、`cd termx-core-v2 && for f in ../fixtures/history/*.ansi; do go run ./cmd/history-harness -cols 12 -rows 3 -in "$f" >/dev/null || exit 1; done`、`python3 scripts/emit_codex_resume_numbered.py --lines 20 --sessions 2 --clear ed2 --redraw-mode all --cjk-every 5 | (cd termx-core-v2 && go run ./cmd/history-harness -cols 40 -rows 6) > /tmp/termx-r427-codex-history.txt` 并抽查 S01/S02/CJK markers、`cd termx-vterm && go test ./... -count=1`、`git diff --check`。
- `R426` 已完成：新增 `NewScreenBackedHistoryJournalRenderer`，作为 screen-backed journal 应用路径：journal 在该路径只更新同一个 `ScreenHistoryBuffer`，返回空 `HistoryMutationBatch`，不再产出旧 logical-line/frame 正文 mutations。补齐 screen buffer 的 alt frame、primary visible rows seal、scroll-out proof seal、mutable resize 保留 sealed rows等边界；ordinary `OrdinaryLineBatch` 在 screen-backed renderer 中被明确隔离为旧 ingest truth，不能重新 seal 成 physical history。接口注释同步为“mutation batch 只服务旧 store 过渡路径”。新增 R426 harness 覆盖 legacy ordinary batch 不进入 screen truth、primary close 直接 seal physical rows 且无 frame mutation、alt transient 退出不污染 primary history。准入已通过 `cd termx-core-v2 && go test ./history -run TestR426 -count=1`、`cd termx-core-v2 && go test ./history -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R425` 已完成：新增 `ScreenBackedHistoryStore`，以 `ScreenHistoryBuffer.ProjectHistory()` 的 screen-backed rows 作为 `HistoryWindow` / freeze / copy projection 来源；latest、older、oldest、newer、frozen token 和 copy 都从 committed physical rows + current main/alt screen rows 投影，保留外部 `HistoryStore` / `HistoryWindow` / cursor / token 合同，不触碰 protocol/TUI。`Apply` 在该实现中不接收旧 logical/frame mutations，避免把旧 reducer mutation 恢复成第二份正文 truth。新增 R425 harness 覆盖 latest->older、oldest->newer、frozen projection 不被后续 repaint 改写、copy 使用 frozen rows、release 后 token 失效，以及 `ReadState` 从 sealed/current screen projection 读取 ownership。准入已通过 `cd termx-core-v2 && go test ./history -run TestR425 -count=1`、`cd termx-core-v2 && go test ./history -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R424` 已完成：production compact journal renderer 现在持有 terminal-scoped `ScreenHistoryBuffer`；primary pseudo-TUI frame/touched-row repaint 会先进入 screen buffer，更新 physical RowID/Version/OwnerKind，再从 screen buffer 当前 main rows 生成旧 `HistoryStore` 仍需的过渡 frame mutation。ED2 boundary 同步清 screen buffer primary rows；RIS/clear scrollback 会重置 screen buffer。普通 shell append path 暂留旧 ordinary journal 逻辑，pseudo-TUI repaint batch 不再把 frame proof 直接作为 ordinary sealed logical rows 追加。新增 R424 harness 覆盖 primary repaint 落入 screen buffer projection、RowOwnerKind=PrimaryFrame、RowID/Version 存在，并确认 batch 不产生 ordinary sealed line；fixed-grid frame 尾部空格在 screen buffer frame path 中保留。准入已通过 `cd termx-core-v2 && go test ./history -run TestR424 -count=1`、`cd termx-core-v2 && go test ./history -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R423` 已完成：新增 `termx-core-v2/cmd/history-harness`，可从文件或 stdin 读取 ANSI bytes，经 `vterm.SemanticSource -> ScreenHistoryBuffer -> ProjectHistory` 输出 committed/current physical rows、projected logical lines 和 projected rows；dump 每行带 RowID、Version、OwnerKind、Sealed、Wrapped、Continued、segment、kind 和 text。新增 `fixtures/history/` 覆盖 plain shell、pseudo-TUI repaint、progress CR repaint、alt-screen、scroll region 和 wide char；fixture smoke 全部能通过 harness。为让真实 vterm narrow-width fixture 经过 screen buffer，补齐 `soft-wrap` physical row continuation 语义和 R423 harness。准入已通过 `cd termx-core-v2 && go test ./history ./cmd/history-harness -run TestR423 -count=1`、`for f in ../fixtures/history/*.ansi; do go run ./cmd/history-harness -cols 12 -rows 3 -in "$f" >/dev/null || exit 1; done`、`cd termx-core-v2 && go test ./... -count=1`。
- `R422` 已完成：新增独立 screen-backed projection layer，不接旧 `HistoryStore` production path；`ScreenHistoryBuffer.ProjectHistory` 从 sealed physical rows 与当前 main/alt screen rows 生成 `ScreenLogicalLine` / `ScreenProjectionRow`，保留 RowID、Version、OwnerKind、Wrapped、Continued、ScreenRow 和 segment，并在拼接 sealed/current 时按 RowID 去重。projection 可转换为现有 `HistoryRow` 和 latest `HistoryWindow`，作为后续 R425 接入点，但 RowID/Version truth 仍保留在 screen projection 中。新增 R422 harness 覆盖 wrapped physical rows 合并 logical line、sealed/current RowID 去重、alt current 不污染 primary、latest window cursor/total 来自 screen projection。准入已通过 `cd termx-core-v2 && go test ./history -run TestR422 -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R421` 已完成：`ScreenHistoryBuffer` 仍保持独立 domain model、不接 terminal/TUI production path；本轮补齐 cursor movement、EL/ED/ECH/DCH/ICH、IL/DL/SU/SD、DECSTBM scroll region、full-row scroll rect、alt-screen 1047/1048/1049、cursor save/restore 1048、sync 2026 no-seal boundary 和 ED2 scroll-out proof seal-once 语义。新增 R421 harness 覆盖 cursor/erase 不 seal、字符编辑保留 RowID、宽字符 continuation 不投影成额外文本、line ops 移动 physical RowID、局部 scroll region 不写 primary history、alt/sync 不污染 main history、ED2 proof 重放不重复 seal。准入已通过 `cd termx-core-v2 && go test ./history -run TestR421 -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R420` 已完成：新增独立 `ScreenHistoryBuffer` domain model，不接 terminal/TUI，不替换 production history path。buffer 复用现有 history `Cell`，维护 main/alt `ScreenGrid`、stable physical RowID、Version、OwnerKind、Cursor、Margins、AppliedSeq、Committed physical rows 和 seal-once set；基础 op 支持 `WriteSpan`、`CR`、`LF/IND/NEL`、`BS`、`TAB/HT`，LF 触发底部滚动时按 RowID seal 顶部 physical row 并移动剩余 row identity。新增 R420 harness 覆盖 scroll-out seal-once/current 与 committed RowID 去重、in-place write Version 递增、CR rewrite、BS/TAB cursor 和 transaction seq 幂等。准入已通过 `cd termx-core-v2 && go test ./history -run TestR420 -count=1`、`cd termx-core-v2 && go test ./... -count=1`。
- `R419` 已完成：已读取用户 screen-backed history 重构目标并勘察当前核心链路。当前实现是 `Terminal.watchOutput` 将 PTY bytes 分给 live `SurfaceTrack` 和 history `SemanticTap`；history path 经 `TerminalSemanticTransaction -> HistoryJournalFromDecision -> terminalHistoryIngestQueue -> HistoryJournalRenderer -> HistoryStore.Apply -> HistoryWindow/Copy`。vterm 已提供 ordered ops、scroll-out proof、PrimaryFrame、AltFrame、PrimaryFrameTouchedRows 和 alt/sync/clear/resize flags；但 pseudo-TUI frame/touched rows 仍在 journal/frame reducer 中转成 logical screen-frame/open-line history，缺少 physical RowID/Version/seal-once truth。本轮新增 `termx-core-v2/docs/screen-backed-history-rebuild-design.md`，把后续 R420-R427 拆成 ScreenHistoryBuffer、VT 语义、projection、ANSI harness、pseudo-TUI 接入、HistoryWindow 迁移、旧 journal 清场和 review 验收。
- `R418` 已完成：已先总结 deep research 报告并处理其中 R417 未覆盖的两个可执行点。vterm `TerminalSemanticTransaction.PrimaryFrameTouchedRows` 现在按 owner 边界分流：`RequiresFullReplace` 继续保留 direct damage touched-row proof；非 full-replace 且存在 ordered semantic ops 时，由 write/EL/char edit、IL/DL/SU/SD、clear/scroll/copy rect、ED/RIS 等 semantic op 范围推导 touched rows，避免 live screen diff 把普通 current-screen 损伤扩大成 frame ownership。TUI render VM 新增 R418 guard：同一 pane 同时有 live surface 和 authoritative history rows 时，只要 copy mode `HistoryRenderable()` 成立，内容 VM 只能来自 `HistoryStore.Rows`，不能拼入 live surface rows。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。

- `R417` 已完成：本轮接受用户对根因的判断，修复点前移到 vterm semantic source 与 core terminal classifier。vterm `terminalSemanticShouldAttachFrame` 现在优先读取 transaction 的 ordered semantic ops，避免普通 `write/cr/lf` 因 live diff touched rows 附带 `PrimaryFrame`；同时为 CR+EL、relative cursor、ECH/DCH/ICH、IL/DL/SU/SD、clear/scroll/copy rect 等 primary edit 事务附带 frame proof。core `historyTransactionLooksLikePrimaryPseudoTUI` 只在 `PrimaryFrame + touched rows + content mutation` 且出现 sync、主屏编辑 op 或非单调写入时进入 primary frame session；`RequiresFullReplace` 仍由原来的保守 full-replace 分支处理。新增 R417 harness 覆盖编辑控制只接管 touched rows、sealed shell tail 不重复、rect/non-monotonic classifier 覆盖，以及 vterm 普通 stream 不带 frame、primary edit 带 frame。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`git diff --check`。

- `R416` 已完成：当前工作已移回正确分支 `feature/infinity-history-335af-direction`。本轮修复的是 core-v2 latest/freeze 入口锚点，而不是 Codex 程序名识别：ordinary shell logical lines 已 sealed 后不会被 current frame 改写；history latest 只按 current primary frame 的 authoritative `ScreenRow` 在 frame 前接入可见物理行数允许的最近 sealed rows，`ScreenRow=0` 时从 current frame 开始。`Freeze` 现在复用同一 latest anchor，避免 protocol `history.window latest -> Freeze -> token LatestWindow` 绕过修复；older cursor 仍保留被入口裁掉的 sealed rows。`CopyEntryProjection` 维持 R376 半追平 materialized preview 合同，读取已应用 frontier 但不创建 frozen token、不冒充完整 latest。新增 memory/backend R416 harness 覆盖 latest、frozen latest 和 older；terminal 链路 harness 覆盖 synchronized scroll-out proof 不在 latest 入口挤入 current frame，但 older 仍可取回。准入已通过 focused `go test ./history -run 'TestR416|TestR323|TestR332|TestR341|TestR362|TestR407' -count=1`、`cd termx-core-v2 && go test ./... -count=1`。

- `R415` 已完成：用真实 daemon `history-dump` 定位到重复发生在 core authoritative history：同一批 shell/`ls` rows 既是 sealed ordinary logical lines，又被后续 Codex 类 synchronized primary frame 作为 committed screen-frame 重复 seal。修复点在 terminal classifier 与 history journal ownership：primary repaint 只要没有 ED2/RIS 等清场边界，即使带 scroll-out proof，也不能升级为整屏 frame ownership；journal 在 touched-row frame 模式下跟踪本 transaction 内 primary ownership 和 side scroll-out 次数，跳过启动时顶出的 pre-existing shell proof，同时把 screen app 自己滚出的 payload 与最终上移后的 frame rows 保留下来。新增 R415 harness 覆盖 synthetic decision+process exit final frame、真实 vterm 短同步和真实 vterm 长同步，锁定 shell rows 只出现一次、current/final screen-frame 不含 sealed shell、`codex line 01` 到最终 frame tail 不丢失。准入已通过 focused R415/R334/R335/R373 回归，完整准入见本切片提交。
- `R414` 已完成：用 tmux 受控 pseudo-TUI 复现“shell prompt + Codex 类 synchronized primary frame”进入 copy/history 后布局变化，定位到两个边界缺口。core `FrameReducer.ReplacePrimaryTouchedRows` 现在只在已触达 current frame row 范围内部补 default blank fixed-grid gap，保留 PTY 语义空白行，不接管未触达的非空 shell tail 或 leading/trailing 空白；TUI `CopyModeStore.AcceptLatest/AcceptMaterializedProjection` 用 authoritative `current-primary-frame` 的 `ScreenRow` 计算入口 viewport top，丢弃超过物理屏幕行锚点的旧 rows，renderer 仍按连续 authoritative rows 渲染，不读 live surface、不恢复 display padding 或程序名分支。tmux 复验 copy/history 中 `ζ codex`、Codex card、Tip 和 fixed-grid 内部空白已对齐；受控脚本另暴露一条靠近 PTY 底部的 `> Explain this codebase` 没进入 authoritative dump，属于后续 scroll/linefeed proof 缺口，不并入本切片。准入已通过 focused core/TUI harness 和真实 tmux 复验，完整准入见本切片提交。
- `R413` 已完成：history 入口不再把 live surface 放进 copy/history 状态链。TUI 删除 `EnteringLive`、entering-live render overlay 和 `liveSurfaceMatchedViewportTop`，`copyModeLatestRequestCols` 只使用 terminal view binding/可见列宽；latest 飞行期间仍可正常显示 live，但该 live render 不参与 HistoryStore、CopyModeStore、viewport、request cols、搜索、复制或分页判断。core-v2 `LatestWindow` 在 current primary frame 带 authoritative `ScreenRow` 且首行不是 0 时，用该 PTY semantic screen row 计算 sealed timeline tail 接入点：只把当前屏物理行号允许的最近 sealed rows 接到 current frame 上方，更早 shell rows 仍通过 older cursor 可达。新增 R413 memory/backend store harness，更新 R404 终端链路 harness 验证 hidden sealed rows 可 older 取回；TUI harness 反向锁定 latest/projection 只按 authoritative rows 定位且 history 请求忽略 live surface cols。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R412` 已完成：真实 tmux harness 的 copy-latest 准入现在必须等待 newest stress 行和 `TERM_X_TUI_STRESS_DONE` 同时出现在历史视图，不能只看到 `PgUp] SCROLL` 状态栏就判定 latest window 已落地。严格 harness 复测通过 `/tmp/termx-r412-10k`、`/tmp/termx-r412-100k`、`/tmp/termx-r412-1m`：10K Python `real 0.22s`、live DONE `304ms`、copy-latest `59ms`；100K Python `real 1.98s`、live DONE `2.155s`、copy-latest `60ms`；1M Python `real 18.58s`、live DONE `18.641s`、copy-latest `60ms`、copy-oldest `1.047s`，live/copy/history captures 均含 oldest/newest/DONE。结论：R411 后 live/copy-latest 本身能追上最新帧；R411 1M 失败是 harness 过早截图造成的错误证据。残留性能风险是 full `history-dump` 诊断导出：1M authoritative dump 完整但耗时 `99.07s`、输出 `328MiB` 文本、history backend 约 `324.7MiB`；perftrace 显示 TUI `history.window` 两次请求合计 `38.7ms`、`history.copy_entry` `26.2ms`，慢点不在 copy/latest 用户路径，而在 CLI 串行分页并格式化全量 dump 的诊断路径。准入已通过 `bash -n scripts/termx_tui_stress_memory.sh`、上述三轮真实 tmux stress、`git diff --check`。
- `R411` 已完成：按 `6cc64c0c55c44e0152b0748caa24145dbe82f87a` 的正确 TUI 模型恢复 copy/history 单一连续 row viewport。`AcceptLatest` 重新使用 `EnteringLive` 只做进入态 viewport 锚点匹配，复制/搜索/render truth 仍只来自 authoritative `HistoryWindow` rows；无 live 匹配时按 newest tail。用户滚轮、PageUp/PageDown、deferred older/newer 和 pending scroll 主路径恢复为 `ScrollViewport` viewport-first，`ScrollCursor` 只保留给显式 cursor/selection。renderer 删除 `ScreenRow` display padding，`copyHistoryLines`、cursor、hit-region 全部使用同一连续 viewport-local row 坐标；copy-history patch 删除 padding 双坐标保护，只保留真实 TTY wrapping 安全保护，并补 R411 patch harness。子 Agent `019f2096-f8dd-7b60-a4f9-3662baca0421` 只读 review 未发现 ScreenRow/display padding、程序名分支、fallback/scrub 或 live truth 混用；唯一低风险 finding 是 patch 测试仍用 `ScrollCursor` 构造 R411 场景，已改成 `ScrollViewport` 并加 cursor viewport-relative offset 断言。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`、真实 `/tmp/termx-r411-10k` 和 `/tmp/termx-r411-100k` tmux stress。10K：Python `real 0.23s`、DONE 可见 `312ms`、history-dump `10004` rows、copy-latest/copy-oldest ok；100K：Python `real 1.95s`、DONE 可见 `2.037s`、history-dump `100004` rows、copy-latest `46ms` ok、copy-oldest `1.08s` ok。残留风险进入 R412：真实 `/tmp/termx-r411-1m` 中 Python `real 18.09s`，TUI live 240s 未显示 DONE，copy-latest 缺 newest/DONE，但 authoritative `history-dump` 有 `1000000` rows 且含 newest/DONE，dump 查询耗时 `86.76s`。
- `R410` 已完成：普通 shell history 继续沿用 newest tail；当 latest window 包含 core 标记的 `current-primary-frame` fixed-grid rows 且带 `ScreenRow/ScreenRowSet` 时，`CopyModeStore.AcceptLatest` 会用 authoritative `ScreenRow` 反推初始 `ViewportTop`，只保留当前 frame 物理行号允许的最近历史 rows，避免把更早 shell scrollback 塞到 Codex 当前屏上方。renderer 的 top padding 同步改为扫描 viewport 内 current frame 首行，当前面历史 rows 不足以填满 `ScreenRow` 时只补 display-only 空白；不读取 live snapshot、不按程序名分支、不改变 core logical-line truth。用户继续反馈滚出当前屏后出现旧帧尾部与 older rows 混排；本轮继续修复 copy-history 增量 patch：patch 路径现在只有在当前可见 rows 不需要 renderer display padding，且每个可见 row 的显示宽度不会超过 terminal 内容宽度时才允许局部滚动/重写，否则回退完整帧，避免真实 TTY 自动换行污染后续行。新增 state/render R410 harness 覆盖“latest rows 少于 viewport 高度但仍应丢弃旧 shell context”的 Codex 入口场景，新增 app patch harness 覆盖 wrapping row 和 current-frame padding 都禁止局部 patch。准入已通过 `cd termx-tui-v3 && go test ./app -run 'TestR410|TestCopyHistory.*Patch|TestCopyModeWheelUpScrollsByLineAndPrefetchesOlderWithoutJump' -count=1`、`cd termx-tui-v3 && go test ./state -run 'TestR410|TestCopyModeAcceptLatest|TestCopyModeBindsLatest' -count=1`、`cd termx-tui-v3 && go test ./render -run 'TestR409|TestR410|TestCopyHistory' -count=1`、`cd termx-tui-v3 && go test ./... -count=1`。
- `R409` 已完成：当前截图问题的根因是 frame reducer 内部虽有 `LogicalLineDraft.Row`，但 core history window/protocol/TUI 投影没有保留 fixed-grid current-frame 的物理 screen row，进入 copy/history 后只能把 authoritative rows 顺排到顶部，导致顶部空白消失、screen app 原地布局被压扁并和旧 prompt 混在同一观察窗口。本轮把 `ScreenRow/ScreenRowSet` 作为 screen-frame payload 元数据从 core frame journal 写入 `LogicalLine`，file-backed payload、indexed projection、`HistoryRow`、protocol unknown fields、TUI adapter/state 全链路传递；renderer 只在 current primary frame 首行使用该 authoritative screen row 做 display-only 顶部锚点，不读取 live snapshot、不恢复 R402 `ViewportTopPadding` 补丁、不改变 logical-line history truth。新增 harness 覆盖 memory/backend latest window 携带 screen rows、binary payload round-trip、protocol row metadata、TUI adapter metadata，以及 copy/history lines/cursor/hit-region 使用 authoritative screen row 对齐首屏。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R408` 已完成：已使用子 Agent `019f1fae-f3e7-7900-a3a4-5791ce02bcd5` 对 R407 `973c6836` 做只读 review。review 未发现 raw parser fallback、snapshot/scrollback/TUI/live rows 反推 history、程序名特殊分支或隐式 storage scrub；确认 live latest wake 主路径已和 history journal 分开。处理 findings：一是修复 vterm compact `TextDamage.Text/Runs` 没有进入 direct damage stats 的风险，direct stats 现在先统计 screen diff 覆盖区间，再用未被 screen diff 覆盖的 compact semantic text 补充 rows/cells/touched proof，避免普通 `WriteWithDamage` 双计，同时保证 `WriteWithSemanticDamage` 的 compact text 能触发 broad full-replace/touched rows；新增 vterm harness 覆盖 compact ASCII、styled runs 和 semantic-only compact text。二是把 tmux stress harness 的 `copy_latest` tail 检查从记录项升级为硬失败，避免 latest copy 缺 newest/DONE 时继续通过；三是 core R407 stress harness 增加 `000000..010000` 全编号单调序列校验，覆盖中间丢行、乱序和重复。未在 R408 大改的 review finding：`HistoryWindow/history-dump` 在 `historyMu` 下逐行读 file backend 导致 100K full dump 约 `7.5s`，这是已记录的后续查询路径性能风险；R408 不扩大到 storage/window 锁模型重构。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`bash -n scripts/termx_tui_stress_memory.sh`、真实 `/tmp/termx-r408-10k-final`、真实 `/tmp/termx-r408-100k-final`、`git diff --check`。真实 100K final：Python `real 1.94s`、DONE 可见 `2.010s`、post-process visible `70ms`、history-dump `100004` rows 且 oldest/newest/DONE ok、copy-latest ok、copy-oldest ok，history backend 约 `32.7MiB`。
- `R407` 已完成：history consumer 从逐 cell logical-line payload 改为支持 compact `CellRun`，ordinary high-pressure path 在 vterm damage、history journal、logical-line store、file backend、window projection 之间保留 run 表示，只有需要编辑/展示窗口时才 materialize cells；history tap ingest queue 改为页式批量所有权移交，避免 worker batch 反复 deep-copy；ordinary scroll-out proof 只在同一内容已由 ordinary ops owner 接管时跳过 transaction-side duplicate proof，避免把 visual fragment 当第二 truth。修复了 R407 过程中暴露的 CJK/空格和 1MiB split 回归：compact run materialize 编辑时保留 trailing spaces，command replay 前 flush 已有 direct sealed lines，LF-only chunk 后的普通行不再误入 command replay。vterm compact ASCII semantic op 测试已更新为同时接受 `Runs`/`Cells`，并保留 style/link 断言。真实 tmux harness 修正了 `history-dump` marker 检查在 `pipefail` 下的 SIGPIPE 误报，以及 `copy_oldest` 本应只要求 oldest marker 却被 full-status 误标的问题。准入结果：`BenchmarkR407HistoryIngestQueueDrain100K` 约 `45.5us/op`，`BenchmarkR407HistoryTapPipelineChunked100K` 约 `2.24s/op`，`BenchmarkR407HistoryTapPipelineCoalesced100K` 约 `2.20s/op`；真实 `/tmp/termx-r407-10k-harness-report-fix` 显示 Python `real 0.23s`、DONE 可见 `307ms`、history-dump `10004` rows 且 oldest/newest/DONE ok、copy-latest ok、copy-oldest ok；真实 `/tmp/termx-r407-100k-report-fix` 显示 Python `real 1.97s`、DONE 可见 `2.026s`、post-process visible `56ms`、history-dump `100004` rows 且 oldest/newest/DONE ok、copy-latest ok、copy-oldest ok，history backend 约 `33.6MiB`。残留风险：长 shell 命令回显在 history 最前方仍表现为终端行编辑/折行后的可观察文本，不作为 stress 数据行 correctness 结论；R408 review 需要重点复查该边界以及 history-dump 全量查询 100K 约 `7.4s` 的查询路径成本。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`bash -n scripts/termx_tui_stress_memory.sh`、上述 10K/100K 真实 tmux harness、`git diff --check`。
- `R406` 已完成：真实 tmux stress harness 现在同时记录 direct tmux PTY baseline、TermX live capture、core-v2 authoritative `history-dump`、TUI copy-latest/copy-oldest、程序 `time -p` 实际耗时、DONE 可见耗时、history 查询耗时、daemon/TUI RSS/CPU 和 history backend 文件大小。脚本修正了 `tmux send-keys` 长命令被当 key name 解释的问题，改为 literal bytes；`wait_for_capture` 从 1s 轮询改为 100ms，减少可见延迟量化误差；内部行数变量从 bash 特殊 `LINES` 改为 `STRESS_LINES`，避免 newest marker 校验被终端高度污染。真实 smoke `/tmp/termx-r406-harness-baseline-smoke` 显示 direct tmux baseline 完整、core `history-dump` 约 51ms 且 oldest/newest/DONE 完整，但 TermX live 在该轮 30s 未显示 DONE，copy-latest/copy-oldest 也缺 newest，后续 R407/R408 不能再只看 core dump 或程序耗时下结论。准入已通过 `bash -n scripts/termx_tui_stress_memory.sh`、`scripts/termx_tui_stress_memory.sh --root /tmp/termx-r406-harness-smoke6 --lines 20 --seed 100 --attach-size 100x30 --profile-mode none --wait-seconds 20 --no-baseline-time`、`scripts/termx_tui_stress_memory.sh --root /tmp/termx-r406-harness-baseline-smoke --lines 50 --seed 101 --attach-size 100x30 --profile-mode none --wait-seconds 30`、`git diff --check`。
- `R405` 已完成：真实 terminal 接入边界现在和 R404 journal reducer 对齐。Resize 真实链路 `ResizeTerminal -> SemanticTap.Resize -> history queue` 改为只构造 decision-aware boundary-only `HistoryJournalFromDecision`，不再用 default transaction journal，也不携带 ordinary/frame/scroll-out payload；无 history worker 时同步应用同一 journal。新增 terminal harness 捕获 resize 入队 journal，确认只有 `boundary:resize`。Process exit/remove 的 final frame 继续是 lifecycle boundary，只通过 `historyRenderer.Close(reason)` seal 当前 primary frame，不伪造成 PTY bytes、journal 或 live snapshot；新增 guard 和 terminal harness 锁定 process exit 后 mutable current frame 被清理，final primary frame 进入 committed history。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R404` 已完成：compact semantic journal 现在按 terminal classifier 的 `HistoryDecision` 构造成唯一 production history 输入，PTY/resize history apply 不再在 journal renderer 与 full transaction renderer 之间 fallback；ordinary sealed/open-line journal 继续先走 journal-only backlog，只有复杂语义才 flush backlog 后拉 transaction 做 decision-aware journal。`HistoryJournalRenderer` 删除 unsupported pre-scan 和自有 open-line state，统一复用 `StreamLineReducer`/`FrameReducer`，覆盖 ordinary、ED2/ED3/RIS、scroll-out proof、sync primary frame、alt transient、resize、prompt 前 close-primary、sync 后同包 alt archive、final frame 和 fixed-grid spaces。新增 guard 禁止旧 fast-path/full-transaction fallback gate 回到 production。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R403` 已完成：用户确认 R402 对 history 入口显示无改善后，当前目标切换为重写 core-v2 history logical-line 构建流程。本轮只做清场切片：删除 R400 `TERMX_HISTORY_TRACE` core/TUI 诊断链路，删除 R402 `ViewportTopPadding`、`liveSurfaceMatchedViewport` 和 renderer 顶部空白 display anchor，`CopyModeStore.AcceptLatest` 重新只按 core authoritative `HistoryWindow` tail 定位 viewport，不再用 live preview 反推 history 首屏；新增 guard 禁止这些补丁路径回到 production。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R402` 已完成：用户截图确认 live normal 画面中最新 prompt 位于窗口底部、上方有多行空白，但进入 history 后相同 prompt/`ζ` 被压到顶部，说明 TUI 进入态把 live viewport 顶部空白当成 outer empty rows 修剪掉了。修复边界在 TUI display projection：`HistoryStore` 和 core authoritative rows 不增加空白 truth；`CopyModeStore.ViewportTopPadding` 只保存进入瞬间的顶部显示锚点，renderer 在 copy/history 首屏前置空白并同步 cursor/hit-region Y 坐标，scroll patch 在 padding 存在时退回完整帧；用户滚动、搜索、resize、reflow、older/oldest 后清零。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`。
- `R401` 已完成：用户不再参与手动验证后，本轮先加 core harness 复现无 synchronized output 的 `CUP home + repaint` 进 history 后重复显示每一帧的问题。失败时 `Starting MCP servers` 出现 8 次且全部是 `committed ordinary`，行内多个空格在 cell 中仍存在，说明问题不是 TUI render trim，而是 classifier 把 primary repaint 错归 ordinary stream。修复后，带 `PrimaryFrame` side proof、包含内容 mutation 且同一 transaction 有 `cup/vpa/hpa/cha` 光标定位的 primary 输出进入 `PrimaryFrameSession`，只按 touched rows 接管 mutable current frame；R334/R398 守卫确认不重发 shell tail、不封存 synchronized 中间帧。准入已通过 `go test ./termx-core-v2 -run 'TestR401|TestR398|TestR334' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R400` 已完成：用户手测确认 R399 后 history 中空格依然消失，本轮不继续盲改模型，先把空格丢失链路打透。`TERMX_HISTORY_TRACE=1` 下，core 现在会在 `core.ingest.transaction` 输出 vterm semantic `PrimaryFrame/AltFrame` 的 sampled row 摘要，并在既有 `core.response.history_window`/`core.store.response_window` 摘要中带上 `cells`、display `width`、尾部空格数 `trail` 和文本预览；TUI 既有 `tui.response.window`、`tui.apply.window`、`tui.render.visible` 摘要也带上相同字段。该切片只用于判断空格是在 semantic source、core store/protocol、TUI projection 还是 render-visible 层丢失，不改变 history truth。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R399` 已完成：用户反馈 R398 后 history 中空格被吞。复查链路后确认 TUI/protocol adapter 已有 fixed-grid padding 保留测试，真正风险在 core `frameReducer.trimmedFrameRows`：它复用 ordinary line 的 `trimTrailingBlankCells`，会把 screen-frame row 尾部被 PTY 写出的 default blank 当作无内容裁掉。screen-frame/final frame 是 fixed-grid payload，行内和行尾空格属于布局 truth；本切片改为只裁整屏尾部纯 default blank rows，不裁非空 screen-frame row 内的 default blank cells。新增 R399 frame reducer 回归覆盖 `model:  gpt ` 这类 fixed-grid row 必须保留空格和 display width。准入已通过 `cd termx-core-v2 && go test ./history -run 'TestR399|TestR334FrameReducer|TestR337FrameReducer|TestR322FrameReducer|TestR384JournalRendererPrimaryFrameRepaintIsMutableOnly' -count=1`、`cd termx-core-v2 && go test . -run 'TestR398|TestR334|TestR373SingleTapSynchronizedTransactionPublishesPrimaryFrame|TestR373SingleTapSyncThenAltKeepsPrimaryHistory' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R398` 已完成：用户手测进入 history 后看到 Codex 类原地更新画面被重复追加，根因是 terminal history classifier 把每次 `SynchronizedBegin` 当成新的 primary session boundary，并在替换 primary frame 前执行 `ClosePrimaryFrameBeforePrimaryReplace`。这违反 R322/R384 的 frame journal 语义：screen app repaint 应只替换 mutable current primary frame，不能把中间帧写进 sealed timeline。本切片取消 synchronized repaint 的 before-replace close，只保留普通输出恢复、alt archive 和 process close 作为真正 lifecycle 边界；新增 R398 回归覆盖第二次 HOME repaint 不保留首帧，以及多次 progress repaint 只暴露最新 current frame、不产生 archived frame。准入已通过 `cd termx-core-v2 && go test . -run 'TestR398|TestR373SingleTapSyncThenAltKeepsPrimaryHistory|TestR373SingleTapPreludeThenSyncThenAltKeepsPrimaryHistory|TestR384JournalRendererPrimaryFrameRepaintIsMutableOnly' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R396` 已完成：R395 的 core 计时/perftrace 不能作为“真实可见已 OK”的结论。用户用真实观察指出 100K stress 仍有序号从 600 多直接跳到 40 万、画面中间停住、pane 内显示约 2 秒但手表实测总展示约 12 秒的问题；这说明 TUI 是观察窗口，减少 `live.screen.get` / `tui.frame` 可能只是减少刷新帧，不等于 live latest 跟上真实输出。本切片把问题重新定性为 single `SemanticTap` 把 live latest screen 绑定到 history semantic write 成本，恢复 live `SurfaceTrack.WriteForLatestFrame` 快路径，并让 history semantic consumer 独立追 PTY bytes 产出 compact journal；response owner 只能是 live `SurfaceTrack`，history truth 仍只由 core-v2 logical-line store 暴露。新增 R396 harness 锁定 live one-shot 只 flush live queue、不等 history tap queue，history disabled 时不创建 history tap，restart 保留屏幕前会先 flush 已入队 live queue，response 只由 live path 回写一次。stress harness 同时修正 DONE marker 回显误判：旧脚本会把输入命令行里的 marker 当成完成。可信 100K 复测 artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.R41leG`：pane 内 Python `real 3.70s`，DONE 可见 `4.09s`，post-process visible 约 `390ms`；live capture 有 `099999` 和 `TERM_X_TUI_STRESS_DONE_01`，copy-oldest 有 `000000`、newest 和 DONE。残留风险：该轮 `copy_latest` capture 仍未落在 newest/DONE tail，说明 TUI copy/latest 入口或 viewport 仍有独立问题，不能把它混入 live consumer 修复里。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`scripts/termx_tui_stress_memory.sh --lines 100000 --no-baseline-time --profile-mode none --wait-seconds 90`、`git diff --check`。
- `R397` 已完成：按用户要求只定 live consumer，不继续扩展 history 优化，也不退回 scrollback/snapshot truth。live/history tap 共用的 `terminalLiveIngestQueue` 从 slice pending 改为固定页 pending，目标是让 history tap backlog 很大时，PTY reader 对 history 的投递不会在锁内因为 slice 扩容或 batch drain 前移复制大量 queue item，从而间接拖慢后续 live 投递。新增 R397 harness 锁定页式队列释放已消费页、保留 flush fence 和 batch limit；stress harness 新增 `--history-disabled`，用 `TERMX_HISTORY_DISABLE=1` 启动 daemon 并跳过 copy/history 校验，专门给 live SurfaceTrack 的真实 tmux `send-keys -> DONE visible` 对照使用。真实 tmux live-only 100K artifact `/tmp/termx-r397-liveonly-100k`：pane 内 Python `real 2.60s`，DONE 可见 `3.097s`，post-process visible 约 `497ms`，live tail 有 `099999`、`100000` 和 DONE，history 文件为 0。默认 history-on 100K artifact `/tmp/termx-r397-historyon-100k`：Python `real 3.05s`，DONE 可见 `4.078s`，post-process visible 约 `1.028s`，live tail 有 newest 和 DONE，copy-oldest 有 oldest/newest/DONE；残留仍是 `copy_latest` 没落在 tail，且 daemon CPU 说明 history consumer 仍重，但本切片不处理 history 慢队列。准入已通过 `cd termx-core-v2 && go test . -run 'TestTerminalLiveIngestQueue|TestR396|TestR397|TestServerNextLiveInvalidation' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`bash -n scripts/termx_tui_stress_memory.sh`、上述两轮真实 tmux stress 和 `git diff --check`。
- `R395` 已完成但不足：问题根因不是需要固定 `25ms` sleep，而是 R393 回滚 one-shot snapshot 时把 `NextLiveInvalidation` 里的 tap queue flush coalescing 也删掉了，导致 TUI 每次 frame-ready 后立即收到下一个中间 revision 并拉一次 `live.screen.get`。本切片恢复 wake-only coalescing：`live.invalidation.next` 返回 wake 前只等待调用时已经进入 core tap queue 的 PTY payload 推进 single `SemanticTap`，把同一 burst 合并到当前 latest revision；它不携带 `NativeScreenSnapshot`，不等待 future output，不等待 history backlog，也不等待 TUI render。新增 harness `TestServerNextLiveInvalidationFlushesPendingTapQueueWithoutSnapshot` 锁定 pending tap queue 期间 wake 不提前返回、flush 后 revision 跳到 latest、screen rows 仍由客户端 pull。实测 `/tmp/termx-r395-flush-wake-100k`：Python `real 2.83s`，copy-oldest 能看到 `000000`、newest 和 DONE；perftrace 从 R394 的 `live.screen.get=1219`、`tui.frame=1242` 降到 `live.screen.get=10`、`tui.frame=31`，但用户真实手表观察仍约 12 秒且出现跳号/停顿，因此 R395 不能作为最终修复。
- `R394` 已完成：按用户反馈“实际执行约 2 秒但展示七八秒”，移除 R393 恢复的 TUI live arm 固定 `25ms` sleep。该窗口原本只是 R390 的临时降采样器：每次 `LiveFrameReadyMsg` 准备重新 arm `live.invalidation.next` 前睡 25ms，用来减少高压输出下 `live.screen.get` 和 frame 数；它不是语义边界，也不是 history truth 边界。多轮追 live 时它会按轮次累积成人为尾延迟，所以本切片改回 one-shot arm 立即进入 core long-poll，仅保留 runtime 队列 latest-only、`Armed/InFlight/Dirty` 状态机去重和背压。同时补上更正确的 latest-only 边界：普通 `live.screen.get` 返回时如果 in-flight 期间已收到后续 invalidation 并标记 dirty，则该 snapshot 已是中间屏，TUI 直接释放 in-flight 并拉下一张 core latest，不把过期 surface 先应用到 reducer/render。实测 `/tmp/termx-r394-dirty-skip-100k`：Python `real 2.87s`，copy-oldest 能看到 `000000`、newest 和 DONE；但 perftrace 仍显示 `live.screen.get=1219`、`tui.frame=1242`，说明 25ms 去掉后不会再人为 sleep，但当前 live loop 仍会在 fetch/render 很快完成时追大量中间 revision，后续应把 one-shot arm/fetch 改成真正的 latest revision 背压，而不是恢复固定 sleep。
- `R393` 已完成：按用户反馈“R391 后 live 输出非常卡、帧率很低”，定向回滚 R391 的 live one-shot snapshot/ready-surface 路径，而不是整体回滚 R391。core `NextLiveInvalidation` 重新只返回 `terminal.live.invalidated` wake + revision，不再在 one-shot 响应中 flush tap queue、构建 `NativeScreenSnapshot` 或携带 screen payload；protocol `LiveScreenInvalidatedData` 删除 `Snapshot` 字段和事件 unknown-field bytes 编解码，避免 live invalidation 重新变成大 screen payload 通道；TUI service `ArmLiveInvalidation` 重新只产出 refresh invalidation，后续仍由 app 背压状态决定何时调用 `live.screen.get` 拉 core latest screen。TUI app 同时恢复 R390 的 `25ms` 可取消 live invalidation arm 合并窗口，减少高压输出下 one-shot callback churn。R391 的 terminal input bytes 合并保留，因为该路径解决逐字符输入 RPC 串行，不是持续输出低帧率的直接原因。本切片没有恢复 double VTerm，也没有把 history truth 放到 TUI rows。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R392` 已完成：本轮没有恢复 double VTerm，也没有继续改 live/TUI consumer，而是把目标收敛到 core-v2 history consumer。`terminalHistoryIngestQueue` 仍在 enqueue 边界 deep-copy compact `HistoryJournal`，保证 backlog 不持有 producer 的 mutable payload；worker 取 batch 后改为一次所有权移交，不再在 `nextBatch` 和 `terminalHistoryIngestBatchJournals` 中重复 deep-copy 同一 logical-line cells。`Terminal.ingestHistoryJournals` 现在把同一 worker batch 中按 SemanticTap seq 顺序得到的 journal 先通过共享 `HistoryJournalRenderer` 转成 mutations，再合并成一次 `HistoryStore.Apply`，减少 file-backed backend 的小事务和 storage flush 次数；authoritative truth 仍只由 `HistoryStore` logical-line/window/cursor 产生。新增 R392 harness 锁定同批两条 ordinary journal 只触发一次 store apply，且 latest window 顺序仍是 `one|two`；新增 100K history-only benchmark，用约 32.3MiB styled logical-line journal payload 测 `journal -> logical-line history`，不包含真实绘制和 vterm 解析。实测 Apple M2 上 `BenchmarkR392HistoryJournalIngest100K` 为 `0.278s`、约 `116.16 MB/s`，`BenchmarkR392HistoryJournalFileBackedIngest100K` 为 `1.159s`、约 `27.86 MB/s`；这说明单看 history consumer，100K/32MiB 已在用户观测的源程序写文件 `1.33s` 同量级内，后续若真实 live 仍跟不上最新帧，应优先看 `PTY bytes -> single SemanticTap/vterm latest screen + journal 构造` 和真实 TUI/TTY 观测链路，而不是把 history store 当主要瓶颈。准入已通过 `go test ./termx-core-v2 -run 'TestR392|TestR385TerminalHistoryIngestQueueStoresCompactJournalsNotTransactions|TestR374TerminalHistoryIngestQueueReportsAppliedTargetSeq' -count=1`、`go test ./termx-core-v2 -run '^$' -bench 'BenchmarkR392HistoryJournal.*100K' -benchtime=1x -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R391` 已完成：没有恢复 double VTerm，也没有把 history truth 放到 TUI rows。`live.invalidation.next` 的 one-shot 响应现在可携带同一 core `SemanticTap` 的 latest native snapshot，TUI service adapter 收到后直接转成 ready live surface；普通广播 event 仍只带 revision，避免 broadcast 路径放大为 screen payload 队列。R390 的固定 `25ms` arm sleep 已删除，取消时不再调用 service；runtime 普通 live surface 不再在消息中途强制写帧，而是等 batch/idle 边界合并。另定位到 10K stress 的 3s+ 可见延迟中，约 400 次逐 key `protocol.request.input` 是主要串行成本之一；runtime 现在只在普通 terminal passthrough 状态下把连续 printable/Enter/Tab/Backspace bytes 合并成一个 `TerminalInputBytesMsg`，发送前由 reducer 重新检查 copy/overlay/prefix/attach 状态，raw special keys、mouse、resize、theme 和 UI 边界不合并。真实 `/tmp/termx-r391-input-batch-10k`：Python `real 0.32s`，visible `2.080s`，post-process visible `1.760s`，copy-oldest 仍能看到 `000000`；perftrace 中 `protocol.request.input` 从约 `424` 降到 `2`，`protocol.request.live.screen.get` 从约 `279` 降到 `1`，`live.invalidation.next` 降到 `8`，TUI frame 从约 `444-503` 降到 `23`，TUI CPU 约 `0.07s`。剩余延迟主要来自 core `live.write_screen` 约 `0.84s`、history journal fanout 约 `0.60s`、one-shot long-poll/flush 等待以及 stress harness `wait_for_capture` 的 1 秒轮询量化；继续优化应优先看 vterm/journal compact proof 或 harness 观测粒度，而不是继续加固定丢帧窗口。
- `R390` 已完成：真实 stress harness 新增 `--perftrace`，分别输出 `daemon-perftrace.json` 与 `tui-perftrace.json`。perftrace 证明 10K 剩余延迟主要不是第二 VTerm 问题，而是 TUI 在压力输出期间拉/渲染过多中间 latest screen：R389/R390 初始 10K 中 `live.screen.get` 约 `265-269` 次、TUI CPU 约 `0.49s`、visible 约 `3.07s`。现在 `LiveFrameReadyMsg` 触发下一次 `live.invalidation.next` 前做 `25ms` 可取消合并等待，让 core 多个 invalidation 合成更少的 latest pull；最终 `/tmp/termx-r390-final-10k` 中 `live.screen.get` 降到 `60` 次，TUI CPU 约 `0.28s`，Python `real 0.33s`，visible `2.058s`，post-process visible `1.728s`，copy-oldest 能回到 `000000`。另在 `SemanticSource` 边界把本次 `WriteDamage` ordered ops 所有权交给 transaction，避免 tap 内部额外 deep clone；fan-out 不变，外部 consumer 仍通过 `SemanticTapResult.Transaction()` 获得 deep copy。剩余约 1-2 秒包含 tmux/TUI 逐字符输入命令的 input RPC 成本约 `1.06s`、daemon semantic write/journal 约 `1.45s` 的并行累计和 `wait_for_capture` 1 秒轮询量化；后续若继续压低，应优先做 terminal input byte 合并和 vterm/journal compact proof，而不是恢复 double vterm。
- `R389` 已完成：core process output 路径现在在 single SemanticTap 更新 latest screen 后先发布 `terminal.live.invalidated`，再进入 history journal fan-out；`SemanticTapResult` 只保存同次 semantic transaction 和 revision，`HistoryJournal()` 在 history fan-out 阶段按需裁剪，避免 journal 构造成本进入 live wake 热路径。新增 harness 证明 blocked `HistoryStore.Apply` 期间 live invalidation 已经可观察，且 tap write/resize 不会提前构造 journal。实测 `/tmp/termx-r389-lazyjournal-10k` 仍是 Python `real 0.32s`、visible `3.079s`、post-process visible `2.759s`，daemon CPU 约 `5.27s`，说明剩余延迟不是 history fan-out 顺序或 journal eager build，而在 single vterm semantic write/full ordered ops 构造等更早热路径；不能通过两个 VTerm consumer 解决。
- `R388` 已完成：TUI runtime 不再把下一次 live invalidation arm 绑定到 `LiveSurfaceMsg` 本身，而是任何成功写出的 frame 只要携带当前 live surface，就通过 `LiveFrameReadyMsg` 回 reducer，由 `ObservedRevision` 和 per-terminal `Armed/InFlight/Dirty` 状态去重。这修复了真实 TTY `LatestFrameSink` 覆盖 `LiveSurfaceMsg` frame 后 live 链路停在初始 snapshot 的问题。准入已通过 `go test ./termx-tui-v3/app -run 'TestAppRuntimeArmsLiveInvalidationAfterFrameWrite|TestAppRuntimeArmsLiveInvalidationAfterNonSurfaceFrameWrite|TestAppRuntimeDoesNotArmLiveInvalidationForDroppedFrame|TestAppRuntimeArmsAllVisibleLiveTerminalsAfterFrameWrite' -count=1`、`go test ./termx-tui-v3/... -count=1`、真实 tmux harness `scripts/termx_tui_stress_memory.sh --lines 10 --no-baseline-time --profile-mode none --wait-seconds 30 --root /tmp/termx-r388-smoke10` 和 `scripts/termx_tui_stress_memory.sh --lines 10000 --no-baseline-time --profile-mode none --wait-seconds 60 --root /tmp/termx-r388-perf10k`。10 行从 R387 的 20 秒 timeout 恢复为 visible，Python `real 0.03s`、visible `1.057s`；10K 从 timeout 恢复为 visible，Python `real 0.31s`、visible `3.074s`，copy_oldest 能回到 `000000`。剩余 10K 约 `2.764s` post-process visible delay 仍需继续定位 core/daemon 写入或 history 背压，不应通过恢复两个 VTerm consumer 解决。
- `R387` 已完成：真实 tmux/TermX stress harness 新增 `stress-latency.tsv`，记录每轮 `status`、`send_unix_ms`、`done_visible_unix_ms`、`observed_ms`、`visible_ms`、pane 内 `python_real_s` 和 `post_process_visible_ms`，timeout 也会落表，避免只用 pane 内 `time` 误判 live 可见延迟。实测暴露当前问题比 R386 状态更严重：10 行 artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.smoke10`，Python `real 0.03s`，20 秒内 DONE 不可见，`stress-latency.tsv` 记录 `status=timeout observed_ms=20399`，live-timeout 仍停在初始 shell prompt；10K perf artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.perf10k`，Python `real 0.32s`，20 秒内 DONE 不可见，perftrace 显示 core 已处理 `core.live.write_screen` 473 批、约 3.36MiB、总耗时约 1.49s，但只有 1 次 `core.protocol.live_screen_snapshot`，TUI CPU 约 0.22s，live-timeout 同样停在初始 prompt。结论：当前复现优先指向 TUI live invalidation/one-shot arm/final latest pull 没有把 rapid output 后的最新 native screen 投到 FrameSink，而不是恢复 double vterm。准入已运行 `bash -n scripts/termx_tui_stress_memory.sh`，并用上述 10 行与 10K 真实 tmux harness 作为诊断复现；stress 命令本身按预期 timeout 暴露缺陷。
- `R386` 已完成：`SemanticTapResult` 现在直接携带同次 semantic pass 产出的 compact `HistoryJournal`，production process hot path 先用 journal-only gate 入队 ordinary line/open-line command journal，不再为了普通输出先拉 full `TerminalSemanticTransaction` 或读取可能阻塞的 `HistoryStore` classifier state；只有 frame/boundary/scroll-out 等复杂语义才 flush backlog 后拉取 transaction 回完整 renderer。保留 `Transaction()` deep-copy 作为复杂 fallback 与 harness 边界，tap 内部不再对 `SemanticSource` 返回的独占 tx 做额外 full clone。真实压力准入通过：100K artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.6GqUNf`，Python `real 2.90s`，daemon RSS peak `131.6MiB`，TUI peak `35.2MiB`，live/copy_oldest 均看到 DONE/oldest；1M artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.1y8jBB`，Python `real 28.10s`，daemon RSS peak `688.7MiB`，TUI peak `37.3MiB`，copy_oldest 能回到 `000000` 且包含 newest/DONE。准入已通过 `cd termx-core-v2 && go test . -run 'TestR37[238]|TestR381|TestR386|TestR385|TestR374HistoryBacklogStatusAndFlushBoundary|TestR374HistoryBacklogSeqDoesNotResetAcrossRestart' -count=1`、`cd termx-core-v2 && go test ./history -run 'TestR38[0-6]|TestR384|TestR383|TestR382' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`scripts/termx_tui_stress_memory.sh --lines 100000 --no-baseline-time --profile-mode none --wait-seconds 120`、`scripts/termx_tui_stress_memory.sh --lines 1000000 --no-baseline-time --profile-mode none --wait-seconds 240`、`git diff --check`。
- `R385` 已完成：history ingest backlog 从 full `TerminalSemanticTransaction` queue 改为 compact `HistoryJournal` queue，queue 在 enqueue/batch handoff 都深拷贝 journal payload，并用清场 guard 禁止 worker 持有 full transaction、raw PTY replay、spool 或 double-vterm 路径。process tap hot path 先把 single SemanticTap transaction 裁剪成 journal；只有 classifier 与 journal renderer 当前能安全接管时才异步入队，遇到 frame/boundary/open-line 等会影响后续判定的 backlog 会先 flush 再判定，unsupported 语义不进 queue，而是按顺序回完整 renderer。terminal-scoped applied/target seq、flush fence、catchup_pending 与 copy-entry materialized projection 继续保留，copy entry 不等待 backlog 追平。准入已通过 `cd termx-core-v2 && go test . -run 'TestR385|TestR376.*CopyEntry|TestTerminalHistoryIngestQueue|TestR374TerminalHistoryIngestQueue|TestR373ProcessOutputFansOutSemanticTapToLiveAndHistory' -count=1`、`cd termx-core-v2 && go test ./history -run 'TestR38[0-5]|TestR384|TestR383|TestR382' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R384` 已完成：journal renderer 接管 frame journal 与 scroll-out proof 应用，primary `FrameEvent` 只替换 current mutable frame，不逐帧 append timeline；alt enter 会 archive existing primary，alt frame 只作为 transient 发布并在 alt exit 清理；clear-time scroll-out proof 会通过共享 frame reducer 过滤 current-frame ownership，只 seal 真实属于 current primary frame 的 rows，避免重复 sealed shell tail。`HistoryJournalFromTransaction` 现在在 sync/alt scope 中禁止 ordered write ops 退回 ordinary batch，screen app 内容只能通过 frame proof 进入 history；Terminal fast path 只在 classifier decision 明确允许 frame/scroll-out proof 时接管 journal，prompt-after-frame、empty sync begin、ordinary ED2 dedupe 等场景仍回完整 renderer。准入已通过 `cd termx-core-v2 && go test ./history -run 'TestR38[0-4]' -count=1`、`cd termx-core-v2 && go test . -run 'TestR38[0-4]|TestR331|TestR333' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R383` 已完成：`HistoryJournalRenderer` 从 ordinary-only 扩展为支持 boundary-only state machine，并与完整 `HistoryLogicalRenderer` 共享同一组 stream/frame reducer 和 id allocator，避免 journal fast path 与 fallback 分裂 ordinary/frame owner。R383 支持 ED2 seal open line + clear primary ownership、ED3 clear-scrollback 软页边界、RIS seal ordinary open line 并清 primary/alt ownership、resize non-history boundary、alt enter/exit 与 sync begin/end 的边界扫描；sync/alt payload、scroll-out proof、frame replace/final 仍原子返回 unsupported，留给 R384 frame journal 切片，且不得从 live snapshot 回填 history。Terminal fast path 现在只允许 sealed ordinary batch 与 R383 boundary-only journal；open-line command、scroll-out proof、frame payload 或需要 current-screen proof 的 decision 继续走完整 renderer。准入已通过 `cd termx-core-v2 && go test ./history -run 'TestR38[23]' -count=1`、`cd termx-core-v2 && go test . -run 'TestR38[23]|TestR331|TestR333' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R382` 已完成：新增 `HistoryJournalRenderer`，ordinary journal fast path 可直接把 sealed logical line batch 写成 history mutation，并用 renderer-owned open-line command 处理 CJK/SGR、CR/BS/EL/CUP 等行内编辑；Terminal 通过 `NewHistoryRenderers` 共享 allocator，让 ordinary journal fast path 与完整 semantic renderer 混用时不产生 logical line/timeline id 冲突。fast path 准入复用 classifier `HistoryDecision`，只有纯 ordinary stream 才接管；遇 ED/RIS/alt/sync/frame、scroll-out proof 或 `ClosePrimaryFrameBeforeStream` 等状态机动作会回到完整 renderer，避免 screen-frame 后 prompt 被投到 frame 前。准入已通过 `cd termx-core-v2 && go test ./history -run 'TestR382' -count=1`、`cd termx-core-v2 && go test . -run 'TestR382|TestR331|TestR333' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R381` 已完成：`SemanticTapResult` 不再携带 `NativeScreenSnapshot`，tap write/resize 只返回 sanitized semantic transaction 和 live revision；生产路径继续用 revision 发布 `terminal.live.invalidated`，`live.screen.get`、测试或诊断需要画面时才通过 `SemanticTap.NativeScreenSnapshot()` 从同一 tap/vterm latest state 按需拉取 deep-copy snapshot。新增 R381 harness 证明 `ApplyPTYWrite` 与 `Resize` 不构造 snapshot，snapshot 只在显式 pull 时生成，同时保留 response owner、write/resize 顺序和 fan-out deep-copy 合同。准入已通过 `cd termx-core-v2 && go test . -run 'TestR381|TestR372SemanticTap' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R380` 已完成：`termx-core-v2/history` 新增 `HistoryJournal` 合同和 `HistoryJournalFromTransaction` recorder，journal source 明确只能是 single SemanticTap 后的 `TerminalSemanticTransaction`，不读取 raw PTY、`SourceDamage` payload、live snapshot diff、TUI rows 或第二个 vterm consumer；新增 `OrdinaryLineBatch`、`HistoryJournalBoundary`、`HistoryJournalScrollOutProof`、`HistoryJournalFrameEvent` 等类型，表达 ordinary line/open-line command、ED2/ED3/RIS/resize/alt/sync boundary、scroll-out proof 和 primary/alt frame command。R380 harness 覆盖 ordinary/SGR/CJK open update、ED2/ED3/RIS、sync begin/end、alt enter/exit、resize boundary、scroll-out proof、primary/alt frame proof、payload deep-copy，以及 resize 不从 full-replace frame snapshot 生成 history。准入已通过 `cd termx-core-v2 && go test ./history -run 'TestR380' -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R379` 已完成：新增 `termx-core-v2/docs/compact-semantic-history-journal-design.md`，把高压 PTY 输出的后续方向定为 history-specific compact semantic journal：single `SemanticTap` 仍是唯一 vterm owner，journal 必须来自同一次 vterm semantic pass，不能来自 raw PTY、live snapshot diff 或第二个 vterm consumer；ordinary stdout 以 line batch / open-line command 降低热路径成本，pseudo-TUI 以 Boundary/FrameEvent 驱动 mutable frame state machine；tap write hot path 后续要移除普通输出 full transaction、full frame/source payload 和 per-write native snapshot clone。任务队列已追加 R380-R386，后续 `/goal` 按合同 harness、latest 快路径、ordinary line batch、boundary state machine、frame journal、journal queue 和压力准入顺序推进。本切片为文档-only，准入运行 `git diff --check`。
- `R378` 已完成：用户报告 `time python scripts/generate_terminal_stress.py --lines 100000` 程序应已结束但 TUI 仍非常久；初始 tmux 复现 artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.KoSQsz` 显示 Python `real 9.07` 秒完成，但 45 秒后 live 仍停在 `076419`，daemon CPU 约 250% 且 RSS 峰值约 1.06GiB。后续 profile 证明普通高压输出在 single `SemanticTap` 后仍构造 full screen damage/transaction side scroll-out 和每 span open-line clone，导致 tap/history backlog 临时分配放大到 5GiB 级 RSS。现在 `SemanticSource` 改走 lightweight semantic damage：普通输出只保留 ordered text/control proof，不再附带 full frame、完整 SourceDamage payload 或普通 transaction-level scroll-out；screen app/sync/ED/RIS 等仍保留必要 side proof。history reducer 增加 transaction 末尾 `FlushOpenLine`，普通 fast lane 和 row ownership 编辑不再每个 span 克隆整条 open line。复测 `scripts/termx_tui_stress_memory.sh --lines 100000 --no-baseline-time --profile-mode none --wait-seconds 45` 通过，artifact `/var/folders/g1/q_9bltyx4kzfr4xpcfr8_xkr0000gn/T/termx-tui-stress-memory.tPmVaQ`：Python `real 2.99` 秒，live/copy_latest 出现 DONE，copy_oldest 能看到 oldest/newest，daemon RSS 峰值约 126MiB，TUI RSS 约 53MiB。准入已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R377` 已完成：TUI copy mode 现在显式区分 `CopyModeEnteringLivePreview`、`CopyModeMaterializedProjection` 和 `CopyModeFrozenHistory`。进入 copy mode 时先冻结 entering live native screen 供即时显示和吞输入，同时并行请求 core `history.copy_entry` materialized frontier 与原 frozen latest；materialized response 只接纳 core 已应用 history frontier，记录 native cols、applied/target history seq、catchup pending 和 capability bits，不绑定 frozen token、不伪装 older/newer/search/backend copy 合同。renderer 允许 materialized projection 无 token 渲染 copy-history rows，并在状态栏暴露 materialized/catchup pending；preview 仍是 live content，不生成 copy-history cursor/hit region。app 输入路径按能力位限制 selection/copy/search/page，catchup pending 或非 pageable materialized projection 不触发 older/newer/oldest；frozen latest 到达后替换 materialized projection，恢复既有 frozen token older/newer/search/full copy 路径。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`。
- `R376` 已完成：新增 core-v2 `CopyEntryProjection` / `CopyEntryCapabilityBits` / `CopyEntryProjectionRequest` 合同，`HistoryStore.CopyEntryProjection` 只读取已应用 history frontier 的 latest projection，不创建 `FrozenHistorySnapshot` token，也不要求调用方先 flush 全量 backlog。`Server.TerminalHistoryCopyEntryProjection` 暴露 native cols、generation、cursor/boundary、applied/target history seq、catchup_pending 和 capability bits；catchup pending 时只允许已应用区域 selectable，不声称完整 copy/search/page 能力，live native screen 只提供 native cols 元数据，不作为可复制 history truth。protocol 新增 `history.copy_entry` 与 `protocol.Client.CopyEntryProjection`，payload 复用 history window projection 并携带 R376 元数据；tui-v3 services 新增 copy-entry request/result 和 adapter 映射，状态三态落地留给 R377。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R375` 已完成：`StreamLineReducer` 增加 ordinary logical-line-first fast lane，普通 stdout 的顺序 `WriteSpan` 直接维护当前 mutable logical line，`LF`/`NEL`/`IND` 按 line feed seal，`CR`/`EL`/`BS`/`CUP` 等有限光标编辑只改当前 open line；scroll-out proof 仍走现有 proof seal 路径，保持 exactly-once payload 语义。快路径只消费已排序 vterm semantic op，不读取 live snapshot、不解析 raw PTY、不判断程序名；一旦遇到 ED/scroll/copy/full-replace、screen app frame 或已有 row ownership，就先把当前 fast lane 物化回原 row ownership reducer，由复杂语义边界继续处理。新增 R375 harness 覆盖线性 stdout seal、CR/BS/CUP/EL mutable 编辑不退回复杂 reducer，以及 ED2 复杂边界必须退出 fast lane。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R374` 已完成：history backlog 现在以 tap 后 semantic transaction 为唯一不可丢单位，并由 `terminalHistoryIngestQueue` 分配 terminal-scoped applied/target seq；`FlushHistory` 捕获调用时的 target seq 等待 `applied >= target`，不再用 flush marker 伪装 queue item，也不会把未来输出纳入本次等待。新增 `HistoryBacklogStatus`、`Terminal.HistoryBacklogStatus`、`Server.TerminalHistoryBacklogStatus` 作为只读诊断，暴露 `applied_history_seq`、`target_history_seq`、`catchup_pending`、pending/in-flight 状态；诊断不 flush、不读取 live snapshot、不构造 window，也不成为 history payload truth。history window/freeze/copy 的 authoritative 路径仍先 flush；live rows/snapshot 可在 history backlog 阻塞时读取最新 tap screen，不被完整 backlog 反压。补充 R374 harness 覆盖 queue applied/target、并发 flush waiter、future output 不进入当前 flush、live hot path 不等 history backlog、history.window 等待 backlog 追平，以及 restart 后 terminal-scoped seq 不倒退。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R373` 已完成：生产 terminal PTY 输出已切到 single `SemanticTap`，`Terminal` 不再持有 live `SurfaceTrack` 或 history `SemanticSource` 双 vterm；tap 同步维护 latest native screen、OSC/DA/DSR response owner，并 fan-out live invalidation 与 history semantic transaction。history backlog 改为只接收 tap 后 deep-copy transaction，禁止 raw PTY spool/replay；history disabled 模式仍只更新 tap/live，不创建 history store payload。新增 tap queue flush fence，`FlushHistory` 先等待已排队 PTY 输入推进 tap，再等待 history transaction worker，resize 不再越过已排队语义输入。补充 R373 harness 覆盖真实 process output fan-out、live-only、response exactly-once、single/split synchronized transaction、sync+alt 同包、prelude+sync+alt 合包、long synchronized scroll-out、队列 flush 边界和 raw backlog guard；修复 classifier 对 sync end 后纯 alt transient 的判定，避免把 alt 写入误判为 ordinary prompt，同时保留 ED2 clear-time proof 去重。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R372` 已完成：新增 `termx-core-v2/docs/semantic-tap-history-copy-execution-plan.md`，明确当前错误链路是 live `SurfaceTrack` 与 history `SemanticSource` 双 vterm、history raw PTY replay、resize/response owner 分裂和 copy enter full flush；目标链路是 PTY bytes/resize 进入 single `SemanticTap`，再 fan-out latest live projection 与 complete semantic transaction。新增 core-v2 `SemanticTap` 合同：同一 vterm owner 顺序消费 PTY bytes 与 resize，维护 latest native screen，产出 transaction deep-copy handoff，并由 tap 独占 OSC/DA/DSR response owner。R372 harness 覆盖 single vterm live/history 同源、write/resize/write seq、response exactly-once、live snapshot coalescing 不丢 emulator state、history consumer 只接 sanitized transaction、tap fan-out parser 后边界、transaction/snapshot deep-copy。旧 `terminalHistoryIngestQueue` 与 `Terminal.historySemantic` 已明确标为 R373 前的 legacy raw backlog/double-vterm 隔离对象，并新增 guard 防止 legacy worker 冒充新 tap transaction queue。子 Agent 只读审核发现 raw input record、handoff immutability 和生产双 vterm 证明边界问题；已移除 tap raw PTY input record，tap transaction 不暴露 raw replay payload，补 deep-copy/handoff harness，并明确生产 fan-out 切换留给 R373。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R371` 已完成：本轮已回退旧 R371 方向错误的 3 个提交，保留 `R369` cursor-driven copy/history 滚动和 `R370` copy latest native 宽度锚点。当前问题重新定性为架构合同错误，而不是单点 storage/flush/TUI reflow 性能问题：真实 PTY 输出目前仍被 live `SurfaceTrack` 和 history `SemanticSource` 两个 vterm 分别消费，history worker 慢时 replay raw PTY 并在 copy latest 的 `Freeze -> FlushHistory` 上阻塞交互。经两个只读子 Agent 审核，后续必须先立 single `SemanticTap` 合同：唯一 vterm owner 顺序消费 PTY bytes 与 resize，维护 latest native screen 和 OSC/DA/DSR response，产出 immutable semantic transaction；live 只合并 downstream render wakeup/snapshot，不能丢 semantic event 后维护 screen；history 完整消费 semantic transaction，不能起第二个 vterm replay raw PTY。copy mode 也必须新增 `CopyEntryProjection`/`MaterializedHistoryProjection` 类合同，不能把现有 `Freeze` 改成半追平 token，也不能把 live native screen 提升为可复制/search/page 的 history truth。任务队列已拆成 R372-R377，后续按 workflow 最早未完成切片推进。
- `R370` 已完成：定位 100K stress 后进入 copy/history 的两个问题：第一，TUI latest 请求用 pane 可见裁剪宽度重排 logical line，follower/observer pane 连接到更宽 terminal native screen 时会改变软换行边界，导致历史模式行号/行首位置和 live 最后一屏不一致；第二，protocol latest 已经先 `TerminalHistoryFreeze` 等 history worker 追平，又马上通过 `TerminalHistoryWindow` 二次 flush 同一 frozen token，压力输出后会多等一轮 history queue。本切片让 copy latest 列宽优先使用 owner binding desired cols；非 owner/follower 才用 live native surface cols，保证共享 terminal 的裁剪 pane 不重排 live native screen，同时不让旧 helper 的 root surface 宽度覆盖 owner 真值。core protocol latest 在 freeze 后用同一 token 读取 no-flush window，公开 `TerminalHistoryWindow`、copy、older 等路径仍保持 flush fence。新增/更新 harness 覆盖 follower pane 按 native surface cols 请求 latest、无 native 时回退 content/binding cols、protocol fake latest 宽度、local reflow/overlap selection，以及 core frozen token no-second-flush helper。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R368` 已完成：调查同一 TUI 两个 panel 绑定同一个 terminal 的压力刷新变慢。现有 runtime 已经在 `liveFrameReadyTargetsFromRoot` 按 terminalID 去重 ready 目标，core wake token 也是 terminal-scoped；缺口在 reducer 没保存“该 terminal 已经 armed one-shot callback”的状态，重复 frame-ready 会重新发起同 token effect，依赖 effect runner 取消旧任务，造成高压下无意义的 callback churn。本切片在 `LiveSurfaceRefreshState` 增加 `Armed`，由 TUI reducer 记录 one-shot callback pending；`LiveFrameReadyMsg` 只有在同 terminal 没有 Armed/InFlight/Dirty 时才 arm，callback 返回的 `LiveEventMsg` 先释放 Armed，再按普通 invalidation 启动一次 latest fetch。两个 pane 共享同一 terminal 的 harness 已锁定：第一次 ready 只产生一个 terminal-scoped arm effect，重复 ready 不重挂，wake 后只发起一次 `live.screen.get`。per-view attach/channel 仍保持 view-scoped，因为它承载输入重试、resize owner/follower 和 pane/floating 身份，不在本切片强行合并。准入已通过 focused `cd termx-tui-v3 && go test ./state -run 'TestTerminalSurfaceRefresh' -count=1`、focused `cd termx-tui-v3 && go test ./app -run 'TestLiveFrameReady|TestAppRuntimeArmsLiveInvalidation|TestAppRuntimeDoesNotArmLiveInvalidationForDroppedFrame|TestAppRuntimeArmsAllVisibleLiveTerminals' -count=1`、`cd termx-tui-v3 && go test ./... -count=1`。
- `R369` 已完成：定位并修复 copy/history 滚轮体验差异。input 层原来把 copy mode 内 wheel-up/PageUp 路由到 older 请求路径，wheel-down 走 mouse fallback；app reducer 的 wheel/PageUp/PageDown/u/d 主要调用 `ScrollViewport`，所以实际移动的是可见窗口，cursor 只是按 viewport offset 被 clamp。本切片把 copy/history 浏览输入收敛成 cursor-driven scroll：wheel/PageUp/PageDown/u/d 先移动 copy cursor，cursor 越过可见边界时 viewport 才跟随；本地窗口内消费不完的剩余行数才触发 core authoritative older/newer window 请求或 pending scroll。input 新增 `IntentRequestNewer`，让 wheel-down/PageDown 明确进入 history newer 路径，不再靠 fallback mouse/key handler。`AcceptLatest` 与 older prepend 后的 deferred scroll 也改用 `ScrollCursor`，避免异步 response 回来时从 cursor 模型跳回 viewport 模型。新增/更新 harness 覆盖 entering wheel、floating hit view wheel 隔离、wheel 先移动 cursor、PageDown 先移动 cursor 再请求 newer、raw wheel down 不漏给 terminal。准入已通过 focused input/state/app tests 和 `cd termx-tui-v3 && go test ./... -count=1`。
- `R367` 已完成：按用户要求删除 R366 临时 16ms/60fps pressure budget，不再用固定时间放行 live frame。TUI 现在把 core `live.invalidation.next` 当作 one-shot enable：attach/refresh 拉到 latest surface 后先渲染，只有本地 FrameSink 确认该 frame 真实写出，runtime 才投 `LiveFrameReadyMsg`，reducer 再 arm 下一次 one-shot wake。`LatestFrameSink` 的完成信号区分 `Written=true/false`，被 latest-only 覆盖丢弃的中间帧不会触发下一次 enable；一次全局 frame 写出会为当前 TUI 已接收的 terminal surface 分别投 ready，避免多 terminal 单槽 completion 丢 wake。core/protocol 合同未变：`ObservedRevision` 仍只是已观察到的 core native screen revision，不是 rendered ack，core 不保存客户端帧队列、不追中间 revision。TUI 侧同时移除 `DirtyCount` 诊断字段，`Refreshes` 只保留 `InFlight/Dirty/Cols/Rows` 背压状态。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`git diff --check`。
- `R365` 已完成：用户在真实 TUI 中复现 `time python scripts/generate_terminal_stress.py --lines 100000` 两秒后直接跳到最终结果，中间缺少连续 live 刷新；100k 裸脚本约 2.16s，总体速度正常，根因之一是 `live.invalidation.next` 仍是 future-only one-shot，TUI fetch/渲染与下一次 arm 之间发生的 native screen revision 没有可重放 wake。本切片给 one-shot arm 增加 observed native screen revision：TUI 只回传上次从 core live.screen/wake 观察到的 revision，core 若 latest 已超过 observed 立即返回一个 wake，否则等待下一次 wake；这不是 rendered revision，也不追中间帧。同时把 live queue 单批从 64KiB 收到 16KiB，并把单个 64KiB PTY read chunk 按换行/UTF-8 安全边界拆成多个 live ingest 批次，避免压力输出先攒成很大的最新屏再 invalidation。tmux 真实 100k stress 采样已能看到中间 live 帧约 `030084 -> 080556 -> 100000`，不再只在最终结果刷新；仍保持 latest-only 合并，不引入定时刷新、逐 revision 追帧或本地 history fallback。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`go test ./termx-proto/... -count=1`、CLI focused `cd termx-cli && go test ./cmd/termx -run 'TestV3PathPolicy|TestV3PingConnectsRealCoreV2Daemon|TestDefaultLocalControlCommandsUseCoreV2Protocol|TestV3LocalControlCommandsRemainAvailable' -count=1`、`git diff --check`。CLI 全量曾遇到既有 tmux emoji smoke 超时，画面已到 resize/emoji 阶段但未等到后续 marker，未作为本切片准入阻塞。
- `R366` 已完成：新增 `TERMX_PERF_TRACE` / `TERMX_PERF_TRACE_INTERVAL_MS` / `TERMX_PERF_TRACE_BUCKET_MS`，daemon/TUI 可输出聚合和时间桶 perf JSON。真实 tmux stress 显示 core live write 约 0.22ms、core snapshot 约 1.2-1.3ms、TUI render+FrameSink 约 0.5ms，不是 renderer/TTY 写出瓶颈；交互输入场景旧路径因 dirty successor 把普通 `LiveSurfaceMsg` 长时间标记为 superseded，约 3.75s 只写 113 帧并出现 0.8s 级视觉停顿。修复后 runtime 在压力下仍 latest-only 丢中间态，但每 terminal 按 16ms frame budget 放行可见 surface 并立即渲染；同一 tmux harness 约 3.55s 写 246 帧，采样不再长时间卡在同一行号。
- `R363` 已完成：用户现场复现 TUI live surface 卡在 pending，并收到 `protocol error 400: protocol: no protobuf params codec for method "live.invalidation.next"`。源码已有 R361 codec case，但本机默认 socket 上仍有旧 wire v1 daemon，导致新 TUI 通过 Hello 后请求旧 daemon 才 400。本切片已把 wire version 升到 2，并让 core/CLI 默认 socket 随 wire version 分代为 `termx-v2-wire2...`，避免新 TUI 继续连旧 daemon；同时新增 core protocol service 端到端 harness，锁定 `live.invalidation.next` terminal-scoped params decode/dispatch 不缺 codec。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -count=1`、`go test ./termx-proto/... -count=1`、`git diff --check`。
- `R364` 已完成：用户复现 `time python scripts/generate_terminal_stress.py --lines 1000000` 输出期间 live display 近似卡住，下一次刷新已到约 60 万行，同时 `Ctrl+V` 无法进入 copy mode。根因在 core live ingest queue 按 1MiB 合并 PTY 输出后才推进 native screen/revision/invalidation；本切片把 live 批次上限收回 PTY 读粒度 64KiB，并用 harness 锁定 stress chunk 不再合并成更大 live 批次。TUI 侧新增 Ctrl+V 压力队列 harness，确认普通 live refresh 已排队时输入会先进入 copy mode pending 并发起 authoritative latest request，不会漏给 terminal input。本切片没有改 history worker/store；history 写入仍是独立 worker，copy/history truth 仍只来自 core authoritative HistoryWindow。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R362` 已完成：默认 file-backed history enabled 的 daemon RSS 线性驻留已回到 history owner 修复。HistoryStore 不再为普通 sealed line 保留一行一个 timeline record，file-backed window/freeze/copy 改用 compact projection metadata，只在当前窗口读取 payload；history semantic ingest queue 改为把未消费 PTY backlog 写入临时 spool，pending queue 只持有 offset/length/flush marker，避免 history 追平落后时 raw payload 常驻 Go heap。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。手动验证：裸 generator 100k/1M 分别约 `real 1.36` / `real 12.81`；100k true stress artifact `termx-tui-stress-memory.IC2YdJ` 输出阶段 daemon/TUI peak 约 45.7/33.3 MiB，copy-oldest 后约 139.8/46.7 MiB；1M true stress artifact `termx-tui-stress-memory.5YIJoK` 输出阶段 daemon/TUI 约 52.5/34.1 MiB，copy-latest 约 74.0/34.7 MiB，1M copy-oldest catch-up 采样 daemon RSS 约 294 MiB、heap alloc 约 136 MiB，已不再复现旧 801 MiB RSS。剩余问题：1M copy-oldest/history catch-up 仍 CPU-bound，未在本切片内作为 oldest latency 优化完成，后续若继续推进应单列性能切片。
- `R361` 已完成：completion audit 发现 `live.invalidation.next` 仍携带 `RenderedRevision`，core `NextLiveInvalidation` 仍按客户端已渲染 revision 过滤 wake，这与用户目标“core 不关心 TUI 已经渲染到哪一帧”冲突。本切片已删除该跨进程渲染进度合同：protocol/client/service request 只携带 terminal scope，core one-shot arm 只等待下一次 terminal live wake，TUI stream 持续 re-arm 但不回传任何 rendered revision，wake 后仍只通过 `live.screen.get` 拉 latest。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R360` 已完成：真实 stress memory harness 已改为统计当前 daemon 默认 file-backed history 目录 `state/termx/history-v2/*.history-lines.bin`，删除无效的旧 `core-v2-history/*.compact` / forced backend 选项，并给报告完成后的 `v3 kill/rm` 诊断清理加超时，避免 1M 压力报告已生成后卡在 cleanup。core-v2 `history.window mode=oldest` 不再复用 `OlderWindow`/prepend，而是从 frozen authoritative projection head 返回 replace window；内存与 file-backed/indexed store 均有 harness 覆盖，terminal 链路也锁定 oldest 返回 replace。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`；真实脚本 1k/100k/1M 均能看到 copy oldest，1M 报告显示 TUI RSS 仍约 35 MiB，daemon history RSS 仍显著增长，后者属于 core history 驻留/回收后续问题，不属于 live latest-only 链路。
- `R359` 已完成：真实 tmux stress harness 暴露出 R355 将 terminal input 发送异步化后，同一长命令的字符会被多个 goroutine 并发送入 core，导致 PTY byte stream 乱序。现在 `FuncEffect` 增加 `SerialKey`，真实 `AsyncEffectRunner` 对相同 domain owner 的异步 effect 串行执行；terminal 普通输入和 copy-mode paste 都使用 `terminal.input:<terminal>/<view>/<channel>` 作为保序边界。输入仍不阻塞 TUI 主循环，不影响 live latest-only fetch 并发。新增 runtime 级串行 effect harness 和 interactive runtime 长命令输入保序 harness。准入已通过 focused TUI app 测试；完整准入本切片提交前运行。
- `R358` 已完成：按用户要求“不适配错误的新接口，接口不对就用以前接口”，core live native screen 迁回 `c4ee7923` 的接口形状。`live.SurfaceTrack` 重新只持有 `vterm.VTerm` 当前屏，写入走 `WriteForLatestFrame`，`SurfaceWriteResult` 不再承载 semantic transaction、damage、primary/alt frame clone；`terminalLiveIngestQueue` 恢复 1MB latest 批次并删除 flush/in-flight fence，`FlushHistory` 只等待 history semantic worker。真实 PTY 输出现在分成 live worker 与 history semantic worker：live worker 只更新 native screen/revision 并发布 invalidation，history worker 用独立 `vterm.SemanticSource` 追平 authoritative history；history.window/freeze/copy 入口会 flush history worker，但不等待 live queue 或 TUI 渲染。为保持 screen app 语义，history worker 只按 private CSI mode 边界拆分 vterm transaction，不做 raw parser/history truth。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R356` 已完成：用户指定 `c4ee7923feb47dbf1e8c0e8829021f473a481cf2` 的实时链路为基准后，本轮把 TUI live loop 迁回该方向。R351-R355 引入的 `wanted_revision/current_revision/written_revision` 追赶状态、`FrameWrittenMsg`、`AfterRenderFunc`、`Frame.OnWritten` 和 `FrameWriteCompletionSink` 已删除；`TerminalSurfaceStore` 恢复为 `Refreshes` 的 `InFlight/Dirty/Cols/Rows` 背压状态。普通 `terminal.live.invalidated` 仍经现有 core `live.invalidation.next` RPC 获取，但 TUI service 在 stream effect 中持续 re-arm，runtime 队列按 terminal 合并 wake-up；reducer 只在无 in-flight 时拉 `live.screen.get`，in-flight 期间只记 dirty，surface 返回后如 dirty 再拉一次 core 当前 latest screen。`LiveSurfaceMsg` 恢复 `Superseded` 压力语义：已有后继或 dirty refresh 时，中间 surface 只推进 reducer 状态，不触发一帧渲染。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`。
- `R357` 已完成：新增 core-v2 history disabled live-only 运行模式。`WithHistoryDisabled()` 让 terminal 创建时不创建 history renderer/store，PTY 写入和测试注入输出只调用 `SurfaceTrack.WriteNativeScreen` 更新 native screen 与 live revision，不再构造 `SurfaceWriteResult`、semantic transaction、primary/alt frame clone 或 store mutation；resize/close/exit 也不进入 history renderer。daemon 可通过 `TERMX_HISTORY_DISABLE=1` 启动该模式，auto-start 会携带同一环境变量；history.window/copy/freeze/release 返回 `ErrHistoryDisabled`，protocol 映射为 unavailable。该模式用于验证压力输出下“只看 latest native screen”的体感和 RSS，默认 daemon 仍保持 file-backed history enabled。
- `R355` 已完成：定位到 latest-screen 设计仍像逐帧追的关键原因在真实 `LatestFrameSink` 与 runtime 的完成边界错位。以前 `runtime.renderFrame` 调用 `WriteFrame` 后立刻执行 `afterRender`，但真实 host 的 `LatestFrameSink.WriteFrame` 只是把完整帧放进 latest-only 队列就返回；这会过早重新 arm `live.invalidation.next`，导致 TUI 继续拉取/渲染中间 latest screen，视觉上仍像在补旧帧。现在 `render.Frame` 支持 `OnWritten` 完成回调，`LatestFrameSink` 只有在后台 writer 真正写完完整帧后才回投 `FrameWrittenMsg`，runtime 只对最新提交帧的完成消息运行 live 后续状态机；state 侧用 `WrittenRevision` 区分“已接收 snapshot revision”和“已显示 revision”。同时 terminal input send effect 改为真实 runtime 异步执行，Ctrl+C 等输入不再同步等待 input ack 卡住 TUI 主循环。新增 perf 点 `tui.frame_written`、`tui.frame_sink_written`。准入已通过 `cd termx-tui-v3 && go test ./... -count=1 -timeout=60s`。
- `R354` 已完成：按用户要求把 live invalidation 从旧持续 stream/伪消息链路完整重构为 one-shot arm。R361 后，`live.invalidation.next` 只携带 terminal scope，core-v2 `Server.NextLiveInvalidation` 不接收、不比较 TUI 渲染进度；它只等待下一次 terminal live wake，不推送 screen payload，也不暴露持续事件流。tui-v3 删除 `LiveFrameRenderedMsg` 和旧 render-completion 合同；arm 返回后才触发一次 `live.screen.get` 拉 latest。service adapter 删除 live union stream，`ArmLiveInvalidation` 只接受 `terminal.live.invalidated`，metadata/lifecycle 不再混入 live wake path。准入以对应切片记录为准。
- `R353` 已完成：定位到 TUI 仍像逐帧追屏的主因在 `LiveRenderRequestState` revision 判定，而不是 core latest API。旧 `terminal.live.invalidated` 如果晚于当前 snapshot 到达，之前仍会启动 `live.screen.get`；in-flight 期间产生的 dirty 即使已被本次 `get latest screen` 返回的更高 revision 覆盖，也仍会在 frame rendered 后自激下一次 fetch。现在 `RequestLiveRender` 会忽略 `revision <= current_revision` 的旧唤醒，`finishLiveRenderFetch` 只有在非零 snapshot revision 仍低于 wanted revision 时才保留 dirty。新增 state/app harness 覆盖 stale invalidation 不触发 fetch、latest snapshot 覆盖 dirty 后清 request、未知 revision 仍保守 follow-up。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R352` 已完成：修正 R351 后仍会补渲染已返回 native screen fetch result 的问题。runtime 现在把普通 `LiveSurfaceMsg` 也纳入 live projection latest-only 队列合并；同一 terminal 的连续普通 surface result 只保留最后一个，避免压力输出后 TUI 把旧 projection 一帧帧写完。合并不会跨普通业务消息，也不会跨 lifecycle/resize/attach/exit 等语义边界；`LifecycleKnown` 或 error surface 仍必须保留。新增 harness 覆盖普通 surface 合并、普通消息边界、lifecycle surface 保留和 stale payload reference 清理。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R351` 已完成：TUI live render loop 改成 interface-first 边界。services 新增 `NativeScreenSource` / `LiveInvalidationSource` 作为 app 依赖接口；state 用 `LiveRenderRequestState` / `RenderRequests` 维护 per-terminal `wanted_revision + current_revision + in_flight + dirty + size`，不再用旧 `Refreshes` 补丁状态。R351 初版只把 `terminal.live.invalidated` wake-up 合并，并把 native screen fetch result 背压交给 reducer；R352 已补上普通 fetch result 的 latest-only 队列语义。FrameSink 写完一帧后才回投 `LiveFrameRenderedMsg`，reducer 此时才消费 dirty 并拉 core latest screen。protocol adapter 的 lifecycle/metadata event 不再顺手拉 live screen；screen、lifecycle、resize-control 继续在消息语义上分离。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R350` 已完成：按用户要求先删后写，旧 live projection 运行时链路已清场。protocol 删除公开 `Snapshot` / `CompactSnapshot` / `GridViewport` / `ScreenUpdate` / `TypeScreenUpdate` / `TypeStreamReady` 合同和 snapshot overlap trimming；core attach channel 不再推 screen stream，只保留 attach/input/resize/lifecycle owner 与 `live.screen.get` 拉取当前 native screen；tui-v3 protocol adapter 不再调用旧 `snapshot`/compact snapshot，普通 live invalidation 只作为 refresh wake-up。剩余 `Snapshot` 命名仅限 workbench/clipboard/session 本地存储、TUI view-model 字段、`live.SurfaceTrack.Snapshot()` 内部 live surface 读法，以及 native screen payload 暂时复用 wirepb 字段，不再是 v3 live display 主合同。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R349` 已完成：core-v2 新增 `LiveRevision`、`NativeScreenSnapshot`、`LiveScreenInvalidated` domain contract；protocol 新增 `live.screen.get` 和 `terminal.live.invalidated` event，live revision 写入独立 live 字段，不再借 `Snapshot/CompactSnapshot.HistoryGeneration`。普通 PTY 输出、resize、restart 和 exit marker 都发布明确 live invalidation；旧 attach `ScreenUpdate` stream 暂时改为监听 live invalidation，等待 R350 删除。`snapshot`/`SnapshotCompact` 仍保留过渡兼容，但不再承载 live revision。新增 protocol/native screen payload roundtrip、live invalidation event roundtrip、core live screen revision 单调、terminal live invalidation 和 scoped transport 覆盖。准入已通过 `go test ./internal/protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R348` 已完成：新增 `termx-core-v2/docs/live-native-screen-rebuild-design.md` 作为 live native screen 重构基准。新设计确认 core 只拥有每个 terminal 的最新 native screen、live revision 和 invalidation wake-up；TUI 拥有 render loop，收到 invalidation 后只标记 dirty/wanted revision，拉取 core 当前最新 snapshot，渲染完成后若期间又 dirty 再拉一次，不补渲染中间帧。文档同时列出清场对象：`Snapshot/CompactSnapshot` 的 live/history 混合字段、`ScreenUpdate` full-replace stream、`grid.viewport`、snapshot overlap trimming、`TerminalLiveEvent` union、lifecycle 混入 live snapshot、runtime live queue coalescer 等；也列出必须保留的 SurfaceTrack、semantic transaction、PTY response、attachment/resize owner、modes/cursor/cell conversion、copy mode entering live snapshot 和 LatestFrameSink 边界。准入已通过 `git diff --check`。
- `R347` 已完成：确认 latest screen 设计本身成立，真正风险在 live projection 的下游边界。core-v2 `Terminal` 现在维护独立的 live projection revision，并在同一 live 锁内随 native screen rows 一起透出；`SnapshotCompact` 复用现有 `HistoryGeneration` wire 字段承载该 revision，但明确只作为 live snapshot stale reject 边界，不是 logical-line history truth generation。TUI app 层把普通 `terminal.changed` 触发的 surface 拉取标记为普通 live projection，即使 service adapter 顺手查到 lifecycle，也不会伪装成 lifecycle boundary，从而允许 dirty successor/latest-only 跳过中间帧；attach 初始 surface 和显式 lifecycle query 仍保留生命周期权威边界。新增 core protocol revision 单调测试、普通 refresh 去 lifecycle 边界测试和 runtime latest-only dirty successor 测试。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R346` 已完成：定位用户看到的两个现象分别来自两条链路。TUI 低帧率不是程序整体执行慢，而是 core live ingest queue 曾按 1MB 合并 PTY 输出，protocol/TUI 又按 4096 个普通 changed 做 latest-only 合并，导致压力输出时 latest screen 只能大块跳变；现在 live ingest 单批降到 PTY read 粒度，core protocol 和 TUI service 的普通 changed 合并窗口降到 64，仍保留 resize/exit/read-error/跨 terminal 等语义边界。RSS 到 2.8G 的主因是生产 `Terminal` 仍用 `NewInMemoryHistoryStore`，R341/R342 的 file backend 只存在于 history 包测试；现在 core-v2 server 支持 `WithHistoryStorageDir`/`WithHistoryStoreFactory`，默认 daemon/v3 daemon 将 sealed logical-line payload 写到 XDG state 下 `history-v2`，backend 创建失败时注册 terminal 直接失败而不静默回退内存。新增测试覆盖生产 terminal 写出 `.history-lines.bin`、backend 失败不 fallback、terminal id 路径转义、默认 daemon 配置 history dir，以及大 backlog 分段让出 refresh。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -count=1`、`git diff --check`。
- `R345` 已完成：全链路抓取确认 live 刷新不足不只在 TUI 渲染端，core protocol 仍有两条旧热路径会把普通 `terminal.changed` 当内容帧逐个推送：channel 0 event stream 和 attach channel full-replace `screen.update` stream。现在 core protocol 对普通 live invalidation 做 bounded latest 合并，保留 resize/exit/metadata/read-error/跨 terminal 边界；TUI protocol adapter 同样改成 bounded yield，避免 R344 的“吞到 channel 为空”在持续输出时饿死刷新。新增 `perftrace` 计数点覆盖 `core.terminal.changed`、`core.protocol.event`、`core.protocol.attach_screen_update`、`tui.protocol.live_event`、`tui.live_event`、`tui.live_surface`、`tui.frame`；bench 显示 10 万 ordinary changed 在 core protocol 和 TUI service 边界都收敛为 25 次 live invalidation。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`，并额外运行 `go test ./termx-core-v2 -run '^$' -bench 'BenchmarkProtocolLiveInvalidationDrain100k' -benchtime=1x` 与 `go test ./termx-tui-v3/services -run '^$' -bench 'BenchmarkProtocolTerminalServiceAdapterLiveEventDrain100k' -benchtime=1x`。
- `R344` 已完成：定位压力输出期间 TUI “一帧一帧追屏”的直接原因是 protocol terminal adapter 在普通 `terminal.changed` backlog 中按固定 `maxProtocolLiveRefreshDrain=64` 分批吐出 refresh invalidation；这会让 app 层反复拉 latest snapshot 并写出中间屏，视觉上从 100 跳到 300/500/1000 逐段追，而不是把 changed 当成 latest-only invalidation。现在 service 层会吞并当前已经排队的同 terminal 普通 refresh backlog，直到 backlog 为空或遇到 resize/exit/read-error/metadata 等语义边界才吐一次 refresh；新增测试覆盖 512 个 changed 只产生一个 refresh，并验证 semantic boundary 不被吞掉。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R343` 已完成：定位 `time python scripts/generate_terminal_stress.py --lines 100000` 卡顿的直接 core-v2 根因之一是 `inMemoryHistoryStore.applyMutation` 每处理一个 mutation 后调用 `reindexCounters()`，全量扫描已有 logical lines、timeline records 和 frame records；10 万行普通输出会把 authoritative history 写入拖成近似 O(n²)，同步压住真实 PTY 输出链路。现在 `Apply` 热路径只观察当前 mutation 携带的 line/record/frame/session id 增量推进辅助计数器，`reindexCounters()` 仅保留给 recovery 构造路径；新增 guard 测试禁止 `Apply`/`applyMutation`/store upsert/flushStorage 热 helper 重新调用全量 reindex，并用连续单行 Apply 验证增量计数器和 latest projection。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R342` 已完成：删除 R341 临时 JSONL payload store，改为二进制 append-only record。文件 record 使用固定 magic/version/header、line id 与长度前缀；payload 自身用小端整数和字符串长度编码 logical line、cells、style，并把连续相同 width/style/link 的 cells 合并成 run，避免 JSON 反射解析和文本膨胀。新增 guard 测试禁止 file backend import `encoding/json` 或回到 `.jsonl` 路径。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R338` 已完成：继续沿 `335af9ac4b7e9e44703540c93e9655a224d3f73d` 方向推进，保留当前 copy/history 交互基础，只收敛 core-v2 pseudo-TUI 历史边界。renderer 已删除跨 transaction 的 stale scroll-out 记忆：ED2 repaint 不再因为更早发生过 payload scroll-out 就把当前尾屏 seal 进 authoritative history；旧屏内容进入历史只能来自同一 transaction 的 clear-time scroll-out proof。新增 R338 renderer harness 覆盖“先 payload scroll-out、后无 clear-time proof ED2 repaint”场景，验证只 clear current ownership 并 publish 最新 frame，不 close stale tail。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R337` 已完成：对照 iTerm2/tmux 后确认这不是 Codex 程序名问题，而是普通 shell 输出恢复时的 frame/session boundary。tmux 的 `scroll-on-clear` 会在整屏 clear 时把屏幕内容滚入 pane history；iTerm2 对“不用 alt-screen、把光标移到 prompt 上方重绘”的程序也会先把可见屏幕保存到 scrollback，并且 ED2 与 ED3 分开处理。我们原来在 `ClosePrimaryFrameBeforeStream` 上直接 seal renderer 里的旧 `PrimaryCurrent`，导致 Codex 退出时一闪而过的 `Shutting down...` transient 行被写进 history，而 prompt transaction 已经带着的最终可见屏幕 proof 没被使用。现在 frame reducer 新增 `ClosePrimaryCurrentFromFrameExcludingRows`，只用同一 transaction 的 `PrimaryFrame` proof 收束 current frame 已拥有的 rows，并排除本次 ordinary stream 触达行；renderer 在普通输出恢复前优先走该路径，prompt 行仍由 stream reducer 消费，已 sealed shell tail 不会被复制进 final screen-frame。新增 R337 reducer/renderer/server harness。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R336` 已完成：用户复现的 Codex 内 `/clear` 后当前屏只显示新内容但 history 上滑看不到清屏前内容，根因是 R333/R329 为避免 transient UI 重复，把 ED2 clear-time scroll-out proof 一律跳过；这把 ED2 错当成了“只清 current frame、不进入 scrollback”的 repaint boundary。按 iTerm2/tmux 语义，ED2 只清 viewport，不等同 ED3 clear scrollback；若清屏前有 primary current frame，vterm 的 clear-time scroll-out proof 表示该 frame 真实离开 viewport，应进入 scrollable authoritative history。现在 classifier 仅在 `state.HasPrimaryCurrent && ED2` 时打开 `ConsumeClearTimeScrollOutProof`，renderer 会 seal 该 proof 并只 clear current frame ownership，不再同时 close 同一旧 frame，避免 proof + frame 双写；没有 primary frame ownership 的 ordinary shell clear-time proof 仍跳过，避免重复已 sealed shell tail。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R335` 已完成：用户复现的“普通 shell 5 行，进入 Codex 后 history 变 10 行”不是 TUI 本地重复，而是 core authoritative history 在 full-replace primary frame 启动路径中把 vterm 整屏 `PrimaryFrame` side proof 全量接管为 current frame。上一轮 R334 只覆盖 synchronized-output touched rows，未覆盖 vterm 因 `broad_direct_cell_damage`/`repeated_direct_damage` 产出 `RequiresFullReplace` 且缺少 ordered content ops 的路径。现在 vterm `WriteDamage` 记录并排序 `DirectDamageTouchedRows`，`TerminalSemanticTransaction.PrimaryFrameTouchedRows` 透传该 proof；core classifier 在已有 sealed timeline 时只允许带 touched-row proof 的 full-replace frame 按行接管 current frame，renderer 用同一 `ReplacePrimaryTouchedRows` 路径发布新 UI 行，不再把已 sealed shell tail 复制为 `current-primary-frame`。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`。
- `R301` 已完成：新增 `termx-core-v2/docs/r301-history-cleanup-audit.md` 记录清场审计结论；本轮追加清场后，旧 `historyANSIParser`、semantic ingest、terminal history pipeline、history queue、history projector 和相关 production/test harness 均已删除。`TestR301HistoryCleanupGuard` 现在防止旧 history implementation 文件、程序名分支和真实 PTY chunk 直接 raw queue 路径重新出现。准入已通过 `cd termx-core-v2 && go test ./... -count=1`。
- `R302` 已完成：`termx-vterm/vterm/semantic_source.go` 新增 `TerminalSemanticSource`、`TerminalSemanticTransaction`、`TerminalSemanticSize`、frame/scroll-out contract 和 `SemanticSource` adapter；`ApplyPTYWrite` / `Resize` 产出 ordered ops、primary scroll-out proof、primary/alt frame、alt enter/exit、synchronized output、full replace/resize boundary。`termx-core-v2/terminal_semantic_contract.go` 目前只保留 vterm contract alias，不再保留旧 history adapter 或旧 semantic ingest 桥接。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`。
- `R303` 已完成：`termx-core-v2/history/memory.go` 新增纯内存 `HistoryProjector` / `InfiniteHistoryStore` domain 骨架，只消费 `TerminalSemanticTransaction`、`ScreenAppDecision`、`HistoryMutation` 和 lifecycle close；`termx-core-v2/history/projector_harness_test.go` 用 fake semantic transaction / fake classifier 覆盖普通 scroll-out commit、CR/CUP/EL frontier mutate、primary synchronized/current repaint current-only、alt transient no-commit、process exit force close、resize non-history boundary 和 frozen copy boundary。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R304` 已完成：`live.SurfaceTrack.WriteWithResult` 现在保留同一 vterm 产出的 `TerminalSemanticTransaction`；`Terminal` 持有 R303 内存 projector/store，普通输出只按 semantic ops 和 scroll-out proof 更新 logical-line history，CR/EL/CUP/BS 等只 mutate frontier，process exit 通过 `HistoryProjector.ForceClose(CloseReasonProcessExit)` 收口 primary mutable frontier。新增 `TerminalHistoryWindow` / `TerminalHistoryCopy` 仅供 core-v2 domain harness 使用；protocol `history.window` / `history.copy` 在 R310 前仍返回 `ErrHistoryNotRebuilt`。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R305` 已完成：terminal history classifier 现在把 synchronized primary transaction 按 terminal semantics 归入 primary screen session，并通过 R303 projector 发布 `current-primary-frame`；repaint 替换 current frame，不增加 ordinary committed depth。`TerminalHistoryFreeze` 仅用于 core-v2 harness 验证 frozen boundary 能指向 current primary frame segment；protocol/TUI 接入仍留给 R310。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R306` 已完成：terminal history classifier 现在按 `AltEntered` / `AltExited` / `AltFrame` terminal semantics 发布 transient alt frame；纯 alt frame 可在 latest 选择但不 ordinary commit，alt exit 释放 transient frame。primary current 进入 alt 前会 archive/hide，退出 alt 后的新 primary output 发布为新的 current frame id，不复活 pre-alt current ownership。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R307` 已完成：resize transaction 现在作为 non-history boundary 进入 projector，不能从 resized live snapshot 生成或重写 committed history；process exit 会把 active primary current frame 作为 final `screen-frame` commit 一次，committed projection 保留 fixed-grid kind 和生成时 `ScreenCols`。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R308` 已完成：新增 history view-time `HistoryTheme` / `ResolveCellStyleForTheme` 边界；default fg/bg 继续以空 token 保存在 payload，只有查看时按 theme 解析，`ansi:N`、`idx:N` 和 `#rrggbb` 明确颜色 token 原样保留。terminal harness 覆盖 default、16 色、256 色和 truecolor SGR 写入 history payload。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R309` 已完成：新增 R309 纯内存 `StorageBackend` 实现与 harness，`StorageTransaction` 明确承载 lines、tombstones、committed index、frontier ids 和 frame records；recover 重建完整 domain state，compact 只删除未被 committed/frontier/frame 引用的 payload，backend 不定义 line mutability。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R310` 已完成：protocol `history.window` / `history.copy` / `history.release` 现在接 core-v2 authoritative `HistoryWindow` / frozen token / copy / release 边界；window payload 保留 row kind、row segment、logical line id、generation、token 和 segment cursor。内存 store 补齐 committed segment older prepend 最小实现，tui-v3 protocol adapter 显式传递 `older` / `oldest` mode，继续只消费 authoritative window/copy，不从 live rows、snapshot、LoadedRows 或本地 row count 推断分页。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`go test ./internal/protocol/... -count=1`、`git diff --check`。
- `R311` 已完成：tmux `capture-pane -S -` 对同类 synchronized output 能看到所有真实经过 PTY 的行；vterm dump 定位到 `WriteDamage.ScrollbackAppend` 已含 `Runs` 文本，但 `TerminalSemanticTransaction.PrimaryScrollOut` 曾只拷贝 `Cells` 导致 proof 变空，且 begin/payload/end 分片中间 payload 缺少 active sync mode 会被误判为 ordinary。现在 vterm 保留 scroll-out `Runs`，`TerminalModes.SynchronizedOutput` / `TerminalSemanticTransaction.SynchronizedActive` 暴露 mode 2026 的 active 状态；core primary session 路径消费同一 transaction 的 scroll-out proof 提交 logical lines，并保留最后一屏为 current primary frame。准入已通过 `cd termx-vterm && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R312` 已完成：修复 Codex history latest 能看到 current frame 但上滑到 frame 之外为空的问题。core `OlderWindow` 现在支持从 `current-primary-frame` / `archived-primary-frame` cursor 接回 committed history；latest freeze token 保存当时 visible primary/alt frame payload，后续 repaint 不会改写 frozen latest window。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R313` 已完成：tmux harness 先挂 pipe 后触发输出，确认 raw PTY bytes 与 `capture-pane -S -` 都包含完整 `log01..log12` 与 current frame；根因是 core/protocol older 已返回 rows，但 prepend response 的 `Boundary.LastLineID` 使用 older page tail，TUI state 会把它判为 stale 并丢弃，导致上滑区域黑/空。现在 older prepend 保留 latest frozen tail boundary，并补齐 core domain、protocol 和 TUI state harness。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check`。
- `R314` 已完成：新增 `termx v3 history-dump <terminal-id> --out <file>`，默认按 core-v2 `history.window` latest + older cursor 分页 dump authoritative history，并在结束时 `history.release` frozen token；dump 文件记录 window token/boundary/cursor、row logical line id、row-in-line、segment、kind 和文本，用于区分 core 存储/分页问题与 TUI 展示问题。CLI harness 覆盖 committed tail + current primary frame + older prepend 场景。准入已通过 `cd termx-cli && go test ./cmd/termx -count=1`、`git diff --check`。
- `R315` 已完成：`/tmp/termx-history.dump` 显示 core authoritative history 有 3 个 windows、1413 rows、1226 committed rows 和 35 current frame rows；同一 `codex resume 019efc61-417a-7682-9ce6-fa8da10ad9e3` 的 tmux capture 只有 71 行可见正文。dump 中 `archived-primary-frame` 有 152 行，其中 90 行是 trailing default blank rows，说明黑/空区域来自 core frame projection 把整屏尾部默认空白当作 history rows 发给 TUI。现在 semantic screen-frame 进入 core history payload 时裁掉 trailing default blank rows，但保留中间空行和带显式样式/链接的空白行。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R316` 已完成：本轮先用 tmux 跑真实 `codex resume 019efc61-417a-7682-9ce6-fa8da10ad9e3` 并抓 `history-pageup-*.txt`，确认最新代码下 6 次 PageUp 均有 30-35 行正文，不再是黑/空屏；再用受控长历史 PTY harness 生成 260 行 committed 输出后进入 history，确认 TUI PageUp 能从 latest frame 翻进 older committed 行。根因在 TUI copy/history 把 PageUp/wheel 当 cursor 移动，容易停在最新 frame 内部；现在新增 `CopyModeStore.ScrollViewport`，PageUp/wheel/进入 pending scroll 均移动 viewport，cursor 只跟随保持可见。renderer 侧同时把 default blank frame cell 交给 viewport 背景，不再固化成终端 ANSI 背景；明确 styled blank/TailFill 仍保留。准入已通过 `cd termx-tui-v3 && go test ./... -count=1`、真实 tmux Codex resume harness、受控长历史 tmux harness。
- `R317` 已完成：分析 `/tmp/termx-history.dump` 确认 core authoritative dump 本身存在投影顺序问题：`archived-primary-frame` 来自早期 `/resume` 前 primary frame，却被 latest 直接追加在全部 committed 日志之后，造成历史看起来乱序。现在 core-v2 memory store 按 transaction sequence 组装 committed/archive/current timeline，latest 只返回 committed tail + active current/alt frame，不再把 archived frame 挂在最新尾部；older 从 current/alt cursor 回翻时按 timeline 返回 archive 再回 committed。新增 domain 和 terminal 链路 harness。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R318` 已完成：新增 `termx-core-v2/docs/history-logical-renderer-design.md` 作为新的 history 语义基准，明确 history 是消费同一份 vterm terminal semantic transaction 的 logical-line renderer；去掉 `commit/committed` 领域概念，改用 `mutable` / `sealed` / `timeline` / `frame journal` / `frozen projection` / storage residency 边界。`workflow.md` 已把该文档列为当前设计基准，旧 screen app final plan 与 core-v2 architecture 已标注为历史背景或待迁移旧词。准入已通过 `git diff --check`。
- `R319` 已完成：补全 `history-logical-renderer-design.md` 的 semantic 覆盖矩阵，并同步代码接口名；删除旧 `memoryHistoryProjector` / `memoryHistoryStore`、旧 projector harness 和 `frameRowsFromWriteOps` fallback；新增 `HistoryLogicalRenderer`、`StreamLineReducer`、`FrameReducer`、`HistoryState`、`SealedTimeline`、`FrameJournal`、`HistoryStore` 等边界。terminal/protocol 在新 renderer 接入前明确返回 `ErrHistoryNotRebuilt`，避免继续暴露旧错误历史。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`；子 Agent 只读审核无阻塞 findings。
- R319 后，旧 `HistoryTrack`、frontier/index/window/snapshot/storage/style、旧 raw parser、旧 projector、旧 terminal history pipeline、旧 `memoryHistoryProjector` / `memoryHistoryStore` 和相关 harness 均已删除。core/server/terminal/protocol 已切断旧 history storage、pipeline 和 window 投影入口；新的 `HistoryLogicalRenderer` / `HistoryStore` 真实实现接入前，`history.window` / `history.copy` / `history.release` 必须显式返回未重建，后续不得沿用已删除补丁路径，也不得通过 stub、fallback 或桥接代码恢复旧模型。
- `R320` 已完成：`termx-core-v2/history/events.go` 新增 `HistorySemanticEventsFromTransaction` 和 `HistorySemanticEventOrderSource`，把 `TerminalSemanticTransaction` 的 ordered ops、alt enter/exit、primary scroll-out proof、primary/alt frame、resize/full-replace 统一归一化为 renderer 内部 event 链。R320 harness 覆盖 WriteSpan row/col、CR、CUP、EL、ECH/DCH/ICH、ScrollRect、CopyRect、PrimaryScrollOut、PrimaryFrame、Alt enter/exit、AltFrame、Resize/full-replace 和真实 vterm transaction；当前 vterm 只给 `Ops` 提供精确 op 间顺序，scroll-out/frame/full-replace side proof 明确标记为 transaction-level order，后续若 reducer 需要 raw 中精确位置，必须最小联动 vterm 增补 ordered proof。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R321` 已完成：新增 `NewStreamLineReducer` 及内部 row ownership/cursor/open-line state；`WriteSpan` 按 row/col 覆盖对应 draft，CR/LF/IND/NEL/soft-wrap 和 BS/CUB/CUF/CHA/HPA/CUP/VPA/CUU/CUD 更新 cursor/row ownership，EL/ED/ClearRect/ECH/DCH/ICH 按 vterm semantic op 修改 mutable draft，ScrollRect/CopyRect 只移动或复制 mutable row ownership，离屏内容只由 `PrimaryScrollOut` proof seal。R321 harness 覆盖 `abc\rX\n`、EL mode 0/1/2、CUP 目标行写入、ED/ClearRect、ECH/DCH/ICH、soft-wrap+LF、ScrollRect/CopyRect 和重复 proof exactly-once。当前 vterm scroll-out proof 仍无独立 proof id，reducer 只能用 payload signature 防止同一 proof 重放；若后续需要区分相同文本的不同 proof，必须先最小联动 vterm 增补 ordered proof id。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R322` 已完成：新增 `NewFrameReducer` 和 primary/alt frame journal state；`ReplacePrimaryCurrent` 只用 vterm `TerminalSemanticFrame` 全量替换 mutable primary current，不从 write ops 重建 frame，也不把 repaint 追加进 timeline；`ArchivePrimaryCurrent` 在 alt enter 等边界把 current primary seal 为 archived frame 并清 current；`ReplaceAltCurrent`/`ClearAltCurrent` 只维护 transient alt frame，不写 primary timeline；`ApplyNonHistoryBoundary(FrameReasonResize)` 只发 non-history boundary，不 seal 或重写 frame；`ClosePrimaryCurrent` 生成 fixed-width final primary frame。R322 harness 覆盖 primary repaint current-only、纯 alt transient no primary timeline、archive 顺序、post-alt new frame id、resize boundary 和 final screen-frame 固定 cols。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R323` 已完成：新增 `NewInMemoryHistoryStore` / `NewInMemoryHistoryStoreFromRecovered`，新的内存 store 只应用 `HistoryMutationBatch`，维护 logical line payload、sealed timeline、open line、frame journal、frame records 和 frozen projection；latest window 返回 sealed timeline tail + active open/current primary/alt projection，older window 按 timeline cursor 回翻，archived frame 按 record sequence 出现在真实位置，不挂 latest 尾部；freeze 保存当时 mutable frame rows，后续 repaint 不改 frozen window；copy 从 frozen/live authoritative rows 取文本；recovery 从 storage backend 的 lines/timeline/frame records 重建投影。R323 projection 目前以 logical line 为 window row，visual wrap 留给后续 protocol/TUI 投影切片。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R324` 已完成：Terminal 现在持有新的 `HistoryLogicalRenderer` 与 `HistoryStore`，write 路径只把 live surface 产出的 vterm semantic transaction 交给 renderer，resize 只作为 non-history boundary，process exit/remove/shutdown 通过 `CloseReason` 收口 open line/current frame；`TerminalHistoryWindow` / `Copy` / `Freeze` / `Release` 已恢复为 core-v2 authoritative store 入口，不再返回 `ErrHistoryNotRebuilt`，也不从 live snapshot 或 raw PTY fallback。新增 R324 terminal/protocol harness 覆盖普通 authoritative window/copy/release、primary synchronized scroll-out + current frame、alt transient 不进 primary timeline、resize 不删历史、process exit 和 terminal remove lifecycle close。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R325` 已完成：protocol history.window/history.copy/history.release 通过 R324 authoritative store 返回真实 rows/copy/token release，并在 harness 中锁定 row kind、row segment、logical line id、generation、cursor boundary 和 copy token。CLI `termx v3 history-dump` 在新 store 分页下恢复可用，`--limit 1 --all` 现在按 older older latest 输出；本轮从 CLI dump 暴露出新 FrameReducer 未裁尾部默认空白 frame rows 的 payload 缺陷，已回到 `termx-core-v2/history/` owner 修复：frame rows 只裁掉尾部纯 default blank rows，保留中间空行和 styled blank 内容。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -count=1`、`git diff --check`。
- `R326` 已完成：新增 renderer 级 shared `historyIDAllocator`，让 StreamLineReducer 与 FrameReducer 在同一个 terminal history renderer 内共享 logical line id 和 timeline record id，修复 scroll-out/open-line 与 frame row id 冲突导致 authoritative payload 被覆盖的问题。新增 R326 tmux/vterm harness：用真实 tmux capture 作为可观察 PTY 基准，并用 core-v2 authoritative history window 验证长 synchronized output、current frame 之外 older paging、alt enter archive、纯 alt transient 不进 primary history、process exit final screen-frame 和 resize 后 archived frame 固定生成宽度。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R327` 已完成：新增 Terminal 级和 history domain 级回归，覆盖 `/resume` 类 synchronized clear+redraw 的 authoritative history、二次 redraw 中相同文本 scroll-out 仍作为新 PTY 事件记录、非 resize full-replace 带 primary frame 时发布 current frame、latest limit 截在 current frame 内时 older 先返回 frame 前半部分、CJK width=0 continuation 不再投影成中文之间的真实空格。实现上移除 scroll-out payload signature 去重，改为按 transaction event 记录；history cell 转换跳过无文本 width=0 continuation，并按 grapheme width 展开 scroll-out runs；older window 改为基于完整 live projection 回翻；classifier 对非 resize `RequiresFullReplace` + `PrimaryFrame` 走 primary frame session。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`git diff --check`。
- `R328` 已完成：tmux 受控序列确认 `ED2` 是“清当前屏并把可见内容按顺序送入 scrollback”，`ED3` 是明确 clear scrollback/history 边界。vterm 现在把 ED2 clear-time rows 挂在 `ed` semantic op 上，core 按 ordered event 在 redraw 前处理 clear boundary；ordinary stream 清屏前会 seal open line，primary frame redraw 只替换 current frame，不把 redraw 同时写成 ordinary timeline；ED3 同时清 store 与 renderer mutable/session 状态，避免二次 `/resume` 沿用旧 session。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`git diff --check`。
- `R329` 已完成：按 vterm 解析 PTY 后的实际产出补齐 history 消费缺口。core classifier 现在读取 authoritative history ownership 边界，区分 ordinary stream 已 seal 内容和 primary current frame 内容：普通 ED2 不重复消费 scrollback proof，primary frame ED2 必须把 vterm scrollback proof 写入 committed history 并清 current ownership。StreamLineReducer 现在消费 `il/dl/su/sd/ri/decstbm/ht/cbt/ris` 等 vterm controls，明确忽略只影响终端能力/状态查询且不产生历史 payload 的 controls，并处理 vterm `TailFill`。RIS 会 seal ordinary open line 并清 current frame/alt transient。新增 R329 domain 与 terminal harness 覆盖 ED2 primary scrollback proof、line controls、scroll region RI、RIS 和 TailFill。准入已通过 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-vterm && go test ./... -count=1`、`git diff --check`。
- `R330` 已完成：新增 `scripts/emit_codex_resume_numbered.py`，默认输出三段编号内容：普通 `S01 001..100`、第一次 clear+redraw `S02 001..100`、第二次 clear+redraw `S03 001..100`；可切换 `ED2`/`ED3`/`RIS`/无清屏、同步输出模式和 CJK marker，并可生成 expected manifest 用于和 TermX/tmux/history dump 对照缺行、混行、截断和中文空格。
- `R331` 已完成：截图中的“shell prompt 插在 `S03` 历史中间”根因不是 manifest，而是 history projection 仍保留 `S03` primary current frame，后续普通 prompt 作为 open line 被投影到 current frame 前；同时 ordinary timeline record 缺少全局 sequence，会被排到 frame record 前。现在 ordinary record、scroll-out record 和 frame record 共享 renderer timeline sequence；普通输出恢复时若仍有 primary current frame，会先关闭 frame 再消费 ordinary stream，prompt 因此排在 `S03 REDRAW_END` 之后。
- `R332` 已完成：真实 tmux 基准确认编号脚本顺序本身正常；复测 current TermX authoritative dump 定位到两个 core 问题：protocol latest 建立的 frozen token 只保存 latest 页，导致 `history-dump --all --limit 40` 的 older 分页只能重复最新页；ordinary stream reducer 内部按 slice index 写入 vterm display column，宽字符后续列被补成真实空格，形成 `中 文 编 号`。现在 frozen token 保存完整 live projection，latest 只裁当前页，older 从同一 frozen projection 向前翻；普通流写入按 display column 计算 cell index，宽字符 continuation 不再落成空格。受控复测 `scripts/emit_codex_resume_numbered.py --lines 100 --sessions 3 --clear ed2 --redraw-mode all --cjk-every 10` 后，authoritative dump 共有 9 个 window，能看到 `S01 001/100`、`S03 026/100`、`TERMX_DONE_MARKER`，且 `bad_cjk=0`。
- `termx-core-v2/docs/history-rebuild-goal-prompt.md` 是给 `/goal` 使用的 R303-R310 连续推进 prompt；它要求每轮只处理最早未完成切片，完成 harness、实现、准入、`workflow.md` 更新和中文提交后再进入下一切片。
