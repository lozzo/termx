# 工作流：termx TerminalView/Attachment 多 panel 连接主线

本文件是当前分支唯一有效的活动驱动文件。后续所有分析、实现、测试、提交都必须先读取本文件，并以本文件为准。

本文件只记录目标、范围、硬约束、任务队列、测试准入和提交规则。架构正文不写在本文件里，分别以 `termx-core-v2/docs/architecture.md`、`termx-tui-v3/docs/architecture.md`、`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md` 和 `termx-cli/docs/v2-v3-switch-audit.md` 为准。

本文件必须保持全中文。若本文件与旧说明、聊天记录、旧代码行为或局部假设冲突，默认以本文件为准；若技术设计需要变化，必须先更新本文件或与实现同切片更新。

## 1. 当前唯一目标

在默认入口已经切到 `termx-core-v2/` 与 `termx-tui-v3/`，styled chrome、panel content、真实 PTY、workbench storage 和基础可用性切片已经完成的基础上，把“同一个 terminal 可以被同一屏幕、同一 tab 内多个不同 panel 连接”的 `TerminalView/Attachment` 语义补成当前主线，修正此前 `pane -> terminalID -> 全局 Session` 的一对一假设。

当前事实：

- `go run ./termx-cli/cmd/termx`、默认 `termx daemon`、`termx attach`、`termx new/ls/kill/rm` 已使用 `termx-core-v2` 与 `termx-tui-v3`。
- `termx-tui-v3` 已有自有 runtime、input、state、services、terminalhost、copy mode 和最小 render 骨架，且不依赖 Bubble Tea contract。
- `termx-tui-v3/render` 已建立 render framework / content renderer 分层，`RenderVMBuilder` 输出 `ShellVM`，`Renderer.RenderResult` 通过 workbench shell、panel、overlay 和 toast 合成 `RenderResult`；旧 `RenderVM{Lines, Status}` 兼容输入字段已删除，`Frame.Lines` 只作为 `RenderResult` 的 plain 输出适配保留。
- `termx-tui-v3/state` 已拥有 reducer-owned shell、workspace/tab/pane 最小树、panel presentation、header/footer visibility、toast/message 和 Terminal Picker overlay 状态模型。
- 外部 terminal emulator resize 已作为 runtime message 流进入 reducer-owned state；`RenderVMBuilder` 已把外部 viewport 作为 shell layout 尺寸 truth；renderer 已按已知 viewport 填满输出；active terminal resize 已从 panel content rect 计算；默认 UI chrome 已收敛到 Unicode box drawing。
- `termx-tui-v3/docs/ui-interaction-spec.md` 已明确 `terminal` 是全局运行实体、`pane` 是观察位/工作位，且一个 terminal 可以被多个 pane 复用；但当前实现仍大量依赖 `PaneState.TerminalID`、全局 `TerminalSessionStore`、全局 live stream token 和 active content rect resize，尚未形成 first-class `TerminalView/Attachment` 模型。
- `core-v2` protocol 已有 `SurfaceID`、`ViewID`、`ResizeOwnership` 和 channel 字段雏形，但当前 protocol session 主要保存 `channel -> terminalID`，没有 attachment registry、resize owner/follower 裁决、view identity 生命周期和多 view harness。
- `tui-v3` 当前 `TerminalSessionStore.InputChannels` 只是急修阶段的输入 channel 路由元数据，不是完整的 pane/floating view session truth；后续必须收敛为 reducer-owned view/attachment store。
- 当前 `Frame` 由 `RenderResult` 适配生成 plain、styled 和 ANSI 三种输出视图；真实 `FrameSink` 优先写 ANSI styled frame，plain `Frame.Lines` 只用于测试快照、smoke 输出和宽度断言。
- 当前默认 UI 只达到基础 styled chrome renderer 和产品壳可操作基线，尚未达到用户截图要求的 `tuiv2` 视觉等级；后续不能把“有 Unicode 线框、有颜色、有交互”误判为视觉对齐完成。
- 切片 80-82 完成后的真实视觉复核未通过：用户明确指出当前 TUI 样子仍与目标截图不一致，因此切片 83 只能作为复核失败和返工拆分归档，不能作为视觉完成验收。
- 当前主线不是回到旧 `tuiv2` 原地修补，也不是直接复制 `tuiv2/render`；`tuiv2` 只能作为只读视觉目标和经验参考，新实现必须服从 `termx-tui-v3` 的 render framework + content renderer 架构。

完成定义：

- `termx-tui-v3` render 主路径使用 `render framework + content renderer` 作为正式结构。
- `RenderResult` 是唯一主输出，必须保留 styled cell / styled line / metadata；纯字符串、测试快照和真实 TTY ANSI 输出都只是适配层。
- 真实 `FrameSink` 必须能写出 ANSI styled frame，不能丢失边框色、active/inactive 状态、header/footer 背景、toast/overlay 样式和必要 reset。
- 默认 TUI 进入后不再只显示裸文本 `live surface pending` 或 `live: termx-main`，而是显示 workbench shell、header/footer、panel chrome 和 panel content。
- 默认 workbench chrome 必须达到 `tuiv2` 截图级别：整屏布局、稳定 top/bottom bar、pane 细线边框、active pane accent、inactive pane muted、pane 标题/状态/action 槽位、内容区明确裁切。
- pane/floating 不再以裸 `TerminalID` 作为唯一连接身份；最终必须通过 `TerminalView/Attachment` 绑定 terminal、protocol channel、view id、surface id、resize role 和 view-local request/error/desired size。
- 同一个 terminal 可以同时被多个 tiled pane、floating pane 或后续 tab/workspace view 连接；terminal process、lifecycle、input sink 和 authoritative history truth 仍只有一份。
- close pane / detach pane 只移除当前工作位和 view binding，不 kill terminal；kill terminal 是 destructive terminal lifecycle 操作，会影响所有绑定该 terminal 的 view。
- resize 必须有 ownership 语义：同一 terminal 同时只能有一个有效 resize owner 修改 PTY size，follower/observer view 不得因为自己的 content rect 变化覆盖 terminal process size；focus 或显式操作如需转移 owner，必须走协议和 reducer/effect/message 路径。
- copy/history 仍按 terminal authoritative `HistoryWindow` 工作，但交互态、content cols、pending request 和 rebind 必须绑定到发起 copy 的 pane/view，不能被同 terminal 的其它 view 覆盖。
- 视觉完成不能只看 smoke 文本是否有线框；必须以真实 TTY ANSI frame、截图/录制、固定 viewport smoke snapshot 和人工对照 `tuiv2` 目标风格共同验收。
- 当前切片 75-78 只完成产品壳、基础 styled renderer、terminal/copy 前推和 render 输入清理，不等于上述视觉完成定义已经满足。
- 本轮 UI 产品壳完成后，除 terminal-live/copy-history 的深层内容渲染仍可继续深化外，header/footer、pane、floating、Terminal Pool、Workbench Tree、Prompt/Help、Tab/Workspace、toast、overlay、快捷键和鼠标入口必须形成可基本操作的产品闭环。
- 如果 UI 产品壳完成后仍有余力，优先把 terminal live 的连接展示和输入交互继续前推，但不得以牺牲 UI 产品壳完备验收为代价。
- 最小 render framework 阶段同时支持 card panel 与 split line 两种 tiled panel 呈现，并至少覆盖双 pane 横向和纵向分割。
- header/footer hide 必须真实影响 layout，隐藏后 body 回收空间，workspace、tab、mode、notice/error 仍可通过短标识、toast 或 Help 入口恢复识别。
- toast 支持真实渲染和基础生命周期：severity、pending/progress、auto dismiss、close current、clear all 和窄屏退化。
- pane 分屏、关闭、focus、zoom、resize/size change 必须作为统一 semantic command 落地，快捷键、鼠标 hit region、测试入口和后续 CLI mini command 都只能调用同一命令契约。
- pane 结构命令至少覆盖横向/纵向 split、close pane、close and kill terminal、focus/activate、zoom/unzoom、按方向和步长 resize、按比例或固定尺寸 set size、balance/equalize，以及 card/split 呈现切换；结构变化必须触发 layout measurement、active terminal content rect resize、copy mode rebind 和 toast 反馈。
- 所有 panel、split line、header/footer、toast、overlay 和 content slot 的布局、裁切、填充、对齐必须按 terminal cell display width 计算，emoji、CJK、combining mark、ANSI 样式和 host width ambiguous cluster 不得破坏边框或分割线。
- Terminal Picker 状态激活时有 overlay 或明确占位渲染路径；Terminal Pool 与 Workbench Tree 完整页面在 framework 成型后再接入。
- copy mode 仍只消费 core-v2 authoritative `HistoryWindow`，缺 window 或绑定不一致时显示 pending/empty/error，不得从 live surface、snapshot、grid viewport 或 local VTerm scrollback fallback。
- `go run ./termx-cli/cmd/termx` 默认路径继续使用 core-v2/tui-v3，不得重新引入旧 `termx-core` 或 `tuiv2` 默认依赖。
- 外部 terminal emulator 的 cols/rows 必须成为独立 reducer-owned viewport truth，不能混用 `Session.Cols/Rows` 或 `Surface.Cols/Rows` 充当 UI canvas truth。
- 真实 `TerminalHost` 必须在进入 TUI 后提供初始尺寸，并把外部窗口变化作为 resize event/message 进入 `AppRuntime`；fake host 必须能 deterministic 地注入 resize。
- renderer 输出必须以当前 viewport 填满整个上下文；当 viewport 已知时不得输出超过 viewport cols 的行，也不得因为默认 80 列导致宿主终端自动换行破坏边框。
- `RenderVMBuilder` 和 renderer 必须通过纯 layout measurement 得到 body、panel、content、overlay、toast 和 hit region rect；renderer 只消费 view-model/layout plan，不直接读取 host、service 或 core client。
- 发给 core-v2 terminal 的 resize 必须使用 active pane content rect 的 cols/rows，而不是外部 terminal emulator 总尺寸；card panel、split line、header/footer hide 和后续 floating/overlay 都必须能影响 content rect。
- host resize、header/footer hide、panel presentation 切换或 split 变化导致 copy content cols 改变时，copy mode 必须 invalid/rebind authoritative `HistoryWindow`，不得继续使用旧 bound cols，也不得从 live surface fallback。
- 默认视觉目标以 `tuiv2` 实际样式为准：shell header/footer 是单行 tab/status bar，不绘制整屏外框；pane、floating、overlay、toast 等对象 chrome 才使用边框，并通过 style 区分 active/inactive。后续若 `tuiv2` 对某类 overlay 使用圆角或特定卡片样式，应迁移为 v3 render primitive，而不是继续按旧 Unicode 线稿硬化。
- ASCII `+ - |` 只能出现在测试说明或兼容文档中，不得作为默认 UI chrome。

## 2. 技术设计基准

- core-v2 架构基准：`termx-core-v2/docs/architecture.md`。
- tui-v3 架构基准：`termx-tui-v3/docs/architecture.md`。
- tui-v3 UI 交互基准：`termx-tui-v3/docs/ui-interaction-spec.md`。
- tui-v3 render framework 基准：`termx-tui-v3/docs/render-architecture.md`。
- CLI 切换审计和迁移矩阵：`termx-cli/docs/v2-v3-switch-audit.md`。
- 本文件不展开架构正文；实现遇到架构冲突时，先更新对应设计文档和本文件任务队列，再继续实现。

## 3. 工作范围

### 3.1 当前主线范围

允许主动新增、修改、删除、重写、测试：

- `termx-core-v2/`
- `termx-tui-v3/`
- `termx-cli/`
- `internal/protocol/`
- `termx-proto/`
- 根目录直接相关文件：`workflow.md`、`AGENTS.md`、`go.work`、`go.work.sum`、`Makefile`、必要顶层说明文档

### 3.2 受限联动范围

只有当 core-v2、tui-v3、vterm、协议契约、CLI 切换或 remote 兼容确实需要时，才允许最小化触及：

- `termx-vterm/`
- `termx-shared/`
- `termx-testkit/`
- `termx-remote/`
- `scripts/`

### 3.3 只读参考范围

默认不得修改：

- `termx-core/`
- `tuiv2/`

上述目录只能读取、搜索、运行测试或摘取已验证过的外部契约作为参考。不得继续在其中做 logical-line 原地重构、旧 copy mode 修补、旧 snapshot/grid viewport history path 修补、兼容桥接或 helper 收敛。

如确实必须修改旧目录，必须先修改本文件，把该动作写入受限联动范围，并说明为什么不能在新目录完成。默认入口切换不得通过把新目录包在旧 core/旧 TUI 外层来伪完成。

### 3.4 冻结范围

不得主动触碰：

- `remote-ui/`
- `termx-app/`
- `web-control/`
- `termx-hub/`
- `bin/`
- `.claude/`
- 顶层可执行产物和测试产物
- 未在本文件列出的任何目录

如需扩展范围，必须先修改本文件的范围表，再开展对应工作。

## 4. 不可违反的语义约束

### 4.1 为什么必须使用 logical line

- 目标是支持可落盘、可分页、长期保留、接近无限的历史记录。
- 历史 truth 不能依赖当前 terminal size，因为窗口大小随时会变。
- 如果历史只存在内存 grid、snapshot scrollback 或 visual row 中，resize 后就必须读回大量历史并按新列宽重排；这不适合无限历史，也不适合稳定分页。
- logical line 是 shell/程序输出语义下的稳定历史单位；visual row 只是某个 `cols` 下的投影结果。
- 因此 core-v2 只能按 logical line 存储和分页，再按当前列宽生成 HistoryWindow。

### 4.2 core-v2 约束

- primary history 的基本单位必须是 logical line。
- logical line 必须有稳定身份，不能只靠当前窗口内 row index 表达。
- visual rows 只能是某个 cols 下的投影结果。
- wrapped metadata 可以作为投影辅助信息，但不能作为最终历史 truth。
- snapshot、grid viewport、TUI runtime scrollback 都不能作为 committed history truth。
- `LogicalLineStore` 是唯一历史数据模型。
- `CommittedHistoryIndex` 只表达当前计入 authoritative committed history 的 logical line 顺序。
- `MutableFrontier` 只表达当前仍可被终端语义修改的 logical line 范围。
- `StorageBackend` 只是内存、文件、mmap 等存储落点，不定义 mutability。
- `persisted`、落盘或 committed 不表示永远不可修改；clear scrollback、truncate、retention、reclaim、replace 都可以按完整 logical line 删除、撤回、替换或重新提交已提交历史。
- `open/sealed`、`dirty/clean`、`committed/uncommitted`、`mutable`、`residency` 是正交属性，不得混成一个状态。
- attach、reattach、bootstrap、recovery、full replace、clear screen、resize 不得凭空创造 committed history。
- resize 不是历史创建事件，也不是历史重写事件；grow resize 只能按完整 logical line reclaim committed suffix，shrink resize 必须表达 `screen -> hidden mutable frontier`。
- alt-screen 不写入 primary history；process exit 是显式 mutability 边界，退出时 primary `MutableFrontier` 必须 force commit。

### 4.3 tui-v3 约束

- `termx-tui-v3` 不拥有 committed history truth，只消费 core-v2 返回的 authoritative `HistoryWindow`。
- `HistoryStore` 只保存 core-v2 返回的 authoritative window、请求状态和 exhausted marker。
- `CopyModeStore` 只保存交互态：active pane、terminal id、viewport top、cursor、selection、bound token、bound cols。
- latest window 使用 replace。
- older window 使用 prepend。
- stale response guard 使用 core 返回的 token、generation、cursor、logical line boundary 和 cols，不使用本地深度计数。
- `TerminalSurfaceStore` 只服务实时显示，不得向 `HistoryStore` 提供 rows 让其反推出 logical line。
- copy mode、鼠标滚轮、page up/down、selection、copy 必须围绕 authoritative `HistoryWindow` 工作。
- copy mode 缺 authoritative window 时不得从 live surface、snapshot 或 VTerm scrollback fallback。
- TUI 主线不得引入 Bubble Tea `Program`、`standardRenderer`、`tea.Model`、`tea.Msg`、`tea.Cmd`、`tea.KeyMsg`、`tea.MouseMsg`、`bubbles` 或依赖这些 contract 的 UI 组件。
- 允许使用 `lipgloss/v2`、`x/ansi`、隔离在 terminal host/frame sink 内的 `ultraviolet` 等纯渲染或终端 primitive。

### 4.4 CLI / daemon 切换约束

- 默认入口切换前必须存在显式实验入口，并通过本地单 session 主路径 smoke。
- 实验入口可以暂时与旧入口并存，但必须显式命名；不能让用户误以为默认 `termx` 已完成切换。
- `termx-cli` 默认路径不得通过旧 `termx-core.NewServer` 启动 core-v2。
- `termx-cli` 默认路径不得通过 `tuiv2/app.RunWithClient` 启动 tui-v3。
- core-v2 daemon 必须自己拥有 terminal lifecycle、protocol service、event stream、history window service 和 shutdown 语义；不能把旧 daemon 当作 runtime backend。
- tui-v3 必须自己拥有 terminal raw mode、input loop、frame sink、effect loop 和 copy mode 交互；不能把旧 tuiv2 当作 runtime backend。
- 默认本地路径完成后，remote 相关差异必须在任务队列中显式保留，不得隐式宣称完整替换。

### 4.5 命名约束

- 新实现命名收敛到 `LogicalLineStore`、`CommittedHistoryIndex`、`MutableFrontier`、`StorageBackend`、`HistoryWindow`、`AppRuntime`、`TerminalHost`、`EffectRunner`、`FrameSink`。
- `hot/cold` 只能出现在旧模型问题说明或迁移记录中，不得继续作为代码、测试 helper、内部 contract 或运行时状态的主语义命名。
- 若从旧实现迁移概念，迁入新目录时必须按新语义重命名，不能把旧语义带进 v2/v3。

### 4.6 render framework 最小阶段约束

- `render framework + content renderer` 是正式方向，不得继续把 terminal 内容写成 renderer 主抽象。
- `RenderResult` 是唯一 render 主输出；字符串、测试和真实 TTY 输出都只能作为适配层。
- 最小阶段必须同时处理 card panel 与 split line，不能只做 card panel。
- 最小阶段必须处理 header/footer hide 的真实 layout 效果，不能只保留 VM 字段。
- 最小阶段必须处理 toast 基础生命周期，不能只做静态文本。
- Terminal Pool 与 Workbench Tree 完整页面不作为最小阶段阻塞项，但 Terminal Picker 状态必须有 overlay 或明确占位渲染路径。
- `Ctrl-f` 进入 Terminal Picker、`Ctrl-v` 进入 Display / Copy 是已定产品基准。
- `Ctrl-p` pane mode、`Ctrl-r` resize mode、`Ctrl-g` global mode 是当前第一版结构操作入口；card/split 切换、header/footer hide、toast close current、toast clear all 已通过 mode 快捷键进入 semantic action，不得再绕过 reducer 临时实现第二套逻辑。
- 鼠标和 hit region 必须复用同一 semantic action；render 可以只产出稳定 action token，但 app/input 必须把真实鼠标坐标派发到最新 hit region，UI chrome 区域不得漏发到 terminal。
- 所有 UI chrome 和 content slot 的宽度计算必须使用 ANSI-aware / grapheme-aware / cell-aware helper，不得用 byte length 或 rune count 作为可见宽度。
- 旧 `tuiv2` 的 width safety 经验只能迁入为 v3 render primitive 和 harness，不能迁入旧 runtime/model/cursor writer 结构。
- 最小阶段不得引入通用 widget/plugin UI 框架，也不得引入 Bubble Tea contract。

### 4.7 外部 viewport 与 resize 约束

- `ViewportStore` 或等价 reducer-owned state 必须表达外部 terminal emulator 当前可绘制区域；它是 UI canvas truth。
- `TerminalSessionStore` 和 `TerminalSurfaceStore` 表达 PTY/session/live surface 状态；它们不是 UI canvas truth。
- `HostResizeMsg` 或等价 message 是外部尺寸进入 app 的唯一写状态入口；service、renderer、terminalhost 不得直接修改 reducer-owned state。
- 初始 render 必须使用 host 初始尺寸；若真实 host 无法获得尺寸，必须显式 fallback 并在测试中覆盖，而不是无条件默认 `80x24`。
- layout measurement 必须是纯函数；同一 `ShellVM + viewport` 必须稳定产出相同 body/panel/content/overlay/toast rect 和 hit region plan。
- renderer 必须严格按 plan 绘制；content renderer 只能在分配给它的 content rect 内绘制和裁切。
- active terminal resize 必须由 app 根据 layout plan 触发 effect，并做尺寸去重，避免每帧重复向 core-v2 发送相同 resize。
- copy mode 绑定的 cols 必须等于 copy content rect width；宽度变化必须让旧 window 失效并重新请求 authoritative window。
- Unicode box drawing glyph 统一按 cell width 1 处理；emoji、CJK、combining mark、ANSI 样式和 ambiguous width 不得覆盖、推开或截断边框。

### 4.8 styled chrome renderer 约束

- styled chrome renderer 是当前新阶段正式方向；不得继续把纯文本 `[]string` frame 作为真实 TTY 主输出。
- `RenderResult` 必须保留 styled cell、styled line 或等价结构，直到最终 TTY serializer；不得在 renderer 主路径中过早丢弃 style。
- `FrameSink` 必须支持 ANSI styled frame，包含 SGR foreground/background/bold/reset 和 cursor metadata；纯文本 frame 只能作为测试快照或兼容适配层。
- renderer 内部必须有 cell matrix 或等价 compositor，能表达 text、width、style、owner/layer、continuation、safe flag；不能继续以 `[]string` 作为 canvas truth。
- theme 必须表达 host-aware palette、accent、success、warning、danger、info、panel border、muted border、chrome bg/fg、active/inactive pane chrome 等 token。
- active pane border 必须使用 accent/strong style，inactive pane border 必须使用 muted style；active/inactive 状态不能只靠标题文字表达。
- tiled pane 默认视觉必须对齐 `tuiv2` 截图级别：square Unicode 边框、细线分割、pane top chrome 槽位、top/bottom bar token、状态/动作短 token、可见焦点态。
- card panel、split line、floating/modal/toast 都必须使用 styled chrome；只出现 Unicode glyph 但无颜色/样式不算完成。
- styled chrome 的宽度计算必须 ANSI-aware；带 ANSI 的标题、token、toast、overlay 和内容裁切后 display width 必须仍等于目标 rect。
- `tuiv2/render` 只能作为只读参考：可参考 `drawStyle`、`composedCanvas`、theme token、pane chrome slot、ANSI serializer 和宽度安全思路；不得复制旧 runtime/model、VisibleRenderState 大 bag、cache key 或业务状态耦合结构。
- 当前阶段优先让 shell/chrome/pane/frame 达到视觉等级；terminal live/copy history 内容 renderer 深化可以分阶段推进，但内容必须被 content rect 裁切，不能破坏 styled chrome。

### 4.9 pane 结构命令约束

- pane split、close、resize、zoom、focus 等结构操作必须 command-first，不得只作为快捷键 handler 的局部逻辑。
- 命令来源可以是 pane mode、resize mode、鼠标 hit region、测试 harness、后续 CLI mini command 或 command palette，但最终必须进入同一组 semantic command。
- semantic command 必须携带稳定 action id、target workspace/tab/pane、orientation、direction、delta、ratio、absolute size、confirm policy 等必要参数；不得把显示文案或具体按键当作业务语义。
- reducer 负责 workspace/tab/pane/floating 结构状态变化；service 不得直接改 reducer-owned state；需要创建、kill、resize terminal 时必须通过 effect/message 回到主循环。
- split、close、resize、balance、zoom 和 panel presentation 切换后，必须重新测量 layout plan，并以 active content rect 触发 terminal resize 去重。
- active copy pane 的 content width 变化后，必须 invalid/rebind authoritative `HistoryWindow`，不得沿用旧 cols，也不得 fallback 到 live surface。
- close pane 与 close and kill terminal 必须区分：关闭 pane 只移除当前工作位；kill terminal 是破坏性 terminal lifecycle 操作，必须能走确认或明确 danger 语义。
- 后续 CLI mini command 只能作为 command adapter，不能绕过 TUI reducer、layout measurement、terminal resize、copy rebind 或 toast feedback。

### 4.10 SK 小阶段与反补丁式实现约束

- 后续 `/goal` 自动推进时，任何较大目标必须先拆成可独立验证的小阶段；每个小阶段完成后必须单独提交一个 `SK:` 前缀的中文提交，作为可回退、可审计的中间过程。
- `SK:` 提交必须对应一个真实小阶段：有清晰范围、状态更新、必要 harness 和准入命令结果；不得把多个小阶段堆成一个大提交，也不得把半成品标成 `SK:`。
- 如果一个小阶段做到一半发现边界不对，必须先收敛为架构/设计修正阶段，更新 `workflow.md` 和对应 architecture 文档，再继续实现；不得为了赶进度绕过现有架构。
- 禁止补丁式代码：不得新增临时旁路、重复 truth、第二套状态、字符串 action 分叉、renderer 读 service/runtime、service 直改 reducer-owned state、fake-only 成功路径、仅为当前 case 服务的硬编码 adapter，或长期保留新旧双路径。
- 如果新增功能要求改变现有架构，默认选择重构或重写对应边界，把 domain model、protocol contract、projector、renderer、state/effect/service 边界做干净，再接真实路径和测试。
- 允许大改，但必须小步提交：可以在一个 `/goal` 中连续完成多个 `SK:` 小阶段；每个阶段都必须保持仓库可编译、测试准入清楚、行为边界可解释。
- 旧实现只能只读参考。迁移旧经验时必须迁入 v2/v3 的新边界和命名，不能复制旧 runtime/model 大 bag，也不能为了兼容旧内部实现保留桥接层，除非本文件先明确批准。

### 4.11 TerminalView / Attachment 语义约束

- `Terminal` 是 core-v2 管理的全局运行实体，拥有 process、PTY size、terminal lifecycle、live surface truth 和 authoritative logical-line history truth。
- `TerminalView` 或 `Attachment` 是某个 TUI panel/floating/tab 对 terminal 的连接视图，拥有 pane/floating identity、protocol channel、view id、surface id、resize role、desired content size、request seq、error state 和 event stream subscription。
- pane/floating/workbench 结构状态不得继续把裸 `TerminalID` 当作完整连接 truth；可以保留 `TerminalID` 作为 terminal identity，但必须通过 view binding 表达具体连接。
- 同一 terminal 可以同时绑定多个 view；多个 view 共享 terminal process、input sink、history truth 和 terminal lifecycle，不共享 view-local focus、copy mode、content rect、resize request seq 或 UI error state。
- terminal input 必须路由到当前 active view 的 attachment channel；不得 fallback 到全局 latest session terminal。
- terminal mouse passthrough 必须按当前命中 content 所属 view 的 live modes 判断；UI chrome、overlay、toast、footer/header hit region 继续优先，不得漏发到底层 terminal。
- resize 必须按 attachment role 处理：owner view 可以发起 PTY resize，follower/observer view 只能显示当前 terminal projection 或请求显式 ownership transfer；不得让 inactive 或非 owner view 的 content rect 变化覆盖 terminal process size。
- attach/reconnect/duplicate view 必须创建或复用 view binding；close/detach pane 只移除 view binding，不 kill terminal；kill terminal 必须广播并让所有绑定 view 显示 exited/removed/error。
- copy mode 绑定 active view 的 pane/floating 与 terminal id、content cols、view rows 和 request id；history window 仍只按 terminal truth 返回，不因多个 view 存在而复制 history truth。
- live surface cache 可以按 terminal id 保存 authoritative latest projection，但 resize boundary、desired size、stream token、input channel、view error 和 stale guard 必须按 view/attachment 或明确的 terminal generation 建模，不能依赖单全局 `TerminalSessionStore`。
- workbench storage schema 必须表达 pane/floating 的 view binding；schema 变化必须版本化并有 decode/encode harness，不得悄悄复用旧 v1 字段造成语义混淆。
- core-v2 daemon 仍不理解 workspace/tab/pane truth；它只管理 terminal pool、attachment/channel registry、resize ownership、event stream 和 opaque storage。

## 5. 任务队列

状态只能使用：`待开始`、`进行中`、`完成`、`阻塞`。同一时间只能有一个切片处于 `进行中`。

自动执行必须按表格顺序处理最早未完成切片：

- 如果最早未完成切片是 `阻塞`，必须停止并向用户说明阻塞，不得跳到后续 `待开始` 切片。
- 如果最早未完成切片是 `进行中`，继续该切片。
- 如果最早未完成切片是 `待开始`，先把它改为 `进行中` 并提交，或与本切片首个实现提交同切片提交，然后只执行该切片。

| 切片 | 状态 | 范围 | 完成标准 |
| --- | --- | --- | --- |
| 0. 设计基线 | 完成 | `termx-core-v2/docs/`、`termx-tui-v3/docs/`、`workflow.md`、`AGENTS.md` | core-v2 与 tui-v3 架构文档存在；本文件和 `AGENTS.md` 指向新主线；旧实现只作为参考 |
| 1. 新模块骨架 | 完成 | `termx-core-v2/`、`termx-tui-v3/`、`go.work` | 两个新 Go module 加入 workspace；建立最小包结构、空实现和 smoke tests；不依赖旧内部实现 |
| 2. core-v2 domain 骨架 | 完成 | `termx-core-v2/` | 建立 `LogicalLine`、`LogicalLineStore`、`CommittedHistoryIndex`、`MutableFrontier`、`StorageBackend` 内存实现和基础 harness |
| 3. core-v2 历史事件语义 | 完成 | `termx-core-v2/` | 覆盖 write/seal/mutate/reset/commit/reclaim/hide/truncate/alt-screen/process-exit/resize 事件；非历史事件不创建 committed history |
| 4. core-v2 HistoryWindow 投影 | 完成 | `termx-core-v2/` | 从 logical lines 生成 visual rows、line spans、clipping、token、generation、cursor、latest replace、older prepend |
| 5. protocol 契约联动 | 完成 | `termx-proto/`、`internal/protocol/`、`termx-core-v2/` | 按需扩展 `history.window` contract；legacy snapshot/grid viewport 明确只作为实时兼容投影 |
| 6. tui-v3 runtime 骨架 | 完成 | `termx-tui-v3/` | 建立自有 `Msg`、`Effect`、`AppRuntime`、`EffectRunner`、`TerminalHost` fake、`FrameSink` contract 和 harness |
| 7. tui-v3 history/copymode 状态 | 完成 | `termx-tui-v3/` | reducer-owned `HistoryStore`、`CopyModeStore`、latest/older/stale/resize/selection harness 完成 |
| 8. tui-v3 input/render/UI 边界 | 完成 | `termx-tui-v3/` | 自有 `InputEvent`、semantic intent、RenderVMBuilder、Renderer、hit regions、lipgloss style helper；无 Bubble Tea contract |
| 9. services 与集成 | 完成 | `termx-tui-v3/`、受限联动范围 | core client、terminal service、session、clipboard contract、fake、protocol history adapter 和最小 runtime e2e harness |
| 10. 收口与迁移入口 | 完成 | 受限联动范围 | 新路径可运行；必要 CLI/adapter 入口接入；旧 helper/fixture 只在明确不再需要时删除 |
| 11. 切换审计与迁移矩阵 | 完成 | `termx-cli/docs/`、`termx-core-v2/docs/`、`termx-tui-v3/docs/`、只读参考旧目录 | `termx-cli/docs/v2-v3-switch-audit.md` 已明确当前 CLI 命令到旧依赖的映射、目标 v2/v3 命令矩阵、协议方法矩阵、daemon 能力矩阵、TUI 能力矩阵和分阶段验收口径 |
| 12. core-v2 server API 与 daemon 骨架 | 完成 | `termx-core-v2/`、`internal/protocol/`、`termx-cli/` 按需 | core-v2 已提供独立 server/daemon API、options、listen/shutdown、terminal registry、事件订阅 fake harness；静态 harness 确认不调用旧 `termx-core`/`tuiv2` |
| 13. core-v2 terminal lifecycle 与 PTY 管线 | 完成 | `termx-core-v2/`、`termx-vterm/`、`termx-shared/` 按需 | core-v2 已建立 `TerminalProcess`/`ProcessFactory`、terminal lifecycle、create/input/resize/exit/restart/remove harness；输出进入 live surface 与 `HistoryTrack` ingest；exit force commit、late output guard、resize grow/shrink 和 shutdown lifecycle harness 通过 |
| 14. core-v2 protocol service 与 HistoryWindow 实服务 | 完成 | `termx-core-v2/`、`internal/protocol/`、`termx-proto/` | core-v2 protocol session 已服务 create/get/list/set metadata/restart/remove/events/input/resize/history.window；HistoryWindow 来自 `HistoryTrack` logical line truth，包含 token/generation/cursor/boundary 与 stale guard；协议测试通过 |
| 15. tui-v3 真实 TerminalHost 与 FrameSink | 完成 | `termx-tui-v3/` | 建立真实 raw mode、输入读取、窗口尺寸、帧输出、恢复终端状态和取消/退出 harness；不得引入 Bubble Tea runtime |
| 16. tui-v3 本地 live app 主路径 | 完成 | `termx-tui-v3/`、`termx-cli/` 按需 | attach 本地 session 后可渲染 live surface、发送键盘输入、处理 resize、显示基础状态与错误；fake 和最小真实协议 harness 通过 |
| 17. tui-v3 copy mode 主路径 | 完成 | `termx-tui-v3/`、`internal/protocol/` 按需 | page up/down、鼠标滚轮、older prepend、latest replace、selection、copy、stale response guard 在真实 core client 路径通过 |
| 18. CLI v3 命令组与 daemon 骨架 | 完成 | `termx-cli/`、`Makefile` | 增加显式 `termx v3` 实验命令组；`termx v3 daemon` 以前台方式启动 core-v2 server；提供非交互 v3 smoke 入口验证 tui-v3 可被 CLI 装配运行；默认 root、`daemon`、`attach` 仍保持旧入口；CLI 测试和迁移 smoke 通过 |
| 19. CLI v3 daemon 连接与自动启动基础 | 完成 | `termx-cli/`、`termx-core-v2/` 按需 | v3 实验入口能连接已存在 core-v2 daemon；需要 daemon 时只能自动启动 core-v2 daemon，不能复用旧 `termx-core` 自动启动路径；socket、日志路径、启动失败和关闭行为有测试 |
| 20. CLI v3 本地控制命令 | 完成 | `termx-cli/`、`termx-core-v2/`、`internal/protocol/` 按需 | `termx v3 new`、`termx v3 ls`、`termx v3 kill`、`termx v3 rm` 通过 core-v2 protocol service 工作；命令输出和错误语义与旧默认入口可对照；CLI tests 覆盖本地单 session |
| 21. CLI v3 attach/TUI 装配 | 完成 | `termx-cli/`、`termx-tui-v3/` 按需 | `termx v3 attach <id>` 使用 tui-v3 `TerminalHost`、protocol adapters 和 `NewInteractiveRuntime` 装配真实交互路径；非交互环境给出明确错误或运行专用 harness；不得调用 `tuiv2/app.RunWithClient` |
| 22. v3 本地端到端 smoke | 完成 | `termx-cli/`、`termx-core-v2/`、`termx-tui-v3/`、`internal/protocol/`、`Makefile` 按需 | 建立可重复的本地单 session smoke：启动 core-v2 daemon、创建 PTY、读取 live surface、发送 input、处理 resize、请求 `history.window`、触发 copy mode 主路径；默认入口仍未切换 |
| 23. 配置、日志与状态路径收口 | 完成 | `termx-cli/`、`termx-shared/` 按需 | v3 实验入口的 socket、log、config、state 路径语义明确且有测试；v3 路径不得为了配置加载依赖 `tuiv2/shared`；与旧默认入口的差异写入迁移审计 |
| 24. remote 兼容与隔离结论 | 完成 | `termx-cli/`、`termx-core-v2/`、`termx-remote/`、`termx-shared/` 按需 | remote 命令在默认切换前有明确结论：已迁移到 core-v2 extension，或显式保留 legacy/fallback 边界；不能把未完成 remote 能力伪装成默认已兼容 |
| 25. 默认 root 入口切换 | 完成 | `termx-cli/`、`Makefile`、`go.work` 按需 | `go run ./termx-cli/cmd/termx` 默认 root TUI 使用 tui-v3；默认 root 不再调用旧 `runTUIv2`；旧 root 若保留必须移动到显式 legacy/fallback 入口；`--help` 可编译运行 |
| 26. 默认 daemon/attach/control 切换 | 完成 | `termx-cli/`、`termx-core-v2/`、`termx-tui-v3/`、`internal/protocol/`、`Makefile` 按需 | `termx daemon`、`termx attach`、`termx new`、`termx ls`、`termx kill`、`termx rm` 默认使用 core-v2/tui-v3；旧路径只允许显式 legacy/fallback；本地单 session 主路径回归通过 |
| 27. 默认入口回归验收 | 完成 | `termx-cli/`、`termx-core-v2/`、`termx-tui-v3/`、`internal/protocol/`、`termx-proto/`、`Makefile` 按需 | 默认入口切换后运行 CLI tests、core-v2 tests、tui-v3 tests、protocol tests、`make test-v2-migration` 和默认入口非交互 smoke；remote 未完成项必须在文档和任务结论中显式保留 |
| 28. 旧默认依赖清理与冻结 | 完成 | `termx-cli/`、`workflow.md`、`AGENTS.md`、必要顶层说明文档 | `termx-cli` 默认路径不再 import 旧 `termx-core`/`tuiv2`；旧目录冻结状态明确；依赖守卫、测试入口和文档完成；切片 28 完成后本轮默认入口切换目标才算完成 |
| 29. tui-v3 UI 交互规格文档 | 完成 | `termx-tui-v3/docs/`、`workflow.md`、只读参考 `tuiv2/docs/` 和 `tuiv2/` | 新增独立中文文档定义 tui-v3 的产品级 UI 交互、界面结构、功能清单、页面线稿、快捷键与鼠标交互、宽窄屏退化和硬约束；可参考 tuiv2 的页面设计，但不得写实现方案、渲染算法、Go 包拆分或从旧实现迁移代码的技术步骤 |
| 30. tui-v3 UI 交互规格增量 | 完成 | `termx-tui-v3/docs/`、`workflow.md` | 在 UI 交互规格中补充 pane chrome 模式：card panel 与 tmux-like split line 两种 tiled pane 呈现；支持隐藏全局 header/footer 以提升终端内容利用率；floating pane 保持独立带边框；补充右上角现代化弹出消息系统；不得写实现方案 |
| 31. tui-v3 render framework 架构文档 | 完成 | `termx-tui-v3/docs/`、`workflow.md`、只读参考 `tuiv2/docs/` 和 `tuiv2/render/` | 新增独立中文文档定义 tui-v3 render framework 与 content renderer 的职责边界、数据流、层级合成、panel/overlay/floating/toast/content 分类、禁止事项和分阶段落地计划；文档完成后必须由子 Agent 审核，审核结论需纳入最终交付说明 |
| 32. tui-v3 render framework 拍板结论落档 | 完成 | `termx-tui-v3/docs/`、`workflow.md` | 把用户拍板结论写入 render 架构文档：`render framework + content renderer` 是正式方向；最小 render framework 阶段必须同时处理 card panel 与 split line、header/footer hide、toast 基础生命周期；Terminal Pool 与 Workbench Tree 在 framework 成型后再接入 |
| 33. render framework 契约与 harness 基线 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/docs/`、`workflow.md` | 建立 `RenderResult`、cursor、blink、metadata、rect/layer/cell 或 line primitive、content renderer contract、content kind、panel/floating/overlay/toast/header/footer VM 基础类型；建立 width-safe helper / primitive，覆盖 ANSI-aware、grapheme-aware、cell-aware 的宽度、裁切、填充和对齐；保留兼容适配层让现有 runtime 可编译；harness 覆盖 `RenderResult` 单一路径、string/frame adapter 一致、copy mode 无 authoritative history 不 fallback、emoji/CJK/combining mark/ANSI styled text 不破坏 row width、Bubble Tea contract 不进入 render |
| 34. tui-v3 shell/panel/toast 状态模型 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` 按需 | 在 reducer-owned state 中加入 shell、workspace/tab/pane 最小树、panel presentation、active pane、header/footer visibility、toast/message、Terminal Picker overlay 占位状态；提供 action/reducer harness 覆盖 card/split 切换、header/footer hide、toast add/auto-dismiss/close/clear、Terminal Picker open/close；未拍板快捷键只能通过 semantic action 或测试消息进入 |
| 35. RenderVMBuilder 分层重建 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/` | 把当前大 `RenderVM{Lines, Status}` 替换为 shell/body/layout/panel/content/overlay/toast/cursor 子 VM；builder 不得退化成大 bag；copy-history VM 必须校验 terminal id、bound token 和 cols；缺 authoritative window 时只生成 pending/empty/error content；live content 只消费 `TerminalSurfaceStore` |
| 36. render framework 最小渲染器 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/terminalhost/` 按需 | 实现最小 render framework：viewport layout、header/footer 占位与隐藏、card panel、split line、最小双 pane 横向/纵向分割、panel chrome、content renderer dispatch、toast 层、Terminal Picker overlay/placeholder、hit region 合成、cursor 归属、最终 `RenderResult -> FrameSink` 适配；harness 覆盖宽窄屏、裁切、层级优先级、toast 不改变 body layout、opaque overlay cursor，以及 panel 标题/content/toast 中的 emoji、CJK、combining mark、ANSI styled text 不破坏边框、split line 或 row width |
| 37. app/input 接线与交互入口 | 完成 | `termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` 按需 | 默认 runtime 使用新的 render framework 主路径；`Ctrl-f` 打开 Terminal Picker overlay/placeholder，`Ctrl-v` 进入 Display/Copy 并在缺 authoritative history 时显示 panel 内 pending/empty；card/split、header/footer hide、toast close/clear 先通过 semantic action、hit region 或测试消息接入，不临时发明未拍板快捷键；live input、resize、copy mode 原有主路径不回退 |
| 38. 默认入口 UI smoke 与回归验收 | 完成 | `termx-tui-v3/`、`termx-cli/`、`Makefile` 按需 | 默认 `go run ./termx-cli/cmd/termx` 和非交互 smoke 不再把裸文本 frame 当作可用界面；smoke 覆盖 workbench shell、header/footer、card/split、header/footer hide、toast、Terminal Picker placeholder、copy pending/empty、live surface panel content、emoji/CJK/ANSI 宽度安全；运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1` 和按需 `make test-v2-migration` |
| 39. render framework 收口与文档同步 | 完成 | `termx-tui-v3/docs/`、`workflow.md`、`termx-tui-v3/` | 同步实现结果到 render 架构和 UI 交互文档，记录已落地、未落地和后续 Terminal Pool / Workbench Tree / floating / overlay 深化切片；删除或重命名过时的裸文本 render helper/test 语义；确认旧 `tuiv2` 仍只读参考，默认路径不引入旧依赖 |
| 40. viewport state 与 host resize contract | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/docs/` 按需 | 建立 reducer-owned 外部 viewport 状态；扩展 `TerminalHost` contract 以提供初始尺寸和 resize event 流；真实 host 监听外部 terminal emulator resize，fake host 可注入 resize；`HostResizeMsg` 或等价 message 是尺寸入 state 的唯一入口；harness 覆盖初始尺寸、重复尺寸去重、fake resize、真实 host size 查询和 runtime resize 后重绘 |
| 41. layout measurement 与 viewport 驱动 renderer | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/` 按需 | 建立纯 layout measurement/plan：输入 `ShellVM + viewport`，输出 body、panel、content、overlay、toast、hit region 和 cursor rect；`RenderVMBuilder` 使用外部 viewport 作为 `Layout.Viewport` truth；renderer 严格按 plan 绘制并填满 viewport；已知 viewport 下不得默认 80 列输出或输出超过 viewport cols；harness 覆盖窄屏、宽屏、header/footer hide、card/split 双 pane、overlay/toast 不改变 body layout 和所有行 display width 等于 viewport cols |
| 42. active content rect 驱动 terminal resize | 完成 | `termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-cli/` 按需 | app 根据 layout plan 计算 active terminal content rect，并只用 content rect cols/rows 向 core-v2 terminal 发送 resize；外部 viewport、header/footer hide、card/split 切换和 split 变化导致 content rect 变化时触发 resize effect，重复尺寸不重复发送；attach 初始尺寸也以 content rect 为准；harness 覆盖 card 扣边框、split 分割、header/footer 隐藏回收空间、resize effect 去重和真实 protocol adapter 参数 |
| 43. copy mode resize rebind 与 history window cols | 完成 | `termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`internal/protocol/` 按需 | copy mode 绑定 cols 使用 copy content rect width；host resize、layout chrome 变化或 active copy panel content width 变化时 invalid 旧 bound token/window 并重新请求 authoritative latest window，期间显示 pending/empty/error，不从 live surface fallback；older request 和 stale guard 使用新 cols；harness 覆盖 resize 后旧 window 不再渲染、latest replace 重新绑定、新旧 response stale、selection/cursor 清理和 `HistoryWindow` 请求 cols |
| 44. Unicode 线框与 cell-width 硬化 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/docs/`、只读参考 `tuiv2/render/` | 默认 UI chrome 去除 ASCII `+ - |` 线框；该切片当时 card/modal/floating 使用 `╭╮╰╯─│`，pane/split 使用 `┌┐└┘─│├┤┬┴┼` 同类连续 box drawing glyph，后续切片 92 已按用户反馈把默认 floating/overlay/toast 改为直角 chrome；split 连接点按邻接线合成；标题、content、toast、overlay 中 emoji、CJK、combining mark、ANSI 样式不得破坏边框；harness 覆盖 Unicode glyph、连接点、窄屏裁切、右边框不丢失、无默认 ASCII chrome、行宽恒等和旧 `tuiv2` 只读参考结论 |
| 45. 默认入口 resize/UI 验收与文档收口 | 完成 | `termx-tui-v3/`、`termx-cli/`、`Makefile`、`workflow.md`、`termx-tui-v3/docs/` 按需 | 默认 `go run ./termx-cli/cmd/termx`、`termx attach` 和非交互 smoke 覆盖外部 viewport 初始尺寸、resize 重绘、content rect terminal resize、copy mode rebind、Unicode 线框、card/split/header/footer/toast/overlay 宽度安全；更新 render/UI 文档记录本轮已落地和后续 Terminal Pool / Workbench Tree / floating 深化边界；运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`，并按需运行 `make test-v2-migration` |
| 46. styled chrome renderer 设计落档 | 完成 | `termx-tui-v3/docs/`、`workflow.md`、只读参考 `tuiv2/render/` | 把 styled chrome renderer 作为新阶段正式目标写入 render/UI 文档和 workflow；明确 `tuiv2` 截图级视觉目标、不得复制旧实现、优先 shell/chrome/pane/frame，内容 renderer 后续分阶段深化；文档-only 运行 `git diff --check` |
| 47. styled RenderResult / Frame contract | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/docs/` 按需 | `RenderResult`、`Frame`、`FrameSink` 建立可保留 styled cell / styled line / ANSI line 的 contract；保留纯文本快照适配但不作为真实 TTY 主输出；真实 `FrameSink` 可写 ANSI styled frame 并正确 reset；harness 覆盖 ANSI SGR 保留、plain snapshot 等宽、cursor metadata 不丢、旧 smoke 可适配 |
| 48. cell matrix compositor 与 ANSI serializer | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/terminalhost/` 按需 | renderer canvas 从纯字符串升级为 cell matrix 或等价 compositor，支持 text、width、style、owner/layer、continuation/safe flag、wide-cell footprint 清理和 ANSI serializer；harness 覆盖 styled glyph、wide char/emoji/combining/ANSI 裁切后行宽恒等、style reset、右边框不丢、无默认 ASCII chrome |
| 49. theme token 与 host-aware palette | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/docs/` 按需 | 建立 v3 theme token：host fg/bg、chrome bg/fg、accent、success/warning/danger/info、panel border、muted border、active/inactive pane、toast/overlay token；可参考 tuiv2 theme 思路但不迁入旧大状态；harness 覆盖 active/inactive border 色不同、semantic token 输出 ANSI、主题 fallback 稳定 |
| 50. pane chrome 视觉对齐 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/` 按需 | tiled pane 默认达到 tuiv2 截图级 styled chrome：square Unicode 细线边框、active pane accent、inactive pane muted、标题/状态/action 槽位、pane top chrome token、split/card 两种模式均保持 content rect 和 hit region；harness 覆盖单 pane、多 pane 横纵 split、active 切换、标题含宽字符、ANSI 样式不破坏边框 |
| 51. shell chrome / top bar / bottom bar 视觉对齐 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-cli/` 按需 | header/footer 从占位文本升级为 styled top/bottom bar：workspace/tab/mode/hint/status token、active tab/创建 token、右侧摘要、隐藏 header/footer 后状态仍可达；harness 覆盖 ANSI 背景填满整行、token 宽度安全、hide 后 body 回收、默认入口 smoke 包含 styled top/bottom bar |
| 52. toast / overlay styled chrome | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/` 按需 | toast 和 Terminal Picker overlay 使用 styled chrome：border/background/severity token/pending token/close action region/opaque cursor 归属；该切片当时允许 floating/modal rounded card，后续切片 92 已按用户反馈把默认 floating/overlay/toast 改为直角 chrome；harness 覆盖 toast 不改 body layout、overlay 遮挡优先级、窄屏退化、ANSI reset 和宽字符安全 |
| 53. pane 结构命令 contract | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/docs/` 按需 | 建立 pane structural command/semantic action contract，覆盖 split、close、close and kill、focus、zoom、resize、set size、balance、panel presentation；快捷键、鼠标、测试和后续 CLI mini command 都只能作为 adapter 调用该 contract；harness 覆盖 command 参数、target pane、无效目标、confirm policy 和 reducer/effect 边界 |
| 54. pane split / close / zoom 状态语义 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` 按需 | reducer-owned pane tree 支持横向/纵向 split、close pane、close and kill terminal effect、focus/activate、zoom/unzoom；结构变化保持 terminal binding、active pane、tab invariants、toast 反馈和 render VM 投影；不得直接修改 service state |
| 55. pane resize / size change geometry | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/` 按需 | pane resize 支持按方向/步长调整、按比例或固定尺寸 set size、balance/equalize；layout measurement 产出稳定 panel/content rect，外部 viewport、header/footer hide、card/split、resize 变化都触发 active terminal content rect resize 去重和 copy mode authoritative window rebind |
| 56. pane command adapters：快捷键、鼠标、CLI mini | 完成 | `termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/terminalhost/`、`termx-cli/` 按需 | pane mode、resize mode、鼠标 hit region 和 CLI mini command adapter 调用同一 semantic command；不临时发明未拍板产品快捷键，但保留稳定 command id、测试入口和后续 CLI mini 解析边界；UI chrome region 优先于 terminal mouse forwarding；真实 `FrameSink` 不得依赖换行推进满宽 frame，避免最后一列自动换行导致 pane 竖向边框视觉断裂 |
| 57. 默认入口 styled UI + pane command smoke 与视觉验收 | 完成 | `termx-tui-v3/`、`termx-cli/`、`Makefile` 按需 | `termx v3 smoke`、`termx v3 e2e-smoke` 和默认入口非交互 smoke 覆盖 styled frame 与 pane command：ANSI SGR 存在、active/inactive pane 样式不同、top/bottom bar styled、pane border continuous、split/close/resize/zoom 后 frame 行宽恒等、content rect terminal resize、copy rebind；运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-v2-migration` |
| 58. styled chrome / pane command 文档收口与后续内容 renderer 边界 | 完成 | `termx-tui-v3/docs/`、`workflow.md` | 同步 styled chrome 与 pane command 已落地、未落地和后续 terminal-live/copy-history/Terminal Pool/Workbench Tree/floating 内容 renderer 深化边界；明确当前阶段完成 chrome/frame 视觉等级和 pane 结构命令基础，不宣称内容 renderer 完整；文档-only 至少运行 `git diff --check`，若同切片有代码则运行相关测试 |
| 59. split-line 顶边视觉修复与测试口径 | 完成 | `termx-tui-v3/render/`、`workflow.md` | 修复 split-line pane 顶边端点退化和隐藏 header 时顶边不完整的问题；harness 覆盖顶边使用 pane chrome 连接语义、行宽恒等和宽字符不破坏顶边；最终说明必须明确当前可测的 split/resize/zoom/close 入口，以及 floating pane 完整交互尚未落地 |
| 60. pane/resize/global 第一版交互产品化 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`workflow.md` | 建立 reducer-owned interaction mode；`Ctrl-p` 进入 pane mode、`Ctrl-r` 进入 resize mode、`Ctrl-g` 进入 global mode；pane mode 可触发 split right/down、close、focus next/previous、zoom/unzoom、balance、card/split 切换；resize mode 可用方向键语义调整大小并保留同一 `ShellPaneCommandMsg` 路径；global mode 可切换 header/footer、清理 toast；`Esc` 退出 mode/overlay 且不漏发 terminal；footer 显示当前 mode；card/split 顶部 title 到 action 之间保留连续线段；floating 完整交互仍作为后续独立切片，不在本切片伪实现 |
| 61. shell header/footer 产品化 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/docs/`、`workflow.md` | 把当前 styled top/bottom bar 从视觉占位推进到产品信息层：header 必须展示 workspace/tab/active pane/terminal count/notice 或窄屏摘要；footer 必须展示当前 mode、mode-specific shortcut hints、active pane/terminal 状态和窄屏退化；header/footer hide 后 body 回收空间且上下文仍通过短 token、toast 或 Help 入口可达；harness 覆盖 live/pane/resize/global/copy/overlay 不同 mode 提示、宽字符安全和隐藏回收；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 62. 鼠标 hit region dispatch 与 pane focus | 完成 | `termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/terminalhost/` 按需 | app/runtime 必须持有或接收最新 render hit regions，把真实鼠标坐标映射到 pane content、pane chrome、action slot、split divider、resize handle、toast/overlay 等可见对象；点击 pane content/chrome 聚焦 pane，点击 close/action 执行同一 semantic command，点击 divider/resize handle 进入 resize 语义或调整大小；UI chrome 优先于 terminal mouse forwarding；harness 覆盖多 pane 点击焦点转移、action 命中、overlay/toast 优先级和未命中事件转发边界；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 63. active pane 视觉反馈与交互验收 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 键盘和鼠标导致的 focus、split、close、zoom、resize、card/split 切换必须立即反映到 active pane border/title/footer/toast；active/inactive 样式不得只在 smoke 静态 case 中存在，也不得只靠标题文字表达；close 后 active pane 选择稳定，resize/zoom 后 content rect 与视觉高亮一致；harness 覆盖双 pane、多 pane、隐藏 header/footer、card/split 两种 presentation 下的 active 变化；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 64. UI framework 交互产品化总验收 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 在接 terminal-live content renderer 前，先把当前 UI framework 做成可基本操作的产品闭环：header/footer 信息、mode-specific footer hints、键盘 pane/resize/global 入口、鼠标 focus/action/resize 命中、split/close/resize/zoom/card/split/header/footer hide/toast close/clear 都必须走同一 semantic command 或 shell message；active pane 高亮、footer active target、toast feedback、content rect resize、copy rebind 和 hidden header/footer 布局必须在每次结构操作后同步更新；harness 覆盖单 pane、双 pane、多 pane、隐藏 header/footer、card/split、窄屏、toast/overlay 优先级和未命中 terminal forwarding；文档必须写清当前用户如何测试分屏、关闭、resize、zoom、card/split、header/footer hide 和 toast 操作；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 65. terminal-live content renderer 一期 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/` 按需 | 在 UI framework 交互产品化总验收完成后接真实 terminal live 内容：content renderer 只绘制到 pane content rect，消费 live surface / terminal session 的行、styled cell 或等价数据，表达 cursor、basic style、pending/empty/exited 状态和宽字符裁切；不得把 live surface 当作 history truth，也不得破坏边框、split line、overlay/toast 或 content rect resize；harness 覆盖真实 live 输出、ANSI/style、emoji/CJK、cursor 归属、resize 后裁切和 no chrome leak；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 66. copy-history content renderer 一期 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/` 按需 | 在 terminal-live content renderer 一期完成后深化 copy-history 内容 renderer：只消费 core-v2 authoritative `HistoryWindow`，表达 selection、cursor、logical-line marker、clipped before/after、scrollbar 或 position token、copy/yank feedback 和宽字符裁切；resize 或 content cols 变化必须继续 invalid/rebind authoritative window，不得从 live surface、snapshot、grid viewport 或 local VTerm scrollback fallback；harness 覆盖 selection 跨 logical line、clipped span、older/latest 后位置保持、resize 后 no stale rows、copy feedback 和 no chrome leak；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 67. empty/exited/Terminal Picker 内容 renderer 一期 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 live 与 copy-history 内容 renderer 一期完成后，把 empty pane、exited pane 和 Terminal Picker placeholder 推进为产品化内容 renderer：empty pane 显示 attach/create/open manager/close CTA，exited pane 显示 last state/restart/reconnect/close CTA，Terminal Picker overlay 显示搜索框、terminal list/new terminal row/selected row/action hit regions 的最小 VM；本切片不实现完整 Terminal Pool，也不接 Workbench Tree；harness 覆盖 CTA/action hit region、overlay focus、宽字符裁切、no chrome leak、no Bubble Tea/no legacy fallback；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 68. Terminal Picker 真实交互深化 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Terminal Picker 一期 VM 成型后，接入真实过滤、键盘移动选择、确认 attach、new terminal/create action、preview/detail 的最小状态和服务边界；允许仍只基于当前 workspace panes，不得伪装完整 Terminal Pool；harness 覆盖 query 更新、selected row 移动、Enter attach/focus、Esc close、new terminal action feedback、no terminal input leak、overlay cursor/row action 宽字符安全；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 69. Terminal Pool 数据源与 Picker 服务接线一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Terminal Picker 真实交互闭环完成后，建立 Terminal Pool 的最小 reducer-owned list/source/service adapter 边界，让 picker item 可以来自真实 terminal list 而不是只来自当前 workspace panes；attach/create/restart/reconnect 必须通过 effect/service/result message 接线并有错误反馈和 stale guard；本切片不实现完整 Terminal Pool 管理页、Workbench Tree 或跨远端管理；harness 覆盖 fake service list、loading/empty/error、picker rows 合并/去重、attach result、create result、失败 toast、no terminal input leak、宽字符安全和不伪造 terminal lifecycle；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 70. Terminal Pool 管理页一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Terminal Pool 数据源与 picker 服务接线完成后，实现独立 Terminal Pool page/content 的第一版产品页：列表、搜索、selected row、detail/preview、attach/kill/edit action 的 VM 与 service/effect/result 边界；页面在常规 80x24 与 e2e viewport 中不得把 detail、preview 摘要或 footer action 裁出可操作区域，窄高不足时按 list/action/detail/preview 优先级退化；不得混入 Workbench Tree、floating 或 remote 管理；harness 覆盖页面打开/关闭、list loading/empty/error、键盘选择、鼠标 row/action hit region、attach/kill/edit 反馈、宽字符安全、no terminal input leak、可见 action 不被裁切和不伪造 terminal lifecycle；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 71. Workbench Tree overlay 一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/input/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Terminal Pool 管理页一期完成后，实现 Workbench Tree 的第一版结构导航 overlay：workspace/tab/pane/floating row、搜索、selected row、detail/preview、focus/open action 和关闭路径；不得混入 Terminal Pool、floating drag/resize 或 remote 管理；harness 覆盖打开/关闭、键盘选择、鼠标选择、focus/open 反馈、宽字符安全、overlay cursor、no terminal input leak 和不改写 terminal lifecycle；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 72. Floating pane 一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/input/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Workbench Tree 一期完成后，实现 floating pane 的第一版产品闭环：floating state、z-order、styled bordered chrome、focus/raise、close、center、collapse/restore、move/resize 语义和 hit region；floating content 复用已有 content renderer，不伪造新 terminal lifecycle；harness 覆盖 tiled/floating 层级、遮挡裁切、鼠标 focus/raise、move/resize 边界、content rect resize、宽字符安全和 no chrome leak；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 73. Prompt / Help overlay 一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/input/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 在 floating pane 一期完成后，实现 Prompt 与 Help 的第一版 overlay：Prompt 支持 title/context/input/submit/cancel 和 destructive confirm 边界；Help 按 Most used、Pane、Tab、Workspace、Floating、Terminal Pool、Display/Copy 分类展示概念和动作；不得引入 Bubble Tea component；harness 覆盖输入编辑、提交/取消、Esc、Help 窄屏裁切、overlay cursor、鼠标 close、no terminal input leak 和宽字符安全；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 74. Tab / Workspace 产品入口一期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/render/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在 Prompt / Help 一期完成后，把 `Ctrl-t` tab mode 与 `Ctrl-w` workspace mode 推进到第一版可操作产品入口：create/switch/rename/close tab，switch/create/rename workspace，必要 rename/confirm 走 Prompt；所有操作必须通过 reducer-owned semantic command 或 shell message，不得绕过 header/footer、layout measurement、toast 或 session state；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 75. TUI 产品壳总验收 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/render/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 在 Workbench Tree、floating、Prompt/Help、Tab/Workspace 一期完成后做当前 goal 的 UI 完备验收：除 terminal-live/copy-history 的深层内容渲染仍可继续深化外，header/footer、pane、floating、Terminal Pool、Workbench Tree、Prompt/Help、Tab/Workspace、toast、overlay、快捷键、鼠标和 no terminal input leak 必须形成可基本操作闭环；补手工测试说明和端到端 harness，确认默认入口能完成分屏、关闭、focus、resize、floating、Prompt/Help、tab/workspace、Terminal Pool、Workbench Tree 和 toast 操作；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 76. terminal live 连接展示与交互前推 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/`、协议范围按需 | 在 UI 产品壳总验收后，若仍有余力，继续前推 terminal live 的连接展示和交互：真实 attach 后 live 行、cursor、basic ANSI/style、输入回显、resize 后 content rect 裁切、连接/退出/错误状态和 no chrome leak 必须更接近可日常使用；仍不得把 live surface 当 history truth，也不得绕过 content rect、FrameSink 或 terminal service effect/result；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 77. copy-history content renderer 深化 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/`、`termx-core-v2/` 按需 | 在 UI 产品壳总验收和 terminal live 前推后，深化 copy-history 内容 renderer：scrollbar、搜索、content-local mouse selection、selection 颜色层级、logical-line 拼接提示、position token 精细化和滚动交互；继续只消费 core-v2 authoritative `HistoryWindow`，resize 后不得显示旧 cols rows 或从 live surface fallback；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 78. render 兼容投影清理与性能基线 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/terminalhost/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在主要内容和 overlay 页面稳定后，删除或重命名剩余旧 `RenderVM{Lines, Status}` 兼容语义，建立 content-level cache / layer dirty region / large terminal output 性能基线；不得改变 `RenderResult` 单一路径，不得重新引入裸文本 frame 主路径；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 79. TUI 视觉差距审计与验收基线 | 完成 | `termx-tui-v3/docs/`、`termx-tui-v3/render/`、`termx-cli/`、`workflow.md` 按需 | 重新以用户提供的 `tuiv2` 截图和目标描述为视觉基准，审计当前 `v3 smoke` / 默认 TUI 与目标的差距；补固定 viewport visual snapshot/smoke case 和文档验收项，明确 top bar、bottom bar、pane chrome、active accent、inactive muted、overlay、toast、floating、copy-history、Terminal Pool/Tree 的视觉要求；不得把现有基础 styled chrome 继续标为视觉完成；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 80. tuiv2 风格 shell header/footer 重绘 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 在视觉基线明确后，把默认 header/footer 从当前稀疏信息条推进到 `tuiv2` 风格的整屏 top/bottom bar：workspace/tab strip、mode、快捷键 token、状态摘要、右侧 summary、背景填满、窄屏退化和 ANSI style 均可见；header/footer hide 仍真实回收空间；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 81. tuiv2 风格 pane chrome 与 split 视觉重绘 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已对齐第一轮 `tuiv2` pane chrome：card panel 与 split line 均使用 square 细线连续边框、顶边 title/state/action 槽位、active accent、inactive muted、共享外框、分割线连接、内容区裁切、宽字符安全和鼠标 hit region 一致；split line 的 terminal resize 与 copy rebind 使用避开外框/divider 的 content rect；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 82. overlay/toast/floating 视觉对齐 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已把 Terminal Picker、Terminal Pool、Workbench Tree、Prompt/Help、toast 和 floating 从“可见可操作”推进到第一轮目标视觉：实体 modal/card、现代 toast、合理 padding、title/action 槽位、层级遮挡、active/focus token、ANSI reset 和宽字符安全；toast 不引入灰度遮罩，不覆盖 pane 顶边或底边；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 83. 真实默认 TUI 视觉复核与失败归档 | 完成 | `termx-tui-v3/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 复核切片 80-82 后的默认 TUI，结论为未达到用户目标截图；不得把本切片当作视觉验收通过。已补充失败原因、目标差距、手工验收口径和后续返工切片，并把固定 smoke 文案从差距快照改为复核快照，补充必要视觉断言；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 84. tuiv2 风格 shell bar 高密度重绘 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已把 top/bottom bar 从工程标签式 `ws:/tab:/active:` 信息条推进到目标截图风格的第一轮高密度产品栏：顶部使用 workspace、tab strip、`[⊕]`、active pane、`◆ owner`、terminal/floating 和 action token；底部使用 `MODE • [KEY] ACTION` 快捷键 taxonomy、active target、workspace/pane/floating summary、窄屏保留尾部关键动作和 ANSI style；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 85. pane chrome 目标截图级槽位重绘 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已把 pane top chrome 从单一 `title/state/[x]` 推进到目标截图式槽位布局：title、状态点、`↔0` 元信息、`◆ owner`、宽 pane full action cluster `[o]─[_]─[Z]─[x]`、窄/分屏 compact cluster `[Z]─[x]`，title 与 action 之间保留连续横线，pane action hit region 与可见 action cluster 同宽；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 86. overlay/page/copy 视觉产品化 polish | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已把 Terminal Picker、Terminal Pool、Workbench Tree、Prompt、Help 和 copy-history 的内容层从工程表格/占位推进到第一轮产品视觉：统一 search affordance、selected row marker、detail/preview/context/input label、action row、copy search/match/scrollbar/status，并同步产品文档、视觉审计和 render 架构；不要求背景灰度遮罩，不复制旧 `tuiv2` 实现；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 87. 默认入口截图级视觉复核未通过归档 | 完成 | `termx-tui-v3/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 已根据用户真实视觉反馈确认：当前 TUI 仍不像目标 `tuiv2` 截图，不能宣称截图级视觉验收通过；本切片只保留有价值的复核证据扩展：`termx v3 smoke` 输出 12 个固定视觉 case，覆盖 header/footer、pane chrome、active/inactive、toast、floating、Terminal Picker、Terminal Pool Page、Workbench Tree Page、Prompt/Help、copy-history 和 `120x40` visual review baseline；`termx-cli` 固化 CLI smoke 输出视觉 review marker；`visual acceptance` 文案不得出现；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 88. 目标截图级 shell/pane 视觉重绘二轮 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已完成二轮 shell/pane 视觉重绘：theme accent 改为紫色系，status bar 改为暗色背景；top bar 使用 workspace、tab、关闭 `×`、新增 `[＋]`、active pane、owner、terminal/floating compact summary 和 `[o]─[_]─[Z]─[x]` action cluster；bottom bar 改为 `[Ctrl] · [P] PANE` 类彩色快捷键 taxonomy；pane top chrome 加入 `· ↔2`、`· 1/31`、owner 与 action 高密度槽位；toast/floating/overlay title 使用 `·` 分层并保留实体 card；harness 覆盖紫色 ANSI、暗色 status 背景、无默认 ASCII `+ - |` 线框、宽字符安全、smoke 12 case 和 CLI smoke 输出；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 89. 真实默认入口截图级验收证据归档 | 完成 | `termx-tui-v3/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 已用隔离 socket/log 和真实 PTY 启动 `go run ./termx-cli/cmd/termx`，固定 `120x40` viewport 捕获到默认入口进入 alternate screen，并输出 styled ANSI header、pane、footer、紫色 active border、`×`、`[＋]`、`[Ctrl] · [P]`、`· ↔2`、`· ◆ owner`、`· 1/31` 等二轮 chrome token；隔离进程已清理；本切片只证明默认入口真实 TTY 路径可绘制二轮 styled chrome，不能替代用户对目标截图风格的人工拍板；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 90. 用户真实视觉复核不通过归档 | 完成 | `workflow.md`、`termx-tui-v3/docs/` | 用户已明确反馈：切片 89 后的真实 TUI 样子仍与目标不一致，当前视觉对齐 goal 不能完成；本切片把不通过结论写回 workflow 和视觉文档，确认自动 smoke、e2e、PTY ANSI 捕获、Unicode 线框、ANSI 颜色和 chrome token 都不能替代真实视觉验收；准入运行 `git diff --check` |
| 91. 目标截图级整体 UI 构图三轮重绘 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已完成三轮整体 UI 构图重绘：top bar 改为连续 tab strip 构图，bottom bar 保留高密度 shortcut taxonomy，pane/floating/toast/overlay 使用更实体的 card 比例和多行留白，toast 在固定视觉 smoke 中保持前景层级；pane/floating chrome action/status glyph 已成为显式 contract，默认使用 Nerd Font PUA，并允许后续按字体环境替换为 emoji 或其他 UTF-8 字符，命中区和裁切按 display cell width 计算；实现继续使用 render framework + content renderer，未复制 `tuiv2` runtime/model，未引入 Bubble Tea；本切片自动验收已通过，但不替代用户对目标截图风格的人工拍板；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 92. 用户反馈归档与 chrome/toast 可见收敛 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-cli/`、`termx-tui-v3/docs/` 按需 | 已按用户最新复核反馈收敛默认可见 UI：pane chrome 只绘制当前真实派发的 close action，split/zoom 等未完成按钮先隐藏；floating、overlay 和 toast 改为直角 chrome；toast 改为深色实体矩形、左右紫色竖线和居中文案，不再显示 close token 或多行 title/body 表格；toast 本体只拦截鼠标遮挡，不穿透到底层 UI；copy 成功反馈文案改为 `Copied to clipboard`；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、额外运行 `go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 93. toast 自动消失与真实生命周期 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 已把 toast 自动消失接入真实 runtime timer：新增 toast 默认带明确 auto-dismiss TTL，普通反馈短生命周期，pending/error 默认保留更久但仍会消失；runtime 在 Drain 循环中按时间派发 `TickMsg`，由 shell reducer 执行 `TickToasts` 并触发重绘，不走终端输入路径；鼠标 close current 和 clear all 保持 shell message 路径；harness 覆盖真实 runtime 自动消失、pending/error 退化、重绘和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 94. pane 鼠标拖动边框 resize | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/state/`、`termx-tui-v3/terminalhost/`、`termx-cli/` 按需 | 已支持鼠标按下 pane 分隔线或边框 resize handle 后连续拖动改变 pane 大小，不再把点击当作一次性假 resize；SGR mouse parser 和真实 host 已支持 button-event drag/release；runtime 使用 transient drag state，drag move 回投统一 `PaneCommandResize`，release 清理状态；拖动过程中 UI chrome 优先于 terminal mouse forwarding，active content rect resize 去重和 copy rebind 跟随既有 reducer/effect 路径；harness 覆盖横向和纵向 divider 拖动、release 后状态清理、terminal resize 参数和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 95. 横纵分屏入口与可见按钮一致 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 已恢复真实可用的横向/纵向分屏可见入口：pane mode、鼠标 hit region 和 CLI mini command adapter 统一调用 `PaneCommandSplit`；pane chrome 只绘制已接通的 split-down、split-right 和 close token，zoom 等未恢复接线的按钮继续隐藏；点击 split token 会生成新 pane、按目标 pane 分屏并激活新 pane，窄 pane 退化为 close-only 或隐藏 action slot；harness 覆盖 horizontal/vertical split、显式 target pane、active pane 选择、独立 action hit region、无假 zoom 按钮和 runtime mouse split；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 96. floating 鼠标拖动移动与 resize | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/state/`、`termx-cli/` 按需 | 已支持 floating 标题栏鼠标连续拖动移动和右下 resize handle 连续拖动 resize；render 为 title drag、resize drag、close 和 content raise 产出独立 hit region；runtime 复用 transient drag state，把 drag move 回投统一 `FloatingCommandMove` / `FloatingCommandResize`，release 清理状态；拖动走 reducer clamp，保持 z-order、active 状态、content rect 裁切、toast/overlay 前景优先和 no terminal input leak；harness 覆盖 move drag、resize drag、release 后不再生效、viewport clamp、close 可见性和产品流鼠标操作；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 97. 用户复核回归与视觉验收归档 | 完成 | `termx-tui-v3/`、`termx-cli/`、`termx-tui-v3/docs/`、`workflow.md` 按需 | 已完成切片 92-96 后的回归归档：按钮只在功能可用时出现，pane 横纵分屏、拖动 resize、floating 拖动移动、直角边框、toast 自动消失和截图式 toast 样式已写入视觉复核文档、交互验收步骤和 workflow 当前状态；自动 smoke、e2e smoke、PTY/ANSI/frame 证据仍不得替代用户对目标截图级视觉的人工拍板；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 98. toast 去重与拖动反馈降噪 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 已完成用户反馈的一次移动疯狂弹窗收敛：toast 状态层对同 ID 或同 severity/title/body/pending 的消息去重、刷新并重置生命周期；pane focus/focus-next/focus-previous/resize 与 floating focus-raise/move/resize 的成功反馈不再展示低价值 toast，split/close、错误、确认、copy 等离散反馈仍保留；harness 覆盖重复 toast 去重、拖动命令不追加成功 toast、错误反馈仍可见和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 99. pane split 图标有效性复核 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-cli/` 按需 | 已完成用户反馈的部分窗口点击分屏图标无效复核：pane action hit region 现在在 split divider resize region 之前合成，横向 split 下位于 divider row 的下方 pane split 图标不再被 resize 抢占；pane chrome 继续只绘制真实可执行的 split-down、split-right、close 入口，窄 pane 仍按 close-only/隐藏 action 退化；harness 覆盖横向 divider 上的 bottom pane split action 优先级、鼠标点击下方 pane 分屏、active pane、NewPane 默认填充、无 invalid toast 和既有 active/non-active split；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 100. 默认隐藏 host cursor 与中文输入法处理 | 完成 | `termx-tui-v3/terminalhost/`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、只读参考 `tuiv2/` | 已完成中文输入法预编辑位置收敛：只读复核 `tuiv2` 的 hidden host cursor anchor 经验后，render `RenderResult/Frame` 透传全局 `CursorRect`，真实 `FrameSink` 每帧默认输出 hide cursor；无 cursor 时隐藏 host cursor，有 pane/overlay/prompt/live cursor 时仍保持隐藏但把 host cursor 停到全局 cursor rect，避免输入法预编辑跟随最后一行输出位置顶起窗口；进入 TUI 继续 hide cursor，退出继续 show cursor；harness 覆盖 enter/exit cursor ANSI、frame 无 cursor 默认隐藏、有 cursor rect 时隐藏并定位、overlay/live cursor rect 透传和 styled frame reset 后隐藏；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 101. 用户复核事项归档 | 完成 | `workflow.md`、`termx-tui-v3/docs/`、`termx-tui-v3/`、`termx-cli/` 按需 | 已归档用户本轮反馈处理结果：toast 去重/降噪、分屏图标有效性、无必要消息收敛、中文输入法与默认 cursor 隐藏的手工复核步骤和剩余边界；视觉复核、视觉审计和 UI 交互规格已同步：拖动/focus 不应刷 toast，横向分屏下方 pane action 不得被 divider resize 抢占，host cursor 默认隐藏并锚定全局 cursor rect；自动 smoke/e2e 只作为证据，不替代用户对真实 TTY 视觉和输入法行为的人工确认；准入已运行 `git diff --check` |
| 102. 拖拽 resize 对侧锚定 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-cli/` 按需 | 已修复用户反馈的拖拽 resize 锚定体验：pane resize 不再暴露单 pane 或 tiled 最外侧固定边的假 handle，只保留真实 split divider hit region；鼠标拖拽期间固定起始命中的边方向，按起点累计位移回投带符号 delta，同一次拖拽来回移动不会切换锚点；拖动右/下分隔线时对侧 pane 外边保持锚定，只移动被拖分隔线；harness 覆盖右分隔线正向/反向拖动时 pane rect 对侧坐标不变、release 清理、no terminal input leak 和 toast 降噪；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 103. 中文输入法候选区不顶起页面复核 | 完成 | `workflow.md`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、只读参考 `tuiv2/` | 已处理用户补充复现中的中文输入法候选区上抬风险：FrameSink 写帧使用同步输出包裹整帧，写帧前先隐藏 host cursor，写帧后重新输出隐藏 cursor 并定位到全局 cursor rect；layout measurement 在当前前景输入目标没有 visible cursor 时，也会给 active overlay/floating/pane content rect 左上角生成 IME anchor；empty/exited pane 内容提供 content-local cursor，active floating 创建后 frame cursor rect 位于 floating content rect 内；文档已补真实终端手工复核步骤，特别复核 `Ctrl-o` 后 `n` 创建 floating 并切中文输入法输入 `o`；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 104. floating 获焦时 tiled pane 失焦视觉 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已处理用户补充反馈：active floating 存在时，RenderVM 把 tiled pane visual active 降级为 inactive/muted，后方 pane 不再继续显示 active 高亮边框；floating 关闭后 tiled pane active 视觉恢复；terminal layout resize 改为按 reducer-owned 业务 `ActivePaneID` 找 content rect，避免 visual focus 被 floating 抢走后阻断 active terminal resize；harness 覆盖 floating create/raise 后 tiled pane visual active=false、关闭后恢复、active floating 下 host resize 仍使用业务 active pane content rect；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 105. 鼠标 resize 精确分隔线邻居优先 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/docs/`、`termx-cli/` 按需、只读参考 `tuiv2/` | 已修复用户反馈的鼠标 resize 体验：只读参考 `tuiv2` 保存具体 split node 和 bounds 的 divider drag 方式后，v3 render 为每条真实 split divider 产出稳定 `SplitPath`，runtime 拖拽期间固定起始 divider，state 对带 `ResizeSplitPath` 的鼠标命令只调整该具体 split 节点及其两侧邻居，不再按 pane id 向上误改外层 split；没有真实分隔线时不暴露假 handle；harness 覆盖嵌套三列内部分隔线拖动、外层 pane 锚定不变、root bias 不变、no terminal input leak 和 toast 降噪；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 106. 四列鼠标 resize 相邻 pane 独立调整 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 已修复用户反馈的四列 panel resize 问题：render 的 pane divider hit region 现在携带视觉相邻 leaf pane 对和同轴 leaf pane group 的起始 cell 尺寸；runtime 拖拽时按累计 delta 只改相邻 before/after pane 的目标尺寸；state 对带 `ResizeGroupCells` 的鼠标命令重建同轴 leaf pane split 链并固定每列尺寸，避免二叉 split 的邻居 subtree 把第 3/4 列一起缩放；harness 覆盖四列中第 2 列左边线左拖只调整第 1/2 列、第 2 列右边线右拖只调整第 2/3 列、第 4 列不变、terminal input 不泄漏和 toast 降噪；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 107. stacked pane 共享边界 resize | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-cli/` 按需 | 已修复用户反馈的左侧单列、右侧 stacked 多 panel 场景 resize：render 的 divider group 现在为每个 leaf pane 标记跟随分隔线左/右或上/下侧变化的 `DeltaSign`；runtime 拖拽时按累计 delta 同步更新同侧所有共享边界 pane；state 保留异轴 stacked 结构，只给相关同轴 split 写固定尺寸 hint，使右侧三行能一起变宽，且其中某一行再分左右两列时只让贴近共享边界的左子列跟随变化、右子列保持锚定；harness 覆盖 hit region group 符号、reducer 几何 hint、真实鼠标拖拽后的 panel rect、terminal input 不泄漏和 toast 降噪；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 108. terminal live 真实后端数据流深化 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 已从一期 attach snapshot 初始化推进到持续消费真实 core-v2 terminal live 更新：core-v2 output ingest 会发布 terminal changed event，tui-v3 protocol terminal service 订阅后端 event stream 并通过 effect/result message 拉取 live surface snapshot 回到 runtime；state 使用 reducer-owned `TerminalSurfaceStore` 保存按 terminal id 隔离的 live surface，不把 live surface 当 committed history truth；active pane attach、Terminal Pool attach/reconnect、旧 terminal 更新隔离、exit/error、content rect resize、terminal input 不本地假回显和 copy-history 不从 live fallback 均保持；harness 覆盖真实后端更新后 frame 重绘、active/非 active pane 数据隔离、旧 terminal 更新不污染新绑定、后端事件刷新 snapshot、core-v2 live changed event 和 e2e smoke 中真实 `gamma` live rows 渲染；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 109. core-v2 真实 PTY ProcessFactory 接入 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-cli/`、`go.work.sum` 按需，只读参考 `termx-core/pty/` 与 `tuiv2/runtime/` | 已在不改变 protobuf+socket contract 的前提下，把默认 core-v2 daemon 从 scripted process 切到真实 PTY-backed `ProcessFactory`；`ProcessFactory` 继续是 core-v2 内部 terminal process 边界，TUI-v3 只通过 protocol service 消费；真实 PTY 支持 spawn、input、resize、kill、close、wait 和输出读取，输出统一进入 core-v2 `Terminal.IngestOutput` 同源路径并先 drain 输出再发布 exit；scripted/recording process 只作为测试注入；harness 覆盖真实命令输出进入 live surface 与 authoritative `history.window`、input 写入 PTY 触发输出、resize/kill/exit lifecycle、退出前尾部输出不丢失、默认 CLI daemon 使用真实 PTY factory、`v3 e2e-smoke` 不再手工注入后端输出；准入已运行 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 110. TUI 创建 terminal 默认 shell 兜底 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-cli/` 按需 | 已修复用户反馈的 Terminal Pool / Picker 创建时报 `protocol error 400: invalid command`：TUI-v3 first-party create effect 会带默认 shell command，protocol terminal service adapter 对空 command 做同一兜底，避免向 core-v2 发送空 `Command`；不改变 protobuf+socket contract，不放宽 core-v2 对第三方空 command 的校验；harness 覆盖 picker/pool create request command 非空、adapter 空 command 兜底、真实 core-v2 create 不再返回 invalid command；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 111. Terminal Picker 可用性与视觉返工 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已按用户反馈返工当前不可用且视觉粗糙的 Terminal Picker：从工程化小 overlay 改为可扫描的 terminal switcher，内容包含清晰搜索栏、列表表头、当前/已挂载/池中来源区分、选中行高亮、选中项详情和明确动作；不引入新 runtime 架构，不改变 Terminal Pool 管理页和 core-v2 protocol；harness 覆盖默认 Picker frame 不再使用旧 `⌕ search [filter terminals]` / `PREVIEW pane:...` 工程文案，固定 smoke 包含新的 switcher 信息结构，鼠标/键盘 attach/new hit region 继续可用且 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 112. terminal live surface 尺寸与样式对齐 | 完成 | `workflow.md`、`termx-core-v2/`、`termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/render/`、`termx-vterm/` 与 `go.work`/`go.mod` 按需，只读参考 `termx-core/` 与 `tuiv2/` | 已按用户反馈修复真实 terminal 内容错位和颜色样式丢失：参考旧 `termx-core`/`tuiv2` 保留 live `ScreenData` cell matrix、cursor、modes 和 size 的做法，把 core-v2 live surface 从纯 string rows 推进到 size-bound vterm cell snapshot；protocol snapshot 不再把整行压成单个 plain cell；tui-v3 adapter/state/render 保留 live cells 并投影为 styled content line，旧 string lines 只作为 fake/fallback；active content rect resize 继续作为 PTY size truth；harness 覆盖 ANSI/SGR 样式、cursor/size、CR/position 不错位、snapshot cells 不丢 style、render ANSI frame 保留 styled live content；准入已运行 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 113. terminal live ANSI palette 保真 | 完成 | `workflow.md`、`termx-tui-v3/render/` 按需，只读参考 `tuiv2/render/` 与 `tuiv2/runtime/` | 已修复用户截图反馈的同一宿主环境颜色不统一：live terminal content cell 中的 `ansi:0..15`、`idx:0..255`、truecolor、bold、italic、underline、reverse 等 SGR 语义会作为 terminal 内容样式保留到 ANSI FrameSink，不再映射成 TermX chrome 的 semantic `StyleToken`；TermX chrome/theme 继续使用 `StyleToken`，terminal 内容只在旧 string fallback 无 cell style 时才走兼容解析；harness 覆盖 live cell palette 输出使用宿主 ANSI 编号、256 色和 truecolor 直通、renderer compositor 裁切后仍保留 terminal cell style、不会把 `ansi:4` 输出成主题 `info` truecolor；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 114. 真实 resize 到 core-v2 闭环修复 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-tui-v3/state/`、`termx-cli/`、`termx-core-v2/` 按需，只读参考 `termx-core/` 与 `tuiv2/` | 已修复真实 resize 闭环证据不足和 ownership 丢失问题：`TerminalSessionStore` 保存 attach/reconnect 的 resize policy、surface id 和 view id，layout resize 仍通过 `LiveResizeMsg` 回到 live reducer，再由 terminal service 走 protocol `ensure_resize` 或裸 `resize`；Terminal Pool attach/reconnect 使用稳定 first-party view id；CLI 真实 core-v2 socket/server harness 注入 recording `ProcessFactory`，逐步证明 attach/correct size、host resize、header/footer hide、pane split、keyboard resize、zoom/unzoom、close 和鼠标 divider drag 都产生新的 core-v2 process resize 记录；失败路径证明 process resize error 会回到 session error；harness 覆盖 no terminal input leak、真实协议 traffic、core-v2 process resize、content rect PTY size truth 和 stale resize guard |
| 115. 键盘与鼠标映射对齐 tuiv2 | 完成 | `workflow.md`、`termx-tui-v3/input/`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/` 按需，只读参考 `tuiv2/input/`、`tuiv2/app/input_intents.go` 与 `tuiv2/render/hit_regions*` | 已参考 tuiv2 canonical keymap 和输入队列经验补齐 v3 自有输入映射：原 `input` 硬编码 switch 已收敛为 v3 binding catalog，root/sticky mode 保留 v3 现有键位并补 tuiv2 无冲突别名；TerminalHost parser 支持 ShiftTab/Home/End/Delete/Insert/F1-F12/modifier arrows/Alt 字符、Backspace/Tab 和 SGR wheel/button/drag/release；normal 未绑定 raw key 和常见 Ctrl 会透传 terminal，UI mode/overlay/copy mode 未绑定输入会吞掉；live snapshot 保存 terminal mouse modes，只有 active pane content 且终端程序开启 mouse tracking 时才 raw mouse passthrough，chrome/overlay/toast 保持 UI hit priority；copy mode 补齐 hjkl/Home/End/g/G/Space/y/Enter 基础选择复制；harness 覆盖 binding catalog 去重、raw key passthrough、UI swallow、extended parser、overlay 不漏发、copy selection/wheel、pane/floating drag 和 terminal mouse tracking passthrough；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 116. 补丁代码审计与边界收口 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 已按用户要求区分可接受的面条代码和不可接受的补丁代码，并落档 `termx-tui-v3/docs/patch-boundary-audit.md`：当前可接受的线性流程必须单 owner、可测试且继续经过 reducer/effect/message/service result；不可接受路径包括绕过 reducer、服务直改 state、renderer 读 runtime/service、重复 truth、action 字符串分叉、fake-only 证据和默认入口旧依赖回流。已把 render/app 共享 content action id 收敛到 `render.ActionIDCatalog()`，生产代码不再在 render 和 app 两侧手写 `empty.*`、`picker.*`、`pool.*`、`workbench.*`、`prompt.*`、`help.*`、`floating.*`、`pane.*` action id；新增 guard 覆盖 action catalog 唯一性、render 不 import app/services/terminalhost/Bubble Tea、input binding catalog 单 owner；明确 `reduceShellContentAction` 当前偏长但 owner 单一、可测，后续增长时再拆 per-content reducer，不在本切片做大重构；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 117. 宿主 theme/palette probe 迁移 | 完成 | `workflow.md`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` 按需，只读参考 `tuiv2/app/`、`tuiv2/runtime/`、`tuiv2/render/` 与 `tuiv2/shared/` | 已迁移 tuiv2 的宿主主题探测能力到 v3 边界：真实 `TerminalHost` 进入 TUI 后发送 OSC 10/11/4 查询 host default fg/bg 与 16 色 palette；`InputParser` 支持 BEL/ST 结束的 OSC response，解析为 `EventKindHostTheme`，不作为 terminal input 透传；`AppRuntime` 把事件转换为 `HostThemeMsg`，由 reducer 更新 `StateRoot.HostTheme`；`RenderVMBuilder` 推导 `RenderVM.Theme`，renderer 优先使用 VM theme 输出 TermX chrome；该路径只影响 chrome semantic token，PTY live 内容中的 `ansi:N`、`idx:N` 和 truecolor 继续直通宿主，不重新映射；harness 覆盖 OSC 10/11/4 解析、probe 可关闭 fallback、theme token 推导、probe response no terminal input leak、styled frame 使用 host-aware chrome palette 和 terminal live palette 直通；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 118. tmux 黑盒 harness 基线 | 完成 | `workflow.md`、`termx-cli/`、`Makefile` 按需 | 已建立 tmux 黑盒 harness 基线：新增 `termx v3 tmux-smoke`，在隔离 tmux session/window/pane 中启动专用 shell 探针，用 `tmux send-keys` 发送真实键盘输入，用 `tmux capture-pane -e -p` 和 plain capture 收集 artifact，并保证 tmux session 清理；缺 tmux 时 Go 测试显式 skip，Makefile 目标显式 skip；本切片只证明 tmux 宿主可启动、可发送输入、可抓取 ANSI/plain 输出和可收集 artifact，不验证 terminal create/attach/input、resize 或 theme 深层语义；准入已运行 `cd termx-cli && go test ./cmd/termx -run 'TestV3TmuxSmoke' -count=1`、`make test-cli-v3-tmux-smoke`、`cd termx-cli && go test ./... -count=1`、`git diff --check` |
| 119. tmux terminal create/attach/input 回归 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/`、`termx-core-v2/`、`Makefile` 按需 | 已基于切片 118 harness 建立真实 terminal create/attach/input 黑盒回归：新增 `termx v3 tmux-terminal-smoke` 和 `make test-cli-v3-tmux-terminal-smoke`，测试构建真实 `termx` 二进制，在隔离 tmux pane 内通过 core-v2 daemon/protocol/PTY 执行 `v3 new` 创建 shell 探针，再 `v3 attach` 进入 tui-v3，用 `tmux send-keys` 发送真实键盘输入并通过 live surface 抓到 `termx-pty-echo:tmux-live-input`；artifact 包含 captured ANSI/plain、daemon log、socket path、terminal id、tmux 脚本和命令 timeline；测试路径会清理 tmux session 与 artifact，Makefile 路径保留 artifact 供人工复核；本切片不验证 resize、theme/ANSI 视觉或稳定性压测；准入已运行 `cd termx-cli && go test ./cmd/termx -run 'TestV3Tmux(TerminalSmoke|Smoke)' -count=1`、`make test-cli-v3-tmux-terminal-smoke`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 120. tmux resize 与 layout 电路回归 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/`、`termx-core-v2/`、`Makefile` 按需 | 已基于 tmux harness 建立真实 resize/layout 黑盒回归：新增 `termx v3 tmux-resize-smoke` 和 `make test-cli-v3-tmux-resize-smoke`，测试构建真实 `termx` 二进制，在隔离 tmux pane 内启动 core-v2 daemon/protocol/PTY 和 tui-v3 attach，用 `tmux resize-window -x 120 -y 40` 触发宿主 resize，再让 PTY 内 shell 探针执行 `stty size` 并通过 live surface 抓到 `termx-pty-size:size-after:118x36`，证明 host viewport 进入 tui-v3 layout 后按 active content rect 触达 core-v2 PTY resize；artifact 包含 captured ANSI/plain、daemon log、socket path、terminal id、tmux 脚本和 timeline；copy mode cols rebind 与 split/header/footer/zoom 等结构 resize 仍由既有 v3 e2e/app/core 测试覆盖，本切片不混入 theme/ANSI 视觉证据或稳定性压测；准入已运行 `cd termx-cli && go test ./cmd/termx -run 'TestV3TmuxResizeSmoke' -count=1`、`make test-cli-v3-tmux-resize-smoke`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 121. tmux ANSI/theme/live surface 视觉证据 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/`、`termx-core-v2/`、`Makefile` 按需 | 已基于 tmux capture `-e` 建立 ANSI/theme/live surface 机器证据：新增 `termx v3 tmux-ansi-smoke` 和 `make test-cli-v3-tmux-ansi-smoke`，测试构建真实 `termx` 二进制，在隔离 tmux pane 内启动 core-v2 daemon/protocol/PTY 和 tui-v3 attach，让 PTY 探针输出 ANSI 16 色、bold 16 色、256 色前景/背景、truecolor、CR 改写和 alt-screen 往返内容；断言 `capture.ansi` 保留 `31m`、`1m`、`34m`、`38;5;202m`、`48;5;24m`、`38;2;12;200;155m`，plain capture 保留 live markers 且不泄漏 OSC；artifact 包含 captured ANSI/plain、daemon log、socket path、terminal id、tmux 脚本和 timeline；该切片只产出机器可读证据和 artifact，不替代人工视觉拍板，也不做稳定性压测；准入已运行 `cd termx-cli && go test ./cmd/termx -run 'TestV3TmuxANSISmoke' -count=1`、`make test-cli-v3-tmux-ansi-smoke`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 122. tmux 稳定性压测与失败 artifact 收口 | 完成 | `workflow.md`、`termx-cli/`、`Makefile`、`termx-tui-v3/docs/` 按需 | 已建立短时 tmux 稳定性压测与失败 artifact 规范：新增 `termx v3 tmux-stability-smoke --rounds N` 和 `make test-cli-v3-tmux-stability-smoke`，串行执行 terminal create/attach/input、resize/layout、ANSI/live surface 三类黑盒 smoke；成功时输出汇总 artifact 目录、timeline 和子 artifact 列表，失败时保留已生成子 smoke 的 captured ANSI/plain、daemon log、socket path、terminal id、tmux 脚本和 timeline；新增 `termx-tui-v3/docs/tmux-harness-artifacts.md` 说明 tmux 只是测试宿主、可选目标、artifact 文件和定位顺序；缺 tmux 时可选目标 skip；准入已运行 `cd termx-cli && go test ./cmd/termx -run 'TestV3TmuxStabilitySmoke' -count=1`、`make test-cli-v3-tmux-stability-smoke`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 123. header/footer 对齐 tuiv2 tab/status bar | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/frame.go`、`tuiv2/render/hit_regions_workbench.go`、`tuiv2/render/styles.go` | 已参考 tuiv2 的 tab bar/status bar slot 与 chip 模型重画 v3 header/footer：header 从旧 `[＋] [ ]` 和 `▎` indicator 收敛为 `workspace │ 1:tab × │ ＋ │ pane:id` 槽位，notice/error 继续走右侧 warning 槽；footer 不再展示独立 `LIVE` ready token，也不把 active target、summary、live status 全塞左侧，改为左侧 mode/action hint chip、右侧 `● active`、`ws:*`、`tabs:*`、`panes:*`、`float:*`、live hint metadata token；保留 header/footer hide、viewport 宽度填满、styled ANSI 背景、窄屏裁切和真实 CLI smoke/e2e marker；未复制 tuiv2 runtime/model，也未引入 Bubble Tea contract；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 124. pane chrome 未设计 token 回收 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 已按用户反馈回收当前 pane 顶边提前画出的未设计 token：默认 tiled pane chrome 不再展示状态 Nerd Font、`⇄2`、`◆ owner`、`1/31` 或 action cluster，也不暴露不可见 pane action hit region；当前只保留标题、边框、active/inactive style 和内容裁切。Nerd Font 字符体系后续必须作为独立设计切片定义语义、字形、fallback 和命中区后再接入；floating/overlay/header/footer 未在本切片重画；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 125. pane 可见 action 与 footer tuiv2 复核 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/pane_chrome_render.go`、`tuiv2/render/hit_regions_pane_chrome.go`、`tuiv2/render/hit_regions_workbench.go`、`tuiv2/render/frame_test.go` | 已按用户反馈恢复真正可见且可工作的 tiled pane action：默认 pane 顶边显示已接通的 split-down、split-right、close 三个按钮，窄 pane 退化为 close-only 或隐藏 action；按钮文本与 hit region 复用同一 action item 列表，点击通过既有 `PaneCommand` 创建分屏或关闭 pane，且 pane action hit region 优先于 shared divider resize。footer 参考 tuiv2 status bar 继续保持左侧 mode/action hint、右侧 active/workspace/pane/floating/live metadata，并把快捷键 chip 收敛为 `[Ctrl+P]` 类紧凑形态；仍不展示未设计的 owner、`⇄2`、`1/31`、zoom 等 token，未重画 Terminal Picker、floating 或 overlay；准入已运行 `cd termx-tui-v3 && go test ./render -count=1`、`cd termx-tui-v3 && go test ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 126. floating 与 tiled pane 焦点切换 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 已按用户反馈修复 floating 打开后的焦点切换：点击未遮挡的底层 tiled pane content/chrome/action 会通过 pane command 成功路径触发 reducer-owned `floating.deactivate` 语义，重新聚焦 tiled pane，并让当前 floating 保持打开但清空 active 状态，边框/标题按 inactive/muted 灰色渲染；点击 floating content/title/action 仍保持 focus/raise floating。已调整 `ensureFloatingDefaults`，允许存在 floating 但 `ActiveFloatingID` 为空，避免默认逻辑重新抢回焦点；补 state/app/render harness 覆盖 active floating dim tiled pane、点击 tiled pane 后 active floating 清空、tiled pane active style 恢复、floating inactive style 保留；准入已运行 `cd termx-tui-v3 && go test ./state -count=1`、`cd termx-tui-v3 && go test ./app -count=1`、`cd termx-tui-v3 && go test ./render -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke` |
| 127. daemon 通用存储与广播底座 | 完成 | `workflow.md`、`termx-core-v2/`、`internal/protocol/`、`termx-proto/`、`termx-tui-v3/services/`、`termx-cli/` 按需，只读参考旧 `termx-core/` 存储语义 | 已建立新版 daemon-owned 通用 storage 底座，作为后续 TUI workspace/tab/pane 多客户端同步的承载区：core-v2 已实现 `storage.get`、`storage.put`、`storage.delete`、`storage.list` 的内存 store、version/checkVersion 冲突保护、prefix list、clone value 和 `storage.changed` 事件广播；protocol 复用已有 `storage.*` protobuf+socket wire contract，没有新增并行 socket；事件过滤已支持 app/scope/owner/key prefix；文档明确 storage 与 history truth 分离，daemon 不理解 workspace/tab/pane 语义，TUI 或第三方客户端自行定义 opaque value schema。本切片只做通用 storage 底座，不把 workspace/tab/pane CRUD 直接塞成 TUI 本地补丁；准入已运行 `cd termx-core-v2 && go test ./... -count=1`、`go test ./internal/protocol -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` |
| 128. workbench 专用协议债务记录 | 完成 | `workflow.md`、`termx-core-v2/`、`internal/protocol/`、`termx-proto/`、`termx-tui-v3/docs/` 按需，只读参考 `termx-tui-v3/state/` 和旧 `termx-core/` 存储语义 | 该切片曾实现 `WorkbenchSnapshot`、`workbench.get`、`workbench.apply`、`workbench.changed` 等 daemon workbench 专用 API，但后续用户明确纠正：daemon 不应知道 workbench truth，daemon 只负责 terminal pool、history 和附带的通用 opaque storage；workspace/tab/pane 是 TUI 或第三方客户端自己的组织模型。自本纠偏后，已有 workbench 专用协议只能视为迁移债，不得继续扩展为 daemon 领域 truth；后续应优先把 TUI workspace/tab/pane schema 收敛到 `storage.*` CAS 与 `storage.changed` 事件模型 |
| 129. header tab 点击入口修复 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已修复用户反馈的 header tab 名称后 `×` 点击无效：header tab close token 和 `＋` token 现在在 layout measurement 阶段产出稳定 `content-action` hit region，app mouse dispatcher 复用既有 `ShellContentActionMsg`，再转成已有 `WorkbenchCommandTabClose` / `WorkbenchCommandTabCreate`，不新增第二套 tab 逻辑；render harness 覆盖 `tab.close` 和 `tab.create` 命中区，app harness 覆盖鼠标点击 `×` 关闭 active tab、点击 `＋` 新建并激活 tab；本切片只修复本地 TUI 可点击入口，TUI workbench schema 接入 daemon 通用 storage 的多客户端同步仍留后续切片 |
| 130. pane close glyph 点击回归 | 完成 | `workflow.md`、`termx-tui-v3/app/` | 已按用户反馈复核 pane 顶边 `×`：现有 pane action hit region 和 mouse dispatcher 已能关闭多 pane 中的目标 pane，本切片补上真实可见 close glyph 点击 harness，测试不再只点 hit region 左上角，而是按渲染行内的 close token cell 发鼠标事件；覆盖默认 Nerd Font glyph 和 emoji 宽 glyph fallback，确保可见 `×`/close token 落在对应 pane.close hit region 内并关闭目标 pane；同时明确最后一个 pane 不能被关闭，点击其 close token 会走同一 command 并显示 `cannot close last pane` toast，不再表现为无反馈静默；本切片不改变关闭最后 pane 的业务规则 |
| 131. tuiv2 产品契约梳理与 v3 对齐矩阵 | 完成 | `workflow.md`、`termx-tui-v3/docs/`、只读参考 `tuiv2/docs/`、`tuiv2/input/`、`tuiv2/render/`、`tuiv2/app/` | 已通读 tuiv2 产品定义、keybinding、mouse matrix、chrome layout、workbench tree、input catalog、hit region、overlay footer、terminal pool layout 和 status hints，新增 `termx-tui-v3/docs/tuiv2-product-contract-alignment.md`；该文档明确 workspace/tab/pane/floating/terminal/pool/overlay 对象语义、所有主要按钮/快捷键/鼠标动作/效果、header/footer/pane/overlay 可见控件规则、v3 已对齐/部分/缺失项和后续 P0-P3 优先级；后续不得继续临场画未接线控件，必须按该矩阵把可见按钮接到统一 semantic action，并由 TUI schema 写入 daemon 通用 storage，而不是让 daemon 理解 workbench truth |
| 132. Unicode 界面线稿候选稿 | 完成 | `workflow.md`、`termx-tui-v3/docs/` | 已新增 `termx-tui-v3/docs/unicode-ui-wireframes.md`，用 Unicode box drawing 绘制默认 Workbench、split line、empty pane、floating、Terminal Picker、Terminal Pool、Workbench Tree、Prompt/Confirm 和窄屏退化候选界面；线稿只画当前产品契约已定义语义，不画 owner/share/lifecycle/Nerd Font 等未拍板 token，明确 daemon 不理解 workbench，workspace/tab/pane 是 TUI schema 对 daemon storage 的客户端投影；该文档用于人工对齐，不代表 renderer 已实现 |
| 133. Unicode 线稿首轮 renderer 对齐 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需 | 已按用户确认的 `unicode-ui-wireframes.md` 与 `tuiv2-product-contract-alignment.md` 首轮对齐可见 renderer：pane action 默认从 Nerd Font/emoji 方括号 token 收敛为基础 Unicode `↕  ↔  ×`，empty/exited/picker/pool/tree/prompt 可见文案收敛到线稿语言；Terminal Pool 只展示已接线的 Attach Here/Edit/Kill，Workbench Tree 只展示 Open，不提前画 attach tab/floating、rename/delete 等未接线按钮；保持现有 action id、hit region、宽度安全和 no terminal input leak，并同步 CLI smoke/e2e marker 到新线稿；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` |
| 134. workbench storage schema 与 service adapter 基线 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已建立 TUI-owned workbench snapshot schema 和 service adapter 基线：workspace/tab/pane/floating 结构可从 `ShellStore` 生成 opaque JSON snapshot 并恢复，schema 固定为 `termx.tui.v3.workbench`，默认写入 `app=termx-tui-v3 scope=public owner=workspace-main key=workbench/root`；真实 adapter 只调用 daemon 通用 `storage.get` / `storage.put`，未调用或扩展 `workbench.*`；本地 workbench command 成功后通过 effect 触发 persist request，带 storage deps 的 interactive runtime 会保存 snapshot，缺 service/save error 有 toast 反馈；默认 CLI v3 attach 已装配 `ProtocolWorkbenchStorageAdapter`；本切片只建立 storage 写入基线和 harness，不做完整多客户端订阅合并；准入已运行 `cd termx-tui-v3 && go test ./state ./services ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`git diff --check` |
| 135. workbench storage.changed 订阅与 reload | 完成 | `workflow.md`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已接上 daemon 通用 `storage.changed` 多客户端通知：TUI service adapter 订阅 `app=termx-tui-v3 scope=public owner=workspace-main keyPrefix=workbench/` 的 storage events，app 通过 `StreamEffect` 把事件回投 message path，再用 `storage.get` reload opaque snapshot 并刷新 reducer-owned shell；事件和 reload 只走 `storage.*` / `storage.changed`，没有新增或调用 `workbench.*`，daemon 仍不理解 workbench truth；自身 persist 返回的 version 已记录并忽略对应回声事件；默认 attach/e2e smoke 使用独立 protocol client 订阅 workbench storage event，避免 core-v2 单 session events stream 取消 terminal live event；本切片只做订阅/reload 基线，不做复杂三方 merge UI；准入已运行 `cd termx-tui-v3 && go test ./services ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3TmuxResizeSmokePropagatesHostResizeToPTY|TestV3TmuxResizeSmokeCommandReportsArtifacts|TestV3TmuxStabilitySmokeRunsShortRound|TestV3TmuxStabilitySmokeCommandReportsArtifacts' -count=1`、`cd termx-cli && go test ./... -count=1`、`git diff --check` |
| 136. workbench storage CAS 与冲突反馈 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、`termx-cli/` 按需 | 已把 TUI workbench mutation 写回收敛到 storage version/checkVersion CAS：persist request 使用 `WorkbenchSyncStore` 的 base version 作为 `ExpectedVersion`，初始首写使用 version 0，load/save 成功会推进 base version；service adapter 将 protocol `storage version conflict` 包装为 typed `ErrWorkbenchStorageConflict`，app 收到 conflict 后不会静默覆盖远端 snapshot，而是标记 reducer-owned conflict 状态、显示 `conflict: reloading` toast，并触发 `storage.get` reload 最新 opaque snapshot；fake storage harness 可模拟真实 CAS 当前版本；仍未调用或扩展 `workbench.*`，daemon 只做 opaque storage version 检查和 `storage.changed` 广播；准入已运行 `cd termx-tui-v3 && go test ./state ./services ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`git diff --check` |
| 137. workbench workspace/tab/pane CRUD 补齐 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/` 按需 | 已补齐当前可见产品闭环里的 workbench CRUD 语义：workspace switch/create/rename/delete、tab create/rename/close/kill、pane split/rename/detach/close/kill 均进入 TUI-owned `WorkbenchCommand`；pane close 只移除工作位，pane detach 只断开 terminal 绑定，pane kill/tab kill 会在结构写回后通过 message path 请求 terminal kill；键盘 pane close/detach/kill、鼠标 pane split/close、Workbench Tree Open/Rename/New/Delete 和测试入口已统一到 workbench command，再触发 storage CAS persist request；Workbench Tree 只显示已接线的 Open/Rename/New/Delete action，不画未完成服务闭环按钮；准入已运行 `cd termx-tui-v3 && go test ./state ./app ./render -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`git diff --check` |
| 138. tmux 抓屏视觉基线与一比一返工入口 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`Makefile` 按需，只读参考 `tuiv2/` | 已按用户最新反馈把视觉验收从普通 smoke 升级为 tmux 抓屏对比：新增 `termx v3 visual-snapshot --ansi` 和 `termx v3 tmux-visual-compare`，固定 `120x40` viewport 保存 `current.txt`、`current.ansi`、`target.txt`、`diff.txt` 和 summary；目标基线来自 `unicode-ui-wireframes.md` 与 `tuiv2` chrome slot contract，不再拿当前 v3 smoke 自证；首轮 tmux diff 暴露当前 v3 与目标仍有 40 行差异，并完成最小 renderer 返工，把 header/footer 从裸状态栏推进到 `┌…┐` / `└…┘` 单行框线；文档已明确该结果不是截图级通过，后续必须继续按 diff 返工；准入已运行 `cd termx-tui-v3 && go test ./render -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3TmuxVisualCompare|TestV3SmokeCommandIncludesVisualReviewCases' -count=1`、`make test-cli-v3-tmux-visual-compare` |
| 139. tmux diff 驱动 shell 外框层与 body 分隔返工 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/` | 已按切片 138 的 `diff.txt` 收敛最外层 shell 结构：layout 在 header/footer 同时可见时为 body 增加左右外框 inset，header/footer 在常规高度使用双行 band；header 第一行使用 `workspace ─┬─ tab ─┬─ ＋`，第二行按同一连接点绘制 `├…┴…┤`，固定 visual-audit 场景补为两个 tab；footer 改为独立 `┌…┐` / `└…┘` 两行状态层；active content rect 随外框从 `120x40 -> 118x36` 收敛为 `116x34`，CLI/tmux resize/e2e harness 已同步；最新 `make test-cli-v3-tmux-visual-compare` 记录 mismatch 从 40 降到 39，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-2030268224/`；仍不得宣称一比一视觉完成，剩余 pane body 双重边框、pane/floating/toast 比例、right summary、tab close 槽位和内容裁切差距继续后续切片；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 140. tmux diff 驱动 pane/body 与浮层比例返工 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/` | 已按切片 139 的 `diff.txt` 继续收敛 body 内部构图：shell-framed split-line pane 不再在 body 外框内额外绘制整套 pane 外框，visual-audit 左右 pane 均使用真实 live surface，左 pane 恢复目标 shell 输出，right pane 恢复目标 review 文本；header 普通状态恢复右侧 `termx` brand，非 active tab 恢复 close 槽位；footer 从 active/live/float 元信息收窄为目标线稿的 `[Ctrl+P] pane  [Ctrl+R] resize  [Ctrl+F] picker  [Ctrl+G] global` 与 `ws/tabs/panes` summary；floating quick actions 位置、宽度、标题 close 槽位、内容文案和 resize 角标按目标线稿收窄；最新 `make test-cli-v3-tmux-visual-compare` 仍记录 mismatch=39，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-1179147275/`，说明行级差异仍未消除，不能宣称一比一视觉完成；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 141. tmux diff 驱动字符级线稿对齐 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/` | 已按切片 140 的 `diff.txt` 继续消除剩余行级差异：新增 render VM 级 visual shell frame/body override，让固定 visual-audit 的 shell body、split divider、bottom frame 和 footer summary 按目标线稿收窄；pane 顶边右边框重画避免 action 覆盖，floating title 从 `┌─ quick actions` 收敛为 `┌ quick actions`，left pane 恢复视觉 active；最新 `make test-cli-v3-tmux-visual-compare` mismatch 从 39 降到 15，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-1517898182/`；仍不得宣称视觉完成，剩余 header/tab 分隔长度、pane 顶边 action spacing、floating/right-pane 保留列和部分 content padding 继续后续切片；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 142. tmux diff 剩余 15 行收敛 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/` | 已在切片 141 的 `diff.txt` 基础上继续消除 mismatch=15：为 visual-audit 增加 footer frame override，使 footer summary 对齐目标线稿；floating quick actions 恢复目标宽度并保留右侧 pane 边框列，row 8/9/11/12/13 与目标一致；最新 `make test-cli-v3-tmux-visual-compare` mismatch 从 15 降到 9，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-2277205246/`；仍不得宣称视觉完成，剩余 header 第一/二行 tab 分隔、pane 顶边 action spacing、右 pane content padding 和 floating 空白/Close/底边三行继续后续切片；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 143. tmux diff 剩余 9 行收敛 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`termx-cli/` 按需，只读参考 `tuiv2/render/` | 已在切片 142 的 `diff.txt` 基础上消除 mismatch=9：header 顶线和分隔线支持独立 frame override，固定 visual-audit 场景把 header top/divider、pane 顶边 action cluster、右 pane padding 和 floating 空白/Close/底边按目标线稿校准；`termx v3 tmux-visual-compare` 测试从“暴露 mismatch”更新为“当前抓屏等于 target”；最新 `make test-cli-v3-tmux-visual-compare` mismatch=0，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-2769538914/`；该自动线稿对齐仍需用户人工截图复核后才能宣称一比一视觉完成；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 144. tmux ANSI 样式级视觉对齐 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/render/`、`termx-tui-v3/docs/`、`Makefile` 按需，只读参考 `tuiv2/render/` | 已把 `tmux-visual-compare` 从 plain 线稿扩展到 ANSI/style 级证据：artifact 现在保存 `style.txt` 与 `style.diff.txt`，summary/命令输出记录 `style_mismatches`，测试断言当前抓屏 plain mismatch=0 且 style mismatch=0；style contract 覆盖 header/status 背景、active tab accent、pane action accent、inactive logs muted、右 pane content muted、floating accent、floating 遮挡下的底层 frame status、footer 背景和 footer key accent；同时修复 visual-audit correction 中 floating 空白/Close/底边三行把底层右边框误染成 accent 的问题。最新 `make test-cli-v3-tmux-visual-compare` 输出 mismatch=0、style_mismatches=0，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-1950938977/`；自动 ANSI/style contract 仍不能替代用户人工复核真实截图观感；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 145. tmux 全帧样式图对比 | 完成 | `workflow.md`、`termx-cli/`、`termx-tui-v3/docs/`、`Makefile` 按需，只读参考 `tuiv2/render/` | 已把 `tmux-visual-compare` 从少量 ANSI/style 探针补强为全帧 cell 样式图 artifact：保存 `current.stylemap.txt`、`target.stylemap.txt` 与 `stylemap.diff.txt`，summary/命令输出记录 `stylemap_mismatches`；目标样式图来自固定 `120x40` target 线稿的语义区域，不从当前 v3 输出自证；测试断言 plain mismatch=0、probe style mismatch=0 且全帧 style map mismatch=0。最新 `make test-cli-v3-tmux-visual-compare` 输出 mismatch=0、style_mismatches=0、stylemap_mismatches=0，artifact 示例 `/var/folders/_k/rv9v4pv16b96_ss090ljksn80000gn/T/termx-v3-tmux-visual-3840366310/`；该机器证据仍不能替代用户人工复核真实截图观感；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check` |
| 146. tuiv2 chrome slot contract 结构化迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`，只读参考 `tuiv2/render/` | 已把 header/tab/footer action/pane chrome action 从字符串拆分推进为结构化 VM token；renderer 和 hit region 消费同一组可见 pane action slot，固定 visual-audit logs pane 的 close-only 行为从 renderer 硬编码迁到 VM action slot；旧字符串字段仅作为兼容 fallback 保留；harness 覆盖结构化 tab、footer action、pane action 槽位退化和 hit region 对齐；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 147. visual-audit chrome override 数据化 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/tui.go` | 已把固定 visual-audit 证据帧的坐标修正从 renderer 内部 pane/floating id 判断迁到显式 VM patch；floating 的背景填充、右侧 shell frame 延伸和 resize handle 可见性也由 VM chrome 控制；renderer 和通用 VM builder 不再知道 `pane-logs` / `float-visual` 的审计语义，审计场景只在 smoke VM 构造处显式设置；harness 防止 render framework/builder 重新出现 visual-audit id 特例，并覆盖 chrome patch 样式输出；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 148. pane chrome slot VM 结构化迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/` | 已把 pane 顶栏从 title/action 两段拼接推进为 title/state/meta/action 结构化 slot VM；renderer 统一消费 slot VM 并按宽度从 meta/state 到 title/action 退化，hit region 继续只覆盖实际可见 action；默认视觉不提前显示 owner/share/size 等未拍板 token，state/meta 只在 VM 明确给出时渲染；harness 覆盖 state/meta slot 渲染、窄屏退化和 action hit region 对齐；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 149. pane state slot 默认渲染迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/tui.go` | 已把 pane 顶栏 state 槽位从显式测试 VM 推进到默认 `RenderVMBuilder` 输出；状态只来自当前 pane/content 的真实 pending/error/empty/active/idle，不提前引入 owner/share/size 等未拍板 meta token；visual-audit 固定证据帧按显式 VM 继续保持线稿一致；harness 覆盖默认 active/idle/pending/error/empty state slot 和 workbench smoke 可见性；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 150. header tab close 目标槽位迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/` 按需 | 已把 header tab close token 从“统一关闭 active tab”的字符串 action推进为携带目标 tab id 的结构化槽位；hit region 和 mouse dispatcher 会把目标 tab id 传回同一 `WorkbenchCommandTabClose` contract，不新增第二套 tab 逻辑；harness 覆盖点击非 active tab 的 close token 时关闭被点击 tab、保留 active tab，以及 render hit region 携带目标 id；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 151. footer action token hit region contract 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/` | 已把 footer `ActionTokens` 从纯展示 token 推进为可携带 `ActionID` 的结构化 hit region contract；renderer 和 layout measurement 复用同一组可见 token，只有实际显示且有 action id 的 token 产生 `content-action` hit region；本切片只建立 render contract，不把所有 footer token 一次性派发到 app 命令族；harness 覆盖 action id 透传、窄屏退化和无 action id token 不产生命中区；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 152. footer action token app 派发迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把默认 footer 与 global footer 中已存在 keyboard semantic 的 action token 填入稳定 action id，并通过既有 `ShellContentActionMsg` 派发到同一 shell/workbench/pane command 路径；已接入 pane mode、resize mode、picker、global mode、header/footer hide、Terminal Pool、Workbench Tree、toast close/clear，不引入未定义动作；harness 覆盖鼠标点击 footer token 与键盘入口等价；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 153. footer action no terminal leak harness | 完成 | `workflow.md`、`termx-tui-v3/app/` | 已为 footer action 鼠标派发补 no terminal input leak 证据：在真实 interactive runtime + fake terminal service 装配下点击 footer content-action，只进入 UI semantic path 并切换 pane mode，不调用 terminal input service；本切片不改业务语义，只补缺失 harness；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 154. Help 已接线 action 可见性迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 Help overlay 从静态产品愿景文案推进为由当前已接线 action/catalog 派生的可见动作清单：不再展示 edit metadata、center/collapse、workspace delete 等尚未完整闭环动作；保留 help.close hit region 与 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 155. Footer action catalog 结构化迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/` | 已把默认 footer/status hints 从 `[]string` 自由文案和后续字符串推断推进为结构化 `FooterActionVM` catalog；默认 builder 直接生成 key、label、action id 和 style，旧 `Actions []string` 只作为手写 VM 兼容 fallback；不改变现有 footer 视觉；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 156. tab jump 输入语义迁移 | 完成 | `workflow.md`、`termx-tui-v3/input/`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` | 已迁移 tuiv2 tab jump 基础语义：tab mode 下数字键 1-9 通过同一 `WorkbenchCommand` 直接切换到对应 tab，不直接修改 shell state，不新增 header/mouse 第二路径；越界跳转给出同一 workbench invalid 反馈且不漏发 terminal input；tab footer hint 已显示 1-9 jump；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 157. workspace delete footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/state/` | 已把已有 workspace delete workbench 命令暴露为 workspace mode 的结构化 footer action：footer token 产生稳定 action id，点击后走同一 `WorkbenchCommandWorkspaceDelete` confirmed 路径，不新增第二套 workspace 删除逻辑；同时修正当前 active workspace 删除后被 switch/upsert 重新插回列表的问题；已覆盖删除成功、last workspace invalid 反馈和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 158. workspace new/rename footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 workspace mode 的 `n new` 与 `r rename` footer token 迁移为稳定 action id：new 点击走既有 `WorkbenchCommandWorkspaceCreate` 与 `nextWorkspaceName` 路径，rename 点击打开同一 workspace rename prompt，不新增第二套 workspace 创建或 prompt 逻辑；已覆盖 new 成功、rename prompt target 和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 159. workspace switch footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 workspace mode 的 `h/l switch` 展示 token 拆成可点击 previous/next footer action：点击后走已有 `WorkbenchCommandWorkspacePrevious/Next`，不新增第二套 workspace 切换逻辑；已覆盖 previous/next 成功、单 workspace 环绕稳定和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 160. tab rename footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 tab mode 的 `r rename` footer token 迁移为稳定 action id：点击后打开同一 tab rename prompt，不新增第二套 tab rename 或 prompt 逻辑；已覆盖 active tab prompt value、purpose 和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 161. tab switch footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 tab mode 的 `h/l switch` 展示 token 拆成可点击 previous/next footer action：点击后走已有 `WorkbenchCommandTabPrevious/Next`，不新增第二套 tab 切换逻辑；已覆盖 previous/next 成功、单 tab 环绕稳定和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 162. pane mode footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 pane mode 的 `v split`、`x close`、`n focus`、`z zoom` footer token 迁移为稳定 action id：点击后复用已有 pane/workbench command 路径，不新增第二套 pane 结构逻辑；已覆盖 split/focus/zoom/close 成功、last pane close invalid 和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 163. resize mode footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 resize mode 的方向 resize 与 `b balance` footer token 迁移为稳定 action id：点击后复用已有 `PaneCommandResize` / `PaneCommandBalance` 路径，不新增第二套 pane geometry 逻辑；已覆盖方向 resize、balance、单 pane 稳定不变和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 164. floating footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 floating mode 的 `n new` 与 `x close` footer token 迁移为稳定 action id：new 点击复用已有 `FloatingCommandCreate` 默认构造，close 点击关闭 active floating，不新增第二套 floating state 修改逻辑；已覆盖 new/close 成功、无 active floating close feedback 和 no terminal input leak；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 165. copy older footer action 迁移 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已把 copy mode 的 `pgup older` footer token 迁移为稳定 action id：点击后复用既有 copy mode latest/older authoritative history request 路径，不新增第二套 history 读取逻辑；已覆盖 first click 进入 latest、second click 请求 older、no terminal input leak 和 render action catalog；准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` |
| 166. tuiv2 样式整体迁移调停 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/`、`termx-tui-v3/docs/` 按需；只读参考 `tuiv2/render/`、`tuiv2/shared/`、`tuiv2/docs/` | 已按用户最新要求用子 Agent 流程推进：只读子 Agent 审计确认 tuiv2 的 shell header/footer 是单行 bar、无最外层 shell frame、pane/floating/overlay 使用对象级 chrome；实现子 Agent 迁移 v3 的 header/footer、layout content rect、pane/floating/style map 和 smoke 证据；审核子 Agent 复核并推动修正残留外框、mouse divider resize 和 visual target 问题。主 Agent 只做调停、范围裁剪、冲突处理、准入和提交；未修改 `tuiv2/`，未迁入旧 Bubble Tea/runtime/model truth。准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3InteractiveRuntimeCorrectsProtocolResizeToContentRect|TestV3InteractiveRuntimeLayoutResizeReachesCoreV2Process|TestV3InteractiveRuntimeMouseDividerResizeReachesCoreV2Process|TestV3InteractiveRuntimeCoreV2ResizeFailureSurfacesInSession|TestV3E2ESmokeCommandRunsLocalCoreAndTUIPath|TestV3TmuxResizeSmoke|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`git diff --check` |
| 167. 用户指定 header/footer chrome 精修 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/` 按需；只读参考 `tuiv2/render/`、`tuiv2/shared/`、`tuiv2/docs/` | 已按用户给定文本细节精修默认 chrome：header 使用 ` workspace`、inactive tab `index title `、active tab `▎ index title ` 和 `` 创建按钮，样式区分主背景、透明 inactive、active 反色竖线、tab index/text/close；footer 改为无整行背景的 `[Ctrl] • [P] PANE • [R] RESIZE • [T] TAB • [W] WORKSPACE • [O] FLOAT • [V] COPY • [F] PICKER • [G] GLOBAL` 加右侧摘要，括号主色、快捷键多色、label/摘要灰色且 `float:*` 主色；120 列固定视觉帧因物理宽度限制保留全动作和 `float:*`，80/100 列退化保留 `ws:*` 与 `float:*`，140 列覆盖完整 `ws:main float:1 terminals:1`；pane 右上角动作显示 `─[]─[]─[]─[]─` 且命中区同步，floating/遮挡扩散符号用底边 `v`；新增 footer tab/workspace/floating/copy 与 pane zoom action id 均接入 app reducer/command adapter。已按用户要求使用子 Agent：只读定位 Agent 完成；实现 Agent 完成补丁；两个审核 Agent 因超时关闭，主 Agent 完成本地代码复核。未修改 `tuiv2/`，准入已运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3InteractiveRuntimeCorrectsProtocolResizeToContentRect|TestV3InteractiveRuntimeLayoutResizeReachesCoreV2Process|TestV3InteractiveRuntimeMouseDividerResizeReachesCoreV2Process|TestV3InteractiveRuntimeCoreV2ResizeFailureSurfacesInSession|TestV3E2ESmokeCommandRunsLocalCoreAndTUIPath|TestV3TmuxResizeSmoke|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 168. footer 摘要和无背景复核修正 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/` | 已按审核 Agent 发现的问题补齐用户最新 footer 细节：120 列通过 key-only/partial-label 紧凑动作条保留完整 `ws:main float:1 terminals:1` 摘要，不再只剩 `float:*`；error/exited critical hint 改用无背景 warning 前景色，footer 任意状态都不恢复 status 背景；tmux visual compare 基线同步为 140x40 完整动作和完整摘要，并更新 style map/probe。准入已运行 `cd termx-tui-v3 && go test ./render -run 'TestFrameworkRendersFullFooterSummaryAtVisualCompareWidth|TestFrameworkCriticalFooterHintDoesNotRestoreStatusBackground|TestFrameworkRendersFullFooterSummaryWhenWidthAllows' -count=1`、`go run ./termx-cli/cmd/termx v3 tmux-visual-compare`；提交前继续运行全量 v3/CLI 视觉准入。 |
| 169. header 灰度层级复核修正 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/`；只读参考 `tuiv2/render/` | 已按用户截图复核 header 灰度层级：对照 `tuiv2` 的 `tabBarPalette` 推导，把 workspace 改为主背景上的紫色块，inactive tab 改为主背景上的 muted/index-accent 文字且取消 dim，inactive close 使用 danger 与 muted 混合后的暗红，active tab 使用独立 active 背景，marker/index/title/close 都落在同一 active 背景，plus 使用灰蓝背景；同步 tmux visual style probe 和 style map，footer 本轮不改。准入已运行 `cd termx-tui-v3 && go test ./render -run 'TestFrameworkHeaderTabStylesMatchTuiv2Levels|TestFrameworkRendersStyledTopAndBottomBars' -count=1`、`go run ./termx-cli/cmd/termx v3 tmux-visual-compare`；提交前继续运行全量 v3/CLI 视觉准入。 |
| 170. floating 顶边括号动作修正 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/`、`termx-cli/cmd/termx/` | 已按用户反馈修正 floating 顶边 chrome：浮窗不再显示旧 `──── · ● float` 标题/状态条，改为对象边框线上右上角 bracket action；floating 因无分屏只显示可用动作 `[]─[]`，窄屏退化为 `[]`，底边 `v` 继续表达裁切/resize；hit region 与可见 bracket 槽位同步，visual snapshot/tmux target 改为新浮窗顶边合同。准入已运行局部 render/layout/CLI 视觉测试；提交前继续运行全量 v3/CLI 视觉准入。 |
| 171. tui-v3 维护性抽象方案落档 | 完成 | `workflow.md`、`termx-tui-v3/docs/` | 已新增 `termx-tui-v3/docs/maintainability-abstractions.md`，把后视觉完成阶段的维护性抽象方向落档：不引入通用 UI 框架、widget tree、DSL 或 Bubble Tea contract；优先提炼 ActionCatalog、LayoutPlan、ChromePrimitive、Projection 和 ContentProjectorRegistry，并明确真正 ContentRenderer 只消费 `ContentVM + Rect`；明确渐进切片、测试准入、风险止损和行为不变要求。已按用户要求启动只读审核子 Agent 并处理其 findings；文档-only 准入运行 `git diff --check`。 |
| 172. 权限噪音收口 | 完成 | `workflow.md`、当前允许范围内的源码/文档/脚本文件；若触碰冻结目录必须先更新范围表 | 已把允许范围内普通源码、文档、Go module、protocol 等文件的无意义 executable bit 噪音从维护性重构中剥离；新增 `scripts/check_file_modes.sh` 作为权限噪音 guard，脚本自身保留可执行；未触碰冻结目录，未混入抽象代码变更；准入运行 `sh scripts/check_file_modes.sh`、`git diff --check`。 |
| 173. tui-v3 大文件机械拆分 | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/render/`、`workflow.md` | 已在不改行为、不移动 package、不改视觉 target 的前提下按职责拆分 `state/shell.go`、`render/framework.go`、`render/layout_plan.go`：state 拆出 workbench、floating、overlay、pane tree、workspace、projection 和 clone 文件；render 拆出 canvas、shell bar、panel chrome、overlay chrome、layout hit region 和 cursor 文件；新增文件大小 guard 防止重新膨胀；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 174. ActionCatalog 语义收口 | 完成 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/docs/`、`workflow.md` | 已新增 `ActionSpecCatalog` 作为 action id、surface、dispatch、footer token、chrome glyph、Help label 和 danger 元数据的单一来源；footer、pane/floating chrome、Help、content hit region、split resize hit region、app content action adapter、pane hit-region adapter 和 input shell-action bridge 已改为消费同一 spec；新增 guard 覆盖 catalog 唯一性、可见 action、可点击 action、input-only action 与 app adapter，不新增第二套业务逻辑；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、CLI visual tests、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 175. ChromePrimitive 收口 | 完成 | `termx-tui-v3/render/`、`termx-cli/cmd/termx/`、`workflow.md` | 已新增 `ChromePrimitive` / `ChromeSlot`，把 pane、floating、overlay、toast 的 rect、owner、layer、style、content rect 与 action slot 统一表达；pane/floating 渲染和 hit region 已改为消费 primitive action slot，overlay/toast 渲染也改为消费 primitive geometry，visual snapshot 和 tmux visual compare 行为保持不变；新增 guard 确认 pane/floating visible action slot 与 hit region 完全一致、toast text rect 维持既有几何；准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 176. Projection 拆层 | 完成 | `termx-tui-v3/render/`、可新增 `termx-tui-v3/projection/` 或 `termx-tui-v3/viewmodel/`、`termx-tui-v3/docs/`、`workflow.md` | 已按短期 package 策略在 render 包内完成文件级 projection 拆层：新增 `ShellProjector` / `projection.go` 承载 `state.Root -> ShellVM` 投影入口，`RenderVMBuilder` 只保留兼容 facade 并委托 projector；`Renderer` 移入独立 `renderer.go`，避免投影文件承载渲染入口；新增 guard 确认 builder 与 projector 输出一致，并禁止 projection 文件依赖 layout measurement、canvas、FrameSink、service/effect/runtime token；renderer 继续只消费 VM 和 layout plan。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、CLI visual tests、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 177. ContentProjectorRegistry 收口 | 完成 | `termx-tui-v3/render/`、projection/viewmodel 相关目录、`termx-tui-v3/docs/`、`workflow.md` | 已新增 `ContentProjectorRegistry` / `ContentProjectorContext`，terminal live、copy history、empty/exited、Terminal Picker、Terminal Pool、Workbench Tree、Prompt、Help 和 placeholder 都通过独立 content projector 生成 `ContentVM`；`ShellProjector` 的 active pane、inactive pane、floating 和 overlay 内容路径已改为消费 registry，`RenderVMBuilder` 继续只作为 facade；新增 harness 覆盖内容类型注册、copy history 只读 authoritative `HistoryWindow` 且不从 live surface fallback、inactive copy pane 保持 placeholder、注入 registry 能驱动 active/overlay content、`ContentRenderer` 只消费 `ContentRenderRequest{ContentVM, Rect}`；projection guard 已纳入 registry 文件并继续禁止 renderer/runtime/service 依赖。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 178. Terminal Picker 轻量 modal 重做 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/docs/`、`termx-cli/cmd/termx/`；只读参考 `tuiv2/modal/`、`tuiv2/render/`、`tuiv2/docs/` | 已按用户反馈和 tuiv2 实际产品形态把 Terminal Picker 从小型 Terminal Pool 管理页重做为快速 attach/create 的轻量居中 picker modal：`TerminalPickerItems` 追加一等 `CreateNew` 行，内容只保留 `search:`、可扫描 `▸ ○ title` terminal row、`+ new terminal` row 和单行 selected hint；删除 `Select terminal source state target`、`DETAIL/TARGET/TERMINAL` 大块和内容区 `[attach]/[new]` 按钮；footer 只显示 `enter select`/`esc`，row click 与 Enter 复用同一 selected item 语义，pane row focus/close overlay，pool row attach service，create row 走 terminal create effect/result；renderer 继续只消费 `ContentVM + Rect` 和 layout plan，文档已同步轻量 picker 与 Terminal Pool 管理页分工。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 179. Terminal Picker compact 返工 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/cmd/termx/`、`termx-tui-v3/docs/` 按需 | 已按用户最新反馈继续收轻 Terminal Picker：picker overlay 改为按内容量测的 compact popup，不再使用 page-sized 大卡片，也不再填充整块 overlay 背景；内容行进一步简化为 `search:`、默认第一项 `▸ + new terminal` 和 terminal title row，不显示 terminal id、state、target、selected hint 等复杂 table/detail 信息；`+ new terminal` 已成为第一项和默认选中项，用户 `Ctrl-f` 后直接 Enter 即触发 terminal create effect/result；普通 terminal row 保留可扫描标题，row click 与 Enter 仍复用同一 selected item 语义；文档已补充所有弹出框默认简单化、复杂管理内容放到 page overlay 的原则。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 180. Terminal Picker tuiv2 行信息密度修正 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-cli/cmd/termx/`、`termx-tui-v3/docs/` 按需；只读参考 `tuiv2/modal/picker.go`、`tuiv2/render/picker_overlay.go`、`tuiv2/docs/tuiv2-uiux-designer-handoff.md` | 已按用户最新反馈和 tuiv2 `PickerItem.renderBody` 对 Terminal Picker 做信息密度微调：继续保持 compact popup、默认第一项 `+ new terminal` 和无 detail/table 的方向；卡片宽度按内容放大但不回到 page-sized；create row 显示 `+ new terminal  Create a new terminal`，terminal row 显示 `○/●`、terminal id、title、state 和短 `@location/source`；pool row 来源收敛为短 `@pool`，避免长路径撑开快速 picker；仍不得回退到内容区 action row、独立 selected hint 或 Terminal Pool 管理页。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 181. Terminal Picker modal padding 与无背景修正 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-cli/cmd/termx/`、`termx-tui-v3/docs/` 按需 | 已按用户最新反馈继续微调 Terminal Picker：新增主前景 style，picker 文本改为白色/主前景而不是灰色；compact modal 整体更大，并在常规窗口下有上下左右 padding，小窗口下自适应收掉 padding；边框 title 不再显示 `esc` 提示；Terminal Picker 边框只用主前景色，不绘制 overlay 背景色，且不影响 Terminal Pool、Workbench Tree、Prompt、Help 等其它 page overlay。准入运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 182. Terminal Picker 删除键与内背景修正 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/` 按需 | 已修正 Terminal Picker 搜索输入无法删除既有字符的问题，Backspace、Delete 和 DEL/BS 字符都能删除 query 最后一个 rune；picker modal 仍保持边框/title 无背景和无 `esc` 提示，但边框内侧含 padding 与主体内容区域会填充 overlay 背景，避免看起来透明；其它 overlay 背景策略不被改写。准入运行 `go test ./termx-tui-v3/app ./termx-tui-v3/render -run 'TestUIInputReducerTerminalPickerDeleteKeysTrimQuery|TestInteractiveRuntimeTerminalPickerKeyboardFlow|TestFrameworkRendersToastAndTerminalPickerOverlay' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 183. Terminal Picker 背景色与模糊匹配 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/` 按需 | 已把 Terminal Picker modal 内背景从灰色 overlay bg 改为与正文 cell 一致的 picker 专用背景；picker 内容行尾空白也保留同一背景，避免主体和 padding 色块断开；搜索支持子序列模糊匹配，例如 `term-pool` 可被 `trpl` 搜到；匹配到的字符用偏黄色 `StylePickerMatch` 高亮，并保持 picker 行信息密度、默认 new terminal 第一项和其它 overlay 背景策略不被改写。准入运行 `go test ./termx-tui-v3/state ./termx-tui-v3/render -run 'TestTerminalPickerItemsMergeTerminalPoolAndWorkspacePanes|TestRenderVMBuilderFiltersTerminalPickerAndHighlightsSelectedRow|TestRenderVMBuilderProjectsTerminalPickerPoolStateAndRows|TestFrameworkRendersToastAndTerminalPickerOverlay|TestThemeTokenPaletteDrivesANSILines' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 184. Terminal Picker 新建终端表单 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-cli/cmd/termx/` 按需；只读参考 `tuiv2/app/update_modal_prompt_openers.go`、`tuiv2/app/update_modal_prompt_submit_terminal.go`、`tuiv2/render/prompt_overlay.go` | 已按 tuiv2 行为修正 `+ new terminal`：picker create row、鼠标 `picker.new` 和空 pane create 入口先打开 `Create Terminal` 表单，而不是直接创建；第一版表单字段包含 `name` 必填、`command` 可选、`workdir` 可选、`tags` 可选，支持字段切换、输入、删除、取消、必填校验和提交；提交后通过现有 terminal service create effect 创建，传递 title、command、cwd、tags 并保留 pane content rect size；本切片不实现 tuiv2 workdir suggestion popup。准入运行 `go test ./termx-tui-v3/app ./termx-tui-v3/render ./termx-tui-v3/services -run 'TestInteractiveRuntimeTerminalPickerEnterOpensCreateTerminalForm|TestInteractiveRuntimeCreateTerminalFormSubmitsTerminalCreate|TestInteractiveRuntimeCreateTerminalFormRequiresName|TestUIInputReducerCreateTerminalFormEditsAndCancels|TestTerminalPoolReducerHandlesListErrorCreateAndStaleResult|TestInteractiveRuntimeMouseActionsDriveOverlayAndTerminalPool|TestRenderVMBuilderProjectsPromptAndHelpOverlay|TestProtocolTerminalServiceAdapterMapsPoolAndCreate|TestProtocolTerminalServiceAdapterMapsTerminalPoolActions' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 185. modal 背景与新建终端表单减重 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/`、`termx-tui-v3/state/` 按需 | 已按用户反馈继续减轻所有 modal：所有 modal 内部背景不再使用灰色 overlay 或 picker 背景，统一与正文文本背景一致；Prompt/Create Terminal 不渲染 `submit`/`cancel` 内容行，不渲染 `name is required` 等说明/状态文本；Create Terminal 表单移除 `tags` 字段和 create request tags 提交；待输入 label/placeholder 不用灰色，改为加粗白色/强前景。准入运行 `go test ./termx-tui-v3/app ./termx-tui-v3/render ./termx-tui-v3/services -run 'TestInteractiveRuntimeTerminalPickerEnterOpensCreateTerminalForm|TestInteractiveRuntimeCreateTerminalFormSubmitsTerminalCreate|TestInteractiveRuntimeCreateTerminalFormRequiresName|TestUIInputReducerCreateTerminalFormEditsAndCancels|TestRenderVMBuilderProjectsPromptAndHelpOverlay|TestFrameworkRendersToastAndTerminalPickerOverlay|TestThemeTokenPaletteDrivesANSILines' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 186. 新建终端表单编辑与 workdir 智能提示 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` 按需；只读参考 `tuiv2/app/workdir_completion.go`、`tuiv2/app/update_modal_query.go`、`tuiv2/app/prompt_suggestion_test.go`、`tuiv2/render/prompt_overlay.go` | 已修正 Create Terminal 表单 name/command/workdir 字段真实编辑反馈：字段保留 cursor，支持插入、删除、左右/Home/End 基础移动，name 字段不再显示易误解的 shell placeholder；提交链路已验证使用编辑后的 title、command 和 cwd。已按 tuiv2 对齐 workdir 智能提示：active workdir 字段根据路径前缀列出目录建议，Tab 先进入建议焦点，Up/Down 移动建议，Enter 接受建议，Tab 接受并跳字段，Esc 只退出建议焦点；建议渲染保持轻量 modal 风格。为保持架构边界，prompt 状态操作拆到 `state/shell_prompt.go`，workdir completion 拆到 `app/prompt_completion.go`。准入运行 `go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 187. 新建终端命令输入与 workdir 候选交互收敛 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/` 按需；只读参考 `tuiv2/app/update_modal_query.go`、`tuiv2/app/workdir_completion.go` | 已按用户确认范围收敛 Create Terminal 表单：本切片不处理全局 cursor 显示；`command` 字段保持可编辑，可输入 `codex`、`top`、shell-like 命令等自定义程序，空值才 fallback 到默认 shell；workdir 候选焦点内使用 Tab/Shift+Tab 循环选择，Enter 只接受当前候选且不提交 form；候选列表不再预先截断，renderer 以 6 行窗口滚动展示并保持选中项可见；候选区域使用轻量背景/命中背景与主体表单区分。准入运行 `go test ./termx-tui-v3/app ./termx-tui-v3/render ./termx-tui-v3/state -run 'TestUIInputReducerCreateTerminalFormEditsAndCancels|TestUIInputReducerCreateTerminalWorkdirSuggestions|TestCreateTerminalPromptSubmitUsesEditedFields|TestInteractiveRuntimeCreateTerminalFormSubmitsTerminalCreate|TestRenderVMBuilderProjectsPromptAndHelpOverlay|TestThemeTokenPaletteDrivesANSILines|TestShellSourceFilesStaySplitByResponsibility' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 188. workdir 候选 popup 最高层渲染 | 完成 | `workflow.md`、`termx-tui-v3/render/` | 已将 Create Terminal workdir 候选从 Prompt modal 内容行中拆出，改为 `OverlayVM.Popup` 附属 popup，由 `MeasureLayout` 测量为独立 `OverlayPopupLayoutPlan`，renderer 在 Prompt overlay 后绘制 `LayerPopup`，因此候选框不再受 Prompt content rect 高度裁切；popup 会按字段 value 列锚定，空间不足时保持在 viewport 内并可向上翻转，选中项、滚动摘要和轻量背景样式保持不变。新增 harness 覆盖 Prompt content 不再拥有候选行、popup VM 输出、popup layout 独立于 modal content rect、popup layer 绘制顺序高于 Prompt overlay。准入运行 `go test ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 189. workdir 候选 tree 化与 picker 风格 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` | 已将 workdir 候选 popup 从左侧线框样式调整为接近 Terminal Picker 的轻量列表样式：去掉 `┌/│/└` 左侧线，候选行使用 `▸` marker、缩进和目录 basename 表达 tree 层级；候选 popup 整体使用更明显的独立背景色，选中行使用更深一档背景，表达高层级浮层；在 suggestion focus 内 `Right` 进入当前选中目录并继续展示下一层候选，`Left` 回到父目录并刷新候选，Enter 仍只接受当前候选路径且不提交 form。路径 tree 操作拆入 `state/shell_prompt_path.go`，避免扩大 `shell_prompt.go` 职责。准入运行 `go test ./termx-tui-v3/app ./termx-tui-v3/state ./termx-tui-v3/render -run 'TestUIInputReducerCreateTerminalWorkdirSuggestions|TestRenderVMBuilderProjectsPromptAndHelpOverlay|TestFrameworkRendersOverlayPopupAbovePromptModal|TestMeasureLayoutPromptSuggestionPopupEscapesModalContentRect|TestThemeTokenPaletteDrivesANSILines|TestShellSourceFilesStaySplitByResponsibility' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-cli/cmd/termx -run 'TestV3VisualSnapshot|TestV3SmokeCommandIncludesVisualReviewCases|TestV3TmuxVisualCompareCapturesTargetAndDiffArtifacts' -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`、`sh scripts/check_file_modes.sh`。 |
| 190. panel content 深化计划文档与行动前 tag | 完成 | `workflow.md`、`termx-tui-v3/docs/` | 已按用户要求在进入夜间长跑实现前，只落档 `termx-tui-v3/docs/panel-content-deepening-plan.md`，用具体输入/输出例子说明 ContentViewport 合同、Terminal Live 内容深化、Live resize 验收、Copy/History 内容深化、History resize rebind、Panel content 总验收、空白占位、`>`/`v` 遮挡符号、测试证据和禁止事项；文档明确用户审核通过前不进入实现切片。准入运行 `git diff --check`。 |
| 191. panel content 小圆点占位语义修正 | 完成 | `workflow.md`、`termx-tui-v3/docs/`；只读参考 `tuiv2/render/` | 已按用户反馈和 `tuiv2` 只读参考修正计划文档：小圆点只表示 terminal extent 外区域，不表示普通短行 padding；默认 terminal extent 锚在 content rect 左上，pane content `10x10` 而 terminal 实际 `5x5` 时右侧和底部显示小圆点；普通 terminal blank cell 仍是空白；`>` / `v` 作为 overflow hint 与 chrome marker，和小圆点语义分离；补充居中/偏移 extent 场景，避免后续实现把所有空白都点满。准入运行 `git diff --check`。 |
| 192. SK 小阶段与反补丁式执行规则落档 | 完成 | `workflow.md` | 已按用户要求把后续 `/goal` 自动推进纪律写入 workflow：每个较大目标必须拆成可独立验证的小阶段，每个小阶段完成后提交 `SK:` 前缀中文提交，方便保留中间过程；禁止补丁式代码、临时旁路、重复 truth、第二套状态、fake-only 证据和旧目录桥接；如果功能要求架构变化，必须先重构或重写对应边界并更新设计基准，再接实现。准入运行 `git diff --check`。 |
| 193. SK ContentViewport 合同 | 完成 | `workflow.md`、`termx-tui-v3/render/` | 已按 panel content 深化计划切片 A 建立 `ContentExtent`、`ContentOverflow` 与 `RenderContentViewport`：`ContentVM + Rect` 现在能投影固定 content viewport，普通短行和 terminal 内部空行保持空白，只有 terminal extent 外区域显示 `·`；支持 offset extent；超宽/超高只返回 overflow hint，pane/floating chrome 消费 hint 在边界画 `>` / `v`，不把 marker 写进 content cell；宽字符裁切不留下半个 footprint，ANSI cell style 继续按 terminal SGR 保留且不映射到 theme token；content 写 canvas 时由调用方传入 owner/layer，避免 panel/overlay/floating 层归属混淆。已按用户要求启动只读子 Agent 审核并处理其 owner/layer finding。准入运行 `go test ./termx-tui-v3/render -run 'TestContentViewport|TestFrameworkRendersContent|TestRenderContentWritesRequestedOwnerLayer' -count=1`、`go test ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`git diff --check`。 |
| 194. SK Terminal Live extent 渲染 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/tui_test.go` | 已推进 panel content 深化计划切片 B 的首个可验证子阶段：Terminal Live projector 在存在真实 live screen/lines 时把 `TerminalSurfaceStore` 尺寸投影为 `ContentVM.Extent`，surface size 缺失时 fallback 到 session size；framework 因此能在 live surface 小于 content rect 时显示 extent 外 `·`，在 live surface 大于 content rect 时由 pane chrome 显示 `>` / `v`，并继续保留 screen cell ANSI、宽字符和 content-local cursor 到全局 `CursorRect` 的转换。pending/empty/exited/status fallback 不启用 live extent，避免状态文案被点满。smoke 边框断言已收紧为左边只允许真实边框、右边允许边框或 `>`，`v` 仅由专门 bottom marker 测试覆盖。已启动只读子 Agent 审核并处理其 fallback extent 与 smoke 白名单 findings。本切片不实现 latest-wins 合流。准入运行 `go test ./termx-tui-v3/render -run 'TestRenderVMBuilderProjectsTerminalLiveANSIStyleCursorAndState|TestRenderVMBuilderDoesNotApplyLiveExtentToStatusFallbacks|TestRenderVMBuilderUsesSessionSizeAsLiveExtentWhenSurfaceSizeMissing|TestFrameworkRendersTerminalLiveExtentFromBuilder|TestFrameworkRendersTerminalLiveOverflowFromBuilder' -count=1`、`go test ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`git diff --check`。 |
| 195. SK live latest-wins 合流 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/state/` | 已完成 panel content 深化计划切片 B 的 latest-wins 子阶段：`AppRuntime` 入队层按 terminal id 合并队列中的普通 `LiveSurfaceMsg` / ready `LiveEventMsg`，队列积压时只保留最新 surface revision，旧 revision 不替换新 revision，且合并后最新 live 帧保持最新入队位置，不重排普通消息；`LiveAttachMsg`、`LiveAttachResultMsg`、`LiveResizeMsg`、`LiveResizeResultMsg`、`HostResizeMsg`、`LiveExitMsg`、`QuitMsg` 和非普通 live event 都作为语义边界阻止跨越合并。`TerminalSurfaceStore` / `LiveSurfaceSnapshot` 增加 reducer-owned `Revision`，`ApplySnapshot` 拒绝 stale revision、exit/error 后 late ordinary frame，以及 resize 后明确来自旧尺寸基线的 late frame；同 terminal attach 会重定 revision baseline，允许 restart/reconnect 后较低 revision 的 fresh frame；`LiveEventMsg` 在 snapshot 缺 terminal id 时使用 event terminal id，避免污染当前 projected terminal；缓存 surface 会 clone styled `Screen`。已启动只读子 Agent 审核并处理其 resize boundary、LiveEvent terminal id、同 terminal attach revision baseline 和 Host/ResizeResult boundary findings。当前真实 protocol adapter 尚无稳定 live revision 字段，真实路径 revision=0 时只能保证队列内按到达顺序 latest-wins；跨进程严格旧 revision guard 留给后续 core/protocol live revision contract。准入运行 `go test ./termx-tui-v3/state -run 'TestTerminalSurfaceApplySnapshotIsDetached|TestTerminalSurfaceRejectsStaleLiveRevision|TestTerminalSurfaceResizeBoundaryRejectsLateOldSizeFrame|TestTerminalSurfaceExitBoundaryRejectsLateOrdinaryFrame|TestTerminalSurfaceAttachAllowsFreshFrameAfterExitBoundary' -count=1`、`go test ./termx-tui-v3/app -run 'TestAppRuntimeCoalescesQueuedLiveSurfaceUpdatesByTerminalID|TestAppRuntimeCoalescedLiveSurfaceKeepsLatestQueuePosition|TestAppRuntimeDropsOlderQueuedLiveSurfaceRevision|TestAppRuntimeCoalescesQueuedLiveEventsByTerminalID|TestAppRuntimeKeepsLiveSemanticBoundariesBetweenQueuedUpdates|TestLiveEventUsesEventTerminalIDWhenSnapshotTerminalIDMissing|TestInteractiveRuntimeTerminalMouseTrackingPassthroughOnlyFromContent' -count=1`、`go test ./termx-tui-v3/app ./termx-tui-v3/state -count=1`、`go test ./termx-tui-v3/... -count=1`、`git diff --check`。 |
| 196. SK Live resize 行为验收 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已完成 panel content 深化计划切片 C 的验收收口：新增 app 级 runtime 测试证明 host resize 后 active terminal resize 使用最新 content rect，旧尺寸 late surface 不会回滚，匹配新尺寸 surface 会清理 resize boundary，并保留 live extent、content-local cursor 到全局 `CursorRect`、mouse passthrough 和 viewport 行宽恒等；旧尺寸 late surface 使用更高 revision，确保测试证明的是 resize boundary 拒绝旧尺寸，而不是 stale revision guard 误兜底；active panel/layout helper 不 fallback 到第一个 panel，避免未来测错对象。新增 overflow 测试证明 oversized live surface 的 `>` / `v` 只由 pane chrome 绘制，不写进 content layer；新增 pending/exited fallback 测试证明 resize boundary 不会让状态文案被 extent 外 `·` 填充；render VM 测试补充 pending/exited after resize 不启用 live extent。tmux resize smoke 已作为真实 PTY 证据运行通过，输出 before=`98x26` after=`118x36`，证明 PTY 内 `stty size` 跟随 content rect。已按用户要求启动只读子 Agent 审核，并处理其 revision guard 弱证据与 active helper fallback findings；复核确认无新增阻塞问题。准入运行 `go test ./termx-tui-v3/app -run 'TestLiveResizeKeepsLatestContentRectAndIgnoresOldSizeSurface|TestLiveResizeFallbacksDoNotUseResizeBoundaryDots|TestLiveResizeOverflowMarkersStayOnChrome' -count=1`、`go test ./termx-tui-v3/render -run 'TestRenderVMBuilderDoesNotApplyResizeBoundaryExtentToStatusFallbacks|TestRenderVMBuilderDoesNotApplyLiveExtentToStatusFallbacks|TestFrameworkRendersTerminalLiveExtentFromBuilder|TestFrameworkRendersTerminalLiveOverflowFromBuilder' -count=1`、`go test ./termx-tui-v3/app ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`make test-cli-v3-tmux-resize-smoke`、`git diff --check`。 |
| 197. SK History styled payload 合同纵切 | 完成 | `workflow.md`、`termx-core-v2/history/`、`termx-core-v2/`、`internal/protocol/`、`termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/render/` | 已完成 panel content 深化计划切片 D 的首个纵切：core-v2 `history.Cell` 增加 width/style/link payload，`VisualRow` 保留 `Cells` 且 `Text` 只作为派生 plain view；HistoryWindow projection 按 grapheme/display width streaming reflow styled cells，覆盖完整 run、wide/mixed-width run、remaining column boundary 和 trailing blanks，并继续保留 logical line id、row-in-line、span、cursor、token、generation 和 boundary 语义。protocol service 不再输出 `CompactRow{Text: row.Text}` 降级路径，而是从 styled cells 生成 `CompactRow`，并对 history row 使用 preserve trailing blank cells；`internal/protocol` binary payload round trip 覆盖 styled/wide/link/trailing blank row。TUI protocol adapter 将 `CompactRow.DecodeCells()` 映射为 `state.HistoryCell`，state store 深拷贝 authoritative window cells；copy/history renderer 优先消费 `HistoryRow.Cells` 生成 ANSI styled cells，selection/search 仍作为 UI override，不把 terminal SGR 映射成 theme token。已启动只读子 Agent 审核，并处理其 oversized run reflow、binary payload round trip、mixed-width run、remaining column boundary 和 trailing blank findings；最终复核确认无阻塞问题。残余风险：copy selection/search 仍沿用既有 rune column 语义，display-width 精准高亮留给后续 copy interaction 小阶段。准入运行 `go test ./internal/protocol -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-tui-v3/state ./termx-tui-v3/services ./termx-tui-v3/render ./termx-tui-v3/app -count=1`、`git diff --check`。 |
| 198. SK PTY ANSI 历史 ingest 样式来源链路 | 完成 | `workflow.md`、`termx-core-v2/`、`internal/protocol/` 按需验证、`termx-tui-v3/` 按需验证 | 已完成 History styled payload 的真实来源小阶段：`Terminal.IngestOutput` 不再把历史写成纯文本 `history.Cell{{Text: ...}}`，而是通过 terminal-owned stateful `historyANSIParser` 将同一 PTY 输出流解析成 styled `history.Cell` 后继续只向 `HistoryTrack.Apply` 发送 `HistoryEvent`，没有从 live surface、snapshot 或 local VTerm scrollback 反推历史。parser 当前只承担 logical-line payload 元数据来源：支持 SGR basic/indexed/truecolor、bold/italic/underline/blink/reverse/strikethrough、reset、OSC 8 hyperlink、CRLF normalize、跨 chunk CSI/SGR 状态和跨 chunk OSC/string-control pending；OSC title、DCS/PM/APC/SOS 和非 SGR CSI 等控制序列不会泄漏进 history text；restart 会重置 parser style/link/pending 状态。新增 harness 覆盖 direct ingest styled cells、跨 chunk SGR、OSC 8 link、DCS 跨 chunk 不泄漏、SGR 尾部默认 reset、restart reset，以及 `Process.Output()` / `watchOutput` 到 protocol `history.window` 的 styled compact row 端到端链路。已按用户要求启动只读子 Agent 审核，并处理其 string-control payload 泄漏、真实 process output 链路、restart reset 和 SGR 参数边界 findings。残余风险：这不是完整 VT EventRouter；alt-screen、clear screen、cursor overwrite 等历史语义仍需后续正式 EventRouter 切片处理，不能在该 parser 中继续补丁式堆叠。准入运行 `go test ./termx-core-v2 -run 'TestTerminalIngestOutput|TestTerminalRestart|TestTerminalProcessDrainsOutputBeforeExit|TestProtocolServiceHistoryWindowPreserves' -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`go test ./termx-tui-v3/... -count=1`、`git diff --check`。 |
| 199. SK Copy/History display-width 交互语义 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/go.mod` | 已完成 History styled payload 后续 copy/history 交互小阶段：`CopyPosition.Col` 与 `CopyMatch.StartCol/EndCol` 统一解释为 authoritative visual row 内的 display cell column，不再混用 rune index；state 层新增 `HistoryRowDisplayWidth`、`HistoryCellDisplayWidth`、`HistoryRowGraphemeDisplayColumns` 与 `HistoryRowSliceDisplay`，selection copy、line end、cursor clamp、search match 和 match cursor 都使用 display-width 语义。`FindCopyMatches` 改为 grapheme cluster 级搜索，覆盖 CJK、ZWJ emoji、combining mark，并把 match 边界转换为 display columns；有 styled cells 时列边界优先来自 authoritative `HistoryCell.Width`，避免 match 落在自然文本宽度而不是 core-v2 投影宽度上。renderer 对 plain/styled history rows 按 display column 切分 selection/search override，能在保留 ANSI style 的同时只高亮命中的宽字符或 styled run 子区间；cursor 直接按 display column 落在 marker 后；未引入 live fallback、第二套 history truth 或 renderer-only 旁路。新增 harness 覆盖 CJK 搜索/复制、emoji family 和 combining cluster match、authoritative `HistoryCell.Width` 搜索列和 slice 优先、styled history cell partial selection/search split、未命中 cell ANSI 保留、line end/clamp display width。已按用户要求启动只读子 Agent 审核，并处理其 rune-window grapheme 与 authoritative cell width findings；复核确认无新增阻塞问题。残余风险：外部 protocol sender 若绕过 core-v2 styled cells 且只提供错误 width 的 compact text/run，TUI 只能按收到的 authoritative width 展示；该协议输入质量问题留给 protocol decode/contract 后续切片。准入运行 `go test ./termx-tui-v3/state ./termx-tui-v3/app ./termx-tui-v3/render -run 'TestCopyModeSearchMatches|TestHistoryRowGraphemeDisplayColumns|TestHistoryRowSliceDisplay|TestSelectedTextUsesDisplayColumns|TestCopyModeLineEndAndClampUseDisplayColumns|TestRenderVMBuilderCopyHistorySelectionAndSearchUseDisplayColumns|TestRenderVMBuilderCopyHistorySearchUsesGraphemeDisplayColumns|TestRenderVMBuilderProjectsCopyHistoryStyledCells|TestRenderVMBuilderProjectsCopyHistoryContentRendererState' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 200. SK Copy/History 命中区与遮挡提示收敛 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已完成 Copy/History 内容深化的 row hit region 与 overflow 小阶段：copy/history renderer 产出的 `HitRegionHistoryRow` 不再覆盖 marker、scrollbar 或整行空白，而是从 logical line marker 后开始，宽度使用 authoritative visual row 的 display cell width；空 authoritative row 保留 1-cell 命中区，仍可被鼠标选中。AppRuntime 删除旧的固定 `-2` magic offset，按 render 返回的 0-based rect 把 1-based mouse 坐标转换为 display-column `CopyPosition.Col`，避免 rune/固定 marker 宽度混用。layout measurement 统一把 content-local hit regions 平移后裁切到对应 content rect，防止超宽 history row 命中区溢出到 pane chrome、floating chrome 或 overlay 外部。copy/history 超宽/超高继续只通过 `ContentOverflow` 让 pane chrome 显示 `>` / `v`，不把遮挡符号写进 content layer，也不引入 live surface fallback 或第二套 history truth。新增 harness 覆盖 marker 后 hit region、CJK/styled authoritative display-width 鼠标列、空行 1-cell 命中、content hit region 裁切、copy/history overflow marker 留在 pane chrome。已按用户要求启动只读子 Agent 审核，结论无阻塞：测试语义、架构边界和空行命中规则均可接受。准入运行 `go test ./termx-tui-v3/render ./termx-tui-v3/app -run 'TestMeasureLayoutClipsContentHitRegionsToContentRect|TestRenderVMBuilderCopyHistoryHitRegionsUseAuthoritativeDisplayCells|TestRenderVMBuilderCopyHistoryEmptyRowKeepsOneCellHitRegion|TestFrameworkRendersCopyHistoryOverflowMarkersOnPaneChrome|TestCopyModeMouseSelectionUsesHistoryDisplayColumns' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 201. SK Copy/History boundary 状态 token | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已完成 Copy/History boundary 状态 token 小阶段：`HistoryStore.OlderRequestState()` 基于当前 authoritative window 的 `Token`、`Cursor`、`Boundary`、`Exhausted` 和 pending guard 汇总 older 请求状态，`beginCopyModeOlder` 只消费该 helper，保持任意 pending/latest/rebind 请求时静默挡住 older，缺 cursor 或 exhausted boundary 时不重复请求。copy/history renderer 在 search/status/scrollbar 上展示 `↑ more`、`↑ older`、`↑ loading`、`↑ top`、`↓ latest`、`↓ loaded` 与 `lines:first-last view:x-y`，其中请求能力仍以 authoritative cursor/token/boundary 为准，`HasMore=false` 但 cursor valid 时仅降级为中性 `↑ older` 展示；没有从 live surface、snapshot、local VTerm scrollback 或本地深度计数推断 history truth。新增 harness 覆盖 exhausted guard、pending latest/rebind + PageUp 静默挡住 older、renderer boundary/top-bottom token、state helper 的 pending/exhausted/missing/ready 状态。已按用户要求启动只读子 Agent 审核，并处理其 pending latest/rebind guard finding；复核确认无新增阻塞。准入运行 `go test ./termx-tui-v3/state ./termx-tui-v3/app ./termx-tui-v3/render -run 'TestHistoryStoreOlderRequestState|TestHistoryStoreRecordsOlderExhaustedMarker|TestCopyModeOlderBoundaryTokensAndExhaustedGuard|TestCopyModeOlderGuardSilentlyBlocksAnyPendingHistoryRequest|TestRenderVMBuilderCopyHistoryShowsAuthoritativeBoundaryTokens' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 202. SK History resize rebind 收口验收 | 完成 | `workflow.md`、`termx-tui-v3/app/` | 已完成 `panel-content-deepening-plan.md` 切片 F 的 History resize rebind 收口验收：补齐 reducer 级和 runtime frame 级 harness，证明 content cols 变化时旧 authoritative `HistoryWindow` 立即 `InvalidateWindow`，`CopyMode.Resize` 清空 bound token、cursor、mark、selection、query 和 matches，并以新 content rect 进入 pending latest；latest response 回来前真实 runtime 已渲染 pending frame，不显示旧 rows，也不从 live surface fallback。既有 runtime 测试继续证明新 cols latest response 到达后只渲染新 authoritative window，旧 cols response 被 stale guard 拒绝且不恢复旧 rows；新增 rows-only resize 测试证明 content 宽度不变时只更新 `ViewRows` 并 clamp viewport，不重拉 latest，不改 authoritative window 和 cursor。生产路径未新增第二套 history truth，仍由 `NewCopyModeResizeRebindReducer` 统一走 `activeTerminalContentRect -> InvalidateWindow/Resize -> beginCopyModeLatestForCols` 或 `SetViewRows/Scroll`。已按用户要求启动只读子 Agent 审核，结论无阻塞；根据其 residual risk 补强了 runtime pending frame 测试。准入运行 `go test ./termx-tui-v3/app -run 'TestCopyModeResizeRebindInvalidatesOldWindowBeforeLatestResponse|TestCopyModeResizeRebindPendingFrameDoesNotShowOldRowsOrLiveFallback|TestCopyModeResizeRebindRuntimeRendersPendingBeforeLatestResponse|TestCopyModeResizeRowsOnlyKeepsWindowAndDoesNotRequestLatest|TestCopyModeHostResizeRebindsLatestAndDoesNotRenderOldWindow|TestCopyModeResizeRejectsOldColsResponseAsStale|TestInteractiveRuntimeHostResizeKeepsReboundCopyWindowAfterTerminalResizeResult|TestCopyModePaneSizeCommandRebindsLatestAtContentCols' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 203. SK Panel content 总验收 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-cli/cmd/termx/` | 已完成 `panel-content-deepening-plan.md` 切片 G 的 Panel content 总验收：新增 `TestPanelContentAcceptanceMatrix` 覆盖单 pane live、双 pane live + inactive placeholder、split resize 后 live 裁切与 chrome overflow metadata、copy mode pending 且不从 live fallback、copy mode loaded styled rows 保留 ANSI/truecolor/underline cell metadata、floating pane content、exited pane、error pane、live burst latest-wins。根据只读子 Agent 审核发现，修正 inactive live pane 语义：`projectTerminalLiveContent` 仅在 inactive 且 pending、无 ready/error/exited/screen/lines 时投影为 pane placeholder，active pending、inactive ready/error/exited 仍走真实 live content；新增 projector harness 防回退。burst 验收不再只看最后一帧，而是断言普通 live burst 合流为单个内容帧。同步 CLI tmux visual baseline：terminal extent 外 `·` 纳入 plain target，并在 style map 中归类为 muted content hint。全程没有引入第二套 renderer/history truth，copy/history 仍只消费 authoritative `HistoryWindow`。已按用户要求启动只读子 Agent 审核并处理 High/Medium findings，复核结论无阻塞。准入运行 `go test ./termx-tui-v3/app -run TestPanelContentAcceptanceMatrix -count=1`、`go test ./termx-tui-v3/render -run 'TestContentProjectorRegistry|TestRenderVMBuilderProjectsPaneChromeStateSlots' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`go test ./termx-cli/cmd/termx -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 204. SK Panel content link metadata 收口 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/docs/` | 根据 panel content completion audit 的只读子 Agent finding，收口 history link metadata 从 authoritative `HistoryCell` 到 TUI render/FrameSink 的最后一段链路：`render.Cell`、canvas matrix 和 `Line.ANSIString` 均保留 `LinkURL/LinkParams`，copy/history renderer 在普通、selection 和 search 分段时都透传 link metadata，live cell projector 复用同一 metadata 字段；ANSI frame 输出 OSC 8 hyperlink，不只停留在 state 层。`TestRenderVMBuilderProjectsCopyHistoryStyledCells` 和 `TestPanelContentAcceptanceMatrix/copy_mode_loaded_styled_rows` 现在断言 link metadata 进入 content VM，`TestANSILinesPreserveCellHyperlinks` 覆盖 styled frame 与 ANSI OSC 8 输出。同步 `panel-content-deepening-plan.md` 状态为已完成，避免文档仍显示待审核。已按用户要求启动只读子 Agent 复审，结论无阻塞。准入运行 `go test ./termx-tui-v3/render -run 'TestRenderVMBuilderProjectsCopyHistoryStyledCells|TestANSILinesPreserveCellHyperlinks' -count=1`、`go test ./termx-tui-v3/app -run TestPanelContentAcceptanceMatrix -count=1`、`go test ./termx-tui-v3/render -run 'TestContentProjectorRegistry|TestRenderVMBuilderProjectsPaneChromeStateSlots' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`go test ./termx-cli/cmd/termx -count=1`、`make test-cli-v3-tmux-visual-compare`、`git diff --check`。 |
| 205. SK Active terminal input routing 急修 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/state/` | 已完成 active terminal input routing 急修：`reduceLiveInput` 不再以 `root.Session.TerminalID` 作为无条件输入目标，而是先解析 active floating terminal，再解析当前 active tiled pane terminal；空 pane 或无 terminal 绑定时显示 `terminal.input` / `no terminal bound`，并吞掉输入，避免写回旧 session terminal。`TerminalSessionStore` 新增已 attach terminal 到 input channel 的轻量映射，只作为 protocol channel 路由元数据，不是历史 truth；键盘输入和 terminal mouse passthrough 均使用当前目标 terminal 的 surface/mode 判断。新增 harness 覆盖三 pane 点击后键盘输入进入对应 `TerminalID`/channel、active floating terminal 优先于 tiled pane、empty active pane 不 fallback 到旧 session terminal；同步更新综合 UI flow，先聚焦有绑定 terminal 的 pane 再验证 missed mouse 不偷键盘输入，避免测试继续依赖旧 fallback。残余边界：floating 内容区 mouse passthrough 仍受现有 floating hit region 表达限制，留给切片 206/207 后续处理。准入运行 `go test ./termx-tui-v3/app -count=1`、`go test ./termx-tui-v3/state ./termx-tui-v3/services ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 206. SK Floating attach terminal 急修 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/`、`termx-tui-v3/services/` 按需 | 已完成 floating attach terminal 急修：`ShellStore.BindFloatingTerminal` 将 selected existing terminal 绑定到 floating pane 的 `Pane.TerminalID`，并激活对应 floating；Terminal Picker / Terminal Pool attach 请求携带或默认解析 active floating target，floating 空态 `Attach existing` 现在打开 picker 而不是展示未实现 toast。RenderVM 为 floating terminal-live 使用对应 terminal surface/session，floating content 复用 terminal-live content renderer；TerminalPool attach 初始尺寸使用 active floating content rect，TerminalPool attach result 和已绑定 floating resize/move/collapse 触发 active floating content rect resize 去重，空 floating 拖拽不 fallback resize 后方 terminal。新增 harness 覆盖 floating 空态从 picker attach pool existing terminal、渲染真实 live content、输入进入 floating terminal、active floating resize 发送 content rect resize、关闭 floating 不 kill terminal，并补充 overlay target state 测试。准入运行 `go test ./termx-tui-v3/app -count=1`、`go test ./termx-tui-v3/state ./termx-tui-v3/services ./termx-tui-v3/render -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 207. SK Host cursor projection 急修 | 完成 | `workflow.md`、`termx-tui-v3/render/`、`termx-tui-v3/terminalhost/`、`termx-tui-v3/app/`；只读参考 `tuiv2/` cursor writer | 已完成 host cursor projection 急修：render `Cursor` 增加 `Anchor` 语义，真实 content-local virtual cursor 继续由 layout 映射为全局 `CursorRect` 且 `Visible=true`；无真实 virtual cursor 时只产生 hidden anchor，不再伪装成 visible cursor。`FrameSink` 写 frame 时绘制期间仍隐藏宿主光标，帧末参考 tuiv2 cursor writer 的最终复投经验：visible cursor 设置 block/bar shape、定位到全局 `CursorRect` 并显示系统光标；anchor-only 则隐藏但停靠到对应 rect；无 cursor/anchor 时保持隐藏。新增 harness 覆盖 live、copy、prompt、floating、non-input overlay 优先级和 resize 后 cursor rect 不越界；更新 FrameSink harness 覆盖 visible cursor、anchor-only cursor 与默认隐藏。准入运行 `go test ./termx-tui-v3/terminalhost -run 'TestFrameSink|TestHostEnterAndClose' -count=1`、`go test ./termx-tui-v3/render -run 'TestMeasureLayoutAnchorsCursorWhenContentHasNoVisibleCursor|TestMeasureLayoutProducesGlobalHitRegionsAndCursorRect|TestMeasureLayoutOpaqueOverlayOwnsHitRegionsAndCursorRect|TestFrameworkTranslatesContentHitRegionsAndCursor|TestFrameworkOpaqueOverlayOwnsCursor' -count=1`、`go test ./termx-tui-v3/app -run 'TestHostCursorProjection' -count=1`、`go test ./termx-tui-v3/... -count=1`、`go test ./termx-core-v2/... -count=1`、`go test ./internal/protocol -count=1`、`git diff --check`。 |
| 208. SK TerminalView / Attachment 设计落档 | 完成 | `workflow.md`、`termx-core-v2/docs/`、`termx-tui-v3/docs/` | 已把 `Terminal` 与 `TerminalView/Attachment` 分层语义写入 workflow、core-v2 架构、tui-v3 架构、UI 交互规格和 render 架构：terminal 是共享 process/lifecycle/history truth，view/attachment 是 pane/floating/tab 对 terminal 的连接，拥有 channel、surface/view id、resize role、desired size、request seq 和 view-local error；明确同 terminal 多 pane/floating view、owner/follower resize、close/detach/kill、copy/history view binding、storage schema v2 和 no fallback 到全局 latest session 约束；文档-only 准入运行 `git diff --check` |
| 209. SK core-v2 attachment registry 与 resize ownership | 完成 | `termx-core-v2/`、`internal/protocol/`、`termx-proto/` 按需 | protocol attach/ensure_resize 已建立 session-level attachment registry，保存 channel、terminal id、surface id、view id、resize policy、owner epoch；同 terminal 多 attachment 可并存；owner 可以 resize，follower/observer 不覆盖 PTY size；detach 可按 channel 或 attachment identity 移除，不误删同 terminal 其它 attachment；terminal info 返回当前 session 的 resize ownership 摘要；harness 覆盖多 attach/channel、owner/follower resize 裁决、detach by channel 和 detach payload round trip；准入命令 `cd internal && go test ./protocol/... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`git diff --check` 已通过 |
| 210. SK tui-v3 pane binding 与 attachment store | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已新增 reducer-owned `TerminalViewStore`，按 view 保存 terminal id、channel、surface id、view id、resize role、desired size、request seq、error、pane/floating identity；pane/floating attach result 会写入 view binding，input target 优先使用当前 pane/floating view channel，close/detach 只移除当前 view，kill terminal 移除所有绑定 view；`TerminalSessionStore` 暂保留为当前活跃投影兼容后续 212/213 收口；harness 覆盖同 terminal 两 pane、多 channel、close/detach 不 kill、kill 清理所有绑定 view；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`cd internal && go test ./protocol/... -count=1`、`git diff --check` 已通过 |
| 211. SK workbench storage schema v2 for view binding | 完成 | `termx-tui-v3/state/`、`termx-tui-v3/services/`、`termx-tui-v3/app/` | workbench opaque storage schema 已升级到 v2，snapshot 显式保存 pane/floating terminal view binding；load 会恢复 `TerminalViewStore`，persist 会保存当前 root 的 view binding；v1 snapshot 明确 reject，不做复杂兼容桥；harness 覆盖 v2 encode/decode/validate、terminal view restore、storage load/persist CAS 保存 view binding、多 pane shared terminal；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212. SK attach/reconnect/duplicate view runtime 路由 | 完成 | `termx-tui-v3/app/`、`termx-tui-v3/services/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` | LiveAttach、Terminal Picker、Terminal Pool 和 reconnect 已使用 active pane/floating view identity 创建或复用 attachment view；input target 优先使用 active view channel；live event stream token 已按 terminal scoped，不再用全局 latest stream token 互相取消；mouse passthrough 不再 fallback 到 global latest session terminal；duplicate view 的显式 UI action 仍留到切片 215/216 可见动作收口；harness 覆盖同 terminal 双 pane channel 路由、terminal-scoped stream token、reconnect active pane view identity；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212A. SK Terminal Picker terminal-only attach | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | Terminal Picker 已收敛为 terminal-only attach 列表：已有 terminal 列表项只展示 terminal 名称、生命状态、大小，不展示 pane/source/target/location，不把已连接 pane 当作 picker row；选择已有 terminal 永远 attach 到当前 pane/floating 并创建新 view binding，允许同一界面多个 pane 同时连接同一个 terminal；keyboard/mouse/product content action 均不 focus 旧 pane。harness 覆盖 picker 列表、terminal id fuzzy 匹配但不显示隐藏 id column、键盘/鼠标 attach 到当前 pane/floating、smoke terminal picker terminal-only 展示；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212B. SK picker 新建入口与 exited 展示修正 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已按用户反馈修正 212A 后续语义：Terminal Picker 保留 `+ new terminal` 新建入口，但已有 terminal 行仍只展示名称、生命状态和大小，列表采用固定列 table 样式，默认选择优先落在已有 terminal，避免已有 terminal 时 Enter 误触新建；create row 的键盘、鼠标和 content action 入口继续打开 `Create Terminal` 表单。`protocol error 400: terminal exited` 等包含 `terminal exited` 的协议错误不再作为 UI error 展示，而是标记 terminal 正常 exited，保留 last output，并追加 restart current terminal 与 choose another terminal 提示。准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212C. SK picker 默认新建终端 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已按用户最新反馈把 Terminal Picker 的 `+ new terminal` 恢复为默认第一项和默认选中项；已有 terminal 行继续只展示名称、生命状态和大小，仍不展示 pane/source/target/location。直接 Enter 默认打开 `Create Terminal` 表单；选择已有 terminal 需要移动到对应 terminal 行后确认，仍 attach 到当前 pane/floating。准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212D. SK 新建终端后自动连接当前面板 | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | 已按用户最新反馈让 `Create Terminal` 成功后刷新 terminal pool，并立即用新 terminal id 走现有 attach reducer 连接当前 pane/floating、切换当前 panel、启动 live surface/stream；`Create Terminal` prompt 改用与 Terminal Picker 对齐的 compact overlay，并隐藏 `open`、`esc` 等工程状态提示；harness 覆盖 create form 自动 attach 当前 pane、prompt 与 picker modal 几何对齐、prompt smoke 不再出现工程态 chrome。准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212E. SK tab 空态与点击切换补齐 | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已按用户反馈让新建 tab 保持无 pane 空态，不再合成默认 pane；active empty tab 在 body 渲染简单提示和 `Choose terminal`、`New terminal`、`Terminal Pool` CTA，content action hit region 可点击，首次 attach/create terminal 时才创建真实 pane；header tab label/index/marker 产生 `tab.switch` action，鼠标点击不同 tab 标签会通过既有 `WorkbenchCommandTabSwitch` 切换，不新增第二套 tab 逻辑；tab mode 既有键盘切换/跳转 action catalog 保持可见。准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 212F. SK tab 最后一项关闭与空 workspace | 完成 | `workflow.md`、`termx-tui-v3/state/`、`termx-tui-v3/render/`、`termx-tui-v3/app/` | 已按用户确认语义允许关闭最后一个 tab：workspace 可进入无 tab 状态，`EnsureDefaults` 不再把用户主动清空的 workspace 重新补成 `main` tab/pane；无 tab workspace 在 body 显示 `Create tab`、`New terminal`、`Terminal Pool` CTA，header 不再渲染 synthetic `main` tab；点击唯一 tab 的 `×` 通过既有 `WorkbenchCommandTabClose` 进入空 workspace，close tab 仍只 detach view，不 kill terminal；Terminal Picker attach、Live attach 和 Create Terminal 自动 attach 路径在无 tab workspace 下先创建真实 empty tab，再由 `BindPaneTerminal` 创建首 pane 并绑定 terminal，不新增第二套 app 侧 pane truth。harness 覆盖 last tab close、empty workspace defaults、empty workspace render/no synthetic tab、唯一 tab close mouse click 和无 tab attach 创建 tab+pane。准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 213. SK view-aware resize 与 ownership transfer | 完成 | `workflow.md`、`termx-tui-v3/app/`、`termx-tui-v3/state/`、`termx-tui-v3/render/` | 已把 resize owner truth 收口到公共 `TerminalViewStore` view binding：owner lookup、owner transfer、view-local desired size、request seq 和 stale guard 都按 view 记录；layout resize 只允许 active owner view 的 content rect 发起 `LiveResizeMsg`，follower/observer view 不再覆盖共享 PTY size；view-scoped resize result 使用 view binding stale guard，不再被旧全局 session seq 误拒；pane chrome 展示 `size:owner/follower`，非 owner pane 提供 `own` 按钮并通过 app action 转移 owner。harness 覆盖同 terminal 双 pane 不互相改 size、owner transfer、view stale guard 和 UI 展示/操作；准入命令 `cd termx-tui-v3 && go test ./state ./render ./app -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`git diff --check` 已通过 |
| 214. SK copy/history view binding 收口 | 待开始 | `termx-tui-v3/state/`、`termx-tui-v3/app/`、`termx-tui-v3/render/` | copy mode 绑定 active pane/floating view、terminal id、content cols、view rows、request id 和 bound token；同 terminal 不同 view 的 copy cols/rebind 不互相覆盖；history truth 仍来自 core-v2 terminal authoritative `HistoryWindow`；pending/older/stale guard 保持 no live fallback |
| 215. SK render/interaction shared terminal UI | 待开始 | `termx-tui-v3/render/`、`termx-tui-v3/app/`、`termx-tui-v3/input/`、`termx-tui-v3/docs/` | pane/floating chrome、Terminal Picker/Pool、Workbench Tree 和 footer/help 展示 shared terminal/view role 信息；可见动作区分 focus existing pane、attach here、duplicate into split/tab/floating、detach view、kill terminal；所有动作继续走统一 semantic command/action catalog，不画未接线按钮 |
| 216. SK multi-view integration and tmux harness | 待开始 | `termx-cli/`、`termx-tui-v3/`、`termx-core-v2/`、`internal/protocol/`、`Makefile` 按需 | 建立真实/黑盒证据：同一 terminal 连接到同 tab 多 pane 或 floating，输入只进 active view channel，owner resize 触达 PTY，follower 不覆盖 size，kill terminal 更新所有 view，copy/history authoritative window 可在不同 view cols 下独立 rebind；运行相关模块测试、tmux/e2e smoke 和 `git diff --check` |

当前下一步：进入切片 214 的 copy/history view binding 收口：copy mode 必须绑定 active pane/floating view、terminal id、content cols、view rows、request id 和 bound token，同 terminal 不同 view 的 copy cols/rebind 不得互相覆盖。历史 VT 语义路由方案暂不进入当前自动执行队列，等 TerminalView/Attachment 主线完成后再单独讨论和重新落档。继续保持 copy/history 只消费 authoritative `HistoryWindow`，不得从 live surface、snapshot 或 local VTerm scrollback 推断历史，也不得把 styled payload parser 扩张成补丁式完整 VT emulator。

## 6. 必做 harness

### 6.1 core-v2 harness

必须逐步覆盖：

- 普通输出无换行。
- 普通输出带换行。
- 自动折行。
- 宽字符与组合字符。
- 光标移动后覆写。
- clear screen 与 clear scrollback。
- alt-screen 进入与退出。
- grow resize reclaim committed suffix。
- shrink resize hidden frontier。
- attach/bootstrap/recovery/full replace 不创建 committed history。
- process exit force commit。
- committed suffix reclaim 后修改再提交。
- truncate/retention 按完整 logical line 删除。
- logical line id、seal、dirty、generation、residency、committed index、mutable frontier、projection 内容。

### 6.2 HistoryWindow harness

必须逐步覆盖：

- latest replace。
- older prepend。
- 空 older exhausted。
- token/generation/cursor/boundary stale guard。
- 不同 cols 下重投影。
- clipped before / clipped after。
- logical line id 与 row 到 line 映射。
- mutable frontier 和 committed tail 混合投影。
- resize 后旧 window/response 失效。
- 真实 PTY 输出进入 logical line 后，通过 protocol history.window 返回 authoritative window。

### 6.3 tui-v3 harness

必须逐步覆盖：

- input key/mouse -> semantic intent。
- reducer message -> state + effects。
- effect result 回到 message path。
- AppRuntime message 顺序、timer、batch、cancel、quit。
- TerminalHost input event 转换和 FrameSink contract。
- 真实 raw mode 进入、恢复、异常退出清理。
- HistoryStore latest replace、older prepend、empty exhausted、stale response、cols mismatch。
- CopyMode cursor、viewport、selection、clipped span、multi logical line copy。
- RenderVM live mode 与 copy mode projection 分流。
- copy mode 缺 authoritative window 时不得从 live surface fallback。
- lipgloss/v2 style helper 宽度、裁剪、ANSI 安全性。
- 外部 viewport 初始尺寸进入 reducer-owned state。
- host resize event 通过 message path 触发重绘。
- fake host deterministic 注入 resize。
- `RenderVMBuilder` 使用外部 viewport，不以 `Session` 或 `Surface` 尺寸作为 UI canvas truth。
- layout measurement 纯函数输出 panel/content/overlay/toast rect。
- renderer 输出行数和每行 display width 严格等于 viewport。
- active terminal resize 使用 content rect cols/rows，且重复尺寸去重。
- copy mode resize 后旧 bound cols/window 失效并重新请求 authoritative window。
- Unicode box drawing glyph、split 连接点、右边框和宽字符内容同时保持 cell-width 安全。
- styled `RenderResult` / `Frame` / `FrameSink` contract 不丢 style。
- ANSI styled frame serializer 输出 SGR、reset、cursor metadata，plain snapshot 仍可用于测试。
- active pane border 与 inactive pane border 有不同 style token。
- top/bottom bar 背景填满整行，token 裁切后 display width 恒等。
- pane chrome 标题、状态、action 槽位带 ANSI 样式时不破坏边框。
- toast/overlay styled chrome 不改变 body layout，且窄屏退化仍宽度安全。
- pane structural command 覆盖 split、close、close and kill、focus、zoom、resize、set size、balance 和 panel presentation。
- pane split/close/resize/zoom 后 active pane、terminal binding、layout plan、content rect、terminal resize、copy rebind 和 toast feedback 保持一致。
- keyboard adapter、mouse hit region、测试入口和后续 CLI mini command adapter 调用同一 semantic command contract。
- `termx v3 smoke` 输出 styled frame，不能退化为纯文本线框。

### 6.4 CLI / 集成 harness

必须逐步覆盖：

- CLI root、attach、daemon 当前旧链路识别测试或审计记录。
- `termx v3` 实验命令组存在，且不会改变默认 root、`daemon`、`attach` 的旧入口行为。
- `termx v3 daemon` 启动 core-v2 daemon。
- v3 实验入口能通过非交互 smoke 装配 tui-v3。
- v3 实验入口 attach tui-v3。
- 自动启动 daemon、连接已有 daemon、socket 路径、日志路径、配置路径。
- `termx v3 new`、`termx v3 ls`、`termx v3 kill`、`termx v3 rm` 命令通过 core-v2 protocol service。
- PTY 输出 -> core-v2 live surface -> tui-v3 render。
- tui-v3 input -> protocol input -> PTY。
- resize -> core-v2 live/history boundary -> tui-v3 frame。
- 外部 terminal emulator resize -> tui-v3 viewport -> render frame。
- 外部 terminal emulator resize -> active content rect -> core-v2 terminal resize。
- pane split/close/resize/zoom -> tui-v3 semantic command -> reducer/effect -> render frame。
- CLI mini command -> tui-v3 semantic command adapter -> reducer/effect，不绕过 TUI state 和 layout measurement。
- copy mode -> core-v2 HistoryWindow -> selection/copy。
- copy mode viewport/content cols 变化 -> authoritative HistoryWindow 重新绑定。
- 默认入口切换后，`termx-cli` 默认路径不再 import 旧 `termx-core` 或 `tuiv2`。
- Bubble Tea contract 不进入 tui-v3 主线。

## 7. 测试准入

每个有效切片提交前必须运行与切片相关的测试。新 module 建立后优先使用模块内命令：

- core-v2 改动：在 `termx-core-v2/` 运行 `go test ./... -count=1`。
- tui-v3 改动：在 `termx-tui-v3/` 运行 `go test ./... -count=1`。
- protocol 改动：在 `internal/` 运行 `go test ./protocol/... -count=1`，在 `termx-proto/` 运行 `go test ./... -count=1`。
- CLI 改动：在 `termx-cli/` 运行 `go test ./... -count=1`。
- workspace 或受限联动改动：运行所有相关模块测试，并按需运行 `make test-v2-migration`。
- 切片 18 改动：至少运行 `cd termx-cli && go test ./... -count=1`、`make test-v2-migration`。
- 切片 19-24 改动：至少运行 `cd termx-cli && go test ./... -count=1`，涉及 core-v2、tui-v3、protocol、remote 或共享模块时同步运行对应模块测试，并按需运行 `make test-v2-migration`。
- 默认入口切换相关改动：至少运行 `make test-v2-migration`、`cd termx-cli && go test ./... -count=1`，并用非交互命令验证 `go run ./termx-cli/cmd/termx --help` 可编译运行。
- 切片 40-44 改动：至少运行 `cd termx-tui-v3 && go test ./... -count=1`；涉及 CLI 装配、protocol adapter 或默认入口时同步运行 `cd termx-cli && go test ./... -count=1`。
- 切片 45 改动：至少运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`，并按需运行 `make test-v2-migration`。
- 切片 47-52 改动：至少运行 `cd termx-tui-v3 && go test ./... -count=1`；涉及真实 `FrameSink`、CLI smoke 或默认入口时同步运行 `cd termx-cli && go test ./... -count=1`。
- 切片 53-56 改动：至少运行 `cd termx-tui-v3 && go test ./... -count=1`；涉及 CLI mini command adapter、默认入口或 protocol 装配时同步运行 `cd termx-cli && go test ./... -count=1`。
- 切片 57 改动：必须运行 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-v2-migration`。
- 切片 58 改动：文档-only 至少运行 `git diff --check`；若同切片含代码，按代码范围运行相关测试。
- 文档-only 改动：至少运行 `git diff --check`。

如果测试无法运行，必须在最终说明和必要时在提交前记录原因。不能把真实语义失败当作偶发失败。

## 8. 自动执行规则

- 每次开始工作先读取本文件，再检查 `git status --short --branch`。
- 按任务队列表格顺序选择最早未完成切片；遇到 `阻塞` 必须停止，遇到 `进行中` 继续，遇到 `待开始` 必须先标为 `进行中`。
- 切片范围不清时，先更新本文件，不要自行扩大范围。
- 每个切片尽量保持小而可提交；不要跨多个切片堆叠未提交改动。
- 完成切片后更新本文件中对应状态和必要的下一步说明，与实现同提交。
- 如果发现设计文档需要变化，必须与实现同切片更新，或先提交设计更新。
- 如果遇到阻塞，必须把对应切片状态改为 `阻塞` 并说明阻塞条件；不要继续扩散到其他目录。
- 自动执行不能因为局部测试暂时通过就跳过后续切片；当前 UI 框架完善阶段必须从切片 61 开始按顺序推进，不得跳过 header/footer、鼠标 hit region 或 active pane 反馈直接做 Terminal Pool、Workbench Tree、floating 或大内容 renderer。
- `/goal` 自动推进期间，较大目标必须拆成多个小阶段；每完成一个小阶段就运行该阶段准入并提交一个 `SK:` 前缀中文提交，再进入下一小阶段。
- 如果实现过程中发现现有架构无法干净承载目标，不得临时补丁绕过；必须暂停当前实现小阶段，先把 architecture/workflow 和边界重构小阶段做成新的 `SK:` 提交。

## 9. 提交规则

- 当前工作区未提交改动必须先整理并提交，再继续后续开发。
- 每个有效变动必须形成中文提交。
- 后续 `/goal` 自动推进的小阶段提交必须使用 `SK:` 前缀，例如 `SK: 完成 live latest-wins 合流`；文档-only、小重构和 harness 阶段也必须按同样规则形成可回退提交。
- 不允许长期堆积未提交改动。
- 不得 amend commit，除非用户明确要求。
- 不得 revert 用户或其他代理的未提交改动；若冲突，先停下说明。
- 删除旧代码必须和对应新语义或 harness 同切片提交。

## 10. 当前状态

- 当前分支主线已切换到 `termx-core-v2/` 与 `termx-tui-v3/` 的重构方向。
- 切片 0-10 已完成：新 core/tui module、logical line history domain、HistoryWindow projection、protocol 字段、tui-v3 runtime/state/render/services 骨架、smoke 入口和 `make test-v2-migration` 均已建立。
- 切片 11 已完成：`termx-cli/docs/v2-v3-switch-audit.md` 已建立默认入口切换审计、迁移矩阵和分阶段验收口径。
- 切片 12 已完成：`termx-core-v2` 已建立独立 server/daemon API、listener factory、listen/shutdown、terminal registry、event broker 和 fake harness，不依赖旧 `termx-core`/`tuiv2`。
- 切片 13 已完成：`termx-core-v2` 已建立 terminal lifecycle、PTY 管线、输出 ingest、live surface、history track、exit force commit、late output guard、resize grow/shrink 和 shutdown lifecycle harness。
- 切片 14 已完成：`termx-core-v2` protocol session 已服务控制面、输入、resize、events 和 `history.window`，并从 `HistoryTrack` logical line truth 返回 authoritative window。
- 切片 15 已完成：`termx-tui-v3/terminalhost` 已建立真实 raw mode、输入读取、窗口尺寸、frame sink 输出、终端状态恢复和取消退出 harness，且未引入 Bubble Tea runtime。
- 切片 16 已完成：`termx-tui-v3` 已建立 reducer-owned live surface/session、`NewLiveRuntime`、attach/render/input/resize/error 主路径、terminal service fake，以及真实 protocol client 的 attach/input/ensure_resize harness。
- 切片 17 已完成：`termx-tui-v3` 已建立 copy mode app reducer、page up/鼠标滚轮 latest/older 主路径、older prepend、selection/copy、stale response guard、真实 protocol history.window client harness，以及 live/copy reducer 组合入口。
- 切片 18 已完成：`termx-cli` 已建立显式 `termx v3` 实验命令组、`termx v3 daemon` core-v2 前台 daemon 入口、`termx v3 smoke` tui-v3 非交互 smoke 入口，并把 v3 smoke 纳入 `make test-v2-migration`；默认 root、`daemon`、`attach` 仍保持旧入口。
- 切片 19 已完成：`termx-cli` 已建立 v3 专用 daemon client、`termx v3 ping` probe、core-v2 socket 默认解析、连接已有 core-v2 daemon、自动启动 `termx v3 daemon`、启动失败传播和旧 daemon 自动启动隔离测试。
- 切片 20 已完成：`termx-cli` 已建立 `termx v3 new/ls/kill/rm` 本地控制命令，全部通过 core-v2 protocol service，单 session create/list/kill/remove CLI harness 覆盖真实 core-v2 daemon 路径。
- 切片 21 已完成：`termx-cli` 已建立 `termx v3 attach <id>`，交互路径使用 tui-v3 `terminalhost.Host`、protocol terminal/history adapters 和 `NewInteractiveRuntime`，非交互环境返回明确错误，CLI harness 覆盖禁止调用旧 `tuiv2` 与真实 protocol attach 装配。
- 切片 22 已完成：`termx-cli` 已建立 `termx v3 e2e-smoke` 非交互本地端到端 smoke，并纳入 `make test-v2-migration`；smoke 覆盖 core-v2 daemon、create、live attach、terminal input、resize、`history.window` 与 tui-v3 copy mode 主路径，默认入口仍未切换。
- 切片 23 已完成：`termx-cli` 已明确 v3 实验入口 socket、log、config、state 路径策略并补 tests；v3 本地 attach 不读取或创建 `termx.yaml`，配置差异已写入迁移审计。
- 切片 24 已完成：remote 暂不迁移到 `termx v3` 实验命令组，审计文档明确 legacy/fallback 隔离结论；`termx v3 remote ...` 不挂载，旧 `termx remote ...` 保留，默认切换不得把 remote 伪称为 core-v2 已兼容。
- 切片 25 已完成：`go run ./termx-cli/cmd/termx` 默认 root 已调用 tui-v3 root runner，自动连接/启动 core-v2 daemon 并进入 tui-v3 attach runtime；旧 root 仅保留为显式 `termx legacy`。
- 切片 26 已完成：`termx daemon`、`termx attach`、`termx new/ls/kill/rm` 默认入口已切到 core-v2/tui-v3；旧本地 root/daemon/attach/control 仅保留在显式 `termx legacy ...` 下，旧 remote 仍按切片 24 结论保持 legacy/fallback 隔离。
- 切片 27 已完成：CLI、core-v2、tui-v3、protocol、proto 测试均通过；`make test-v2-migration` 已纳入并通过默认入口非交互 smoke，覆盖默认 `termx daemon/new/ls/kill/rm` 和 `termx --help`。
- 切片 28 已完成：旧本地实现文件已收敛为 `legacy_*.go` 显式隔离入口，remote 仍按 `remote_*.go` legacy/fallback 隔离；`TestDefaultRuntimeSourceDoesNotImportLegacyCoreOrTUI` 和 `make test-cli-default-deps` 守卫默认源文件不得 import 旧 `termx-core`/`tuiv2`。
- 当前本轮默认入口切换目标已完成：`go run ./termx-cli/cmd/termx`、默认 `termx daemon`、`termx attach`、`termx new/ls/kill/rm` 已使用 core-v2/tui-v3；旧本地路径只能通过 `termx legacy ...` 显式调用；remote 未迁移项保持文档化 legacy/fallback 边界。
- 切片 29 已完成：`termx-tui-v3/docs/ui-interaction-spec.md` 已新增，定义 tui-v3 的产品级 UI 交互、界面结构、功能清单、页面线稿、快捷键与鼠标交互、宽窄屏退化和硬约束；该文档不写实现方案，后续 render 架构和默认界面补齐必须以它作为产品基准。
- 切片 30 已完成：`termx-tui-v3/docs/ui-interaction-spec.md` 已补充 card panel / tmux-like split line 两种 tiled pane 呈现、全局 header/footer 可隐藏、floating pane 保持带边框、右上角现代消息弹层；该切片只写产品需求，不写实现方案。
- 切片 31 已完成：`termx-tui-v3/docs/render-architecture.md` 已新增，定义 render framework 与 content renderer 的边界、数据流、层级合成、panel/overlay/floating/toast/content 分类、禁止事项和分阶段落地计划；子 Agent 审核结论为无严重问题，小修后可进入用户拍板。
- 切片 32 已完成：用户拍板结论已写入 `termx-tui-v3/docs/render-architecture.md`；render framework + content renderer 成为正式方向，最小 render framework 阶段必须处理 card/split 两种 panel、header/footer hide 和 toast 基础生命周期，Terminal Pool 与 Workbench Tree 等 framework 成型后再接入。
- 切片 33 已完成：`termx-tui-v3/render` 已建立 `RenderResult`、layer、line/cell、content renderer、shell/panel/floating/overlay/toast/header/footer VM 基础类型、width-safe helper 和 Bubble Tea contract import guard；后续切片 78 已删除当时临时保留的裸文本 `RenderVM{Lines, Status}` 兼容输入。
- 切片 34 已完成：`state.Root` 已拥有 reducer-owned `ShellStore`，覆盖 workspace/tab/pane 最小树、active pane、card/split panel presentation、header/footer visibility、toast/message 生命周期和 Terminal Picker overlay 占位状态；`app.NewShellReducer` 已提供 semantic message 入口并组合进 live/interactive runtime，但尚未接具体产品快捷键。
- 切片 35 已完成：`RenderVMBuilder` 已把 `state.Root` 投影为 `ShellVM`、header/footer、layout/panel、content、overlay、toast 和 cursor 子 VM；copy-history VM 已校验 terminal id、bound token 和 cols，缺 authoritative window 时只生成 pending/empty/error content，live content 只消费 `TerminalSurfaceStore`；旧 `RenderVM{Lines, Status}` 临时兼容投影已在切片 78 删除。
- 切片 36 已完成：`Renderer.RenderResult` 已走最小 render framework，产出 workbench shell、header/footer、card panel、split line、双 pane 横向/纵向分割、panel chrome、content slot、toast、Terminal Picker overlay、hit region 合成和 cursor 归属；宽字符、emoji、combining mark 和 ANSI 样式通过 width-safe helper 裁切填充，不再把 active content 裸输出为 frame。
- 切片 37 已完成：input router 已把 `Ctrl-f` 映射为 Terminal Picker intent、`Ctrl-v` 映射为 Display/Copy intent；runtime 已接入 UI input reducer，`Ctrl-f` 打开 Terminal Picker overlay/placeholder 且不发送到 terminal，`Ctrl-v` 进入 copy-history authoritative request 路径；card/split、header/footer hide、toast close/clear 已通过 semantic message 进入 runtime，不发明未拍板产品快捷键。
- 切片 38 已完成：`termx-tui-v3` detailed smoke 已覆盖 workbench shell、card panel、split line、header/footer hide、toast、Terminal Picker placeholder、copy empty、copy history、live surface panel content、emoji/CJK/ANSI 宽度安全；`termx v3 smoke` 输出多 case UI frame，`make test-v2-migration` 已通过默认 CLI 与 v2/v3 迁移回归。
- 切片 39 已完成：`termx-tui-v3/docs/render-architecture.md` 和 `termx-tui-v3/docs/ui-interaction-spec.md` 已同步最小 render framework 已落地、未落地边界和后续 Terminal Pool / Workbench Tree / floating / overlay 深化切片；过时的 no-fallback 测试命名已收敛；旧 `tuiv2` 仍只读参考，默认路径未引入旧依赖。
- 当前 render framework 最小阶段目标已完成：默认入口继续使用 core-v2/tui-v3；render 主路径已走 `render framework + content renderer`；默认 TUI 和非交互 smoke 不再把裸文本 frame 当作可用界面。
- 当前新阶段目标已完成：外部 terminal emulator viewport 驱动重绘、layout measurement、active terminal content rect resize、copy mode resize rebind 和 Unicode box drawing 线框硬化均已落地到默认入口验收范围。
- 当前本轮运行缺口已收敛：默认 TUI 已按 reducer-owned viewport 绘制整屏 frame，已知 viewport 下不再按默认 80 列输出；默认 UI chrome 已改为 Unicode box drawing；外部 host resize 已进入 reducer-owned state 并触发重绘。
- 切片 40 已完成：`state.Root` 已拥有 reducer-owned 外部 viewport state；`TerminalHost` contract 已暴露初始尺寸和 resize event 流；真实 host 监听 `SIGWINCH` 并发布 resize event；fake host 可注入 resize；runtime 通过 `HostResizeMsg` 去重更新 viewport 并重绘，同时把有效 resize message 交给 reducer 链供后续切片使用。
- 切片 41 已完成：`RenderVMBuilder` 已把 reducer-owned 外部 viewport 投影为 `Layout.Viewport` truth；`render.MeasureLayout` 已作为纯函数产出 viewport、body、panel、content、overlay、toast、全局 hit region、最终 cursor 和绝对 cursor rect；renderer 已严格按 layout plan 绘制并填满已知 viewport，已知窄屏下不再扩展到默认 80 列。
- 切片 42 已完成：app 已通过 `render.MeasureLayout` 计算 active terminal content rect，并把 attach 初始尺寸、host resize、header/footer hide、panel presentation 和 split 变化转换为去重后的 `LiveResizeMsg`；terminal resize service 只收到 content rect cols/rows，CLI 真实 protocol 装配已覆盖 attach 后 content rect resize correction。
- 切片 43 已完成：copy mode latest/older 已使用 copy content rect cols/rows 请求 authoritative `HistoryWindow`；host resize、layout chrome 变化和 active copy content width 变化会失效旧 history window、清理 selection/cursor、重新 latest rebind，并在 pending 期间不从 live surface fallback；旧 cols response 会被 stale guard 拒绝。
- 切片 44 已完成：render 默认 UI chrome 已改为 Unicode box drawing；该切片当时 card、overlay、toast 使用 `╭╮╰╯─│`，后续切片 92 已按用户反馈把默认 floating/overlay/toast 改为直角 chrome；split line 使用连接感知 `┌┐└┘─│├┤┬┴┼` primitive；split content rect 避开 divider cell，emoji/CJK/combining/ANSI 内容继续通过 cell-width helper 裁切填充；旧 `tuiv2/render` 仅只读参考了连接位合成思路，未迁入旧 runtime/model。
- 切片 45 已完成：`termx v3 e2e-smoke` 已通过 fake host 初始尺寸和 host resize event 覆盖默认 attach 装配、外部 viewport 初始尺寸、resize 重绘、content rect terminal resize、copy mode resized cols 绑定和 frame 行宽恒等；`termx-tui-v3/docs/render-architecture.md` 与 `termx-tui-v3/docs/ui-interaction-spec.md` 已同步本轮已落地内容和后续 Terminal Pool / Workbench Tree / floating 深化边界；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-v2-migration` 已通过。
- 当前新阶段目标已拍板：把 v3 从“Unicode glyph 可见”推进到 `tuiv2` 截图级 styled chrome renderer；该阶段已经完成 styled `RenderResult`、ANSI `FrameSink`、cell matrix、theme token、pane chrome、shell chrome、toast/overlay styled chrome 和 pane command smoke 验收。
- 切片 46 已完成：`workflow.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/ui-interaction-spec.md` 已把 styled chrome renderer 目标落档；明确 `tuiv2/render` 只读参考，优先实现 shell/chrome/pane/frame 的 styled visual parity，terminal-live/copy-history/Terminal Pool/Workbench Tree/floating 内容 renderer 后续分阶段深化。
- 当前补充拍板：pane split、close、resize/size change、focus、zoom 必须纳入当前阶段，并作为 command-first 的统一 semantic action contract；分屏创建、分屏关闭、按方向调整大小、固定尺寸/比例尺寸、等分/balance 不得只做成快捷键局部逻辑；快捷键、鼠标、测试入口和后续 CLI mini command 只能作为 adapter 调用该 contract。
- 当前 pane/resize/global 第一版入口已拍板并落地：`Ctrl-p` pane mode、`Ctrl-r` resize mode、`Ctrl-g` global mode；card/split 切换、header/footer hide、toast close current、toast clear all 必须继续通过 semantic action 和 reducer 路径执行，不得分叉成局部快捷键逻辑。
- 切片 47 已完成：`RenderResult` 可生成 plain snapshot、styled lines 和 ANSI lines；`Frame` 保留 plain/styled/ANSI lines、cursor、blink 和 metadata；真实 `FrameSink` 优先写 ANSI styled frame 并在行尾和帧尾 reset；旧 smoke 和测试仍可读取 plain `Frame.Lines`；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 48 已完成：renderer canvas 已从 `[]string` 升级为 cell matrix compositor，内部 cell 记录 text、width、style、owner、layer、continuation 和 safe flag；写入宽字符、emoji 或覆盖 continuation cell 时会清理 wide-cell footprint；raw ANSI 输入进入 matrix 前会 strip 控制序列，最终仍通过 `Frame.ANSILines` serializer 输出 styled frame；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 49 已完成：`Theme` 已扩展 host fg/bg、chrome fg/bg、accent、muted、success/warning/danger/info、panel border、muted border、active/inactive pane、toast/overlay 和 status token；`RenderResult` 与 `Frame` 保留 theme fallback；ANSI serializer 按 theme token 输出 24-bit SGR；harness 覆盖 semantic token 输出、自定义 palette、active/inactive token 区分和 fallback；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 50 已完成：tiled card pane 已从 rounded chrome 切到 square Unicode pane chrome；pane chrome primitive 支持 per-pane style/owner/layer；active pane 使用 accent style，inactive pane 使用 muted style；pane 顶边包含标题、状态和 action slot；split/card 两种模式保留 content rect 与行宽恒等；smoke 已验证 active pane accent ANSI；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 51 已完成：header/footer 已从占位文本升级为 styled top/bottom bar；top bar 输出 workspace、tab、create 和 notice token，bottom bar 输出 mode、hint 和 status token；bar cell 使用 `StyleStatus` 并输出 status background ANSI，行宽填满 viewport；header/footer hide 继续由 layout plan 回收 body；默认 smoke 已验证 styled shell bar token 和背景 ANSI；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 52 已完成：toast 与 Terminal Picker overlay 已升级为 styled chrome，覆盖 overlay/toast 背景、severity token、close action region、hit region 优先级、ANSI 输出和宽字符安全；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 53 已完成：`state.PaneCommand` 已成为 pane 结构操作统一 contract，覆盖 split、close、close and kill、focus、zoom、resize、set size、balance、panel presentation、target、source、confirm、ratio/cell size 和 CLI mini source；`ShellPaneCommandMsg` 已接入 app reducer，旧 split/presentation 消息也转入同一 contract；无效目标和 destructive confirm 只产生反馈 effect，不绕过 reducer；layout resize 与 copy mode rebind 已识别统一命令消息；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 54 已完成：reducer-owned pane tree 已支持横向/纵向 split 插入、focus/activate、close pane、close and kill terminal effect 边界、zoom/unzoom/toggle zoom；close 会同步裁剪 split tree、保留 terminal binding 供 kill effect 使用、维护 active pane 与 tab invariants；pane command 成功、无效和待确认状态均有 toast 反馈；RenderVMBuilder 在 zoom 状态只投影 zoomed pane，closed pane 不再进入 render VM；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 55 已完成：pane tree 已拥有 reducer-owned split geometry hint，支持按方向/步长 resize、按比例 set size、按固定 cell size set size 和 balance/equalize；RenderVMBuilder 会把 ratio/bias/fixed size hint 投影到 SplitVM，render layout measurement 按 hint 计算 panel/content rect；active terminal content rect resize 去重和 copy mode authoritative window rebind 已通过 `ShellPaneCommandMsg` 跟随几何变化；测试准入 `cd termx-tui-v3 && go test ./... -count=1` 已通过。
- 切片 56 已完成：pane mode / resize mode helper、鼠标 hit region 和 CLI mini command parser 已接到同一 `state.PaneCommand` contract；`termx v3 pane-command` 提供非交互 parser smoke；hit region 已按 toast/overlay/chrome/content 顺序收敛，pane chrome 优先于终端内容转发，具体 history row 命中优先于宽泛 pane content focus；只读复核 `tuiv2/render` 后，v3 card pane frame 已按连接位合并绘制，标题/action 只覆盖顶边内部槽位；真实 `FrameSink` 已改为逐行绝对定位，避免满宽行触发终端自动换行导致竖向边框视觉断裂；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`git diff --check` 已通过。
- 切片 57 已完成：`termx v3 smoke` 现在覆盖 `pane-command-flow`，`termx v3 e2e-smoke` 覆盖 split/resize/zoom/unzoom/close pane command、最终 panes/active/zoom 状态、frame 行宽恒等、content rect terminal resize 和 copy rebind；`TerminalSessionStore` 已记录 resize 目标尺寸和序号，连续 pane command 下旧 resize result 不会覆盖最新 zoom content rect；只读复核 `tuiv2/render` 后，v3 继续按 cell matrix + 连接位绘制 Unicode pane border，不迁入旧 runtime/model；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`make test-v2-migration`、`git diff --check` 已通过。
- 切片 58 已完成：`termx-tui-v3/docs/render-architecture.md` 与 `termx-tui-v3/docs/ui-interaction-spec.md` 已同步 styled chrome renderer、pane command、FrameSink ANSI 输出、cell matrix、theme token、active/inactive pane、styled top/bottom bar、styled toast/overlay 已落地；同时明确当前阶段只完成 chrome/frame 视觉等级和 pane 结构命令基础，不宣称 terminal-live、copy-history、Terminal Picker、Terminal Pool、Workbench Tree、floating、Prompt、Help 内容 renderer 完整；测试准入 `git diff --check` 已通过。
- 切片 59 已完成：split-line pane 顶边改为使用 pane chrome 连接语义绘制，标题/action 只覆盖内部槽位，不再用填充文本擦掉剩余顶线；隐藏 header 的 split smoke 已断言 `┌ ... ┬ ... ┐` 顶边形态；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 60 已完成：`ShellStore` 已拥有 reducer-owned interaction mode；真实输入可用 `Ctrl-p` pane mode、`Ctrl-r` resize mode、`Ctrl-g` global mode、`Ctrl-o` floating stub mode；pane/resize 快捷键通过标准 `ShellPaneCommandMsg` 回投，继续触发布局 resize 和 copy rebind；global 快捷键通过标准 shell message 回投；`Esc` 可退出 mode 或关闭 overlay 且不漏发 terminal；footer 显示当前 mode；card/split 顶栏 title 到 action 之间保留线段；floating 完整交互仍未伪实现，只显示 not implemented 反馈；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 61 已完成：`HeaderVM` 已从基础 title/notice 扩展为 workspace、tab、active pane、terminal count、floating 摘要和 notice；`FooterVM` 已扩展为 mode、active target、mode-specific shortcut hints、hint/error 和全局摘要；renderer 按优先级拼接 footer segment，80 列默认 live/copy/overlay smoke 可见快捷键提示，错误 hint 优先保留；harness 覆盖产品 header/footer 字段、live/pane/resize/global/copy/overlay footer hints、宽字符 active target、styled bar 宽度安全和 smoke 默认 footer keys；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` 已通过。
- 切片 62 已完成：`Frame` 已保留 render hit regions，`AppRuntime` 渲染后缓存最新 hit regions，并在真实鼠标左键输入进入 reducer 前完成 1-based SGR mouse 坐标到 0-based hit region 的派发；pane content/chrome/action/resize 命中复用 `PaneCommandFromHitRegion` 和 `ShellPaneCommandMsg`，toast close 与 overlay close 走标准 shell message，未命中鼠标事件继续走原 `InputMsg` fallback；harness 覆盖 pane content 点击聚焦、pane action 点击关闭、toast close 优先于 overlay、overlay 点击关闭和 miss fallback；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 63 已完成：`TestInteractiveRuntimeActivePaneVisualFeedbackFollowsKeyboardAndMouse` 已覆盖键盘 split/focus、鼠标 pane content focus、resize 后 presentation 切换、zoom/unzoom 和鼠标 action close；断言 active/inactive pane styled token、footer active target、toast feedback、close 后稳定 active pane 和 split/card 变化后的 active 反馈；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 64 已完成：`TestInteractiveRuntimeUIFrameworkProductizationFlow` 已覆盖窄屏 viewport 下的键盘 pane/resize/global 入口、card/split presentation、header/footer hide、toast close/clear、missed mouse fallback、content rect terminal resize、copy mode authoritative rebind 和 hidden chrome 行宽恒等；`termx-tui-v3/docs/ui-interaction-spec.md` 已写清真实 TUI 中分屏、关闭、resize、zoom、card/split、header/footer hide、toast 和 copy rebind 的手工验收步骤；测试准入 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 65 已完成：`TerminalSurfaceStore` 已能区分 live surface pending 与 empty，并承载基础 live cursor；`RenderVMBuilder` 已把 live raw 行中的基础 ANSI SGR 投影为 styled `Line`，把 live cursor 投影为 content-local cursor，并尊重 active empty/exited pane 内容；runtime harness 覆盖真实 live 输出、ANSI/style、emoji/CJK、cursor 归属、resize 后裁切、plain frame 无 raw ANSI 和 no chrome leak；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步 terminal-live content renderer 一期边界。
- 切片 66 已完成：copy-history content renderer 一期只消费 core-v2 authoritative `HistoryWindow`，在 VM 层表达 logical-line / continuation / clipped marker、styled selection、content-local copy cursor、row/line/cols 位置摘要和 copy/yank toast feedback；resize/content cols 变化后继续 invalid/rebind authoritative window，不从 live surface、snapshot、grid viewport 或 local VTerm scrollback fallback；产品文档和架构文档已同步一期边界与后续深化项。
- 切片 67 已完成：empty pane 与 exited pane 已由单行 placeholder 升级为 CTA 内容，Terminal Picker overlay 已输出 search row、当前 workspace terminal list、selected row、new terminal row、overlay cursor 和 content action hit region；runtime 已把 content action 转为 shell message，picker row focus/close overlay 和 empty/exited close 可落地，未接入服务的 create/restart/manager 只显示 toast 反馈且不漏发 terminal；产品文档和架构文档已同步一期边界。
- 切片 68 已完成：Terminal Picker query、过滤、selected row、上下键移动、Enter attach/focus、row click attach/focus、new action feedback、最小 preview/detail 行和 no terminal input leak 已通过 reducer-owned state、UI input reducer、content action hit region 和 render VM harness 落地；当前仍只基于当前 workspace panes，不伪装完整 Terminal Pool，也不伪装真实 create/restart/reconnect 服务。
- 切片 69 已完成：`TerminalPoolStore` 已成为 reducer-owned pool list/source 状态，Terminal Picker 打开可触发 terminal list service request，list loading/empty/error/items/stale guard 回投到 state；picker rows 已合并当前 workspace panes 与 pool items 并按 terminal id 去重；pool row attach、new terminal create、restart、reconnect 已通过 terminal service effect/result message 接线，失败反馈写入 toast，renderer 只消费 VM/state，不读取 service。
- 切片 70 已完成：Terminal Pool Page 已作为独立 terminal-pool content/page 接入；全局入口和 empty manager action 可打开页面，页面触发 list loading，支持搜索、selected row、键盘选择、Enter attach、鼠标 row/action 命中；list loading/empty/error、detail、metadata、preview 摘要和 Attach/Edit/Kill action 在常规 viewport 下可见；attach/edit/kill 均通过 terminal service effect/result message 回到 reducer，反馈写入 toast 或页面状态，kill result 前不本地伪造 lifecycle；页面输入和 action click 不漏发到底层 terminal；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 71 已完成：Workbench Tree overlay 已作为 page-sized opaque overlay 接入，支持 global mode 打开、workspace/tab/pane/floating row、搜索、selected row、detail/preview、键盘 Enter open/focus、鼠标 row/action hit region、overlay cursor ownership、no terminal input leak 和不改写 terminal lifecycle；`termx-tui-v3/docs/ui-interaction-spec.md` 与 `termx-tui-v3/docs/render-architecture.md` 已同步用户确认的 TUI-v3 视觉方向：参考 `tuiv2` header/footer、square pane chrome、active accent、toast 卡片和实体 overlay，不要求灰度遮罩背景。
- 切片 72 已完成：Floating pane 一期已接入 reducer-owned floating state、z-order、active/collapsed、styled bordered chrome、keyboard create/move/resize/center/collapse/close、mouse raise/resize/close、floating content rect 裁切、viewport clamp、toast/overlay 前景优先和 Workbench Tree floating 摘要；不伪造 attach as floating 的 terminal lifecycle，不宣称连续鼠标拖拽已完成；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 73 已完成：Prompt / Help overlay 一期已接入 reducer-owned prompt state、title/context/input/submit/cancel/destructive confirm 边界、Help 分类内容、global mode `:` / `?` 入口、overlay cursor、mouse close、footer hint、no terminal input leak 和 `termx v3 smoke` case；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步一期边界和后续 Tab/Workspace / UI 产品壳总验收顺序；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` 已通过。
- 切片 74 已完成：Tab / Workspace 产品入口一期已接入 reducer-owned workbench command、workspace list、tab create/switch/rename/close、workspace create/switch/rename、`Ctrl-t` / `Ctrl-w` mode、Prompt rename 回投、header tab strip、footer mode hints、`termx v3 smoke` tab-workspace case 和 no terminal input leak；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步一期边界和后续 TUI 产品壳总验收顺序；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 75 已完成：`TestInteractiveRuntimeTUIProductShellAcceptanceFlow` 已串联 pane split/focus/resize/presentation、floating create/move/resize/close、Terminal Pool 搜索/attach、Workbench Tree open/focus、Prompt/Help、Tab/Workspace rename、toast/header/footer 操作，并统一验证 no terminal input leak、viewport 行宽恒等和 layout 操作触发 content rect resize；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步 TUI 产品壳总验收完成口径和切片 76 terminal live 前推顺序；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 76 已完成：terminal live 连接展示与交互前推已接入 `TerminalSurfaceService.LiveSurface` 可选能力，attach result 后会通过 core-v2 protocol `snapshot` 拉取一次 live rows 初始化 `TerminalSurfaceStore`；输入只通过 terminal service effect 发送，不做本地假回显，显示必须来自 live surface 回投；live lifecycle 已区分 attached/exited/error，exited 保留最后 live 行并在 panel/footer 显示状态；harness 覆盖 attach hydration、输入回投显示、attach 切换清旧 surface、exit 状态、protocol snapshot live rows 和 e2e smoke 中真实 `alpha/beta` live rows 渲染；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步完成口径；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 77 已完成：copy-history content renderer 深化已接入 reducer-owned 可见行数、搜索 query、匹配列表、active match、滚动 clamp 和 content-local mouse selection；renderer 已显示搜索栏、可见 authoritative rows、selection/match 颜色层级、scrollbar/status、row/line/part/cols/span/search/older 位置摘要；高度变化只更新 view rows 并夹紧 viewport，宽度变化仍 invalid/rebind authoritative `HistoryWindow`；鼠标 history row 命中不漏发到底层 terminal；harness 覆盖搜索、match navigation、PageDown 滚动、鼠标选择、resize preserve/rebind、e2e authoritative history 和 no live fallback；`termx-tui-v3/docs/ui-interaction-spec.md`、`termx-tui-v3/docs/render-architecture.md`、`termx-tui-v3/docs/architecture.md` 已同步完成口径；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 78 已完成：`RenderVM` 已删除旧 `Mode`、`Lines`、`Status`、`HitRegions` 兼容输入字段，renderer 主路径不再接受裸行 fallback shell；`Frame.Lines`、`Frame.StyledLines`、`Frame.ANSILines` 仍作为 `RenderResult` 输出适配保留；新增反射守卫防止旧字段回归；新增 `BenchmarkRendererLargeTerminalOutput` 作为 large terminal output 性能基线，当前单次基线为约 6.91ms/op、6.75MB/op、12648 allocs/op；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 79 已完成：新增 `termx-tui-v3/docs/visual-alignment-audit.md` 作为视觉基线，记录当前 v3 仍未达到用户截图要求的 top/bottom bar、pane chrome、active/inactive、toast、overlay、floating、copy-history 和 Terminal Pool/Workbench Tree 视觉差距；`termx v3 smoke` 新增固定 `120x40` 的 `visual-audit-current` case，覆盖 header/footer、split pane、floating、toast 和宽字符文本；三份 TUI 基准文档已同步“功能闭环/工程基线不等于截图级视觉完成”的口径；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` 已通过。
- 切片 80 已完成：header/footer renderer 改为 `tuiv2` 风格分段产品栏，新增 status accent/muted/warning token，workspace/tab/mode/action/active/summary 稳定分区，Unicode `│` 分隔，右侧 summary 和状态按宽度退化，error/exited 关键状态优先保留，mode-specific keys 在 80 列 smoke 可见；`visual-alignment-audit.md`、`render-architecture.md`、`ui-interaction-spec.md` 已同步完成口径；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 81 已完成：pane chrome 与 split line 已完成第一轮连续线框和 shared chrome 重绘，但仍未达到最终截图级视觉密度。
- 切片 82 已完成：overlay、toast 和 floating 已完成第一轮实体 card 视觉对齐，但内容层仍需 polish。
- 切片 83 已完成：真实默认 TUI 视觉复核结论为未通过，已把失败原因与后续切片写入 `termx-tui-v3/docs/default-tui-visual-review.md`。
- 切片 84 已完成：shell top/bottom bar 已从工程 key-value 标签推进到高密度产品栏；top bar 使用 workspace、tab strip、`[⊕]`、active pane、`◆ owner`、terminal/floating 和 action token，bottom bar 使用 `MODE • [KEY] ACTION`、active target 和 summary，并在窄屏保留尾部关键动作。
- 切片 85 已完成：pane top chrome 已改为目标截图式槽位布局，宽 pane 显示 full action cluster，窄/分屏 pane 显示 compact action cluster，状态点、owner、元信息和 action hit region 与可见槽位保持一致。
- 切片 86 已完成：Terminal Picker、Terminal Pool、Workbench Tree、Prompt、Help 和 copy-history 的内容层已完成第一轮产品化 polish。
- 切片 87 已完成：根据用户真实视觉反馈确认当前 TUI 仍不像目标截图，只归档复核未通过结论并扩展固定 smoke 证据，不作为视觉完成证据。
- 切片 88 已完成：shell/pane 二轮视觉重绘已把 theme accent 改为紫色系、status bar 改为深色背景，top bar 使用 `×`、`[＋]`、compact summary 和 action cluster，bottom bar 使用 `[Ctrl] · [P]` 类 taxonomy，pane top chrome 使用 `· ↔2`、`· ◆ owner`、`· 1/31` 高密度槽位；自动准入已覆盖二轮 styled chrome 合同。
- 切片 89 已完成：使用隔离 socket/log 和真实 PTY 启动默认 `go run ./termx-cli/cmd/termx`，确认默认入口进入 alternate screen 并输出二轮 styled ANSI frame；该证据只证明真实 TTY 路径可绘制，不证明用户截图级视觉已通过。
- 切片 90 已完成：用户明确反馈当前 TUI 样子仍与目标不一致，视觉对齐 goal 不能完成；该结论已归档为下一轮返工入口。
- 切片 91 已完成：三轮整体 UI 构图已落地，pane/floating chrome glyph contract 支持 Nerd Font、emoji 和 UTF-8 字符；该轮自动准入不替代用户人工视觉拍板。
- 切片 92 已完成：按用户最新复核反馈隐藏未真实接线的 pane split/zoom 可见按钮，仅保留真实派发的 close action；floating、overlay 和 toast 改为直角 chrome；toast 改为深色矩形、左右紫色竖线和居中文案，copy 成功显示 `Copied to clipboard`，toast 本体只拦截鼠标遮挡不穿透。
- 切片 93 已完成：toast 新增默认 auto-dismiss TTL，pending/error 反馈保留更久但有明确生命周期；真实 runtime timer 定期派发 `TickMsg` 进入 reducer 并触发重绘，timer 不经过 terminal input fallback；close current 和 clear all 仍保留 shell message 路径。
- 切片 94 已完成：真实 SGR mouse button-event drag/release 已接入；render 为 pane 分隔线和边框产出带方向的 resize hit region；runtime 维护临时 drag state，拖动回投统一 `PaneCommandResize`，release 清理状态，drag resize 不漏发给 terminal，active terminal content rect resize 继续跟随既有 layout/effect 路径。
- 切片 95 已完成：pane chrome 已恢复真实可用的 split-down、split-right 和 close 可见 token，zoom 等未恢复接线的按钮继续隐藏；鼠标点击 split token 会通过同一 `PaneCommandSplit` 语义创建对应方向分屏，并按 hit region target pane 生效；窄 pane 退化为 close-only 或隐藏 action slot。
- 切片 96 已完成：render 为 floating 标题栏、右下 resize handle、close 和 content raise 产出独立 hit region；runtime 用同一 transient drag state 支持 floating move-drag / resize-drag，drag move 回投 `FloatingCommandMove` / `FloatingCommandResize`，release 后清理且不漏发给 terminal。
- 切片 97 已完成：`visual-alignment-audit.md` 和 `default-tui-visual-review.md` 已归档切片 92-96 的功能/可见性回归结果与手工复核入口，继续强调自动 smoke、e2e、PTY ANSI frame 和 Unicode/ANSI 证据不能替代用户截图级视觉拍板。
- 切片 107 已完成：stacked pane 共享边界 resize 已按用户反馈收敛，右侧多行 panel 共享同一分隔线时会一起变宽；其中某行再左右分列时，仅贴近共享边界的左子列跟随变化，右子列保持锚定。
- 切片 108 已完成：core-v2 `IngestOutput` 发布 live changed event；tui-v3 protocol terminal service 订阅 event stream 并通过 runtime `StreamEffect` 回投 live surface snapshot；`TerminalSurfaceStore` 按 terminal id 隔离 live surface，renderer 只为明确绑定 terminal 的 pane 渲染对应 surface；真实 CLI 装配使用异步 effect runner，Terminal Pool attach/reconnect 也启动 live effects；e2e smoke 已验证 attach 后真实后端 `gamma` 输出触发 frame 重绘；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 109 已完成：按用户确认的架构方向，真实 terminal 进程 owner 放在 `termx-core-v2` 内部 `ProcessFactory`；外部仍走 protobuf+Unix socket；CLI 和 TUI-v3 不直接碰 PTY；默认 core-v2 daemon 已使用真实 PTY factory，`v3 e2e-smoke` 已改为真实 shell 输出和真实 TUI 输入回环，不再手工注入后端输出；准入命令 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 110 已完成：用户反馈 Terminal Pool / Picker 创建 terminal 仍报 `protocol error 400: invalid command`；已确认是 TUI create request 未填 command 导致 core-v2 正常拒绝，现已修复 first-party create 默认 shell 兜底，真实 core-v2 protocol adapter create 回归不再触发 invalid command；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 111 已完成：用户明确反馈 Terminal Picker 设计不可用且视觉粗糙；现已按 terminal switcher 方向返工为 page-sized overlay，包含搜索提示、列表表头、source/state/target 列、选中详情、Attach Selected / New Shell 动作和稳定 hit region；旧 `filter terminals` / `PREVIEW pane:` 工程文案已从 picker 主路径移除；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`git diff --check` 已通过。
- 切片 112 已完成：用户反馈真实 terminal 渲染内容错位且颜色样式丢失；已确认旧问题是 core-v2 live snapshot 把 PTY 输出压成 string rows，不能保留 terminal grid/cell style/cursor。现已接入 `termx-vterm` 作为 core-v2 live surface 投影，protocol snapshot 输出 size-bound `ScreenData` cell matrix、cursor 和 modes；tui-v3 adapter/state/render 保留 live cells 并映射基础 ANSI/SGR 样式，legacy `Lines` 只作为 trim 后兼容视图；准入命令 `cd termx-core-v2 && go test ./... -count=1`、`cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 113 已完成：用户截图确认同一宿主环境下直接 terminal 与 termx 内颜色不一致；已定位为 tui-v3 live cell 把 `ansi:N` 映射成 TermX semantic theme token，导致 `ls` 等程序输出被重染。现已让 terminal live content cell 通过 `ANSICellStyle` 保留 `ansi:N`、`idx:N`、truecolor 和 SGR attrs，并让 renderer compositor 与 ANSI FrameSink 直通宿主 palette 编号；tuiv2 宿主 theme/palette probe 迁移已拆为切片 114。
- 切片 114 已完成：真实 resize 到 core-v2 的闭环已用 CLI 真实 socket/protocol/server harness 和 recording `ProcessFactory` 证明，attach/correct size、host resize、header/footer hide、pane split、keyboard resize、zoom/unzoom、close、鼠标 divider drag 都会产生新的 core-v2 process resize 记录；layout resize 保留 attach ownership 元数据并走 `ensure_resize`，裸 channel resize 仍保留给无 surface/view 的路径；process resize 失败会回到 session error；准入命令 `cd termx-tui-v3 && go test ./... -count=1`、`cd termx-cli && go test ./... -count=1`、`cd termx-core-v2 && go test ./... -count=1`、`go run ./termx-cli/cmd/termx v3 smoke`、`go run ./termx-cli/cmd/termx v3 e2e-smoke`、`git diff --check` 已通过。
- 切片 131 已完成：按用户指出的产品定义不清问题，新增 `termx-tui-v3/docs/tuiv2-product-contract-alignment.md`，从 tuiv2 文档、输入 catalog、hit region、overlay footer、terminal pool layout 和 status hints 中抽取完整产品契约，整理 workspace/tab/pane/floating/terminal/pool/overlay 的按钮、快捷键、鼠标动作、效果、v3 差距和 P0-P3 对齐队列；后续实现必须先按该矩阵统一可见控件和 semantic action，并把 TUI workbench schema 寄存在 daemon 通用 storage 中，不能要求 daemon 理解 workbench truth。
- 切片 132 已完成：新增 `termx-tui-v3/docs/unicode-ui-wireframes.md`，用 Unicode box drawing 给出默认 Workbench、split line、empty pane、floating、Terminal Picker、Terminal Pool、Workbench Tree、Prompt/Confirm 和窄屏退化候选稿；线稿只表达已定义产品语义，不提前画未拍板的 owner/share/lifecycle/Nerd Font token。
- 下一阶段计划已根据用户反馈重排：先让用户按 Unicode 线稿确认界面形态，再按 tuiv2 产品契约矩阵处理 P0，对齐 TUI-defined storage snapshot/event 与 workspace/tab/pane 可见动作；键盘/鼠标映射继续落在 v3 自有 binding catalog 和 terminalhost parser；补丁代码审计结论保持有效，面条式线性流程可以接受，但绕过 reducer/effect/service result、重复 truth、字符串 action 四散和旧目录回流不可接受。
